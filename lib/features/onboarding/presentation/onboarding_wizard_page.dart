import 'package:flutter/material.dart';

/// Placeholder onboarding wizard page.
///
/// Full implementation in Plan 08-02 (Onboarding Wizard UI).
/// This stub exists so the go_router redirect guard can reference the route.
class OnboardingWizardPage extends StatelessWidget {
  const OnboardingWizardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Onboarding Wizard — coming in Plan 08-02'),
      ),
    );
  }
}
