name: ime_super_editor_app
description: Minimal Flutter app for testing CJK IME compatibility with super_editor.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  super_editor: ^0.3.0-dev.20
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
