---
context: default
phase: none
task: n/a
total_tasks: n/a
status: paused
last_updated: 2026-06-16T12:29:30.150Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> 这些是本会话通过实际操作确认的约束，非预测。

- [ ] CONSTRAINT: **rj4 全量测试套件未跑** — providers.dart part 拆分仅以 `flutter analyze` 全仓 0 issues 验证（编译全部 114 消费方），未跑 `flutter test`（1647）。纯机械重构（搬声明零逻辑改动）下 analyze-0 是强证据，但恢复后**第一步应跑 `flutter test` 确认零回归**。

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| GLM 子代理截断返回 | gsd-planner/executor 返回 "Acknowledged."/"完成。"/"Complete." 截断信息，工作实际已完成 | advisory | 用 `git log` + `flutter analyze` + `flutter test` 自验产出，勿因截断而重试 |
| worktree cleanup 脏阻断 | use_worktrees=true 下 cleanup-wave 因未跟踪 SUMMARY.md 报 worktree_dirty 阻断 | advisory | cp 抢救 SUMMARY → rm 清空 worktree → 重跑 cleanup；或 context 紧时直接用非 worktree executor |

<current_state>
本会话由 `/goal 使用skill根据你的建议执行优化` 驱动，连续用 `gsd-quick` skill 完成两项 `03-flutter-standards.md`「最大 800 行」红线消除重构，**均已完成并提交，工作树干净**（git status 空）。无进行中的 phase。
</current_state>

<completed_work>

- **260616-qxe**（commit f204855，完全验证）: `anti_ai_scent_processor.dart` 1091→555 行。13 个 static const/final 词库表（_synonymMap 20 类 / _highlightOnlyPhrases / _structuralPatterns 12 正则 / 5 类 _xCliches / _mannerAdverbStems 等）转同库顶级私有常量搬入新建 `anti_ai_scent_lexicon.dart`（part of anti_ai_scent_processor.dart）。analyze 0 + anti_ai_scent_test 57/57 全绿 + git diff 仅 2 文件。
- **260616-rj4**（commit 3a6a13f + docs 24fbf9a，**测试待确认**）: `providers.dart` 856→103 行（仅留 ~50 import + `library;` + 5 part 指令）。~45 provider 声明 + 2 Notifier 类按 feature 域分入 5 part：providers_core(101)/providers_ai(199)/providers_knowledge(174)/providers_structure(254)/providers_stats(59)，各首行 `part of 'providers.dart';` 且 **0 import**。flutter analyze 全仓 No issues found（编译全部 114 消费方证零破坏）。
</completed_work>

<remaining_work>

- **恢复首步**: `cd /home/re/code/MuseFlow && flutter test` → 期望 1647 passing（确认 rj4 零回归）
- **下一候选**: `editor_with_sidebar.dart`（887 行，仍超 800 红线）——UI widget 重构，需提取子组件、有行为风险，**不适合 quick 机械抽取**，建议走 `/gsd-quick --discuss` 或 `/gsd plan` 独立规划
- 其余 >500 行文件见 STATE.md（provider_management_page 737 / format_cleaner 680 / consistency_analysis_service 632 等，均未超 800 红线，优先级低）
</remaining_work>

<decisions_made>

- 用 **Dart part/part of 同库机制**拆分超长文件（非迁 domain 层）：part 让私有成员与裸名引用不变，消费方 import 路径零变更，纯机械零行为变更。迁 domain 会动 8+ 消费方导入路径，超出 quick 范围。
- rj4 用**非 worktree executor** + 跳过 --validate 的 plan-checker/verifier agent（context 收紧 75%）：非 worktree 避免 merge/cleanup 机器 + SUMMARY 救援；内联全仓 analyze 替代 verifier agent。
- qxe 用 worktree executor（默认配置），遇到 SUMMARY 救援已解决并记忆。
</decisions_made>

<blockers>
- 无阻塞。两项重构均落地提交。
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — Quick Tasks Completed 表（260616-qxe / 260616-rj4 两行已记录）
2. `.planning/quick/260616-rj4-providers-dart-feature-part/260616-rj4-PLAN.md` — providers 拆分的精确分组与验收
3. `.claude/rules/03-flutter-standards.md` — 文件大小红线依据

## Infrastructure State
- 无运行中的服务/watcher
- kimi-webbridge daemon 本沙箱未运行（视觉 UAT 留待真机，见 STATE.md）
- Flutter SDK 3.44.0 / Dart 3.5.4，全仓 analyze 基线为 0

<context>
两项重构都是「找违反 800 红线的最大文件 → 用 part 机制机械拆分 → analyze/test 验证零行为变更」的同一模式。处理器（数据表）与 providers（DI 声明）都适合 part 拆分因为它们是「独立的声明集合」。下一项 editor_with_sidebar 是 UI 组件，不是声明集合，需要不同的重构手法（提取子 widget），风险更高，故单独留作 phase 级工作。
</context>

<next_action>
Start with: `cd /home/re/code/MuseFlow && flutter test`（确认 rj4 providers 拆分零回归，期望 1647 passing）。若绿，则两个 800 红线违规（processor + providers）已闭合。继续优化可选 editor_with_sidebar.dart（887，UI 重构需独立 plan）。
</next_action>
