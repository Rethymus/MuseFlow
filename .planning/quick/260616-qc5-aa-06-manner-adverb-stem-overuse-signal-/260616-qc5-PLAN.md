---
phase: quick-260616-qc5
plan: 01
subsystem: ai/application
tags: [anti-ai-scent, register, manner-adverb, product-soul, aa-06]
status: complete
requires:
  - lib/features/ai/application/anti_ai_scent_processor.dart (_buildReviewSignals 7 signals, _countPhraseHits helper)
  - lib/features/editor/application/editor_ai_notifier.dart:255 (process() call site — progressText granularity)
provides:
  - "第 8 个 review signal：叠词/程度副词堆砌（_mannerAdverbStems 10 词干）"
  - "覆盖固定短语 synonym map 漏检的分布性 AI 叠词寄存器过度依赖"
affects:
  - 任何类型创作得叠词堆砌反馈；补全反AI味信号矩阵的分布性寄存器维度
tech-stack:
patterns:
  - "Sibling List<String> _mannerAdverbStems（裸词干，非固定短语）+ _countPhraseHits 复用"
  - "阈值 ≥5 fire / ≥8 high（progressText 段落级粒度校准，避免稀疏自然用词误报）"
key-files:
  modified:
    - lib/features/ai/application/anti_ai_scent_processor.dart
    - test/features/ai/application/anti_ai_scent_test.dart
decisions:
  - "新增分布性信号类型：synonym map 捕固定短语（缓缓说道），本信号捕裸词干跨任意动词的过度堆砌（缓缓起身/推门/抬手）——正交，非重复"
  - "10 词干全为 2 字叠词（缓缓/微微/淡淡/轻轻/深深/默默/静静/渐渐/隐隐/悄悄），互不子串重叠"
  - "阈值 ≥5 fire：经核验全测试集最大词干密度 1/行，零既有测试误触发；process() 粒度=progressText（单次 AI 输出段落级），≥5 叠词为真实过度依赖"
  - "精度优先（产品灵魂=不烦扰作者）：加稀疏自然用词不触发的负向测试"
requirements:
  - AA6C-MANNER-ADVERB-OVERUSE
---

# AA-06：叠词/程度副词堆砌信号（分布性 AI 寄存器过度依赖）

## 背景
`_buildReviewSignals` 现有 7 个信号（转场/类型/结尾/句长/结构/情感/描写公式），全部基于**固定短语**或**句长节奏**。无一覆盖**叠词 softener 词干的分布性过度使用**——中文 AI 文本最典型 tell 之一。`_synonymMap`（20 类 200+ 条）只捕固定短语（"缓缓说道"）；作者/AI 在任意动词上反复用同一批叠词（缓缓起身/缓缓推门/缓缓抬手）时，无信号反馈。

## Tasks
1. TDD（RED）：① 6 词干密集文本触发 '叠词/程度副词堆砌' + medium（<8 high 阈值）；② 1 词干稀疏自然文本不触发（精度负向）。
2. GREEN：加 `_mannerAdverbStems`（10 裸词干）+ `_buildReviewSignals` 末尾 count 信号（≥5 fire / ≥8 high）。
3. 验证：analyze 0 + targeted 全绿 + 全量 ≥1645（1643+2）零回归；红线：既有 7 信号不碰、信号标题不与现有冲突。

## Verification
- analyze 0；targeted（2 新 + 既有 genre/signal 测试）全绿；全量 ≥ 1645 零回归。
