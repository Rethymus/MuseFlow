---
phase: quick-260616-rj4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/presentation/providers.dart
  - lib/core/presentation/providers_ai.dart
  - lib/core/presentation/providers_knowledge.dart
  - lib/core/presentation/providers_structure.dart
  - lib/core/presentation/providers_stats.dart
  - lib/core/presentation/providers_core.dart
autonomous: true
requirements: [REFACTOR-REDLINE-800]
tags: [refactor, dart-part, redline, providers]

must_haves:
  truths:
    - "主文件 providers.dart 行数 < 800（消除 03-flutter-standards.md 红线违规）"
    - "全仓 flutter analyze 零错误（编译 114 消费方证明零行为破坏）"
    - "全测试套件绿（当前 1647 tests，providers 被全量引用）"
    - "114 个消费方 import 路径 'core/presentation/providers.dart' 与所有 provider 导出零变更"
    - "provider 间相互引用（activeAdapterProvider 读 openaiAdapterProvider/claudeAdapterProvider 等）裸名解析不变"
    - "每个 provider/Notifier 声明恰好搬一次，零逻辑改动（方法体/provider body 字符不变）"
    - "5 个 part 文件首行均为 'part of providers.dart;' 且无任何 import 语句"
  artifacts:
    - path: "lib/core/presentation/providers.dart"
      provides: "library 主文件，保留全部 import + 加 library/part 指令 + 留少量声明 + 注释 section header"
      contains: "library;"
    - path: "lib/core/presentation/providers_core.dart"
      provides: "core 域 provider 声明"
      contains: "part of 'providers.dart';"
    - path: "lib/core/presentation/providers_ai.dart"
      provides: "ai 域 provider + 2 Notifier 类声明"
      contains: "part of 'providers.dart';"
    - path: "lib/core/presentation/providers_knowledge.dart"
      provides: "knowledge 域 provider/notifier 声明"
      contains: "part of 'providers.dart';"
    - path: "lib/core/presentation/providers_structure.dart"
      provides: "story_structure + manuscript + templates 域 provider/notifier 声明"
      contains: "part of 'providers.dart';"
    - path: "lib/core/presentation/providers_stats.dart"
      provides: "stats 域 provider/notifier 声明"
      contains: "part of 'providers.dart';"
  key_links:
    - from: "lib/core/presentation/providers.dart"
      to: "5 个 providers_*.dart"
      via: "part 指令"
      pattern: "^part 'providers_[a-z]+\\.dart';$"
    - from: "5 个 providers_*.dart"
      to: "providers.dart"
      via: "part of 指令"
      pattern: "^part of 'providers\\.dart';$"
    - from: "providers_ai.dart 内 activeAdapterProvider"
      to: "openaiAdapterProvider/claudeAdapterProvider（同 part 文件）"
      via: "裸名解析（同库）"
      pattern: "ref\\.watch\((openai|claude)AdapterProvider\)"
---

<objective>
重构 lib/core/presentation/providers.dart（856 行，违反 03-flutter-standards.md「最大 800 行」红线）拆分为 5 个 feature 分组 part 文件，纯机械抽取零行为变更。

Purpose: 消除红线违规；与 260616-qxe（commit f204855 anti_ai_scent_processor part 拆分）同构；为后续 provider 维护降低单文件体积。
Output: 主文件 < 800 行 + 5 个新 part 文件，114 消费方零改动，1647 tests 零回归。
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/rules/03-flutter-standards.md
@lib/features/ai/application/anti_ai_scent_processor.dart
@lib/features/ai/application/anti_ai_scent_lexicon.dart
@lib/core/presentation/providers.dart
</context>

<interfaces>
<!-- qxe 参考模式（commit f204855，验证 part/part of 同库机制零行为变更） -->

主文件结构（anti_ai_scent_processor.dart）：
```
/// <doc comment>
library;

import 'dart:math' as math;
// ... 其他 imports

part 'anti_ai_scent_lexicon.dart';

// 类、枚举、provider 声明（方法体裸名引用同库顶级常量自动解析）
```

part 文件结构（anti_ai_scent_lexicon.dart）：
```
part of 'anti_ai_scent_processor.dart';

/// <doc comment 说明拆分原因>

// 顶级私有常量/声明（无 import）
```

关键硬约束（来自 Dart part/part of 同库机制）：
- part 文件不能有自己的 import 语句
- 主文件必须保留全部 import（否则 part 内类型解析失败）
- 同库 → provider 间相互引用裸名解析不变
- 消费方 import 路径与 provider 导出零变更

待重构 providers.dart provider 分组（executor 最终确定边界，但每个声明必须恰好搬一次）：

**core (留主文件或搬 providers_core.dart)**：fragmentRepository (94-99), settingsRepository (105-132), onboardingProgress (139-144), secureStorageService (206-208), providerRepository (213-219), providerService (224-228), activeProvider (230-233), apiKeyFuture (235-240), activeApiKey (242-245)

**ai**：openaiAdapter (252-254), claudeAdapter (260-262), activeAdapter (272-278), promptPipeline (283-304), antiAIScentProcessor (307-309), tokenBudgetCalculator (312-314), guardianContextBuilder (320-325), editorPromptPipeline (331-354), editorChapterMemoryContextBuilder (357-365), diffCalculator (371-373), selectiveUndoService (379-381), autoDeviationCheck + AutoDeviationCheckNotifier (149-173), creativityLevel + CreativityLevelNotifier (175-203)

**knowledge**：characterCardRepository (386-391), worldSettingRepository (396-401), skillRepository (403-406), characterCardNotifier (412-415), worldSettingNotifier (421-424), nameIndexService (426-428), knowledgeInjectionMiddleware (430-458), skillEnforcementMiddleware (460-467), skillGenerationService (469-483), skillGenerationNotifier (485-488), skillListNotifier (490-493), deviationDetectionService (495-508), editorialReviewService (512-526), deviationNotifier (528-531), characterRelationshipRepository (673-677), characterRelationshipNotifier (683-687)

**structure** (story_structure + manuscript + templates)：worldTemplateRepository (533-537), templateInstantiationService (539-553), templateCompletionService (555-568), openingGeneratorService (570-584), foreshadowingRepository (644-649), foreshadowingReminderService (655-658), foreshadowingNotifier (664-667), plotNodeRepository (692-697), plotNodeNotifier (703-706), nodePositionRepository (711-716), nodePositionNotifier (719-722), guardianAnnotationRepository (728-732), guardianNotifier (737-740), guardianCheckService (746-763), logicGuardianService (769-782), exportService (788-790), manuscriptRepository (797-802), chapterRepository (805-810), manuscriptNotifier (813-816), chapterNotifier (819-820), chapterAutoSave (826-831), manuscriptPurgeService (837-846), styleProfileRepository (851-856)

**stats**：writingStatsRepository (586-593), writingStatsCollector (595-602), writingStatsNotifier (604-607), tokenAuditRepository (610-615), tokenAuditService (617-625), tokenAuditNotifier (627-630), achievementService (632-634), achievementNotifier (636-639)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 5 个 part 文件并搬运对应 provider/Notifier 声明</name>
  <files>lib/core/presentation/providers_ai.dart, lib/core/presentation/providers_knowledge.dart, lib/core/presentation/providers_structure.dart, lib/core/presentation/providers_stats.dart, lib/core/presentation/providers_core.dart</files>
  <action>创建 5 个新 part 文件，每个文件结构：
- 第 1 行：`part of 'providers.dart';`（注意：'providers.dart' 不是相对路径，是 part 文件相对于主文件的名称）
- 第 2 行：空行
- 第 3 行起：简短文档注释说明拆分原因（参考 anti_ai_scent_lexicon.dart 注释风格：「Extracted from providers.dart to satisfy the 03-flutter-standards.md file-size cap. Same library — providers reference each other via bare names unchanged.」）
- 之后：按 <interfaces> 中分组的 provider/Notifier 声明逐字从 providers.dart 搬运（包括 doc comment + 完整声明体），零字符改动

5 个文件分组（严格按 <interfaces> 块执行）：
1. **providers_core.dart**：fragmentRepository / settingsRepository / onboardingProgress / secureStorageService / providerRepository / providerService / activeProvider / apiKeyFuture / activeApiKey（行 94-245 中 core 域声明）
2. **providers_ai.dart**：autoDeviationCheck + AutoDeviationCheckNotifier 类（行 149-173）、creativityLevel + CreativityLevelNotifier 类（行 175-203）、openaiAdapter / claudeAdapter / activeAdapter（行 252-278）、promptPipeline（283-304）、antiAIScentProcessor（307-309）、tokenBudgetCalculator（312-314）、guardianContextBuilder（320-325）、editorPromptPipeline（331-354）、editorChapterMemoryContextBuilder（357-365）、diffCalculator（371-373）、selectiveUndoService（379-381）
3. **providers_knowledge.dart**：characterCardRepository / worldSettingRepository / skillRepository / characterCardNotifier / worldSettingNotifier / nameIndexService / knowledgeInjectionMiddleware / skillEnforcementMiddleware / skillGenerationService / skillGenerationNotifier / skillListNotifier / deviationDetectionService / editorialReviewService / deviationNotifier / characterRelationshipRepository / characterRelationshipNotifier（行 386-531 + 673-687）
4. **providers_structure.dart**：worldTemplateRepository / templateInstantiationService / templateCompletionService / openingGeneratorService / foreshadowingRepository / foreshadowingReminderService / foreshadowingNotifier / plotNodeRepository / plotNodeNotifier / nodePositionRepository / nodePositionNotifier / guardianAnnotationRepository / guardianNotifier / guardianCheckService / logicGuardianService / exportService / manuscriptRepository / chapterRepository / manuscriptNotifier / chapterNotifier / chapterAutoSave / manuscriptPurgeService / styleProfileRepository（行 533-584 + 644-647 + 655-667 + 692-790 + 797-856）
5. **providers_stats.dart**：writingStatsRepository / writingStatsCollector / writingStatsNotifier / tokenAuditRepository / tokenAuditService / tokenAuditNotifier / achievementService / achievementNotifier（行 586-639）

硬约束：
- part 文件首行必须为 `part of 'providers.dart';`（无任何 import 语句）
- 搬运时保留完整 doc comment（`///` 行）
- 每个声明恰好搬一次，零字符改动（方法体/provider body 字符不变，包括 try/catch/throw '未配置可用的 AI 模型' 等）
- 不动 provider body 逻辑，不改成 @riverpod codegen，不改消费方
- 同库机制保证：providers_ai.dart 内 activeAdapterProvider 引用 openaiAdapterProvider/claudeAdapterProvider 裸名自动解析，无需任何调整

完成后此 task 不运行 analyze（主文件尚未加 library/part 指令，单独 part 文件无法编译）。</action>
  <verify>echo "files exist:"; ls -la lib/core/presentation/providers_{ai,knowledge,structure,stats,core}.dart; echo "---each starts with part of + has no import:"; for f in providers_ai providers_knowledge providers_structure providers_stats providers_core; do echo "== $f =="; head -1 lib/core/presentation/$f.dart; grep -c "^import " lib/core/presentation/$f.dart | xargs -I{} echo "import_count={}"; done</verify>
  <done>5 个 part 文件存在；每个首行为 `part of 'providers.dart';`；import_count=0；声明内容与原 providers.dart 对应行段字符一致</done>
</task>

<task type="auto">
  <name>Task 2: 主文件加 library+part 指令、删已搬声明、全量验证</name>
  <files>lib/core/presentation/providers.dart</files>
  <action>修改主文件 providers.dart：
1. 在文件最顶端（第 1 行 `import 'dart:convert';` 之前）插入：
   ```
   /// MuseFlow core presentation providers library.
   ///
   /// Split across 5 part files (providers_core/ai/knowledge/structure/stats.dart)
   /// to satisfy the 03-flutter-standards.md file-size cap. All providers live
   /// in the same library — consumers import this file unchanged.
   library;
   ```
   然后空行，再保留原有全部 import 块（行 1-83，约 50 个 import）零改动。**关键硬约束**：主文件必须保留全部 import，因为 part 文件不能有自己的 import。
2. 在 import 块后（即原行 83 `import 'package:museflow/features/editor/infrastructure/style_profile_repository.dart';` 之后、原行 84 `export ...` 之前），插入 5 个 part 指令：
   ```
   part 'providers_core.dart';
   part 'providers_ai.dart';
   part 'providers_knowledge.dart';
   part 'providers_structure.dart';
   part 'providers_stats.dart';
   ```
   然后空行，再保留原有 3 个 export 语句（行 84-89：contextAnchorNotifierProvider/editorProvider/editorAINotifierProvider）零改动。
3. 删除所有已搬到 5 个 part 文件的 provider/Notifier 声明（按 <interfaces> 分组清单逐段删除，包括其 doc comment）。**保留**：library 指令、全部 import、5 个 part 指令、3 个 export 语句。可选保留章节注释 header（如 `// === Core Providers ===`），但不必。
4. 保存。预期主文件 < 100 行（仅 library + imports + parts + exports）。

不得：
- 改任何 import 路径
- 改任何 export 语句
- 改任何 provider body 逻辑
- 把 provider 改成 @riverpod codegen
- 改 114 个消费方

然后运行全量验证（见 verify）。</action>
  <verify>echo "=== line count ==="; wc -l lib/core/presentation/providers.dart; echo "=== grep library + part directives ==="; grep -nE "^library|^part 'providers_" lib/core/presentation/providers.dart; echo "=== flutter analyze (FULL repo, expect 0 issues) ==="; flutter analyze 2>&1 | tail -20; echo "=== flutter test (FULL, expect 1647 passing) ==="; flutter test 2>&1 | tail -10; echo "=== git diff touched files ==="; git diff --name-only; git status --short</verify>
  <done>providers.dart < 800 行；含 `library;` 与 5 个 `part 'providers_xxx.dart';` 指令；保留全部 import + 3 个 export；flutter analyze 零错误零警告；flutter test 全绿（1647 passing）；git diff --name-only 仅 providers.dart + 5 个新 part 文件，114 消费方零改动</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 重构边界 | 同库 part 拆分，无新信任边界（纯机械搬运，零行为变更） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-rj4-01 | Tampering | provider 声明搬运过程 | mitigate | 每个 provider body 字符零改动（Task1 验证 head/grep + Task2 analyze 全仓 0 + 1647 tests 绿） |
| T-rj4-02 | Information Disclosure | 114 消费方导入路径 | accept | part/part of 同库机制保证导入路径与 provider 导出零变更（qxe commit f204855 已验证同模式） |
| T-rj4-03 | Denial of Service | 主文件遗漏 import 导致 part 类型解析失败 | mitigate | Task2 action 硬约束「主文件保留全部 ~50 import」，flutter analyze 编译全部消费方即证明 |
| T-rj4-SC | Tampering | 无 npm/pip/cargo 安装（纯 Dart 代码重构） | accept | 无第三方包安装步骤，slopcheck 不适用 |
</threat_model>

<verification>
## 整体验证（Task 2 末尾完成）

1. **行数**：`wc -l lib/core/presentation/providers.dart` → < 800
2. **指令**：`grep -nE "^library|^part 'providers_" providers.dart` → 1 library + 5 part
3. **保留 import/export**：`grep -c "^import " providers.dart` ≈ 50；`grep -c "^export " providers.dart` = 3
4. **part 文件合规**：5 个 part 文件首行 `part of 'providers.dart';`，无 import
5. **flutter analyze**（全仓无路径参数）：零错误零警告
6. **flutter test**（全量）：1647 passing（零回归）
7. **git diff --name-only**：仅 providers.dart + 5 个新 providers_*.dart，114 消费方零改动
8. **行为不变**：providers_ai.dart 内 activeAdapterProvider 仍能 `ref.watch(openaiAdapterProvider)`（同库裸名解析）
</verification>

<success_criteria>
- [ ] providers.dart < 800 行（消除红线违规）
- [ ] 5 个 part 文件存在，首行 `part of 'providers.dart';`，无 import
- [ ] 主文件含 `library;` + 5 个 `part` 指令 + 全部 import + 3 个 export
- [ ] 每个 provider/Notifier 声明恰好搬一次，零字符改动
- [ ] flutter analyze 全仓 0 issues
- [ ] flutter test 全量 1647 passing（零回归）
- [ ] git diff 仅触 providers.dart + 5 新 part 文件，114 消费方零改动
</success_criteria>

<output>
Create `.planning/quick/260616-rj4-providers-dart-feature-part/260616-rj4-SUMMARY.md` when done
</output>
