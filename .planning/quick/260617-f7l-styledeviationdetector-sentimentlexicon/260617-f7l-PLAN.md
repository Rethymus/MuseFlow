---
phase: quick-260617-f7l
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/features/editor/application/style_deviation_detector_test.dart
  - lib/features/editor/application/style_deviation_detector.dart
autonomous: true
requirements: [AA-CONSISTENCY]
tags: [anti-ai-scent, style-deviation, sentiment-lexicon, refactor]

must_haves:
  truths:
    - "StyleDeviationDetector._computeEmotionalTone 与 StyleAnalyzer._computeEmotionalTone 对同一文本产出完全一致的 warmth/intensity/overall"
    - "StyleDeviationDetector 不再自带任何内联情感词表（positives/negatives Set 全部删除）"
    - "StyleDeviationDetector 不再自带 warmth/intensity/classifyTone 公式（全部委托 SentimentLexicon）"
    - "emotionTone 维度偏差分不再因内联表漂移而被扭曲（与权威方同源后偏差分稳定）"
    - "现有 detector 测试（aiScentScore 范围、emotionalTone explanation 含 平淡、clamp 0-1）在新公式下仍语义正确"
  artifacts:
    - path: "lib/features/editor/application/style_deviation_detector.dart"
      provides: "复用 SentimentLexicon 的 _computeEmotionalTone，删除内联表与重复公式"
      contains: "import 'package:museflow/features/editor/infrastructure/sentiment_lexicon.dart'"
    - path: "test/features/editor/application/style_deviation_detector_test.dart"
      provides: "lexicon 一致性 RED→GREEN 回归测试"
      contains: "should compute emotionalTone consistent with StyleAnalyzer"
  key_links:
    - from: "lib/features/editor/application/style_deviation_detector.dart (_computeEmotionalTone)"
      to: "lib/features/editor/infrastructure/sentiment_lexicon.dart"
      via: "SentimentLexicon.countPositive/countNegative/warmthScore/intensityScore/classifyTone"
      pattern: "SentimentLexicon\\.(countPositive|countNegative|warmthScore|intensityScore|classifyTone)"
    - from: "lib/features/editor/application/style_deviation_detector.dart (_computeEmotionalTone)"
      to: "lib/features/editor/application/style_analyzer.dart (_computeEmotionalTone)"
      via: "同源 SentimentLexicon 公式"
      pattern: "SentimentLexicon\\.warmthScore"
---

<objective>
消除 StyleDeviationDetector 的情感基调分析双量尺缺陷：把自带的 42 词内联表（positives 22 / negatives 20）+ 自创 warmth/intensity 公式替换为共享 SentimentLexicon，使偏差检测方与 profile 构建权威方（StyleAnalyzer）完全同源。

Purpose: 反AI味是产品灵魂，emotionalTone 维度偏差分是该灵魂的核心信号之一。近期 3 个 bug（260617-05c dup 双计、260617-1uk 裸 爱/恨 子串过计与极性反转）都是内联表持续漂移的征兆——根因是 detector 自带一份与权威方不一致的情感词表+公式。统一到 SentimentLexicon 后，偏差检测的"基准"与"测量"使用同一把尺子，从根本上杜绝此类漂移。

Output: 单文件改造 style_deviation_detector.dart（删 ~90 行内联表+公式，加 1 行 import，改写 _computeEmotionalTone 为 ~10 行委托调用）+ 回归测试，TDD RED→GREEN。
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@lib/features/editor/application/style_deviation_detector.dart
@lib/features/editor/application/style_analyzer.dart
@lib/features/editor/infrastructure/sentiment_lexicon.dart
@lib/features/editor/domain/author_style_profile.dart
@test/features/editor/application/style_deviation_detector_test.dart

<interfaces>
<!-- 权威方 StyleAnalyzer._computeEmotionalTone 的完整实现（style_analyzer.dart:254-272） -->
<!-- StyleDeviationDetector._computeEmotionalTone 改造后必须与此完全同源（除了 CJK 计数工具名差异） -->

From lib/features/editor/application/style_analyzer.dart:254-272:
```dart
EmotionalTone _computeEmotionalTone(String text) {
  final positiveCount = SentimentLexicon.countPositive(text);
  final negativeCount = SentimentLexicon.countNegative(text);
  final totalCjk = _cjkCharCount(text);

  final warmth = SentimentLexicon.warmthScore(positiveCount, negativeCount);
  final intensity = SentimentLexicon.intensityScore(
    positiveCount,
    negativeCount,
    totalCjk,
  );
  final overall = SentimentLexicon.classifyTone(warmth, intensity);

  return EmotionalTone(
    overall: overall,
    warmth: warmth,
    intensity: intensity,
  );
}
```

From lib/features/editor/infrastructure/sentiment_lexicon.dart (SentimentLexicon 公共 API):
```dart
class SentimentLexicon {
  static const Set<String> positiveWords;   // ~120 词，含 温馨/慈爱/相伴/相守/携手/拥抱/依偎/呵护/守护
  static const Set<String> negativeWords;   // ~120 词
  static int countPositive(String text);    // indexOf 循环计数（非 allMatches，安全）
  static int countNegative(String text);
  static double warmthScore(int positiveCount, int negativeCount);  // pos/(pos+neg)，0 计数→0.5
  static double intensityScore(int positiveCount, int negativeCount, int totalChars);  // <100 字→0.3；密度/4 clamp
  static String classifyTone(double warmth, double intensity);  // 平静温和/冷静克制/热烈奔放/温暖明亮/沉重压抑/冷淡疏离/张力充沛/张弛有度
}
```

StyleDeviationDetector 现有 CJK 工具方法（保留，无需改动）：
```dart
List<String> _extractCjkChars(String text);  // line 565-575
// 注意：StyleAnalyzer 用 _cjkCharCount (返回 int)，detector 用 _extractCjkChars(...).length
// 改造时用 _extractCjkChars(text).length 作为 totalCjk 传入 intensityScore
```

待删除的 3 个私有方法（style_deviation_detector.dart）：
- _countPositiveSentiment (line 459-492, 22 词 inline Set)
- _countNegativeSentiment (line 494-525, 20 词 inline Set)
- _classifyTone (line 527-533, 5 类自创分类，与 SentimentLexicon.classifyTone 8 类不同)

待改写的方法：
- _computeEmotionalTone (line 437-457, 自创 warmth/intensity 公式 + 调用上述 3 个私有方法)
</interfaces>

<root_cause>
StyleDeviationDetector._computeEmotionalTone (style_deviation_detector.dart:437) 自带 42 词内联表（positives 22 / negatives 20）+ 自创 warmth/intensity 公式，而构建 profile 的权威方 StyleAnalyzer._computeEmotionalTone (style_analyzer.dart:254) 用 infrastructure/SentimentLexicon（~240 词 + ratio/density 公式）。两套不一致量尺比较 → emotionalTone 维度偏差分系统扭曲（反AI味=产品灵魂核心信号）。这是近期 3 个 bug（dup 双计 commit 9122b01 / 裸爱过计 / 裸恨极性反转 commit 034b529）的共同根因——内联表持续漂移。注释 437 行自承 "// Simplified emotion analysis without sentiment lexicon dependency"。

architecture_note: style_analyzer.dart:19 同处 application 层已 `import '...infrastructure/sentiment_lexicon.dart'` 且 flutter analyze 零错误通过，证明此 lexicon 是纯 Dart const 数据类（无 Flutter/平台依赖），application→infrastructure 的"依赖方向"在 02-museflow-architecture.md 语境下对此 const 数据类是已建立先例（非真 infrastructure 副作用）。StyleDeviationDetector 同样 import 合规。
</root_cause>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): 添加 detector emotionalTone 与 StyleAnalyzer 一致性的失败回归测试</name>
  <files>test/features/editor/application/style_deviation_detector_test.dart</files>
  <behavior>
    - Test 1 "should compute emotionalTone consistent with StyleAnalyzer on lexicon-only positive words":
      构造文本 = "她的微笑温馨慈爱。两人相伴相守，携手走过四季。他守护着她，依偎在春风里。"
      （密集出现 SentimentLexicon 独有词 温馨/慈爱/相伴/相守/携手/守护/依偎/春风/微笑，且不含任何内联表 22 词如 温暖/幸福/快乐 等）
      调用 detector.analyze(text, profile)，取 emotionalTone 维度的 DimensionDeviation
      断言：toneDev.textValue（即 detector 测出的 intensity）应与 SentimentLexicon 直接对该文本的测量一致——即 positiveCount>=5（命中 温馨/慈爱/相伴/相守/携手/守护/微笑/春风 中多个），intensity 高于中性带（> 0.4）。
      具体：通过 StyleAnalyzer 同源公式反推——先用 SentimentLexicon.countPositive(text) 拿到 expectedPositiveCount，断言 expectedPositiveCount >= 5（保证 fixture 强制 RED：内联表 0 命中，lexicon >=5 命中），再断言 toneDev.textValue > 0.35（detector 用 lexicon 后 intensity 会因 positive 命中而升高；pre-fix 内联表 0 命中 → intensity 由 _extractCjkChars 公式算出接近 0.5 中性带或更低 → RED）。
      reason 字段必须说明：pre-fix detector 用 22 词内联表漏掉 温馨/慈爱/相伴/相守/携手/守护/依偎/微笑/春风，positiveCount=0 → intensity 偏低；fix 后复用 SentimentLexicon 命中 ≥5 → intensity 升高 → 与 StyleAnalyzer 对同文本测量一致。

    - Test 2 "should not carry any inline sentiment word table after refactor (structural guard)":
      这个测试在 GREEN 阶段作为护栏，确保 detector 文件不再含 _countPositiveSentiment/_countNegativeSentiment/_classifyTone 三个私有方法。
      由于无法直接 grep 生产代码，改用行为间接验证：构造一段纯中性文本（"今天天气不错。我们去公园散步。路上看到很多树。"）断言 detector 测出的 emotionalTone warmth 等于 SentimentLexicon.warmthScore(SentimentLexicon.countPositive(text), SentimentLexicon.countNegative(text))（精确浮点相等，因为 detector 应直接调用此函数）。pre-fix detector 用自创公式 ((pos-neg)/(cjk*0.05+1)*0.5+0.5)，中性文本 pos=neg=0 → 0.5；lexicon.warmthScore(0,0) 也=0.5 → 这个 case 巧合相等不 RED。
      因此 Test 2 用非中性文本加强：文本 = "温暖阳光下的幸福时光。"（内联表与 lexicon 都含 温暖/幸福/阳光/时光——阳光/时光 不在任何表中，只有 温暖/幸福 命中）。pre-fix detector warmth = ((2-0)/(cjk*0.05+1)*0.5+0.5).clamp；lexicon warmthScore(2,0)=1.0。两公式不等 → RED。post-fix detector 直接调用 warmthScore → =1.0 → GREEN。
      断言：toneDev for warmth 维度 — 由于 detector 不直接暴露 warmth，需通过 explanation 或 deviationScore 间接。改为：构造两个 detector 实例分析同文本，断言 emotionalTone deviation 的 profileValue 与 textValue 差值与 StyleAnalyzer 一致。简化为：断言 toneDev.textValue == SentimentLexicon.intensityScore(countPositive, countNegative, cjkLen) 精确相等（double closeTo 容差 1e-9）。

    最终采用 Test 1（lexicon-only 词强制 RED）+ Test 2（公式同源精确相等强制 RED）双保险。Test 1 是主防线，Test 2 是结构同源防线。
  </behavior>
  <action>
    在 test/features/editor/application/style_deviation_detector_test.dart 的 main() 内、现有 'sentiment substring overcount regression' group 之后，新增 group('sentiment lexicon consistency (260617-f7l)')。

    在文件顶部增加 import：
    - import 'package:museflow/features/editor/infrastructure/sentiment_lexicon.dart';

    Test 1 实现：
    - test('should compute emotionalTone using SentimentLexicon, not an inline table (lexicon-only words must register)', () { ... })
    - 构造 const text = '她的微笑温馨慈爱。两人相伴相守，携手走过四季。他守护着她，依偎在春风里。'
    - final profile = _testProfile(warmth: 0.5, intensity: 0.5);  // 中性 profile 让偏差分主要反映 textValue
    - final result = detector.analyze(text: text, profile: profile);
    - final toneDev = result.deviations.firstWhere((d) => d.dimension == StyleDimension.emotionalTone);
    - final expectedPositive = SentimentLexicon.countPositive(text);
    - expect(expectedPositive, greaterThanOrEqualTo(5), reason: 'fixture 必须命中 lexicon 独有词 温馨/慈爱/相伴/相守/携手/守护/依偎/微笑/春风 中至少 5 个');
    - expect(toneDev.textValue, greaterThan(0.35), reason: 'pre-fix 内联表 0 命中 → intensity ~0.5 中性或更低；post-fix 复用 SentimentLexicon，positive>=5 → intensity 升高与 StyleAnalyzer 一致');

    Test 2 实现（公式同源精确相等）：
    - test('should compute intensity via SentimentLexicon.intensityScore (same formula as StyleAnalyzer)', () { ... })
    - const text = '温暖阳光下的幸福时光，微风拂面，岁月静好。我们走在小路上，看着远方的山。'
    - final profile = _testProfile();
    - final result = detector.analyze(text: text, profile: profile);
    - final toneDev = result.deviations.firstWhere((d) => d.dimension == StyleDimension.emotionalTone);
    - final cjkLen = text.runes.where((r) => (r>=0x4E00 && r<=0x9FFF) || (r>=0x3400 && r<=0x4DBF) || (r>=0x3000 && r<=0x303F)).length;
    - final expectedIntensity = SentimentLexicon.intensityScore(SentimentLexicon.countPositive(text), SentimentLexicon.countNegative(text), cjkLen);
    - expect(toneDev.textValue, closeTo(expectedIntensity, 1e-9), reason: 'detector 必须复用 SentimentLexicon.intensityScore 公式，与 StyleAnalyzer 同源；pre-fix 用自创 (pos+neg)/(cjk*0.03+1)*0.5+0.5 公式 → 不等');

    运行测试验证 RED（两条新测试应失败，其余现有测试仍应通过或保持现状——此时还没改生产代码）。

    注意：若运行后发现 Test 1 的断言门槛 0.35 在 pre-fix 下也通过（巧合），需提高门槛到 lexicon 公式实际产出值。pre-fix 内联表对此文本 0 命中，公式 intensity = (0+0)/(cjk*0.03+1)*0.5+0.5 = 0.5；post-fix lexicon intensity = (positiveCount+negativeCount)/(cjk/100) / 4 clamp。用 expectedPositive>=5 保证 post-fix 明显 > 0.5（5/40char *100=12.5 /4=3.125 clamp 1.0 或类似高值），门槛 0.35 足够区分。若 cjk 不足 100（intensityScore 返回 0.3），需把文本扩到 >=100 CJK 字。检查文本 CJK 字数，必要时扩写 fixture 到 >=100 CJK 字（加更多中性描述句）确保 intensityScore 走密度公式而非 <100 早返回 0.3。这是关键：fixture 必须让 intensityScore 不走 early-return 分支，否则 Test 1 的 post-fix intensity 恒为 0.3，门槛 0.35 永远 RED——错误。
  </action>
  <verify>
    <automated>flutter test test/features/editor/application/style_deviation_detector_test.dart --plain-name "sentiment lexicon consistency"</automated>
    预期：2 tests FAILED（RED）。同时确认现有 13+2=15 测试不受影响（此任务未改生产代码，全量测试 1649+2 仍绿，仅 2 新测试红）。完整命令：flutter test test/features/editor/application/style_deviation_detector_test.dart 应显示 17 total（15 pass + 2 fail）。
  </verify>
  <done>
    2 个新测试存在并以 RED 失败（断言明确指向 SentimentLexicon 公式一致性，而非巧合）。现有 15 测试零回归。git commit: "test(quick-260617-f7l): add failing regression for detector lexicon consistency"。
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN + REFACTOR): 改造 _computeEmotionalTone 复用 SentimentLexicon，删除内联表与公式</name>
  <files>lib/features/editor/application/style_deviation_detector.dart</files>
  <behavior>
    - 改造后 _computeEmotionalTone(text) 必须与 StyleAnalyzer._computeEmotionalTone 对同文本产出 byte-for-byte 相同的 EmotionalTone（warmth/intensity/overall 三字段）
    - 不再存在 _countPositiveSentiment / _countNegativeSentiment / _classifyTone 三个私有方法
    - 不再存在任何 inline sentiment Set 字面量
    - 现有 15 测试（13 base + 2 裸单字回归）在新公式下仍语义正确：若 aiScentScore < 50 / explanation 含 平淡 / clamp 0-1 等断言因数值变化失败，按"明显AI味→高分 / 一致→低分"语义调整预期值，不为通过测试扭曲语义
  </behavior>
  <action>
    Step A — 加 import：
    在 style_deviation_detector.dart 顶部 import 块（line 13-16 后）加：
    import 'package:museflow/features/editor/infrastructure/sentiment_lexicon.dart';

    Step B — 改写 _computeEmotionalTone（替换 line 437-457）：
    删除自创 warmth/intensity 公式与 _classifyTone 调用，改为完全镜像 style_analyzer.dart:254-272：
    EmotionalTone _computeEmotionalTone(String text) {
      final positiveCount = SentimentLexicon.countPositive(text);
      final negativeCount = SentimentLexicon.countNegative(text);
      final totalCjk = _extractCjkChars(text).length;

      final warmth = SentimentLexicon.warmthScore(positiveCount, negativeCount);
      final intensity = SentimentLexicon.intensityScore(
        positiveCount,
        negativeCount,
        totalCjk,
      );
      final overall = SentimentLexicon.classifyTone(warmth, intensity);

      return EmotionalTone(
        overall: overall,
        warmth: warmth,
        intensity: intensity,
      );
    }
    （注意：detector 用 _extractCjkChars(text).length 而非 StyleAnalyzer 的 _cjkCharCount(text)，因 detector 无此静态方法；两者算法等价——都是 CJK Unified + Ext A + CJK Symbols 的 runes 计数。）

    Step C — 删除 3 个私有方法（line 459-533）：
    - _countPositiveSentiment (含 22 词 inline Set 与 NOTE 注释)
    - _countNegativeSentiment (含 20 词 inline Set 与 NOTE 注释)
    - _classifyTone (5 类自创分类，与 SentimentLexicon.classifyTone 8 类不同)
    全部删除，不留任何残留 inline sentiment 数据。

    Step D — 删除已无引用的注释：
    原 _computeEmotionalTone 内 "// Simplified emotion analysis without sentiment lexicon dependency" 注释一并删除（不再简化、已有依赖）。

    Step E — 运行 GREEN 验证：
    flutter test test/features/editor/application/style_deviation_detector_test.dart
    预期 Task 1 的 2 新测试转 GREEN。

    Step F — 处理现有测试数值变化（按语义调整，不扭曲）：
    运行全 detector 测试，若以下断言因公式切换失败，按语义处理：
    1. "should produce low AI-scent for text matching author style" — 断言 aiScentScore < 50。若新公式下分数仍 < 50 则无需改；若因 emotionalTone 维度偏差增大导致超 50，检查 fixture 文本——"林风走进院子。月光洒在青石板上..." 是中性偏冷文本，profile warmth=0.6 intensity=0.4，新公式 detector 测出 warmth/intensity 与 profile 偏差应在合理范围。若确实超 50 且语义上文本确实"匹配作者风格"，可放宽到 < 60（在 reason 注释说明"lexicon 统一后 emotionalTone 维度数值变化"）；若超 60 说明 fixture 不再"匹配"，需调整 profile 参数使其匹配。优先调整 profile 而非断言门槛，保持测试语义。
    2. "should detect flat emotion curve (AI pattern)" — 断言 explanation contains '平淡'。新公式 intensityScore 对该 fixture（"林风走在路上。路边的树木郁郁葱葱..."）会算出什么？该文本无明显情感词，positiveCount=negativeCount=0 → intensityScore(cjk<100?0.3 : 0/... =0)。若 cjk<100 intensity=0.3 < 0.3 阈值不成立 → isFlat (intensity>0.35 && <0.65) 为 false → explanation 不含"平淡"。此时需检查 detector._analyzeEmotionalTone 的 isFlat 判断逻辑（line 324）：isFlat = textTone.intensity > 0.35 && textTone.intensity < 0.65。新公式 intensity=0.3 → isFlat=false → 走其他分支。此时应调整 fixture 让文本 CJK 字数 >=100 且无情感词 → intensityScore 走密度公式 =0/... =0 → 仍 <0.35。问题：detector 的 "flat emotion" 检测依赖 intensity 在 0.35-0.65 中性带，但 SentimentLexicon.intensityScore 对无情感词文本返回接近 0（密度公式 0/4=0）。这是公式语义差异：旧公式对 0 计数返回 0.5（中性带），新公式返回 0（低强度）。detector 的 isFlat 逻辑（0.35-0.65）是基于旧公式 0.5 中性带设计的。需同步调整 detector._analyzeEmotionalTone 的 isFlat 逻辑以适配新公式语义——但这超出本任务"仅复用 lexicon"范围。决策：isFlat 的语义是"情感曲线平淡，缺乏起伏"，对应 SentimentLexicon 的"低 intensity"（< 0.3）。把 isFlat 判断改为 intensity < 0.3（与新公式"无情感词→intensity 0"一致），相应 explanation 分支语义不变。这是 GREEN 阶段必要的语义适配，非扭曲。
    3. "should generate appropriate summary" / "should produce no deviations summary for matching text" — 依赖 hasDeviations 与 summary 文案，这些逻辑在 _analyzeEmotionalTone 外，不受公式直接影响。若失败检查是否 emotionalTone deviationScore 变化导致 hasDeviations 翻转，按语义调整 profile 参数。
    4. "deviation scores should be clamped to 0-1 range" — 新公式 warmth/intensity 本身已 clamp 0-1（warmthScore 用 .clamp(0,1)，intensityScore 用 .clamp(0,1)），此测试应继续通过。
    5. 现有 2 个裸单字回归测试（260617-1uk）——断言 toneDev.textValue < 0.6 对"恨不得×3"和"可爱/爱好/爱情"。新公式：这些文本 positiveCount=negativeCount=0（lexicon 不含裸 恨/爱，也不含 恨不得/可爱/爱好/爱情 复合词）→ warmthScore(0,0)=0.5，intensityScore(0,0,cjk)=cjk<100?0.3:0。两 fixture 都 <100 CJK → intensity=0.3 < 0.6 → 测试继续 GREEN。✓

    调整原则（重申）：优先调整 fixture 文本或 profile 参数让测试语义在新公式下成立，其次才考虑放宽断言门槛。每处调整在测试代码加注释说明"lexicon 统一后数值变化（260617-f7l）"。绝不通过删除断言、改为 tautology（expect(x, anything)）等方式让测试空转。

    Step G — REFACTOR（可选）：检查 _computeEmotionalTone 是否能进一步简化（已 10 行最简），_extractCjkChars 仍被 _analyzeVocabulary 等多处复用，不抽取。

    运行 flutter analyze 确认零 error/warning。
  </action>
  <verify>
    <automated>flutter analyze lib/features/editor/application/style_deviation_detector.dart test/features/editor/application/style_deviation_detector_test.dart && flutter test test/features/editor/application/style_deviation_detector_test.dart</automated>
    预期：analyze 0 issues；全 17 测试 GREEN（13 base + 2 裸单字 + 2 lexicon consistency）。若 Step F 调整了现有测试预期值，记录在 SUMMARY。
    额外验证 grep 护栏（确认内联表彻底删除）：
    grep -c "_countPositiveSentiment\|_countNegativeSentiment\|_classifyTone\|positives = \|negatives = " lib/features/editor/application/style_deviation_detector.dart → 应为 0
    grep -c "SentimentLexicon" lib/features/editor/application/style_deviation_detector.dart → 应 >= 5（import + 5 处调用）
  </verify>
  <done>
    _computeEmotionalTone 完全镜像 StyleAnalyzer 同源实现；3 个私有方法+内联表+NOTE 注释全部删除；新增 import；17 测试 GREEN；analyze 0；grep 护栏通过。git commits: "fix(quick-260617-f7l): unify detector emotionalTone to SentimentLexicon"（GREEN）+ 若有测试预期调整 "test(quick-260617-f7l): adjust expectations for lexicon-unified formula"。
  </done>
</task>

<task type="auto">
  <name>Task 3: 全量回归验证（确认 lexicon 统一无副作用）</name>
  <files>(无文件改动，仅验证)</files>
  <action>
    运行全量测试套件确认零回归。detector 改造可能间接影响：
    - 任何消费 StyleDeviationResult.aiScentScore 或 emotionalTone deviation 的 widget/integration 测试（如 style_thermometer_dashboard、editor_ai_notifier、synthesis_notifier 中涉及 deviation 的路径）
    - profile 持久化测试（emotionalTone 字段格式不变，应无影响）

    命令：flutter test（全量，预期 ~1649 tests + 2 新增 = ~1651）
    若有失败：
    1. 定位失败测试是否消费 detector emotionalTone/aiScentScore
    2. 若是数值断言失败（如 dashboard 显示特定分数），按 Task 2 Step F 原则调整预期值——这些是 lexicon 统一的下游数值变化，非回归
    3. 若是结构性失败（如 widget 不渲染、抛异常），需排查——但本任务仅改 _computeEmotionalTone 内部，返回类型 EmotionalTone 不变，理论上无结构性影响

    若全量测试 context 太重，退化为分层验证：
    - flutter test test/features/editor/（editor feature 全量）
    - flutter analyze（全仓，确认零 error）
    记录实际运行的命令与结果到 SUMMARY。
  </action>
  <verify>
    <automated>flutter test test/features/editor/ 2>&1 | tail -5 && flutter analyze 2>&1 | tail -3</automated>
    预期：editor feature 全量测试全绿（含 detector 17 + analyzer + profile + dashboard 等）；analyze 0 issues。
  </verify>
  <done>
    editor feature 全量测试零回归；analyze 0；lexicon 统一改造的下游影响已确认无破坏。最终 commit（若有测试调整）合入 Task 2 commit 或独立 "test(quick-260617-f7l): align downstream expectations"。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 本任务是纯内部重构（统一两份情感词表为一份），不引入新外部输入/输出/API。所有文本输入仍来自编辑器内已信任的 AI 生成文本，无新攻击面。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f7l-01 | Tampering | 情感词表（SentimentLexicon const Set） | accept | 词表是编译期 const 数据类，非用户输入，无运行时篡改面。统一后反而减少篡改面（从 2 份词表→1 份）。 |
| T-f7l-02 | Information Disclosure | emotionalTone 维度偏差分 | mitigate | 偏差分通过统一量尺获得语义正确性，减少误导性 disclosure（此前双量尺偏差分扭曲可能误导作者以为文本有 AI 味而实则没有，反之亦然——这是认知层面的"信息 disclosure 不准确"，统一后 mitigated）。 |
| T-f7l-SC | N/A | 无 npm/pip/cargo 安装 | accept | 纯 Dart 代码改造，无包管理器操作，slopcheck 不适用。 |

</threat_model>

<verification>
- [ ] Task 1: 2 新测试存在并 RED（flutter test 显示 15 pass + 2 fail）
- [ ] Task 2: _computeEmotionalTone 镜像 StyleAnalyzer；3 私有方法+内联表删除；import 添加；17 测试 GREEN；analyze 0
- [ ] Task 2 grep 护栏：grep -c "_countPositiveSentiment\|_countNegativeSentiment\|_classifyTone" style_deviation_detector.dart == 0；grep -c "SentimentLexicon" >= 5
- [ ] Task 3: editor feature 全量测试零回归；全仓 analyze 0
- [ ] TDD 纪律：RED commit 在 GREEN commit 之前（可验证 git log 顺序）
</verification>

<success_criteria>
**必须达成（MUST）：**
1. StyleDeviationDetector._computeEmotionalTone 与 StyleAnalyzer._computeEmotionalTone 对任意文本产出完全相同的 EmotionalTone（warmth/intensity/overall 三字段浮点相等）
2. style_deviation_detector.dart 中不再存在任何内联情感词表（positives/negatives Set）或自创 warmth/intensity/classifyTone 公式
3. 现有 detector 测试在新公式下语义正确（允许调整 fixture/profile/预期值，但不得删除断言或改为 tautology）
4. flutter analyze 零 error；editor feature 全量测试零回归

**应当达成（SHOULD）：**
5. 全量测试套件 ~1651 tests 零回归
6. SUMMARY 记录所有测试预期调整的理由（lexicon 统一数值变化）

**不会做（WON'T）：**
- 不改 SentimentLexicon 本身（权威方词表保持不变）
- 不改 StyleAnalyzer（权威方已正确）
- 不改 EmotionalTone 域类（数据结构不变）
- 不重构 _analyzeEmotionalTone 的 isFlat/explanation 逻辑（仅在新公式语义下做必要适配，见 Task 2 Step F.2）
- 不引入新依赖（SentimentLexicon 已在 pubspec 内）
</success_criteria>

<output>
Create `.planning/quick/260617-f7l-styledeviationdetector-sentimentlexicon/260617-f7l-SUMMARY.md` when done. SUMMARY 必须记录：
1. RED→GREEN TDD 轨迹（commit hash 序列）
2. 删除的代码行数（内联表+3 方法 ≈ 90 行）vs 新增（import + 10 行委托）
3. Task 2 Step F 中所有测试预期调整的具体清单与理由
4. 全量/editor feature 测试结果（通过数、是否有回归）
5. grep 护栏结果
6. 对 STATE.md Quick Tasks Completed 表的追加条目建议（描述、commit、影响面）
</output>
