---
phase: quick-260616-ci2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart
  - lib/features/ai/application/prompt_pipeline.dart
  - test/features/ai/application/dialogue_act_middleware_test.dart
autonomous: true
requirements: [CI-01-WIRING]
tags: [paths, dialogue-act, prompt-pipeline, response-strategy, ai]

must_haves:
  truths:
    - "DialogueActMiddleware 实现 PromptMiddleware 契约（const 构造 + apply）"
    - "additionalInstruction 为 null/空时 no-op（首次 synthesis 无 regenerate instruction）"
    - "confidence == 0 的纯模糊消息 no-op（不注入噪音，尊重 classifier 默认深度设计）"
    - "followUp 行为（无论是否有信号）no-op（追问深入走默认深度，_strategies 无此键）"
    - "styleAdjustment/contentExploration/intentRevision/injection 四行为各注入中文响应策略系统消息"
    - "管线注册：DialogueActMiddleware 紧随 SystemPromptMiddleware 之后"
    - "红线：dialogue_act_classifier.dart / dialogue_act.dart 零改动；synthesis_notifier 零改动"
  artifacts:
    - path: "lib/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart"
      provides: "DialogueActMiddleware（PATHs 响应策略路由 middleware）"
      contains: "class DialogueActMiddleware extends PromptMiddleware"
    - path: "lib/features/ai/application/prompt_pipeline.dart"
      provides: "管线注册 DialogueActMiddleware"
      contains: "DialogueActMiddleware"
    - path: "test/features/ai/application/dialogue_act_middleware_test.dart"
      provides: "8 单元测试覆盖四行为注入 + 三种 no-op + 管线集成"
      contains: "DialogueActMiddleware"
  key_links:
    - from: "dialogue_act_middleware.dart apply()"
      to: "DialogueActClassifier.classify"
      via: "const DialogueActClassifier().classify(instruction)"
      pattern: "DialogueActClassifier\\(\\)\\.classify"
    - from: "prompt_pipeline.dart withDefaultMiddlewares middlewares 列表"
      to: "DialogueActMiddleware"
      via: "const DialogueActMiddleware()（SystemPromptMiddleware 之后）"
      pattern: "const DialogueActMiddleware\\(\\)"
---

<objective>
CI-01 DialogueAct→prompt 响应策略路由 wiring：把已交付的纯逻辑 `DialogueActClassifier`（260614-ci1，9 测试全绿，全库零消费方）接入 prompt pipeline，完成 PATHs（对话行为→响应策略）端到端闭环。

用户在 synthesis regenerate 时给自由文本 instruction，按其对话行为（风格调整/内容探索/意图修订/内容注入/追问）适配系统 prompt 的响应策略——让 AI 不再把每次 refine 当成一回事，而是按作者意图调整响应方式。

Purpose: 260614-ci1 交付了 classifier 但悬空无消费方，本 plan 是缺失的闭环。
Output: 3 文件（1 新 middleware + 1 行管线注册 + 1 测试文件），零回归。
</objective>

<execution_context>
@/home/re/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# 已交付契约（红线只读：禁止改 classifier / dialogue_act.dart）
@lib/features/ai/application/dialogue_act_classifier.dart
@lib/features/ai/domain/dialogue_act.dart
# 接入点（红线只读：仅追加 import + middlewares 列表插入一行）
@lib/features/ai/application/prompt_pipeline.dart
@lib/features/ai/application/prompt_middlewares/system_prompt_middleware.dart
</context>

<interfaces>
<!-- 红线只读：PromptMiddleware 契约 -->
From lib/features/ai/application/prompt_pipeline.dart:
```dart
abstract class PromptMiddleware {
  const PromptMiddleware();
  PromptContext apply(PromptContext context);
}
// PromptContext.additionalInstruction (String?) = regenerate 传入的用户自由文本
// PromptContext.addMessage(ChatMessage) → 追加消息的新 context
```

<!-- 红线只读：DialogueActClassifier API（纯消费，不改）-->
From lib/features/ai/application/dialogue_act_classifier.dart:
```dart
class DialogueActClassification {
  final DialogueAct act;
  final List<String> matchedKeywords;
  final double confidence;  // [0,1]，无任何匹配 = 0.0
  const DialogueActClassification({required this.act, required this.matchedKeywords, required this.confidence});
}
class DialogueActClassifier {
  const DialogueActClassifier();
  DialogueActClassification classify(String message);
}
```

<!-- 红线只读：DialogueAct enum + .label -->
From lib/features/ai/domain/dialogue_act.dart:
```dart
enum DialogueAct {
  styleAdjustment('风格调整'),
  contentExploration('内容探索'),
  intentRevision('意图修订'),
  followUp('追问深入'),
  injection('内容注入');
  final String label;
}
```

<!-- 接入点：prompt_pipeline.dart withDefaultMiddlewares（line 263 middlewares 列表）-->
```dart
middlewares: [
  SystemPromptMiddleware(),          // ← 在此之后插入 const DialogueActMiddleware()
  PersonaInjectionMiddleware(),
  const DynamicPersonaMiddleware(),
  const FewShotMiddleware(),
  BannedListMiddleware(),
  const ContrastiveSubtractionMiddleware(),
  ?knowledgeInjectionMiddleware,
  ?skillEnforcementMiddleware,
  UserContentMiddleware(),
],
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 DialogueActMiddleware（先写测试再实现）</name>
  <files>test/features/ai/application/dialogue_act_middleware_test.dart, lib/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart</files>
  <behavior>
    - no-op when additionalInstruction is null
    - no-op when additionalInstruction is empty / whitespace-only
    - inject「响应策略·风格调整」+「风格偏好」system msg for "改成口语风格"
    - inject「内容探索」for "如果换一种方向会怎样"
    - inject「意图修订」+「真实意图」for "不对，我要的是紧张感"
    - inject「内容注入」+「忠实融入」for "加一段对话"
    - no-op for ambiguous no-signal message "好的"（confidence 0）
    - no-op for followUp-with-signal "为什么这么写"（_strategies 无 followUp 键）
    - 管线集成：withDefaultMiddlewares().build(styleAdjustment context) 产出含策略系统消息
  </behavior>
  <action>
    RED 阶段先写 `test/features/ai/application/dialogue_act_middleware_test.dart`：
    - imports: flutter_test, openai_dart, prompt_pipeline.dart, dialogue_act_middleware.dart
    - helper `_apply(String? instruction)` → `const DialogueActMiddleware().apply(PromptContext(fragments: const [], additionalInstruction: instruction))` → return result context
    - helper `_systemTexts(context)` → `context.messages.where((m) => m.role == ChatMessageRole.system).map((m) => m.content).join('\n')`（提取所有 system 消息文本）
    - 9 测试（见 behavior）。no-op 测 expect `result.messages.isEmpty`（middleware 是链中唯一消息源，fresh context 无既有消息）。注入测试 expect `systemText.contains('响应策略·风格调整')` 且 `.contains('风格偏好')`。
    - 管线集成测：`final msgs = PromptPipeline.withDefaultMiddlewares().build(PromptContext(fragments: const [], additionalInstruction: '改成口语风格'));` → expect `msgs.any((m) => m.role == ChatMessageRole.system && m.content.contains('响应策略·风格调整'))`
    跑 `flutter test test/features/ai/application/dialogue_act_middleware_test.dart` → 应全 FAIL（middleware 还不存在，编译错误）

    GREEN 阶段新建 `lib/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart`：
    ```dart
    /// PATHs dialogue-act → response-strategy middleware (CI-01 wiring).
    ///
    /// Reads [PromptContext.additionalInstruction] (the user's free-text refine
    /// instruction from synthesis regenerate), classifies it via
    /// [DialogueActClassifier], and injects an act-specific Chinese system
    /// instruction that adapts the AI's response strategy — closing the PATHs
    /// loop (260614-ci1 delivered the pure-logic classifier; this is its first
    /// consumer).
    ///
    /// No-op cases (respects classifier design, avoids noise on every refine):
    ///   - null / empty / whitespace instruction (first synthesis, no refine)
    ///   - confidence == 0 (purely ambiguous, no matched signal)
    ///   - [DialogueAct.followUp] (depth is the safe default; no special strategy)
    library;

    import 'package:museflow/features/ai/application/dialogue_act_classifier.dart';
    import 'package:museflow/features/ai/application/prompt_pipeline.dart';
    import 'package:museflow/features/ai/domain/dialogue_act.dart';
    import 'package:openai_dart/openai_dart.dart';

    /// Injects a dialogue-act-aware response-strategy system message.
    class DialogueActMiddleware extends PromptMiddleware {
      const DialogueActMiddleware();

      /// Chinese response-strategy instruction per actionable act.
      /// [DialogueAct.followUp] is intentionally absent — depth is the safe
      /// default and injecting a strategy for every follow-up adds noise.
      static const Map<DialogueAct, String> _strategies = {
        DialogueAct.styleAdjustment:
            '用户正在调整文风或语气。本次重写须严格尊重其风格偏好，仅调整语气与措辞，不得改变情节走向与人物行为。',
        DialogueAct.contentExploration:
            '用户正在探索内容可能性。可提供分支方向，但每条须紧扣碎片主题，并标注为可选方向而非定稿。',
        DialogueAct.intentRevision:
            '用户纠正了你的理解。请重新对齐其真实意图，丢弃上一版方向，按新意图重写。',
        DialogueAct.injection:
            '用户要求插入或补充指定内容。请忠实融入所求内容，保持上下文连贯，不得删改既有主体。',
      };

      @override
      PromptContext apply(PromptContext context) {
        final instruction = context.additionalInstruction;
        if (instruction == null || instruction.trim().isEmpty) return context;

        final classification = const DialogueActClassifier().classify(instruction);
        if (classification.confidence == 0) return context;

        final strategy = _strategies[classification.act];
        if (strategy == null) return context;

        return context.addMessage(
          ChatMessage.system('【响应策略·${classification.act.label}】$strategy'),
        );
      }
    }
    ```
    跑 `flutter test test/features/ai/application/dialogue_act_middleware_test.dart` → 应全 PASS。
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter test test/features/ai/application/dialogue_act_middleware_test.dart 2>&amp;1 | tail -15 &amp;&amp; echo "---MIDDLEWARE_TESTS_DONE---"</automated>
  </verify>
  <done>9 测试全绿（四行为注入 + null/空/模糊/followUp 四种 no-op + 管线集成）；middleware < 60 行；红线守住 classifier/dialogue_act 零改动。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 管线注册 DialogueActMiddleware</name>
  <files>lib/features/ai/application/prompt_pipeline.dart</files>
  <behavior>
    - withDefaultMiddlewares 的 middlewares 列表在 SystemPromptMiddleware 之后插入 const DialogueActMiddleware()
    - 顶部加 import（字母序：contrastive 与 dynamic 之间）
    - 现有 middleware 顺序其余不变
  </behavior>
  <action>
    在 `lib/features/ai/application/prompt_pipeline.dart`：
    1. import 块（line 14 contrastive 与 line 15 dynamic 之间）加：
       `import 'package:museflow/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart';`
    2. withDefaultMiddlewares middlewares 列表（line 264 `SystemPromptMiddleware(),` 之后、line 265 `PersonaInjectionMiddleware(),` 之前）加：
       `const DialogueActMiddleware(),`
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter analyze lib/features/ai/application/prompt_pipeline.dart lib/features/ai/application/prompt_middlewares/dialogue_act_middleware.dart 2>&amp;1 | tail -5 &amp;&amp; echo "---ANALYZE_DONE---"</automated>
  </verify>
  <done>prompt_pipeline.dart analyze 0 issue；middlewares 列表含 DialogueActMiddleware 紧随 SystemPromptMiddleware；既有 middleware 顺序不变。</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 全量回归 + analyze 零警告</name>
  <files></files>
  <action>
    全量测试（基线 1621，+8 新增 = 1629）+ flutter analyze 0 issue + dart format。
    特别关注：
    - test/features/ai/application/ 既有 dialogue_act_classifier_test.dart 仍绿（classifier 未改）
    - lib/features/ai/presentation/synthesis_notifier 相关测试仍绿（透传无感）
    - 任何 pipeline 相关测试若断言具体 messages 长度/顺序，可能因新增 system 消息 +1 而需调整——优先检查此类断言并修复（不算回归，是预期 wiring）
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter analyze 2>&amp;1 | tail -3 &amp;&amp; echo "---ANALYZE---" &amp;&amp; flutter test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>flutter analyze 0 issues；flutter test 全绿 ≥ 1629（1621 + 8 新增）；dart format 通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户自由文本 → prompt | additionalInstruction 已是受信用户输入（synthesis 面板文本框），不跨外部信任边界；classifier 是纯本地关键词匹配，无注入向量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ci2-01 | Tampering | 用户 instruction 含恶意关键词误导分类 | accept | 分类结果只影响「响应策略措辞」这一中文系统消息，不改变代码执行路径；最坏情况是策略措辞不太贴切，无安全影响 |
| T-ci2-02 | Information Disclosure | 策略消息暴露内部路由逻辑 | accept | 策略消息是给 LLM 的指令（本就在 prompt 里），非用户可见日志；label「风格调整」等是产品术语非敏感信息 |
| T-ci2-SC | Tampering | 无 npm/pip/cargo 安装 | accept | 纯 Dart 内部 wiring，零外部依赖新增 |
</threat_model>

<verification>
- Task 1: `flutter test test/features/ai/application/dialogue_act_middleware_test.dart` 9 测全绿
- Task 2: `flutter analyze lib/features/ai/application/prompt_pipeline.dart` 0 issue
- Task 3: `flutter analyze` 全库 0 issues + `flutter test` 全绿 ≥ 1629 tests
- 红线：`git diff --name-only` 仅 3 目标文件，dialogue_act_classifier.dart / dialogue_act.dart / synthesis_notifier.dart 不在内
</verification>

<success_criteria>
- DialogueActMiddleware 实现 PromptMiddleware，按 act 注入中文响应策略
- 四种 no-op（null/空/confidence0/followUp）正确避免噪音
- 管线注册紧随 SystemPromptMiddleware
- 全库零回归，analyze 0，tests ≥ 1629
- 红线守住：classifier / dialogue_act.dart / synthesis_notifier 零改动
</success_criteria>

<output>
Create `.planning/quick/260616-ci2-dialogue-act-middleware/260616-ci2-SUMMARY.md` when done
</output>
