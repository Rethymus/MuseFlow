---
phase: quick-260617-05c
status: complete
tags: [fix, dart, sentiment, double-count, style-deviation]
---
# 修复 style_deviation_detector 情感词双计 bug

## What Was Built
`_countPositiveSentiment`/`_countNegativeSentiment` 用 `const [...]` List，含重复词条。计数循环 `for(word in list) count += word.allMatches(text).length` 把重复词命中算两次 → 虚高 positive/negative 计数 → 扭曲 warmth/intensity → 影响 `_classifyTone` 与偏差检测（lines 317-347）。

修复：两 List 字面量改 `const <String>{...}` Set 字面量（自动去重 + 结构上杜绝复发，与 sentiment_lexicon.dart `Set<String>` 先例一致；for-in 迭代 Set 计数等价），移除 4 重复条目（positives 喜悦×2 去重；negatives 痛苦/愤怒/恐惧 各×2 去重）。

commit 9122b01，1 file，+4/-8（净删 4 行 dup）。

## How to Verify
- `flutter analyze lib/features/editor/application/style_deviation_detector.dart` → No issues (0.1s)
- `flutter test test/features/editor/application/style_deviation_detector_test.dart` → 13 passed
- `flutter test`（全量）→ 1647 passed, 12 skipped（exit 0，零回归）。私有为 _countPositive/NegativeSentiment，无直接断言，经 detect() 间接由既有套件覆盖确认零回归。

## Deviations
无。Set 转换暴露 equal_elements_in_const_set 编译错误（预期），顺势移除 dup 条目闭合。

## Threat Flags
无——纯 bug 修复，零行为面新增。
