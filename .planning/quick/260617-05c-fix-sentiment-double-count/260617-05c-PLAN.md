---
phase: quick-260617-05c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/editor/application/style_deviation_detector.dart
autonomous: true
requirements: []
---

# 修复 style_deviation_detector 情感词双计 bug

<objective>
`_countPositiveSentiment`/`_countNegativeSentiment` 用 `const [...]` List，含重复词条（positives: 喜悦×2；negatives: 痛苦×2/愤怒×2/恐惧×2）。计数循环 `for(word in list) count += word.allMatches(text).length` 把重复词命中算两次，虚高 positive/negative 计数，扭曲 warmth/intensity → _classifyTone 与偏差检测（lines 317-347）。
</objective>

<fix>
两个 List 字面量改 Set 字面量：`const positives = [` → `{`，闭合 `];` → `};`（两处同理）。Set 自动去重 + 结构上杜绝复发，与 sentiment_lexicon.dart 的 Set<String> 先例一致。for-in 迭代 Set 与 List 计数等价。两 const 方法内局部作用域，无外部引用。
</fix>

<verification>
- flutter analyze 0
- flutter test test/features/editor/application/style_deviation_detector_test.dart 全绿
- 全量 1647 零回归（若断言受影响精确值则更新为修正后正确值）
- grep 确认两 const 为 Set 字面量 `{`/`}`
</verification>
