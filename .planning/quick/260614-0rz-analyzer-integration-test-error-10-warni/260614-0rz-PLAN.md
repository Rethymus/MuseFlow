---
phase: quick
plan: 260614-0rz
type: execute
wave: 1
depends_on: []
files_modified:
  - integration_test/manuscript_flow_test.dart
  - test/features/editor/presentation/editor_shortcuts_test.dart
  - test/features/reports/application/editorial_review_service_test.dart
  - test/features/story_structure/application/export_service_test.dart
autonomous: true
requirements: [TECH-DEBT-ANALYZER]
user_setup: []

must_haves:
  truths:
    - "flutter analyze (全量 lib+test+integration_test) 报告 'No issues found!'"
    - "flutter test 全量套件仍为 1524 passed / 0 failed（无回归）"
    - "flutter test integration_test/manuscript_flow_test.dart 至少能编译通过（无 analyzer error）"
    - "ExportDialog.onExport 回调在集成测试中正确接收 markdown 文本的 textContent 命名参数"
  artifacts:
    - path: "integration_test/manuscript_flow_test.dart"
      provides: "修正后的 onExport 签名（命名参数 textContent/binaryContent）"
      contains: "textContent, binaryContent"
    - path: "test/features/editor/presentation/editor_shortcuts_test.dart"
      provides: "清理后的测试文件（5 个未用 import 删除 + _NoOpAuditService 删除）"
    - path: "test/features/reports/application/editorial_review_service_test.dart"
      provides: "清理后的测试文件（2 个未用 import 删除）"
    - path: "test/features/story_structure/application/export_service_test.dart"
      provides: "修复后的 _decodeFile helper（移除恒真类型检查与死代码）"
  key_links:
    - from: "integration_test/manuscript_flow_test.dart"
      to: "lib/features/story_structure/presentation/export_dialog.dart"
      via: "onExport 回调签名一致性"
      pattern: "textContent, binaryContent"
---

<objective>
修复 analyzer 报告的技术债：1 个真实回归 ERROR（集成测试编译失败）+ 10 个测试文件 WARNING（未用 import / 死代码 / 恒真类型检查）。

Purpose: DOCX 导出功能（commit 7fa2ad2）将 `ExportDialog.onExport` 签名改为命名参数 `{textContent, binaryContent}`，导致集成测试的旧签名回调编译失败；同时 v1.5 迭代遗留若干死代码 warning。恢复 analyzer 零错误的基线（项目规则要求 `flutter analyze` 零错误）。

Output: 4 个文件修正，`flutter analyze` 从 11 issues 回到 "No issues found!"，`flutter test` 维持 1524 passed 不回归。
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/re/code/MuseFlow/.planning/STATE.md
@/home/re/code/MuseFlow/CLAUDE.md

权威签名定义（不可改，改的是测试侧）：
- `lib/features/story_structure/presentation/export_dialog.dart:21-26` —
  `Future<void> Function(ExportFormat format, String path, {String? textContent, List<int>? binaryContent}) onExport`
- 真实调用范例：`lib/features/story_structure/presentation/story_structure_page.dart:433` —
  `onExport: (format, path, {textContent, binaryContent}) async {...}`

当前 analyzer 基线（已运行确认，11 issues）：
- 1 error • `argument_type_not_assignable` @ integration_test/manuscript_flow_test.dart:155:25
- 5 warning • `unused_import` @ test/features/editor/presentation/editor_shortcuts_test.dart:10,11,12,13,19
- 1 warning • `unused_element` (_NoOpAuditService) @ test/features/editor/presentation/editor_shortcuts_test.dart:103:7
- 2 warning • `unused_import` @ test/features/reports/application/editorial_review_service_test.dart:9,12
- 1 warning • `unnecessary_type_check` @ test/features/story_structure/application/export_service_test.dart:16:7
- 1 warning • `dead_code` @ test/features/story_structure/application/export_service_test.dart:19:3

测试基线：`flutter test` = 1524 passed / 0 failed（集成测试不在常规套件中，故全绿掩盖了其编译错误）。
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复集成测试 onExport 回调签名回归（ERROR）</name>
  <files>integration_test/manuscript_flow_test.dart</files>
  <behavior>
    - 修复前：`flutter analyze` 报 `argument_type_not_assignable` @ line 155（旧签名 `(format, path, content)` 位置参 dynamic 第三参）
    - 修复后：集成测试编译通过，onExport 回调使用命名参数 `{textContent, binaryContent}`，markdown 文本断言改走 `textContent`
    - 集成测试 `manuscript_flow_test.dart` 单独 analyze 无 error
  </behavior>
  <action>
    修改 `integration_test/manuscript_flow_test.dart` 第 155-159 行的 onExport 回调，使其匹配 `export_dialog.dart:21-26` 的权威签名。

    将：
    - `onExport: (format, path, content) async {` + `expect(content, contains('剑道苍穹'));`

    改为：
    - `onExport: (format, path, {textContent, binaryContent}) async {`
    - 断言 `expect(textContent, contains('剑道苍穹'));`（markdown 是文本格式，textContent 有值、binaryContent 为 null）

    format 与 path 的断言保持不变。不删除、不改动该测试文件中其他任何代码（onExport 之外）。签名来源：D-DOCX-EXPORT（commit 7fa2ad2，export_dialog.dart:21-26）。

    RED 信号：修复前运行 `flutter analyze integration_test/manuscript_flow_test.dart` 必报 1 个 error。GREEN 信号：修复后该文件 analyze 零 error。
  </action>
  <verify>
    <automated>flutter analyze integration_test/manuscript_flow_test.dart 2>&1 | grep -E "(error|warning)" | grep -v '^#' | grep -c . | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>集成测试 onExport 使用命名参数 {textContent, binaryContent}，markdown 文本断言走 textContent，该文件单独 analyze 无 error/warning。</done>
</task>

<task type="auto">
  <name>Task 2: 清理 editor_shortcuts_test 死代码与未用 import（6 warnings）</name>
  <files>test/features/editor/presentation/editor_shortcuts_test.dart</files>
  <action>
    纯删除操作，不改任何保留代码逻辑。两处改动：

    1. 删除 5 个未用 import（保留其余 import 如 flutter/material、flutter/services、flutter_test、editor/domain/editor_ai_state.dart、stats/application/token_audit_service.dart、stats/domain/audit_operation_type.dart、stats/domain/token_audit_record.dart、openai_dart/openai_dart.dart —— 这些都被实际引用，必须保留）：
       - line 10: `import 'package:flutter_riverpod/flutter_riverpod.dart';`
       - line 11: `import 'package:museflow/core/domain/fragment.dart';`
       - line 12: `import 'package:museflow/core/presentation/providers.dart';`
       - line 13: `import 'package:museflow/features/capture/presentation/capture_provider.dart';`
       - line 19: `import 'package:super_editor/super_editor.dart';`

    2. 删除未被引用的 `_NoOpAuditService` 类（analyzer 报 line 103:7 `unused_element`）。用 grep 确认该标识符除定义外无任何引用后，整段删除类定义（class 声明到其闭合大括号）。删除前先 grep 全文确认 `_NoOpAuditService` 仅出现 1 次（即定义处），若有多处引用则停下报告。

    谨慎：删除 import 前对每个待删 import 用 grep 确认该 import 路径下的符号在文件其余部分确实未被使用（如 fragment.dart 的 `Fragment`、providers.dart 的任何 provider、capture_provider.dart 的任何符号、flutter_riverpod 的 `ProviderScope`/`ConsumerWidget`、super_editor 的任何符号）。若发现某 import 实际被使用，则不删该 import 并在 SUMMARY 中记录差异。
  </action>
  <verify>
    <automated>flutter analyze test/features/editor/presentation/editor_shortcuts_test.dart 2>&1 | grep -E "(error|warning)" | grep -v '^#' | grep -c . | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>editor_shortcuts_test.dart 单独 analyze 零 issue；6 个原 warning（5 unused_import + 1 unused_element _NoOpAuditService）全部消失；保留的测试逻辑不变。</done>
</task>

<task type="auto">
  <name>Task 3: 清理 editorial_review_service_test 未用 import + 修复 export_service_test 类型检查死代码（4 warnings）</name>
  <files>test/features/reports/application/editorial_review_service_test.dart, test/features/story_structure/application/export_service_test.dart</files>
  <action>
    两个独立文件、零文件冲突，同任务完成。

    文件 A — `test/features/reports/application/editorial_review_service_test.dart`：
    删除 2 个未用 import（删前 grep 确认 `EditorialReview` 与 `TokenAuditRecord` 在该文件无引用）：
    - line 9: `import 'package:museflow/features/reports/domain/editorial_review.dart';`
    - line 12: `import 'package:museflow/features/stats/domain/token_audit_record.dart';`
    其余 import 全部保留（token_budget_calculator、ai_adapter、editorial_review_service 的 service 类、token_audit_service、audit_operation_type、token_audit_repository、openai_dart 均被使用）。

    文件 B — `test/features/story_structure/application/export_service_test.dart` 的 `_decodeFile` helper（line 14-20）：
    当前代码：
    ```
    String _decodeFile(ArchiveFile f) {
      final bytes = f.content;
      if (bytes is List<int>) {
        return utf8.decode(bytes);
      }
      return bytes.toString();
    }
    ```
    analyzer 报 line 16 `unnecessary_type_check`（恒为 true）+ line 19 `dead_code`（因此 `return bytes.toString();` 不可达）。
    修复为（移除恒真检查与死分支，直接 decode）：
    ```
    String _decodeFile(ArchiveFile f) => utf8.decode(f.content);
    ```
    `f.content` 在 archive 包此版本返回 `List<int>`，`utf8.decode` 接受 `List<int>`，签名匹配。`dart:convert` 的 import（line 1）已存在，无需新增。`archive` 包的 `ArchiveFile` 类型来自 line 4 已有的 `package:archive/archive.dart` import，无需改动。
  </action>
  <verify>
    <automated>flutter analyze test/features/reports/application/editorial_review_service_test.dart test/features/story_structure/application/export_service_test.dart 2>&1 | grep -E "(error|warning)" | grep -v '^#' | grep -c . | xargs -I{} test {} -eq 0</automated>
  </verify>
  <done>两个文件各自单独 analyze 零 issue；editorial_review 删 2 unused_import；export_service_test 的 unnecessary_type_check 与 dead_code 消失，_decodeFile 简化为单行 utf8.decode。</done>
</task>

<task type="auto">
  <name>Task 4: 全量验证 — analyzer 零 issue + 测试套件无回归</name>
  <files></files>
  <action>
    端到端验证（不写代码，只跑命令）：

    1. 全量 analyzer（覆盖 lib + test + integration_test）：
       `flutter analyze` —— 必须输出 `No issues found!`（从 11 issues 归零）。

    2. 全量单元/组件测试（不含集成测试）：
       `flutter test` —— 必须维持 1524 passed / 0 failed（基线来自 STATE.md last activity + 诊断说明）。若数字变化，说明改动引入回归，必须回查 Task 2/3 是否误删了被引用的代码。

    3. 集成测试编译验证（headless 环境可能跑不完，但必须编译通过）：
       `flutter test integration_test/manuscript_flow_test.dart` —— 至少编译通过、无 analyzer error。若环境限制无法跑完（如缺桌面资源），编译成功即视为通过；记录实际结果到 SUMMARY。

    若任一命令失败：不修改本任务之外的代码，回到对应 Task 复查（Task 1=签名，Task 2=editor_shortcuts 删除项，Task 3=两个文件）。

    全绿后准备两条提交（遵守项目 commit 规范）：
    - `fix(test): correct onExport callback signature in manuscript integration test` （Task 1，integration_test）
    - `chore(test): remove unused imports and dead code from test files` （Task 2+3，test/）
    本任务只验证，提交由 execute-plan 流程在 SUMMARY 后处理。
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -c "No issues found!" | xargs -I{} test {} -eq 1</automated>
  </verify>
  <done>`flutter analyze` = "No issues found!"（11→0）；`flutter test` = 1524 passed / 0 failed（无回归）；`flutter test integration_test/manuscript_flow_test.dart` 编译通过无 analyzer error。</done>
</task>

</tasks>

<verification>
- `flutter analyze` → "No issues found!"（lib + test + integration_test 全量）
- `flutter test` → 1524 passed / 0 failed（不得回归，基线来自 STATE.md v1.5 迭代）
- `flutter test integration_test/manuscript_flow_test.dart` → 至少编译通过（headless 可能跑不完，编译无 error 即可）
- 基线对照：修复前 analyze 11 issues / test 1524 passed；修复后 analyze 0 issues / test 1524 passed
</verification>

<success_criteria>
- 集成测试 onExport 回调使用命名参数 `{textContent, binaryContent}`，markdown 断言走 textContent
- 4 个文件修正完成，analyzer 全量零 issue
- 测试套件零回归（仍 1524 passed）
- 无新增功能代码，纯回归修复 + 死代码清理（符合 quick 任务定位）
</success_criteria>

<output>
Create `.planning/quick/260614-0rz-analyzer-integration-test-error-10-warni/260614-0rz-SUMMARY.md` when done
</output>
