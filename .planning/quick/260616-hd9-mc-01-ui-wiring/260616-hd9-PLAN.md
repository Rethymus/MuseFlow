---
phase: quick-260616-hd9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/knowledge/presentation/knowledge_base_page.dart
  - test/features/knowledge/knowledge_base_page_staleness_test.dart
autonomous: true
requirements:
  - MC-01-UI
tags: [mc-01, knowledge-base, staleness, ui-wiring, presentation]

must_haves:
  truths:
    - "打开知识库页面，角色卡/世界观列表项在 stale（≥10章未验证）时显示 amber 徽章含 message 文案"
    - "在 veryStale（≥20章未验证）时显示红色 error 徽章含「严重过期」语义文案"
    - "在 fresh 时（<10章 或 legacy null）不显示任何徽章，UI 与改造前一致"
    - "用户可点击「标记为已验证」操作 → 弹确认对话框 → 确认后调用 notifier.save(copyWith(lastVerifiedChapter: 当前章节数))，徽章消失"
    - "改造未触及 KbStalenessChecker 本体、domain 实体定义、provider 定义"
  artifacts:
    - path: "lib/features/knowledge/presentation/knowledge_base_page.dart"
      provides: "_CharacterCardTile / _WorldSettingTile 内接入 staleness 徽章 + 标记为已验证动作"
      contains: "KbStalenessChecker"
    - path: "test/features/knowledge/knowledge_base_page_staleness_test.dart"
      provides: "三态渲染（fresh/stale/veryStale）+ 标记为已验证动作的 widget 测试"
      contains: "KbStalenessLevel.veryStale"
  key_links:
    - from: "lib/features/knowledge/presentation/knowledge_base_page.dart"
      to: "lib/features/knowledge/application/kb_staleness_checker.dart"
      via: "const KbStalenessChecker().check(card.lastVerifiedChapter, chapterCount)"
      pattern: "KbStalenessChecker\\(\\)\\.check"
    - from: "lib/features/knowledge/presentation/knowledge_base_page.dart"
      to: "lib/core/presentation/providers.dart#chapterNotifierProvider"
      via: "ref.watch(chapterNotifierProvider).asData?.value.length"
      pattern: "chapterNotifierProvider"
    - from: "lib/features/knowledge/presentation/knowledge_base_page.dart"
      to: "CharacterCardNotifier.save / WorldSettingNotifier.save"
      via: "ref.read(notifierProvider.notifier).save(entity.copyWith(lastVerifiedChapter: count))"
      pattern: "lastVerifiedChapter:"
---

<objective>
将已交付的 MC-01 `KbStalenessChecker` 接入知识库管理页（`KnowledgeBasePage`），让 `_CharacterCardTile` / `_WorldSettingTile` 在 stale/veryStale 状态下显示中文徽章 + 提供「标记为已验证」动作，形成「检测 → 提醒 → 作者校准 → 重新保鲜」的闭环。

Purpose: MC-01 核心逻辑（checker + 实体字段）已在 260614-mc1 交付，但 UI 未接入，作者无法感知知识库过期，也无法触发校准。本计划收尾 P0 项，把已写好的检测能力暴露到用户可见可操作的界面。

Output:
- 改造后的 `knowledge_base_page.dart`（仅 UI wiring，禁止改 domain/checker/provider）
- 新建 widget 测试覆盖三态渲染 + 标记为已验证动作
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/re/code/MuseFlow/.planning/PROJECT.md
@/home/re/code/MuseFlow/.planning/STATE.md
@/home/re/code/MuseFlow/CLAUDE.md
@/home/re/code/MuseFlow/.claude/rules/03-flutter-standards.md

@/home/re/code/MuseFlow/lib/features/knowledge/presentation/knowledge_base_page.dart
@/home/re/code/MuseFlow/lib/features/knowledge/application/kb_staleness_checker.dart
@/home/re/code/MuseFlow/lib/features/knowledge/domain/character_card.dart
@/home/re/code/MuseFlow/lib/features/knowledge/domain/world_setting.dart

<interfaces>
<!-- 已验证的契约 — executor 直接使用，无需再 grep 代码库 -->

From lib/features/knowledge/application/kb_staleness_checker.dart:
```dart
enum KbStalenessLevel { fresh, stale, veryStale }

class KbStalenessResult {
  final KbStalenessLevel level;
  final int? chaptersSinceVerified;  // null for legacy entries
  final String message;               // 中文 UI 文案
  const KbStalenessResult({required this.level, required this.chaptersSinceVerified, required this.message});
}

class KbStalenessChecker {
  const KbStalenessChecker();
  static const int staleThreshold = 10;       // ≥10 → stale
  static const int veryStaleThreshold = 20;   // ≥20 → veryStale
  KbStalenessResult check(int? lastVerifiedChapter, int currentChapterCount);
  // lastVerifiedChapter == null → fresh（legacy，不 nag）
  // since == 0 → fresh（本章刚验证）
}
```

From lib/core/presentation/providers.dart:
```dart
// L412 — characterCardNotifierProvider
final characterCardNotifierProvider =
    AsyncNotifierProvider<CharacterCardNotifier, List<CharacterCard>>(CharacterCardNotifier.new);

// L421 — worldSettingNotifierProvider
final worldSettingNotifierProvider =
    AsyncNotifierProvider<WorldSettingNotifier, List<WorldSetting>>(WorldSettingNotifier.new);

// L819 — chapterNotifierProvider
final chapterNotifierProvider =
    AsyncNotifierProvider<ChapterNotifier, List<Chapter>>(ChapterNotifier.new);
```

From lib/features/knowledge/application/{character_card,world_setting}_notifier.dart:
```dart
class CharacterCardNotifier extends AsyncNotifier<List<CharacterCard>> {
  Future<void> save(CharacterCard card) async { /* update repo + invalidateSelf */ }
}
class WorldSettingNotifier extends AsyncNotifier<List<WorldSetting>> {
  Future<void> save(WorldSetting setting) async { /* update repo + invalidateSelf */ }
}
```

From lib/features/knowledge/domain/{character_card,world_setting}.dart:
```dart
// 两个实体都有 final int? lastVerifiedChapter 字段 + copyWith(lastVerifiedChapter: ...)
// 均向后兼容（默认 null）
```

From test/features/manuscript/presentation/chapter_sidebar_test.dart（widget test 范式）:
```dart
await tester.pumpWidget(
  ProviderScope(
    overrides: [
      chapterNotifierProvider.overrideWith(() => _FakeChapterNotifier()),
    ],
    child: const MaterialApp(home: Scaffold(body: SomeWidget())),
  ),
);
await tester.pumpAndSettle();
```
</interfaces>

<constraints>
**禁止修改（已交付的核心逻辑，零回归红线）:**
- `lib/features/knowledge/application/kb_staleness_checker.dart`
- `lib/features/knowledge/domain/character_card.dart`
- `lib/features/knowledge/domain/world_setting.dart`
- `lib/core/presentation/providers.dart`（任何 provider 定义）
- 任何 Notifier 类的 `save` 方法

**只允许新建/修改:**
- `lib/features/knowledge/presentation/knowledge_base_page.dart`
- `test/features/knowledge/knowledge_base_page_staleness_test.dart`（新建）

**项目标准（CLAUDE.md + 03-flutter-standards.md）:**
- `ConsumerWidget` 已是，保持
- 状态读取 `ref.watch`，操作 `ref.read`
- 不可变更新：`card.copyWith(lastVerifiedChapter: count)`
- `const` 构造
- 单文件 < 800 行（当前 ~326 行，wiring 后预计 ~450-500 行，OK）
- 测试命名：`should [行为] when [条件]`
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 _CharacterCardTile / _WorldSettingTile 接入 staleness 徽章 + 标记为已验证动作</name>
  <files>lib/features/knowledge/presentation/knowledge_base_page.dart</files>
  <behavior>
    - 角色卡/世界观 tile 在 fresh 时：与改造前视觉一致（无徽章），delete 按钮保留
    - 角色卡/世界观 tile 在 stale（10≤since<20）时：subtitle 下方显示 amber 警示徽章，文案含 checker.message（如「该条目已 12 章未验证...」）
    - 角色卡/世界观 tile 在 veryStale（since≥20）时：subtitle 下方显示红色 error 徽章，文案含「严重过期」语义（来自 checker 的 veryStale message）
    - legacy 条目（lastVerifiedChapter == null）→ fresh，不显示徽章
    - 用户点击「标记为已验证」→ 弹确认对话框 → 确认后调用 notifier.save(entity.copyWith(lastVerifiedChapter: currentChapterCount)) → 该 tile 重新进入 fresh，徽章消失
    - delete 按钮保留（与「标记为已验证」并存于 trailing，建议用 PopupMenuButton 统一两个动作，避免视觉拥挤）
    - chapterCount = ref.watch(chapterNotifierProvider).asData?.value.length ?? 0
    - 当前章节数为 0 时（新项目）：即便 lastVerifiedChapter 是 null 也走 fresh 分支（legacy 语义），不报错
  </behavior>
  <action>
    1. 在文件顶部 import 区追加：`import 'package:museflow/features/knowledge/application/kb_staleness_checker.dart';`

    2. **抽取共享的 staleness 徽章 widget**：在文件底部新增私有 widget `_StalenessBadge`（ConsumerWidget 不可，但它是纯展示组件，用 StatelessWidget + 接收 `KbStalenessResult staleness` 参数即可）。规则：
       - `staleness.level == KbStalenessLevel.fresh` → 返回 `const SizedBox.shrink()`（不渲染）
       - `stale` → `Container` 或 `DecoratedBox` 内放 `Row(children: [Icon(Icons.warning_amber_rounded, size: 14, color: Colors.amber[700]), SizedBox(width:4), Expanded(child: Text(staleness.message, ...))])`，背景 `Colors.amber.withValues(alpha: 0.12)`，圆角 6，padding EdgeInsets.symmetric(horizontal:8, vertical:4)
       - `veryStale` → 同上但图标 `Icons.error_outline`，颜色 `Theme.of(context).colorScheme.error`，背景 `colorScheme.errorContainer.withValues(alpha: 0.3)`
       - text style：`Theme.of(context).textTheme.bodySmall`，颜色随等级
       - 单行 ellipsis（maxLines: 1, overflow: ellipsis）

    3. **改造 `_CharacterCardTile.build`**：把 `ListTile` 的 `subtitle` 从单 `Text` 改为 `Column(crossAxisAlignment: CrossAxisAlignment.start, mainAxisSize: MainAxisSize.min, children: [Text(personality/desc...), if (staleness.level != fresh) ... [SizedBox(height:4), _StalenessBadge(staleness: staleness)]])`。
       - 顶部计算：`final chapterCount = ref.watch(chapterNotifierProvider).asData?.value.length ?? 0;`
       - `final staleness = const KbStalenessChecker().check(card.lastVerifiedChapter, chapterCount);`
       - trailing：把单一 delete IconButton 改为 `PopupMenuButton<String>(itemBuilder: (_) => [PopupMenuItem(value: 'verify', child: const ListTile dense leading: Icon(Icons.verified), title: Text('标记为已验证'), contentPadding: EdgeInsets.zero)), PopupMenuItem(value: 'delete', child: const ListTile dense leading: Icon(Icons.delete_outline, color: Colors.red), title: Text('删除'), contentPadding: EdgeInsets.zero)], icon: const Icon(Icons.more_vert), onSelected: (v) => v == 'delete' ? _confirmDelete(context, ref) : _confirmVerify(context, ref, chapterCount))`
       - `onTap` 保留（`context.go('/knowledge/character/${card.id}')`）

    4. **新增 `_CharacterCardTile._confirmVerify(BuildContext, WidgetRef, int chapterCount)`**：弹 `AlertDialog`，标题「标记为已验证」，内容「确认将"${card.name}"标记为截至第 $chapterCount 章已验证？验证后该条目将不再提示陈旧。」，按钮「取消」+「确认」。确认时：`ref.read(characterCardNotifierProvider.notifier).save(card.copyWith(lastVerifiedChapter: chapterCount));` 注意 `save` 是 `Future<void>` 但 fire-and-forget 即可（notifier 内部会 invalidateSelf 触发 watch 重建）；可选 `.then` 或忽略。**注意**：`card.copyWith(lastVerifiedChapter: chapterCount)` — 因 copyWith 中 `lastVerifiedChapter ?? this.lastVerifiedChapter`，而 chapterCount 可能是 0，0 会被当作有效值传入（0 不是 null），故必须用 `lastVerifiedChapter: chapterCount` 直接传值，不要走 `?? this` 旁路（这里就是直接传值，正确）。

    5. **同样改造 `_WorldSettingTile`**：与步骤 3-4 对称，但 notifier 用 `worldSettingNotifierProvider`，路由 `/knowledge/setting/${setting.id}`，确认文案中的实体描述用 setting.name，subdivision 文案用 setting.description。

    6. **删除按钮的红色样式**：PopupMenu 删除项使用 `color: Colors.red`（或 `colorScheme.error`）让 delete 仍有视觉警示。

    7. **常量构造检查**：所有新增 widget 用 `const` 构造能 const 的都 const（PopupMenuItem 内的 ListTile 需要 const）。

    8. **不要**改动 `_CharacterCardList` / `_WorldSettingList` 的结构、搜索逻辑、empty state、`KnowledgeBasePage` 本身、tab controller、FAB — 仅 tile 内部。

    9. 保持单文件 < 800 行。
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter analyze lib/features/knowledge/presentation/knowledge_base_page.dart 2>&1 | tail -5 && echo "---ANALZE DONE---"</automated>
  </verify>
  <done>
    - `flutter analyze` 对该文件零 error / 零 warning
    - `_CharacterCardTile` / `_WorldSettingTile` 都 import 并调用了 `KbStalenessChecker`
    - 都 `ref.watch(chapterNotifierProvider)` 拿章节数
    - 都有 PopupMenu 含「标记为已验证」「删除」两项
    - fresh 态不渲染徽章
    - 文件总行数 < 800
    - 未触碰 checker/domain/providers 文件（git diff 验证）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: widget 测试三态渲染 + 标记为已验证动作</name>
  <files>test/features/knowledge/knowledge_base_page_staleness_test.dart</files>
  <behavior>
    - 当 currentChapterCount=5 且 lastVerifiedChapter=null（legacy）→ 无 `_StalenessBadge`/amber/error 徽章渲染
    - 当 currentChapterCount=15 且 lastVerifiedChapter=2 → since=13 → stale → 找到 amber 徽章 + 文案含「13 章未验证」
    - 当 currentChapterCount=25 且 lastVerifiedChapter=2 → since=23 → veryStale → 找到 error（红色）徽章 + 文案含「严重过期」
    - 用户点击 PopupMenu「标记为已验证」→ 确认对话框 → 点击「确认」→ 捕获到 `save` 被调用，参数是 `card.copyWith(lastVerifiedChapter: 15)`（或对应 chapterCount）
    - veryStale 渲染用同一 widget 实例，仅 level 不同（避免重复写 3 个独立 widget tree）
  </behavior>
  <action>
    1. **新建文件** `test/features/knowledge/knowledge_base_page_staleness_test.dart`。imports：
       ```dart
       import 'package:flutter/material.dart';
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import 'package:flutter_test/flutter_test.dart';
       import 'package:museflow/core/presentation/providers.dart';
       import 'package:museflow/features/knowledge/application/character_card_notifier.dart';
       import 'package:museflow/features/knowledge/application/world_setting_notifier.dart';
       import 'package:museflow/features/knowledge/domain/character_card.dart';
       import 'package:museflow/features/knowledge/domain/world_setting.dart';
       import 'package:museflow/features/knowledge/presentation/knowledge_base_page.dart';
       import 'package:museflow/features/manuscript/application/chapter_notifier.dart';
       import 'package:museflow/features/manuscript/domain/chapter.dart';
       ```

    2. **伪造 ChapterNotifier**：`class _FixedChapterNotifier extends ChapterNotifier { final int count; _FixedChapterNotifier(this.count); @override Future<List<Chapter>> build() async => List.generate(count, (i) => Chapter(id:'c$i', manuscriptId:'m1', orderIndex:i, title:'第${i+1}章', content:'', createdAt: DateTime(2026,1,1))); }` —— Chapter 构造签名以实际 domain 为准，executor 需 grep `class Chapter` 确认 required 参数；若 Chapter 有别的 required 字段，按实际填充。`count` 决定 `chapterNotifierProvider` watch 出来的 list.length。

    3. **伪造 CharacterCardNotifier / WorldSettingNotifier**：extends 对应类，build 返回固定 List，并 override `save` 记录调用参数到 `late CharacterCard savedCard` / `late WorldSetting savedSetting` 字段（用 `@override Future<void> save(...) async { savedCard = card; }`）。这样可断言 `lastVerifiedChapter`。

    4. **辅助 builder 函数**：`Widget _wrap(ProviderContainer container, Widget child)` → `UncontrolledProviderScope(container: container, child: MaterialApp(home: Scaffold(body: child)))`。或用 `ProviderScope(overrides:[...])` 直接 pumpWidget，参考 chapter_sidebar_test.dart 的范式。

    5. **三个 stale-state 测试 + 一个 fresh 测试**：用同一份 `pumpWidget → pumpAndSettle → expect` 模板，分别构造：
       - `lastVerifiedChapter: null` + chapterCount=5 → `find.byIcon(Icons.warning_amber_rounded)` 和 `find.byIcon(Icons.error_outline)` 都 `findsNothing`；PopupMenu 图标 `Icons.more_vert` `findsOneWidget`（确认 tile 渲染了）
       - `lastVerifiedChapter: 2` + chapterCount=15 → `find.byIcon(Icons.warning_amber_rounded)` `findsOneWidget`；`find.textContaining('13 章未验证')` `findsOneWidget`
       - `lastVerifiedChapter: 2` + chapterCount=25 → `find.byIcon(Icons.error_outline)` `findsOneWidget`；`find.textContaining('严重过期')` `findsOneWidget`
       - 同上对 `_WorldSettingTile` 重复一组（至少一组 stale + 一组 veryStale），用对称 setting 数据

    6. **「标记为已验证」动作测试**（2 个，character + world）：
       - pumpWidget stale 场景（chapterCount=15）
       - `await tester.tap(find.byIcon(Icons.more_vert)); await tester.pumpAndSettle();`
       - `await tester.tap(find.text('标记为已验证')); await tester.pumpAndSettle();`
       - 断言确认对话框出现 `find.text('确认')` `findsOneWidget` 或 `find.widgetWithText(AlertDialog, ...)` 命中
       - `await tester.tap(find.text('确认')); await tester.pumpAndSettle();`
       - 断言 fake notifier 的 `savedCard.lastVerifiedChapter == 15`（character）/ `savedSetting.lastVerifiedChapter == 15`（world）

    7. **测试组结构**：
       ```dart
       void main() {
         group('_CharacterCardTile staleness', () { ... 4 个测试 });
         group('_WorldSettingTile staleness', () { ... 3 个测试 });
         group('mark-as-verified action', () { ... 2 个测试 });
       }
       ```
       测试命名遵循 `should ... when ...`。

    8. **不直接渲染 KnowledgeBasePage**：用更小的 `Scaffold(body: _CharacterCardTile(card: card))` 起手测试，避免 tab controller 干扰；但如果 `_CharacterCardTile` 是私有类，会无法 import —— 此时必须通过 `KnowledgeBasePage` 间接测，pumpWidget 后切到对应 tab（默认 Tab 0 是角色卡）。executor 决策：私有 tile 不能跨文件引用 → 改用 `KnowledgeBasePage` 入口，pumpAndSettle 后断言列表项内的图标/文本。`_StalenessBadge` 也是私有，所以测试用 `find.byIcon` / `find.text` 间接断言。
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/knowledge/knowledge_base_page_staleness_test.dart --reporter=expanded 2>&1 | tail -20 && echo "---TEST DONE---"</automated>
  </verify>
  <done>
    - 全部测试通过（≥7 个测试：3 character 三态 + 2 world 至少 stale+veryStale + 2 标记动作；fresh legacy 也覆盖）
    - fake notifier 的 save 被调用且 lastVerifiedChapter 参数 == 当前章节数
    - `flutter analyze` 对新测试文件零 issue
    - 测试用 `ProviderScope` override 注入 fake data，无真实 Hive / repository 依赖
    - 整套测试 < 60s 运行完毕
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户点击 → UI 动作 | 用户在 tile 上点击 PopupMenu「标记为已验证」→ 确认对话框 → 调用 `save(copyWith(lastVerifiedChapter: count))`。所有触发都是本地 UI 状态变更，不跨网络。 |
| 章节数据读取 | `chapterNotifierProvider` 提供当前章节数；来自本地 Hive 数据。数据来源可信（本地存储）。 |
| knowledge_base_page → repository | 通过 Notifier.save → repository.update 链路写回 Hive。Hive 已加密（AES-256 CBC，flutter_secure_storage 管 key）。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mc01-ui-01 | Tampering | 「标记为已验证」动作写入 lastVerifiedChapter | accept | 仅本地状态更新，无未授权风险；用户主动触发，且有确认对话框二次确认；数据本身不是安全敏感（故事元数据）。 |
| T-mc01-ui-02 | Information Disclosure | 徽章 message 文案显示 chaptersSinceVerified | accept | 信息面向本机用户（作者本人），不跨设备/网络显示；不泄露任何敏感数据。 |
| T-mc01-ui-03 | Denial of Service | chapterNotifierProvider watch 失败导致 UI 异常 | mitigate | 用 `.asData?.value.length ?? 0` 优雅降级；fresh 态不渲染徽章；测试覆盖 loading/error/empty chapter 场景由既有 KnowledgeBasePage 测试保证。 |
| T-mc01-ui-SC | Tampering | 无新增 npm/pip/cargo 依赖 | accept | 纯 Dart 代码 wiring，零外部包安装；本计划不引入任何新 pub 依赖（已使用的 flutter_riverpod/flutter/material 都在 pubspec）。 |

**Security enforcement note:** 本计划无新增第三方包安装、无网络代码、无 PII 处理、无认证/授权变更。STRIDE 全部 accept 或已 mitigate。无需 human package-legitimacy checkpoint。
</threat_model>

<verification>
1. **Lint 干净**：`flutter analyze lib/features/knowledge/presentation/knowledge_base_page.dart test/features/knowledge/knowledge_base_page_staleness_test.dart` 零 issue。
2. **新测试全绿**：`flutter test test/features/knowledge/knowledge_base_page_staleness_test.dart` 全通过（≥7 个 test cases）。
3. **零回归**：`flutter test test/features/knowledge/` 全套通过（不破坏 character_card / world_setting / character_relationship / skill 等既有测试）。
4. **改造范围红线**：`git diff --name-only HEAD` 输出**仅**包含：
   - `lib/features/knowledge/presentation/knowledge_base_page.dart`
   - `test/features/knowledge/knowledge_base_page_staleness_test.dart`
   不应出现 `kb_staleness_checker.dart`、`character_card.dart`、`world_setting.dart`、`providers.dart`、任何 notifier 类。
5. **格式化**：`dart format lib/features/knowledge/presentation/knowledge_base_page.dart test/features/knowledge/knowledge_base_page_staleness_test.dart`（CI formatting gate）。
6. **全量回归**（最后跑一次确保无关联破坏）：`flutter test` 全绿。
</verification>

<success_criteria>
- 知识库页面角色卡/世界观列表项在 stale/veryStale 时展示中文徽章（amber/red），fresh 时不展示
- 用户可通过 PopupMenu「标记为已验证」校准条目，触发 `save(copyWith(lastVerifiedChapter: count))`，徽章消失
- 改造仅触及 presentation 层 1 个文件 + 新建 1 个测试文件；checker/domain/providers 零修改
- ≥7 个 widget 测试覆盖三态 + 动作，全绿
- `flutter analyze` 零 issue，`dart format` 已应用
- 全套 `flutter test` 零回归
</success_criteria>

<output>
Create `.planning/quick/260616-hd9-mc-01-ui-wiring/260616-hd9-SUMMARY.md` when done. Include:
- 改了哪些文件（精确到 widget 方法名）
- 新增了哪些测试（按 group/test 列表）
- 验证命令的实际输出尾部（analyze / test）
- 任何 executor 调整（如 PopupMenu vs IconButton 的最终选择、Chapter 构造签名校正）
- 后续 wiring 待办（CI-01 act→prompt、AA-04 编辑器高亮）作为 STATE.md Next step 提示
</output>
