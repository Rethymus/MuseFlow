---
phase: 00-technical-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark/super_editor_app/pubspec.yaml
  - benchmark/super_editor_app/lib/main.dart
  - benchmark/super_editor_app/lib/benchmark_runner.dart
  - benchmark/appflowy_editor_app/pubspec.yaml
  - benchmark/appflowy_editor_app/lib/main.dart
  - benchmark/appflowy_editor_app/lib/benchmark_runner.dart
  - benchmark/shared/test_text_generator.dart
  - benchmark/results/SCORECARD.md
  - benchmark/results/PERFORMANCE_DATA.md
  - benchmark/results/API_EXTENSIBILITY.md
autonomous: false
requirements:
  - EDIT-01 (spike)
  - EDIT-04 (spike)

must_haves:
  truths:
    - "Both editors render a 100K+ character Chinese document without crashing"
    - "Frame time measurements are captured for scroll performance at multiple document sizes"
    - "API extensibility evaluation covers custom blocks, floating toolbar, and document queryability"
    - "Weighted scorecard produces a clear numerical winner with rationale"
    - "Human has verified scroll smoothness on both editors with their own eyes"
  artifacts:
    - path: "benchmark/super_editor_app/pubspec.yaml"
      provides: "Separate Flutter project for super_editor benchmark"
      contains: "super_editor"
    - path: "benchmark/appflowy_editor_app/pubspec.yaml"
      provides: "Separate Flutter project for appflowy_editor benchmark"
      contains: "appflowy_editor"
    - path: "benchmark/results/SCORECARD.md"
      provides: "Weighted scoring matrix with IME/perf/API/community scores"
      min_lines: 30
    - path: "benchmark/results/PERFORMANCE_DATA.md"
      provides: "Raw performance measurements"
      min_lines: 20
  key_links:
    - from: "benchmark/super_editor_app/lib/benchmark_runner.dart"
      to: "super_editor package"
      via: "pubspec dependency"
      pattern: "super_editor"
    - from: "benchmark/appflowy_editor_app/lib/benchmark_runner.dart"
      to: "appflowy_editor package"
      via: "pubspec dependency"
      pattern: "appflowy_editor"
---

<objective>
Editor benchmark spike: measure large Chinese document performance and evaluate API extensibility for both super_editor and appflowy_editor using identical benchmark suites.

Purpose: Per D-01, test BOTH editors with identical benchmark suite. Per D-02, weighted scoring (IME 40% + perf 30% + API 20% + community 10%). Per D-03, API extensibility evaluates custom block components, floating toolbar API, and document model queryability. Per D-04, the winner proceeds to Phase 1. This spike directly determines the editor foundation for the entire project.

Output: Two separate benchmark Flutter apps (one per editor), performance data at 10K/50K/100K/300K Chinese characters, API extensibility evaluation, and a weighted scorecard with editor recommendation.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-technical-validation/00-CONTEXT.md
@.planning/phases/00-technical-validation/00-RESEARCH.md
@.planning/phases/00-technical-validation/00-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create separate benchmark apps for both editors</name>
  <files>
    benchmark/super_editor_app/pubspec.yaml,
    benchmark/super_editor_app/lib/main.dart,
    benchmark/super_editor_app/lib/benchmark_runner.dart,
    benchmark/appflowy_editor_app/pubspec.yaml,
    benchmark/appflowy_editor_app/lib/main.dart,
    benchmark/appflowy_editor_app/lib/benchmark_runner.dart,
    benchmark/shared/test_text_generator.dart
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Standard Stack" section for version compatibility, "Working Dependency Set" for Dart 3.5.4 compatible versions
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Common Pitfalls" section for editor coexistence conflict
  </read_first>
  <action>
    BEFORE creating anything, run `flutter upgrade` then `dart --version` to check if Dart upgraded past 3.5.4. Record the result -- it determines which package version set to use.

    Create two SEPARATE Flutter projects under `benchmark/` directory (per RESEARCH.md Pitfall 2 -- editors CANNOT coexist in same pubspec):

    1. `benchmark/super_editor_app/` -- Flutter app with super_editor 0.3.0-dev.20 (pin EXACT version, no caret syntax per RESEARCH.md Pitfall 4). Use the working dependency set from RESEARCH.md. If flutter upgrade brought Dart to 3.9+, use CLAUDE.md aspirational versions instead.

    2. `benchmark/appflowy_editor_app/` -- Flutter app with appflowy_editor 6.0.0 (not 6.2.0 which needs Dart 3.6+). Same version selection logic based on dart --version result.

    Both apps share `benchmark/shared/test_text_generator.dart` -- a utility that generates Chinese text at specified character counts (10K, 50K, 100K, 300K characters). Generate realistic Chinese prose (not lorem ipsum) -- use paragraphs of 200-500 characters with mixed punctuation (，。！？、；：""''《》). The text must be deterministic (same seed produces same text) so both editors receive identical input.

    Each app's `main.dart` provides a simple UI: an editor widget filling the window with a toolbar showing buttons to load documents at each size step (10K, 50K, 100K, 300K) and a "Run Benchmark" button. The benchmark_runner.dart contains the performance measurement logic using `SchedulerBinding.instance.addTimingsCallback` (per RESEARCH.md "Don't Hand-Roll" section -- use Flutter's built-in frame timing).

    Each app's `pubspec.yaml` includes ONLY the editor being tested plus minimum required deps (flutter, window_manager for desktop). Do NOT install the full project dependency set here -- that happens in Plan 00-03.

    After creating both projects, run `flutter pub get` in each directory to verify dependency resolution succeeds. If it fails, adjust versions per RESEARCH.md working set and re-run.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow/benchmark/super_editor_app && flutter pub get && cd /home/re/code/MuseFlow/benchmark/appflowy_editor_app && flutter pub get</automated>
  </verify>
  <done>
    Both benchmark apps resolve dependencies successfully. shared/test_text_generator.dart generates deterministic Chinese text at 10K/50K/100K/300K characters. Each app has an editor widget that can load generated text and a benchmark trigger mechanism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run performance benchmarks and produce weighted scorecard</name>
  <files>
    benchmark/results/PERFORMANCE_DATA.md,
    benchmark/results/API_EXTENSIBILITY.md,
    benchmark/results/SCORECARD.md
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-CONTEXT.md -- D-02 weighted scoring weights, D-03 API extensibility criteria
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Editor Comparison" section for known issues, "Editor API Extensibility Comparison" table
    @benchmark/shared/test_text_generator.dart -- the test text generator created in Task 1
  </read_first>
  <action>
    **Performance benchmarks** (per D-02, 30% weight):

    For each editor app, programmatically measure frame times during scroll at each document size. Use `SchedulerBinding.instance.addTimingsCallback` to capture frame build+render times. The benchmark loads text, triggers programmatic scroll (via `ScrollController` animated scroll from top to bottom), captures frame timings, then reports: average frame time, 95th percentile frame time, number of frames exceeding 16ms, and maximum frame time.

    Document sizes: 10K (baseline), 50K (medium), 100K (threshold per ROADMAP), 300K (target per EDIT-04).

    Record raw measurements in `benchmark/results/PERFORMANCE_DATA.md` with tables per editor per size.

    **API extensibility evaluation** (per D-02, 20% weight; per D-03 three capabilities):

    Write `benchmark/results/API_EXTENSIBILITY.md` evaluating each editor on three capabilities:
    1. Custom block components -- can the editor register custom block types for story structure overlays? Document the API surface (class names, registration methods).
    2. Floating toolbar API -- does the editor provide a built-in floating toolbar widget, or must it be custom-built? Document the approach and estimated effort.
    3. Document model queryability -- can the document model be queried for node types, positions, custom metadata (needed for provenance tracking)? Document the query API.

    Rate each capability 1-5 for each editor. Use RESEARCH.md "Editor API Extensibility Comparison" as starting point but verify claims by reading each editor's source code (pub cache).

    **Community activity** (per D-02, 10% weight):
    Use GitHub data from RESEARCH.md (stars, last push, open issues, release cadence). Rate each editor 1-5.

    **IME compatibility** (per D-02, 40% weight):
    Leave this section as placeholder in SCORECARD.md -- it will be filled by Plan 00-02 (CJK IME validation). Note "PENDING -- filled by Plan 00-02" for the IME scores.

    **Weighted scorecard** (per D-02):

    Create `benchmark/results/SCORECARD.md` with the weighted calculation:
    - Total Score = IME (40%) + Performance (30%) + API Extensibility (20%) + Community (10%)
    - Each category scored 1-5, weighted sum produces final score
    - Include a "Recommendation" section noting that the final recommendation requires IME scores from Plan 00-02
  </action>
  <verify>
    <automated>test -f /home/re/code/MuseFlow/benchmark/results/SCORECARD.md && test -f /home/re/code/MuseFlow/benchmark/results/PERFORMANCE_DATA.md && test -f /home/re/code/MuseFlow/benchmark/results/API_EXTENSIBILITY.md && echo "All result files exist"</automated>
  </verify>
  <done>
    PERFORMANCE_DATA.md contains frame time measurements for both editors at 10K/50K/100K/300K characters. API_EXTENSIBILITY.md evaluates both editors on custom blocks, floating toolbar, and document queryability with 1-5 ratings. SCORECARD.md has weighted scoring with IME section marked pending. All three files have substantive content (not placeholder headings).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two editor benchmark apps with performance measurements and API extensibility evaluation</what-built>
  <how-to-verify>
    1. Open `benchmark/results/PERFORMANCE_DATA.md` and review frame time measurements
    2. Run the super_editor benchmark app: `cd benchmark/super_editor_app && flutter run -d windows`
       - Click each document size button (10K, 50K, 100K, 300K)
       - Scroll through the document manually
       - Observe: Does it feel smooth? Any visible jank? Does text render correctly?
    3. Run the appflowy_editor benchmark app: `cd benchmark/appflowy_editor_app && flutter run -d windows`
       - Repeat the same manual scroll test at each document size
    4. Compare the two editors' scroll experience subjectively
    5. Review `benchmark/results/API_EXTENSIBILITY.md` -- do the API evaluations match your expectations?
    6. Review `benchmark/results/SCORECARD.md` -- are the performance and API scores reasonable?
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Plan 00-02 (IME validation), or describe issues to address</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → pubspec | Package versions from registry; pin exact versions for dev-channel packages |
| Editor widget → Document model | Editor renders untrusted text content; no script injection risk in Flutter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-01 | Tampering | npm/pub packages | mitigate | Pin exact versions for dev-channel (super_editor); verify pub.dev provenance |
| T-00-01-SC | Tampering | pub.dev installs | accept | All packages [ASSUMED] per RESEARCH.md audit; verified via live pub get on actual toolchain |

</threat_model>

<verification>
- Both benchmark apps build and run on Windows: `flutter run -d windows` succeeds for each
- Performance data exists at all 4 document sizes for both editors
- API extensibility evaluation covers all 3 capabilities per D-03
- Scorecard applies D-02 weighted formula correctly
- IME section in scorecard marked pending for Plan 00-02
</verification>

<success_criteria>
- Frame time measurements captured for both editors at 10K, 50K, 100K, 300K characters
- API extensibility rated 1-5 for both editors on custom blocks, floating toolbar, document queryability
- Weighted scorecard populated (IME pending) with clear methodology
- Human has verified scroll performance on both editors
- Both editors render Chinese text correctly (no garbled characters)
</success_criteria>

<output>
Create `.planning/phases/00-technical-validation/00-01-SUMMARY.md` when done
</output>
