---
phase: 00-technical-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/core/domain/entities/.gitkeep
  - lib/core/application/ports/.gitkeep
  - lib/core/infrastructure/.gitkeep
  - lib/core/presentation/.gitkeep
  - lib/features/editor/domain/.gitkeep
  - lib/features/editor/application/.gitkeep
  - lib/features/editor/infrastructure/.gitkeep
  - lib/features/editor/presentation/.gitkeep
  - lib/features/knowledge/domain/.gitkeep
  - lib/features/knowledge/application/.gitkeep
  - lib/features/knowledge/infrastructure/.gitkeep
  - lib/features/knowledge/presentation/.gitkeep
  - lib/features/ai/domain/.gitkeep
  - lib/features/ai/application/.gitkeep
  - lib/features/ai/infrastructure/.gitkeep
  - lib/features/ai/presentation/.gitkeep
  - lib/features/capture/domain/.gitkeep
  - lib/features/capture/application/.gitkeep
  - lib/features/capture/infrastructure/.gitkeep
  - lib/features/capture/presentation/.gitkeep
  - lib/shared/theme/.gitkeep
  - lib/shared/constants/.gitkeep
  - lib/shared/utils/.gitkeep
  - test/streaming/sse_streaming_test.dart
  - test/streaming/test_api_server.dart
  - benchmark/results/COMPATIBILITY_MATRIX.md
  - benchmark/results/SSE_VALIDATION.md
autonomous: false
requirements:
  - AI-03 (spike)
  - TECH-03 (spike)
  - TECH-04 (spike)

user_setup:
  - service: "OpenAI or DeepSeek API"
    why: "SSE streaming validation requires a real API endpoint (per D-11)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Dashboard -> API Keys, or DeepSeek Platform -> API Keys"
      - name: OPENAI_BASE_URL
        source: "https://api.deepseek.com/v1 (for DeepSeek), or omit for OpenAI"
    dashboard_config: []

must_haves:
  truths:
    - "All core packages from CLAUDE.md resolve in a single pubspec.yaml without version conflicts"
    - "The project skeleton has the four-layer architecture directory structure per D-07"
    - "SSE tokens stream from a real AI API and insert into the winning editor without jank"
    - "flutter_secure_storage initializes correctly on Windows (per TECH-04)"
    - "The project skeleton builds and runs as a Windows desktop app"
    - "The winning editor is installed in the project (not both editors)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Full project dependency manifest with all core packages"
      contains: "flutter_riverpod"
    - path: "lib/main.dart"
      provides: "App entry point with Hive init, secure storage, and window management"
      min_lines: 30
    - path: "lib/features/editor/presentation/.gitkeep"
      provides: "Four-layer directory structure placeholder"
    - path: "test/streaming/sse_streaming_test.dart"
      provides: "SSE streaming integration test"
      contains: "stream"
    - path: "benchmark/results/COMPATIBILITY_MATRIX.md"
      provides: "Package compatibility results for all CLAUDE.md dependencies"
      min_lines: 30
  key_links:
    - from: "test/streaming/sse_streaming_test.dart"
      to: "openai_dart SDK"
      via: "SSE streaming client"
      pattern: "createChatCompletionStream|stream"
    - from: "pubspec.yaml"
      to: "all core packages"
      via: "dependency resolution"
      pattern: "flutter_riverpod|hive_ce|openai_dart"
---

<objective>
Package compatibility matrix and SSE streaming validation: install all core dependencies in a single project skeleton, verify they resolve without conflict, and validate SSE streaming from a real AI API into the winning editor.

Purpose: Per D-05, create a full Flutter project skeleton (not throwaway) that Phase 1 builds upon. Per D-06, install ALL dependencies from CLAUDE.md in one step to validate package compatibility. Per D-07, directory structure follows the four-layer architecture. Per D-11, validate full end-to-end SSE streaming with a real API. Per D-04, only install the WINNING editor (selected from Plans 00-01/00-02 results).

Output: A runnable Flutter project skeleton with all dependencies resolved, four-layer directory structure, SSE streaming validated with real API, and a compatibility matrix documenting which packages resolved at which versions.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-technical-validation/00-CONTEXT.md
@.planning/phases/00-technical-validation/00-RESEARCH.md
@.planning/phases/00-technical-validation/00-VALIDATION.md
@.planning/phases/00-technical-validation/00-01-SUMMARY.md
@.planning/phases/00-technical-validation/00-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton with all dependencies</name>
  <files>
    pubspec.yaml,
    lib/main.dart,
    lib/core/domain/entities/.gitkeep,
    lib/core/application/ports/.gitkeep,
    lib/core/infrastructure/.gitkeep,
    lib/core/presentation/.gitkeep,
    lib/features/editor/domain/.gitkeep,
    lib/features/editor/application/.gitkeep,
    lib/features/editor/infrastructure/.gitkeep,
    lib/features/editor/presentation/.gitkeep,
    lib/features/knowledge/domain/.gitkeep,
    lib/features/knowledge/application/.gitkeep,
    lib/features/knowledge/infrastructure/.gitkeep,
    lib/features/knowledge/presentation/.gitkeep,
    lib/features/ai/domain/.gitkeep,
    lib/features/ai/application/.gitkeep,
    lib/features/ai/infrastructure/.gitkeep,
    lib/features/ai/presentation/.gitkeep,
    lib/features/capture/domain/.gitkeep,
    lib/features/capture/application/.gitkeep,
    lib/features/capture/infrastructure/.gitkeep,
    lib/features/capture/presentation/.gitkeep,
    lib/shared/theme/.gitkeep,
    lib/shared/constants/.gitkeep,
    lib/shared/utils/.gitkeep,
    benchmark/results/COMPATIBILITY_MATRIX.md
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Standard Stack" section for working dependency set; "CRITICAL: Version Compatibility Gap" for the version crisis
    @.planning/phases/00-technical-validation/00-01-SUMMARY.md -- editor selection result (which editor won)
    @.planning/phases/00-technical-validation/00-02-SUMMARY.md -- IME validation result and final scorecard
    @.claude/rules/02-museflow-architecture.md -- four-layer directory structure specification
    @CLAUDE.md -- Technology Stack section for full dependency list
  </read_first>
  <action>
    Read the SUMMARY files from Plans 00-01 and 00-02 to determine the winning editor. Only install the winning editor -- do NOT install the other editor (per RESEARCH.md Pitfall 2 on transitive conflicts).

    **Step 1: Create Flutter project**
    Run `flutter create --project-name museflow --org com.museflow --platforms windows,android .` in the project root. This creates the base Flutter project structure.

    **Step 2: Create four-layer directory structure** (per D-07)
    Create all directories under `lib/`:
    - `core/domain/entities/`, `core/application/ports/`, `core/infrastructure/`, `core/presentation/`
    - `features/editor/domain/`, `features/editor/application/`, `features/editor/infrastructure/`, `features/editor/presentation/`
    - `features/knowledge/` (same sub-layers)
    - `features/ai/` (same sub-layers)
    - `features/capture/` (same sub-layers)
    - `shared/theme/`, `shared/constants/`, `shared/utils/`
    Place `.gitkeep` files in each empty directory so git tracks them.

    **Step 3: Write pubspec.yaml with all dependencies** (per D-06)
    Use the version set determined by the Dart SDK version check from Plan 00-01 Task 1:
    - If Dart is 3.9+: use CLAUDE.md aspirational versions
    - If Dart remains 3.5.4: use the working dependency set from RESEARCH.md

    Install ALL packages from CLAUDE.md Technology Stack section:
    - Core: flutter_riverpod, freezed, freezed_annotation, hive_ce, hive_ce_flutter, flutter_secure_storage
    - AI: openai_dart, anthropic_sdk_dart, ollama_dart
    - Editor: ONLY the winning editor
    - Navigation: go_router
    - Desktop: window_manager
    - Supporting: uuid, logger, markdown, path_provider, share_plus, url_launcher, connectivity_plus, json_annotation, google_fonts, file_picker (version determined by winning editor's transitive dependency)
    - Dev: build_runner, json_serializable

    **Step 4: Initialize main.dart**
    Create a minimal `lib/main.dart` that:
    - Initializes Hive via `Hive.initFlutter()` (per TECH-03)
    - Sets up window_manager for desktop (window size, title "MuseFlow 灵韵")
    - Runs a placeholder MaterialApp with a Scaffold showing "MuseFlow 灵韵" text and the winning editor widget
    - This verifies the editor widget renders in the project skeleton

    **Step 5: Run flutter pub get and verify**
    Run `flutter pub get` and record which packages resolved at which versions. If resolution fails, resolve conflicts using dependency_overrides or version adjustments and document the resolution.

    **Step 6: Build verification**
    Run `flutter build windows --debug` to verify the full project builds. Fix any compilation errors.

    **Step 7: Document compatibility matrix**
    Create `benchmark/results/COMPATIBILITY_MATRIX.md` with a table listing every package, the CLAUDE.md specified version, the actually resolved version, and notes on any gaps. Include the final pubspec.yaml content in the report.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter pub get && flutter build windows --debug 2>&1 | tail -5 && echo "BUILD OK"</automated>
  </verify>
  <done>
    Flutter project created with four-layer architecture directory structure. All CLAUDE.md dependencies installed and resolving without conflict. Only the winning editor is in pubspec.yaml. main.dart initializes Hive, window_manager, and renders a placeholder with the editor widget. Project builds successfully on Windows. COMPATIBILITY_MATRIX.md documents all resolved versions.
  </done>
</task>

<task type="auto">
  <name>Task 2: SSE streaming validation with real API</name>
  <files>
    test/streaming/sse_streaming_test.dart,
    test/streaming/test_api_server.dart,
    benchmark/results/SSE_VALIDATION.md
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Pattern 2: SSE Streaming Buffer" for the batched insertion approach, "Code Examples" for openai_dart streaming pattern, "Common Pitfalls" Pitfall 5 on AI SDK feature gaps at downgraded versions
    @.planning/phases/00-technical-validation/00-CONTEXT.md -- D-11 full end-to-end streaming, D-12 no Ollama, D-13 user provides API key
    @pubspec.yaml -- to verify openai_dart version and its API surface
  </read_first>
  <action>
    **Step 1: Implement SSE streaming test**

    Create `test/streaming/sse_streaming_test.dart` as an integration test that:

    a) Reads API credentials from environment variables (per D-13):
       - `OPENAI_API_KEY` -- required
       - `OPENAI_BASE_URL` -- optional, defaults to OpenAI; set to DeepSeek URL for DeepSeek testing
       - If env vars are missing, the test should SKIP (not fail) with a clear message: "Set OPENAI_API_KEY env var to run SSE streaming test"

    b) Creates an `openai_dart` client with the API key and optional base URL (for DeepSeek compatibility). Check the actual API surface of the resolved openai_dart version -- if 0.4.5, verify it has streaming support. If it does not, document this in SSE_VALIDATION.md as a blocking finding.

    c) Sends a streaming chat completion request with a Chinese prompt: "请用三句话描述一个武侠场景" (three sentences describing a martial arts scene). This tests Chinese text streaming.

    d) Implements the StreamingBuffer pattern from RESEARCH.md -- buffer tokens, batch-insert every 100ms into a document model. For the test, use a simple StringBuffer as the document stand-in (since this is a streaming test, not an editor integration test).

    e) Collects streaming metrics: total tokens received, time to first token, total streaming duration, average tokens per second.

    f) Verifies: stream completes without error, response contains Chinese characters, no garbled text.

    **Step 2: Test API key secure storage**

    Create `test/streaming/test_api_server.dart` (or add to the streaming test) a verification that `flutter_secure_storage` can write and read a test value on Windows. This validates TECH-04 (API key secure storage).

    **Step 3: Document SSE validation results**

    Create `benchmark/results/SSE_VALIDATION.md` with:
    - openai_dart version used and its streaming capabilities (or lack thereof)
    - Streaming test results: time to first token, total duration, tokens/sec
    - Whether Chinese text streams correctly (no encoding issues, no garbled characters)
    - Whether the batch-insertion pattern (StreamingBuffer) works without per-token overhead
    - flutter_secure_storage test result (write/read/delete cycle)
    - If openai_dart 0.4.5 lacks streaming: document this as a BLOCKING finding that requires Dart upgrade or alternative SDK
    - Recommendations for Phase 2 (AI adapter implementation)
  </action>
  <verify>
    <automated>test -f /home/re/code/MuseFlow/test/streaming/sse_streaming_test.dart && test -f /home/re/code/MuseFlow/benchmark/results/SSE_VALIDATION.md && echo "SSE test and report exist"</automated>
  </verify>
  <done>
    SSE streaming test exists that connects to real OpenAI/DeepSeek API with env-var credentials. Test validates Chinese text streaming, token metrics, and batch-insertion pattern. flutter_secure_storage read/write verified on Windows. SSE_VALIDATION.md documents results including openai_dart streaming capability assessment. If streaming is unavailable in the resolved SDK version, this is documented as a blocking finding.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full project skeleton with all dependencies, four-layer architecture, and SSE streaming validation</what-built>
  <how-to-verify>
    1. **Dependency resolution:** Run `flutter pub get` -- verify it succeeds without errors or dependency_overrides warnings
    2. **Build:** Run `flutter run -d windows` -- verify the app launches showing "MuseFlow 灵韵" with the winning editor widget
    3. **SSE streaming:**
       - Set environment variable: `export OPENAI_API_KEY=your-key-here`
       - Optionally: `export OPENAI_BASE_URL=https://api.deepseek.com/v1` for DeepSeek
       - Run: `flutter test test/streaming/sse_streaming_test.dart`
       - Verify the test passes (or review skip reason if no API key)
    4. **Review reports:**
       - Check `benchmark/results/COMPATIBILITY_MATRIX.md` -- any major version gaps from CLAUDE.md specs?
       - Check `benchmark/results/SSE_VALIDATION.md` -- streaming works? Chinese text correct? SDK adequate?
    5. **Directory structure:** Verify `lib/` has the four-layer architecture per CLAUDE.md
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 0, or describe issues to address</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI API → SSE stream | Untrusted external API responses; validate chunk structure before processing |
| flutter_secure_storage → Windows Credential Manager | Platform credential store; API keys must never appear in plaintext |
| pub.dev → pubspec | Package versions from registry; pin exact versions for critical packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | Information Disclosure | API key storage | mitigate | Use flutter_secure_storage exclusively; never log or print API keys; env vars for test only |
| T-00-03-02 | Tampering | SSE response parsing | mitigate | Validate SSE chunk structure before document insertion; catch malformed responses |
| T-00-03-03 | Tampering | Dependency versions | mitigate | Pin exact versions for critical packages (editor, AI SDKs); document resolved versions in COMPATIBILITY_MATRIX.md |
| T-00-03-SC | Tampering | pub.dev installs | accept | All packages [ASSUMED] per RESEARCH.md audit; verified via live pub get |

</threat_model>

<verification>
- `flutter pub get` succeeds with all CLAUDE.md dependencies
- `flutter build windows --debug` succeeds
- Four-layer directory structure exists under lib/
- Only the winning editor is in pubspec.yaml (not both)
- SSE streaming test exists with env-var API key handling
- SSE_VALIDATION.md documents streaming capability assessment
- COMPATIBILITY_MATRIX.md lists all resolved package versions
</verification>

<success_criteria>
- Full project skeleton builds and runs on Windows as a desktop app
- All core packages resolve without version conflicts in a single pubspec.yaml
- Four-layer architecture directory structure created per D-07
- SSE streaming from real AI API validated with Chinese text
- flutter_secure_storage works on Windows for API key storage
- Winning editor installed and rendering in the project
- Compatibility matrix documents every package version gap from CLAUDE.md specs
- Phase 1 can start building directly on this skeleton (per D-05)
</success_criteria>

<output>
Create `.planning/phases/00-technical-validation/00-03-SUMMARY.md` when done
</output>
