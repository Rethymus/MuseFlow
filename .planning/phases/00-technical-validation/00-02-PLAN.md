---
phase: 00-technical-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - benchmark/ime_super_editor_app/pubspec.yaml
  - benchmark/ime_super_editor_app/lib/main.dart
  - benchmark/ime_appflowy_editor_app/pubspec.yaml
  - benchmark/ime_appflowy_editor_app/lib/main.dart
  - test/ime/super_editor_ime_test.dart
  - test/ime/appflowy_editor_ime_test.dart
  - benchmark/results/IME_VALIDATION.md
autonomous: false
requirements:
  - TECH-02 (spike)

must_haves:
  truths:
    - "Automated tests simulate the IME composing-to-committed text lifecycle for both editors"
    - "Manual test protocol covers Sogou Pinyin, Wubi, and Microsoft Pinyin on both editors"
    - "IME candidate window position is explicitly checked during manual testing"
    - "IME scores are produced standalone in IME_VALIDATION.md for consolidation into SCORECARD.md by Plan 00-03"
    - "Both editors have been tested with physical keyboard on Windows"
  artifacts:
    - path: "benchmark/ime_super_editor_app/pubspec.yaml"
      provides: "Separate Flutter app for super_editor IME testing"
      contains: "super_editor"
    - path: "benchmark/ime_appflowy_editor_app/pubspec.yaml"
      provides: "Separate Flutter app for appflowy_editor IME testing"
      contains: "appflowy_editor"
    - path: "test/ime/super_editor_ime_test.dart"
      provides: "Automated IME composition tests for super_editor"
      contains: "composing"
    - path: "test/ime/appflowy_editor_ime_test.dart"
      provides: "Automated IME composition tests for appflowy_editor"
      contains: "composing"
    - path: "benchmark/results/IME_VALIDATION.md"
      provides: "IME test results and standalone scores for both editors"
      min_lines: 40
  key_links:
    - from: "test/ime/super_editor_ime_test.dart"
      to: "super_editor TextEditingDelta"
      via: "widget test"
      pattern: "TextEditingDelta|composing"
    - from: "benchmark/results/IME_VALIDATION.md"
      to: "benchmark/results/SCORECARD.md"
      via: "IME scores produced standalone here; Plan 00-03 consolidates into final scorecard"
      pattern: "IME.*score"
---

<objective>
CJK IME validation spike: validate that Chinese input methods (Sogou Pinyin, Wubi, Microsoft Pinyin) work correctly in both super_editor and appflowy_editor on Windows.

Purpose: Per D-08, dual validation with automated TextEditingDelta composition simulation AND manual keyboard testing. Per D-09, manual testing covers exactly 3 input methods: Sogou Pinyin, Wubi, Microsoft Pinyin. Per D-10, automated tests simulate the composing-to-committed text lifecycle. This is the highest-weighted criterion (40% per D-02) because CJK IME compatibility is existential for Chinese novel authors on Windows.

Output: Two IME test apps (one per editor), automated composition tests, manual IME test results, and standalone IME scores. These IME scores are consolidated into the final weighted scorecard by Plan 00-03 (Wave 2, depends on both 00-01 and 00-02).
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-technical-validation/00-CONTEXT.md
@.planning/phases/00-technical-validation/00-RESEARCH.md
@.planning/phases/00-technical-validation/00-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IME test apps and automated composition tests</name>
  <files>
    benchmark/ime_super_editor_app/pubspec.yaml,
    benchmark/ime_super_editor_app/lib/main.dart,
    benchmark/ime_appflowy_editor_app/pubspec.yaml,
    benchmark/ime_appflowy_editor_app/lib/main.dart,
    test/ime/super_editor_ime_test.dart,
    test/ime/appflowy_editor_ime_test.dart
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Editor Comparison: CJK IME Status" section for known bugs (#2588, #2728, #696), "Code Examples" section for automated IME composition test pattern
    @.planning/phases/00-technical-validation/00-CONTEXT.md -- D-08 dual validation, D-09 manual testing scope, D-10 automated composing lifecycle
    @.planning/phases/00-technical-validation/00-VALIDATION.md -- "Manual-Only Verifications" table for IME test instructions
  </read_first>
  <action>
    Create two SEPARATE minimal Flutter apps (per RESEARCH.md Pitfall 2 -- editors cannot coexist in same pubspec):

    1. `benchmark/ime_super_editor_app/` -- Minimal app with super_editor widget. Single text area, no toolbar, no formatting. Purpose: a clean surface for IME manual testing. Use same version as Plan 00-01 benchmark app. Include `window_manager` for proper desktop window.

    2. `benchmark/ime_appflowy_editor_app/` -- Minimal app with appflowy_editor widget. Same minimal design. Use same version as Plan 00-01 benchmark app.

    Each app's `main.dart` shows the editor filling the window with a simple title label indicating which editor is being tested. The editor should start empty for manual IME input testing.

    **Automated IME composition tests** (per D-10):

    Create `test/ime/super_editor_ime_test.dart` and `test/ime/appflowy_editor_ime_test.dart`. These are Flutter widget tests that simulate the IME composing-to-committed text lifecycle. Each test file tests the following scenarios using `TextEditingDelta` simulation (pattern from RESEARCH.md Code Examples):

    1. Pinyin composing lifecycle: simulate typing "jin" (composing state with TextRange(start: 0, end: 3)), then committing to "今" (composing cleared, TextRange.empty). Verify the final document contains the committed character.

    2. Multi-character composition: simulate "jintian" composing, then committing "今天". Verify both characters appear.

    3. Composition cancellation: simulate "jin" composing, then simulate deletion (backspace during composing). Verify composing text is removed and committed text is unchanged.

    4. Mixed Chinese and ASCII: simulate typing "hello" (no composing), then "nihao" composing to "你好". Verify both segments coexist correctly.

    IMPORTANT: These tests verify the TEXT CONTENT is correct. They cannot verify IME candidate window POSITION (that requires manual testing per RESEARCH.md Pitfall 3).

    For super_editor tests, note that `flutter_test_robots` is the companion test package for super_editor -- check if it provides IME simulation utilities. If so, use them instead of raw TextEditingDelta.

    Run `flutter pub get` and `flutter test` in each test directory to verify the tests execute (they may fail if the editor's IME handling differs -- that IS the point of the spike).
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow/benchmark/ime_super_editor_app && flutter pub get && cd /home/re/code/MuseFlow/benchmark/ime_appflowy_editor_app && flutter pub get && echo "Both IME apps resolve"</automated>
  </verify>
  <done>
    Both IME test apps created with minimal editor UI. Automated composition tests exist for both editors covering pinyin composing, multi-character commit, cancellation, and mixed input. Tests compile and run (pass or fail results recorded).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run automated tests and prepare standalone IME validation report</name>
  <files>
    benchmark/results/IME_VALIDATION.md
  </files>
  <read_first>
    @.planning/phases/00-technical-validation/00-RESEARCH.md -- "Editor Comparison: CJK IME Status" for known bugs to check against
    @.planning/phases/00-technical-validation/00-CONTEXT.md -- D-02 weighted scoring, D-08 dual validation approach
  </read_first>
  <action>
    Run the automated IME composition tests for both editors. Record pass/fail results for each test scenario in each editor.

    Create `benchmark/results/IME_VALIDATION.md` as a STANDALONE report with these sections:

    **Section 1: Automated Test Results**
    Table with columns: Test Scenario | super_editor Result | appflowy_editor Result | Notes
    Rows: Pinyin composing, Multi-character commit, Composition cancellation, Mixed Chinese/ASCII
    Result values: PASS / FAIL / ERROR (with error details)

    **Section 2: Known Bug Verification Checklist**
    For each known CJK IME bug from RESEARCH.md:
    - super_editor #2588 (IME candidate window position) -- status: needs manual verification
    - super_editor #2728 (Chinese composing crash) -- status: check if automated tests triggered a crash
    - appflowy_editor #696 (Sogou garbled character order) -- status: needs manual verification with Sogou
    Mark each as "Automated: PASS/FAIL" or "Requires Manual Testing"

    **Section 3: Manual Test Protocol** (per D-08, D-09)
    Prepare the exact steps the human will follow for manual IME testing. For each of the 3 input methods (Sogou Pinyin, Wubi, Microsoft Pinyin), on each of the 2 editors (6 combinations total):

    1. Launch the IME test app for the editor
    2. Switch to the target input method
    3. Type a test phrase (specify the exact phrase per IME)
       - Sogou Pinyin: type "jintian tianqi bucuo" expect "今天天气不错"
       - Wubi: type "wgkq" expect "今天" (or provide the correct Wubi code)
       - Microsoft Pinyin: type "women shi zhongguo ren" expect "我们是中国人"
    4. Verify: composing underline appears, candidate window appears near cursor, committed text is correct, no garbled characters
    5. Explicitly check: IME candidate window position (per RESEARCH.md Pitfall 3 and super_editor #2588) -- is it below the cursor or in the wrong position?
    6. Test deletion during composing: type pinyin, press backspace during composing, verify clean cancellation
    7. Test selection during composing: type pinyin, use arrow keys to select alternative candidate, verify correct character committed

    **Section 4: IME Scoring Template** (per D-02, 40% weight)
    Create a scoring template where each IME method is rated 1-5 per editor:
    - Sogou Pinyin score (1-5)
    - Wubi score (1-5)
    - Microsoft Pinyin score (1-5)
    - IME candidate window position score (1-5)
    - Average IME score (this is produced standalone; Plan 00-03 will merge it into SCORECARD.md)

    The scores are left as "PENDING MANUAL TESTING" -- the human checkpoint will fill them in.
  </action>
  <verify>
    <automated>test -f /home/re/code/MuseFlow/benchmark/results/IME_VALIDATION.md && grep -c "PENDING MANUAL TESTING" /home/re/code/MuseFlow/benchmark/results/IME_VALIDATION.md | grep -qv "^0$" && echo "IME validation report exists with pending manual slots"</automated>
  </verify>
  <done>
    IME_VALIDATION.md contains automated test results for both editors, known bug verification status, complete manual test protocol for all 3 IMEs on both editors (6 combinations), and a standalone scoring template with "PENDING MANUAL TESTING" for manual scores. Plan 00-03 will consolidate these IME scores into the final SCORECARD.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>IME test apps for both editors, automated composition tests, and manual test protocol</what-built>
  <how-to-verify>
    **Automated tests:**
    1. Review automated test results in `benchmark/results/IME_VALIDATION.md` Section 1

    **Manual IME testing (6 combinations):**
    For each editor (super_editor then appflowy_editor):
    1. Launch the IME test app:
       - super_editor: `cd benchmark/ime_super_editor_app && flutter run -d windows`
       - appflowy_editor: `cd benchmark/ime_appflowy_editor_app && flutter run -d windows`

    2. Test each input method following the protocol in IME_VALIDATION.md Section 3:
       a. **Sogou Pinyin**: Type "jintian tianqi bucuo" -- verify "今天天气不错" appears correctly. Check candidate window position (is it below cursor?). Try backspace during composing.
       b. **Wubi**: Type Wubi codes -- verify correct characters. Check candidate window position.
       c. **Microsoft Pinyin**: Type "women shi zhongguo ren" -- verify "我们是中国人". Check candidate window position.

    3. For each combination, rate 1-5 based on:
       - 5: Perfect -- composing underline, correct candidates, correct commit, candidate window in right position
       - 3: Usable with minor issues -- text commits correctly but candidate window position is off
       - 1: Broken -- garbled text, crashes, or characters out of order

    4. Fill in the IME scores in `benchmark/results/IME_VALIDATION.md` Section 4

    5. Update `benchmark/results/SCORECARD.md` with the IME scores (if it exists from Plan 00-01; otherwise Plan 00-03 will consolidate all scores)

    6. After updating both files, review the COMPLETE scorecard -- all 4 categories should now be scored for both editors.
  </how-to-verify>
  <resume-signal>Type "approved" with final editor choice (super_editor or appflowy_editor), or describe issues found during IME testing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System IME → Flutter editor | Platform IME sends composing deltas; editor must handle correctly |
| Editor widget → Document model | Committed text from IME becomes document content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Tampering | IME input | accept | System IME is trusted; input is local user content |
| T-00-02-SC | Tampering | pub.dev installs | accept | Same packages as Plan 00-01, already verified |

</threat_model>

<verification>
- Both IME test apps build and run on Windows
- Automated composition tests execute for both editors (results recorded regardless of pass/fail)
- Manual test protocol covers all 3 IMEs on both editors (6 combinations)
- IME_VALIDATION.md produced standalone with scoring template
- Plan 00-03 will consolidate IME scores into the final SCORECARD.md
</verification>

<success_criteria>
- Automated IME composition test results documented for both editors
- Human has manually tested Sogou Pinyin, Wubi, and Microsoft Pinyin on both editors
- IME candidate window position explicitly checked (per super_editor #2588 concern)
- IME scores (1-5) recorded for both editors in IME_VALIDATION.md
- Standalone IME report ready for consolidation into final weighted scorecard by Plan 00-03
- Human approves final editor choice (per D-04)
</success_criteria>

<output>
Create `.planning/phases/00-technical-validation/00-02-SUMMARY.md` when done
</output>
