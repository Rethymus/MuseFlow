---
phase: 03-editor-ai-toolbar
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/features/editor/domain/diff_state.dart
  - lib/features/editor/application/diff_calculator.dart
  - lib/features/editor/infrastructure/provenance_attribution.dart
  - lib/features/editor/presentation/diff_display.dart
  - lib/features/editor/presentation/status_bar.dart
  - lib/features/editor/presentation/editor_page.dart
  - lib/features/editor/application/editor_ai_notifier.dart
  - lib/core/presentation/providers.dart
autonomous: true
requirements:
  - EDIT-05

must_haves:
  truths:
    - "After an AI operation completes, the result is shown as sentence-level inline diff in the editor"
    - "Deleted sentences are shown with red strikethrough, inserted sentences shown with green background"
    - "User can accept or reject individual sentences via a floating action bar on selection"
    - "After accepting all sentences, the accepted text has a blue provenance background"
    - "Status bar shows count of pending AI modifications"
  artifacts:
    - path: "lib/features/editor/domain/diff_state.dart"
      provides: "DiffResult, SentenceDiff, DiffStatus immutable entities"
    - path: "lib/features/editor/application/diff_calculator.dart"
      provides: "DiffCalculator that pairs old/new sentences and produces DiffResult"
    - path: "lib/features/editor/infrastructure/provenance_attribution.dart"
      provides: "AI provenance NamedAttribution constant and stylesheet integration"
    - path: "lib/features/editor/presentation/diff_display.dart"
      provides: "Inline diff rendering with red deletions and green insertions"
    - path: "lib/features/editor/presentation/status_bar.dart"
      provides: "Status bar widget showing pending AI modification count"
  key_links:
    - from: "lib/features/editor/application/editor_ai_notifier.dart"
      to: "lib/features/editor/application/diff_calculator.dart"
      via: "After streaming completes, calculate diff between original and AI text"
      pattern: "DiffCalculator\\.calculate"
    - from: "lib/features/editor/presentation/diff_display.dart"
      to: "lib/features/editor/domain/diff_state.dart"
      via: "Reads DiffResult to render per-sentence highlights"
      pattern: "DiffResult.*SentenceDiff"
    - from: "lib/features/editor/infrastructure/provenance_attribution.dart"
      to: "super_editor Attribution system"
      via: "NamedAttribution with unique id 'ai_provenance'"
      pattern: "NamedAttribution.*ai_provenance"
---

<objective>
Text provenance tracking -- inline diff display and AI-modified text visual distinction

Purpose: After an AI operation completes (Plan 01), show the result as sentence-level inline diff with red deletions and green insertions. Users accept/reject per sentence. Accepted text gets a blue provenance background. Status bar tracks pending modifications. This enables Plan 03 (selective undo) to operate on the diff state.

Output: DiffState domain entities, DiffCalculator for sentence-level diffing, provenance attribution system, inline diff display widget, accept/reject floating action bar, and status bar integration.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-editor-ai-toolbar/03-CONTEXT.md
@.planning/phases/03-editor-ai-toolbar/03-RESEARCH.md
@.planning/phases/03-editor-ai-toolbar/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 03-01 (created in wave 1) -->

From lib/features/editor/domain/editor_ai_state.dart:
```dart
class EditorAIState {
  final bool isStreaming;
  final EditorAIOperation? operation;
  final String? progressText;
  final String? error;
  final String selectedText;
  final String selectionNodeId;
  final int selectionStartOffset;
  final int selectionEndOffset;
  final String? userInstruction;
  EditorAIState copyWith({...});
}

enum EditorAIOperation { toneRewrite, paragraphPolish, freeInput }
```

From lib/features/editor/application/editor_ai_notifier.dart:
```dart
class EditorAINotifier extends Notifier<EditorAIState> {
  void startOperation(EditorAIOperation operation, String selectedText, DocumentRange range, {String? userInstruction});
  void cancel();
  void reset();
}
```

From lib/features/editor/application/sentence_segmenter.dart:
```dart
class SentenceSegmenter {
  static List<String> segment(String text);
}
```

<!-- From existing codebase -->

From super_editor attributions (RESEARCH.md verified):
```dart
class BackgroundColorAttribution implements Attribution {
  const BackgroundColorAttribution(this.color);
  String get id => 'background_color';
  final Color color;
}

// Custom attribution approach (avoids id conflict per Pitfall 2):
class NamedAttribution implements Attribution {
  const NamedAttribution(this.id);
  final String id;
  bool canMergeWith(Attribution other) => this == other;
}
```

From super_editor document mutation:
```dart
// DeleteContentRequest: editor.execute([DeleteContentRequest(documentRange: range)])
// InsertTextRequest: editor.execute([InsertTextRequest(documentPosition: pos, textToInsert: text, attributions: {attribution})])
// ToggleTextAttributionsRequest: editor.execute([ToggleTextAttributionsRequest(documentRange: range, attributions: {attribution})])
```

From lib/features/editor/presentation/editor_page.dart:
```dart
// EditorPage has _editor (Editor) and exposes via editorProvider
// SuperEditor(editor: _editor, ...) -- will add selectionLayerLinks in Plan 01
```

From lib/features/editor/presentation/editor_provider.dart:
```dart
final editorProvider = NotifierProvider<EditorHolderNotifier, Editor?>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DiffState entities, DiffCalculator, and provenance attribution</name>
  <files>
    lib/features/editor/domain/diff_state.dart,
    lib/features/editor/application/diff_calculator.dart,
    lib/features/editor/infrastructure/provenance_attribution.dart,
    lib/core/presentation/providers.dart,
    test/features/editor/domain/diff_state_test.dart,
    test/features/editor/application/diff_calculator_test.dart,
    test/features/editor/infrastructure/provenance_attribution_test.dart
  </files>
  <read_first>
    lib/features/editor/domain/editor_ai_state.dart
    lib/features/editor/application/sentence_segmenter.dart
    lib/core/domain/fragment.dart
  </read_first>
  <behavior>
    - DiffStatus enum: pending, accepted, rejected
    - SentenceDiff: immutable with fields originalText (String?), newText (String?), status (DiffStatus), nodeId (String), startOffset (int), endOffset (int); isDeletion returns originalText != null && newText == null; isInsertion returns originalText == null && newText != null; isModification returns originalText != null && newText != null
    - DiffResult: immutable with fields sentences (List<SentenceDiff>), originalRange (DocumentRange), nodeId (String); pendingCount getter returns count of sentences where status == DiffStatus.pending; allResolved getter returns pendingCount == 0
    - DiffCalculator.calculate(originalText, aiText) -> DiffResult: uses SentenceSegmenter to split both texts into sentences, pairs sentences by position (simple index-based pairing for same-count inputs, or greedy matching for different counts), produces SentenceDiff list with DiffStatus.pending for each pair
    - DiffCalculator handles: equal sentence counts (1:1 pairing), more AI sentences than original (extra are insertions), fewer AI sentences than original (missing are deletions)
    - aiProvenanceAttribution: const NamedAttribution('ai_provenance') -- uses unique id to avoid BackgroundColorAttribution conflict per Pitfall 2
    - provenanceColor: const Color(0x1A2196F3) -- blue with 10% opacity per D-09
    - ProvenanceAttributions helper class with static methods: applyProvenance(Editor, DocumentRange), removeProvenance(Editor, DocumentRange) -- use ToggleTextAttributionsRequest
  </behavior>
  <action>
    Create DiffState entities in lib/features/editor/domain/diff_state.dart. Define DiffStatus enum (pending, accepted, rejected). Define SentenceDiff as an immutable class with: originalText (String? -- null for pure insertions), newText (String? -- null for pure deletions), status (DiffStatus, default pending), nodeId (String), startOffset (int), endOffset (int). Include computed getters: isDeletion, isInsertion, isModification. Include copyWith. Define DiffResult as an immutable class with: sentences (List<SentenceDiff>), originalRange (DocumentRange -- from super_editor), nodeId (String). Include pendingCount getter and allResolved getter. All classes follow the Fragment entity pattern (manual == and hashCode).

    Create DiffCalculator in lib/features/editor/application/diff_calculator.dart. A pure utility class with static method `calculate(String originalText, String aiText, String nodeId, int startOffset) -> DiffResult`. Implementation: (1) segment both texts using SentenceSegmenter.segment(), (2) pair sentences by index -- for equal counts, 1:1 pairing produces modification SentenceDiff entries, (3) for unequal counts, pair as many as possible and mark extras as insertions or deletions, (4) calculate offsets: each SentenceDiff's startOffset/endOffset are computed by accumulating sentence lengths from startOffset. The originalRange is a DocumentRange spanning the full original text. For the common case (same sentence count), each SentenceDiff is a modification with both originalText and newText set, status pending.

    Create provenance_attribution.dart in lib/features/editor/infrastructure/. Define: (1) `const aiProvenanceAttribution = NamedAttribution('ai_provenance')` using super_editor's NamedAttribution class (not BackgroundColorAttribution, per Pitfall 2 -- unique id avoids conflict), (2) `const provenanceColor = Color(0x1A2196F3)` per D-09 (blue, 10% opacity), (3) a ProvenanceAttributions utility class with static methods `applyProvenance(Editor editor, DocumentRange range)` that executes ToggleTextAttributionsRequest with the provenance attribution, and `removeProvenance(Editor editor, DocumentRange range)` that toggles it off. Per D-10: provenance is removed on accept.

    Register diffCalculatorProvider in lib/core/presentation/providers.dart as a simple Provider returning a DiffCalculator instance (stateless utility, but consistent with other providers).

    Write tests following TDD: DiffResult pendingCount/allResolved, DiffCalculator.calculate for equal/equal sentence counts, provenance attribution constants.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/editor/domain/diff_state_test.dart test/features/editor/application/diff_calculator_test.dart test/features/editor/infrastructure/provenance_attribution_test.dart --reporter compact</automated>
  </verify>
  <done>
    - DiffStatus enum with pending/accepted/rejected values
    - SentenceDiff immutable entity with isDeletion/isInsertion/isModification getters
    - DiffResult with pendingCount and allResolved computed properties
    - DiffCalculator produces correct DiffResult for equal and unequal sentence counts
    - aiProvenanceAttribution uses NamedAttribution with unique id 'ai_provenance'
    - ProvenanceAttributions.applyProvenance/removeProvenance execute correct editor requests
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Inline diff display, accept/reject action bar, and status bar</name>
  <files>
    lib/features/editor/presentation/diff_display.dart,
    lib/features/editor/presentation/status_bar.dart,
    lib/features/editor/presentation/editor_page.dart,
    lib/features/editor/application/editor_ai_notifier.dart,
    lib/core/presentation/providers.dart
  </files>
  <read_first>
    lib/features/editor/presentation/editor_page.dart
    lib/features/editor/presentation/floating_toolbar.dart
    lib/features/editor/domain/diff_state.dart
    lib/features/editor/domain/editor_ai_state.dart
    lib/features/editor/infrastructure/provenance_attribution.dart
    lib/features/editor/application/editor_ai_notifier.dart
    lib/shared/theme/app_theme.dart
  </read_first>
  <action>
    Extend EditorAIState in lib/features/editor/domain/editor_ai_state.dart by adding a `diffResult` field (DiffResult?, default null). Update copyWith to include it. When an AI operation completes in EditorAINotifier, calculate the DiffResult using DiffCalculator and store it in state.diffResult. Add an `acceptSentence(int index)` method and `rejectSentence(int index)` method to EditorAINotifier. acceptSentence: (1) find the SentenceDiff at index, (2) if it is a modification, delete the original range and insert newText with aiProvenanceAttribution applied per D-10, (3) if it is a deletion, delete the original range, (4) if it is an insertion, insert newText with provenance, (5) update the sentence's status to accepted in state.diffResult. rejectSentence: (1) if modification, keep original text (do nothing to document, just mark rejected), (2) if insertion, delete the inserted range, (3) if deletion, re-insert the original text, (4) update status to rejected. Per RESEARCH.md Pitfall 5: batch delete+insert in a single editor.execute() call to create one undo entry. Add `acceptAll()` and `rejectAll()` convenience methods.

    Create diff_display.dart in lib/features/editor/presentation/. This file contains the DiffOverlayBuilder -- a widget that renders inline diff highlights in the editor. The overlay reads the current DiffResult from editorAIState. For each pending SentenceDiff, it renders an overlay highlight: (1) deletions: red background with strikethrough style (Color(0x33FF0000) -- red 20% opacity), (2) insertions: green background (Color(0x3300FF00) -- green 20% opacity). The overlay uses the editor's document layout to calculate the position of each sentence range. For accepted/rejected sentences, no overlay is shown (accepted text already has provenance attribution from the document model). This widget is added to the documentOverlayBuilders list in EditorPage alongside the floating toolbar overlay.

    Create the AcceptRejectBar -- a floating widget that appears when the user selects text that overlaps with pending diff sentences. It uses the same SelectionLayerLinks + Follower pattern as the floating toolbar. Shows two buttons: "接受" (green) and "拒绝" (red). If the selection covers multiple pending sentences, show "全部接受" and "全部拒绝" instead. Per D-03: the bar appears on text selection, not permanently visible. The bar reads from editorAIState.diffResult to determine which sentences are in the selection range and pending.

    Create status_bar.dart in lib/features/editor/presentation/. A simple ConsumerWidget that shows at the bottom of the editor area (below the SuperEditor). Per D-11: displays "当前文档有 N 处AI修改待确认" when N > 0, where N = editorAIState.diffResult.pendingCount. When N == 0, the status bar is hidden (SizedBox.shrink). Style: subtle text on surfaceContainerLow background, single line height.

    Modify editor_page.dart to: (1) add DiffOverlayBuilder to documentOverlayBuilders list, (2) add AcceptRejectBar to the overlay builders (only when diffResult has pending sentences), (3) add StatusBar widget below the Expanded(editor) area in the Column layout. The status bar sits at the bottom of the page, below the editor scroll area.

    Per D-04: Add a WillPopScope or NavigatorObserver check -- when navigating away from the editor page, if editorAIState.diffResult.pendingCount > 0, show a confirmation dialog "有 N 处未确认的AI修改，确定离开吗？" with "留在当前页" and "放弃修改" options. This can be implemented as a PopScope widget wrapping the editor page body.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter analyze lib/features/editor/presentation/diff_display.dart lib/features/editor/presentation/status_bar.dart lib/features/editor/presentation/editor_page.dart lib/features/editor/application/editor_ai_notifier.dart 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Inline diff display shows red deletions and green insertions for pending sentences (D-01, D-02)
    - AcceptRejectBar appears on selection overlapping pending diff sentences (D-03)
    - Accept sentence replaces original with AI text + provenance attribution (D-10)
    - Reject sentence keeps/restores original text
    - Status bar shows pending count > 0 (D-11)
    - Leave-page warning dialog when unresolved diffs exist (D-04)
    - All editor.execute() calls batch delete+insert for single undo entry (Pitfall 5)
    - flutter analyze passes with zero errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI response -> Diff state | Streamed AI text is processed into diff state entities |
| Diff state -> Document model | Accept/reject modifies the editor document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Document mutation on accept | mitigate | All mutations use Editor.execute() requests (not direct AttributedText mutation), preserving undo/redo chain |
| T-03-05 | Information Disclosure | Diff state in memory | accept | DiffResult is ephemeral in Riverpod state, not persisted. No sensitive data beyond user's own text |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages added |
</threat_model>

<verification>
1. `flutter test test/features/editor/` passes
2. `flutter analyze lib/features/editor/` shows zero errors
3. After AI operation, inline diff appears with red/green highlights
4. Selecting a pending sentence shows accept/reject action bar
5. Accepting a sentence applies provenance blue background
6. Rejecting a sentence restores original text
7. Status bar shows pending count
8. Navigating away with pending diffs shows warning dialog
</verification>

<success_criteria>
- Sentence-level inline diff displayed after AI operations (D-01, D-02)
- Accept/reject per sentence via floating action bar on selection (D-03)
- Accepted text has blue provenance background (D-09, D-10) -- provenance is applied on accept and persists as a marker that this text was AI-modified
- Pending diff state (red/green highlights) clears on accept/reject -- the inline diff overlay disappears, replaced by the persistent provenance background
- Status bar shows pending AI modification count (D-11)
- Leave-page warning for unresolved diffs (D-04)
- All tests pass, zero analyze errors
</success_criteria>

<output>
Create `.planning/phases/03-editor-ai-toolbar/03-02-SUMMARY.md` when done
</output>
