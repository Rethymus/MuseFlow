---
phase: 16-analysis-reports
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - lib/features/reports/application/token_cost_report_service.dart
  - lib/features/reports/application/pain_point_report_service.dart
  - lib/features/reports/presentation/token_cost_report_page.dart
  - lib/features/reports/presentation/pain_point_report_page.dart
  - lib/features/reports/presentation/charts/cost_projection_chart.dart
  - lib/features/reports/presentation/severity_indicator.dart
  - lib/features/reports/providers.dart
  - test/features/reports/application/token_cost_report_service_test.dart
  - test/features/reports/application/pain_point_report_service_test.dart
  - test/features/reports/presentation/token_cost_report_page_test.dart
  - test/features/reports/presentation/pain_point_report_page_test.dart
autonomous: true
requirements: [REPORT-01, REPORT-02]
user_setup: []
must_haves:
  truths:
    - "Token cost report aggregates all audit records and computes per-operation-type and per-chapter breakdowns"
    - "Token cost report extrapolates to 50万字 (500k chars) with variance range (low/high multipliers)"
    - "Token cost report provides template-based optimization suggestions"
    - "Token cost report page renders 4 summary cards (input tokens, output tokens, API calls, actual word count) + pie chart + projection section + optimization suggestions"
    - "Pain point report catalogs all 6 Phase 14 issues + 0 Phase 15 issues, classified by category and severity"
    - "Pain point report page renders categorized issue list with severity indicators (高=red, 中=yellow, 低=grey)"
    - "Both report pages have export button in AppBar that triggers Markdown file export"
  artifacts:
    - path: "lib/features/reports/application/token_cost_report_service.dart"
      provides: "TokenCostReportService -- aggregates audit data, computes projection"
      min_lines: 60
    - path: "lib/features/reports/application/pain_point_report_service.dart"
      provides: "PainPointReportService -- catalogs known issues from ISSUE-LOG files"
      min_lines: 40
    - path: "lib/features/reports/presentation/token_cost_report_page.dart"
      provides: "REPORT-01 detail page with summary cards, pie chart, projection, suggestions"
      min_lines: 120
    - path: "lib/features/reports/presentation/pain_point_report_page.dart"
      provides: "REPORT-02 detail page with categorized issue list"
      min_lines: 100
    - path: "lib/features/reports/presentation/charts/cost_projection_chart.dart"
      provides: "BarChart comparing actual vs projected costs"
      min_lines: 60
    - path: "lib/features/reports/presentation/severity_indicator.dart"
      provides: "Colored dot + label widget for severity levels"
      min_lines: 25
    - path: "lib/features/reports/providers.dart"
      provides: "Riverpod providers for token cost and pain point reports"
      min_lines: 30
  key_links:
    - from: "lib/features/reports/application/token_cost_report_service.dart"
      to: "lib/features/stats/infrastructure/token_audit_repository.dart"
      via: "loadAll() to get all TokenAuditRecord entries"
      pattern: "loadAll|buildSnapshot"
    - from: "lib/features/reports/application/token_cost_report_service.dart"
      to: "lib/features/reports/domain/token_cost_report.dart"
      via: "constructs and returns TokenCostReport"
      pattern: "TokenCostReport"
    - from: "lib/features/reports/application/pain_point_report_service.dart"
      to: "lib/features/reports/domain/pain_point_report.dart"
      via: "constructs and returns PainPointReport with hardcoded Phase 14/15 issues"
      pattern: "PainPointReport"
    - from: "lib/features/reports/providers.dart"
      to: "lib/features/reports/presentation/token_cost_report_page.dart"
      via: "Riverpod AsyncNotifier consumed by ConsumerWidget"
      pattern: "tokenCostReportProvider"
---

<objective>
Build REPORT-01 (Token Cost Analysis) service + page and REPORT-02 (Pain Point Report) service + page. Token cost aggregates existing audit records with 50万字 extrapolation. Pain points catalogs known issues from Phase 14/15 ISSUE-LOGs as a structured document.

Purpose: Deliver two of four analytical reports. REPORT-01 provides actionable cost insights. REPORT-02 documents known pain points for product improvement.

Output: 2 report services, 2 report pages, 1 chart widget, 1 severity widget, providers barrel, 4 test files
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-analysis-reports/16-RESEARCH.md
@.planning/phases/16-analysis-reports/16-UI-SPEC.md
@.planning/phases/16-analysis-reports/16-VALIDATION.md

# Domain models (created in Plan 01)
From lib/features/reports/domain/token_cost_report.dart:
- TokenCostReport: totalInputTokens, totalOutputTokens, totalCalls, actualWordCount (double), costByType (Map<AuditOperationType, int>), costByChapter (Map<String, int>), projection (TokenCostProjection), optimizationSuggestions (List<String>)
- TokenCostProjection: targetWordCount (500000.0), multiplier (double), estimatedInputTokens (int), estimatedOutputTokens (int), estimatedCalls (int), lowEstimateMultiplier (double), highEstimateMultiplier (double)

From lib/features/reports/domain/pain_point_report.dart:
- PainPointReport: issues (List<PainPointIssue>), totalHigh (int), totalMedium (int), totalLow (int)
- PainPointIssue: id, category (String: 功能缺陷/体验摩擦/缺失需求), severity (String: 高/中/低), requirement (String?), title, description, status

From lib/features/reports/application/report_export_service.dart:
- ReportExportService with buildTokenCostMarkdown(TokenCostReport) and buildPainPointMarkdown(PainPointReport)

# TokenAuditRepository interface
From lib/features/stats/infrastructure/token_audit_repository.dart:
```dart
class TokenAuditRepository {
  Future<List<TokenAuditRecord>> loadAll() async { ... }
  Future<TokenAuditSnapshot> buildSnapshot() async { ... }
}
class TokenAuditSnapshot {
  final int totalInputTokens;
  final int totalOutputTokens;
  final int totalCalls;
  final List<TokenAuditRecord> records;
}
```

# AuditOperationType enum groups
8 types in 4 groups: organize (synthesis), edit (rewrite, polish, freeInput), worldview (skillGen, opening, deviationDetect), template (templateComplete)

# Existing chart patterns
From lib/features/stats/presentation/charts/ (per RESEARCH.md):
- BarChart from fl_chart: maxY * 1.2, gridData show:false, borderData show:false, bottomTitles with getTitlesWidget
- Chart section height: 220px, card wrapper pattern from WritingStatsPage _ChartSection

# Phase 14 ISSUE-LOG data (for PainPointReportService)
6 issues:
1. P14-04-GLM-01: 功能缺陷, 高, closed (Serial GLM generation -- fixed by enforceD11Bounds)
2. P14-04-AUTO-01: 缺失需求, 中 (IME/pixel-level toolbar -- partially automated, IME deferred)
3. P14-04-AI-01: 功能缺陷, 中, closed (Anti-AI-scent phrases -- fixed by P14-05)
4. P14-07-UI-01: 体验摩擦, 低 (Dark theme text contrast -- fixed)
5. P14-07-HUMAN-01: 缺失需求, 中 (Chinese IME -- deferred to native device)
6. P14-07-HUMAN-02: 缺失需求, 中, closed (DeviationWarningWidget -- fixed by P14-09)
Phase 15 ISSUE-LOG: 0 issues.

# UI-SPEC copywriting for TokenCostReportPage
Page title: "Token 消耗分析", subtitle: "实际成本核算与长篇推算。"
Summary cards: "输入 Token", "输出 Token", "API 调用次数", "实际字数"
Chart section: "按操作类型分布"
Projection section: "50万字长篇推算" with rows "预估总 Token", "预估 API 调用", "估算范围"
Optimization section: "优化建议" with lightbulb icons
Export CTA: "导出报告"

# UI-SPEC copywriting for PainPointReportPage
Page title: "痛点报告", subtitle: "功能缺陷、体验摩擦与缺失需求汇总。"
Sections: "功能缺陷", "体验摩擦", "缺失需求"
Severity colors: 高=colorScheme.error, 中=colorScheme.tertiary, 低=colorScheme.onSurfaceVariant
Empty state: "创作验证过程中未发现痛点。"
Export CTA: "导出报告"

# UI-SPEC chart spec for CostProjectionChart
BarChart from fl_chart, height 220px, 2 grouped bars (actual=primary, projected=tertiary), bar width 48px per group, X-axis labels "输入 Token", "输出 Token", "API 调用"

# StatsSummaryCard interface
```dart
class StatsSummaryCard extends StatelessWidget {
  const StatsSummaryCard({super.key, required this.icon, required this.title, required this.value, this.subtitle});
}
```

# Token audit provider pattern
From lib/core/presentation/providers.dart:
```dart
final tokenAuditRepositoryProvider = FutureProvider<TokenAuditRepository>((ref) async { ... });
final tokenAuditNotifierProvider = ... // AsyncNotifier<TokenAuditSnapshot>
```

# ExportService pattern for file writing
```dart
typedef FileWriter = Future<void> Function(String path, String content);
class ExportService {
  static Future<void> dartFileWriter(String path, String content) async { ... }
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build TokenCostReportService and PainPointReportService with providers</name>
  <files>
    lib/features/reports/application/token_cost_report_service.dart
    lib/features/reports/application/pain_point_report_service.dart
    lib/features/reports/providers.dart
    test/features/reports/application/token_cost_report_service_test.dart
    test/features/reports/application/pain_point_report_service_test.dart
  </files>
  <read_first>
    lib/features/stats/infrastructure/token_audit_repository.dart
    lib/features/stats/domain/token_audit_record.dart
    lib/features/stats/domain/audit_operation_type.dart
    lib/features/reports/domain/token_cost_report.dart
    lib/features/reports/domain/pain_point_report.dart
    lib/features/reports/application/report_export_service.dart
    .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
    .planning/phases/16-analysis-reports/16-RESEARCH.md
  </read_first>
  <behavior>
    - Test: TokenCostReportService.generate() with empty records returns zero-value report
    - Test: TokenCostReportService.generate() with sample records aggregates totalInputTokens, totalOutputTokens, totalCalls correctly
    - Test: TokenCostReportService.generate() groups records by AuditOperationType into costByType map
    - Test: TokenCostReportService.generate() groups records by chapterId into costByChapter map
    - Test: TokenCostReportService.generate() computes actualWordCount from chapter content lengths (requires ChapterRepository -- mock in test)
    - Test: TokenCostReportService.generate() computes projection with multiplier = 500000.0 / actualWordCount
    - Test: TokenCostReportService.generate() computes lowEstimateMultiplier as 0.8 * multiplier and highEstimateMultiplier as 1.2 * multiplier
    - Test: TokenCostReportService.generate() provides at least 2 optimization suggestions when records exist
    - Test: PainPointReportService.generate() returns PainPointReport with exactly 6 issues from Phase 14 (0 from Phase 15)
    - Test: PainPointReportService.generate() correctly categorizes issues: 3 功能缺陷, 1 体验摩擦, 2 缺失需求
    - Test: PainPointReportService.generate() correctly counts severity: 1 高 (closed), 3 中, 2 低
    - Test: PainPointReportService.generate() issues have correct status (closed/open/deferred) per ISSUE-LOG
    - Test: Providers barrel exports tokenCostReportProvider and painPointReportProvider as AsyncNotifierProvider
  </behavior>
  <action>
    Create `TokenCostReportService` in `lib/features/reports/application/token_cost_report_service.dart`. Constructor takes `TokenAuditRepository` and `ChapterRepository`. The `generate()` method:
    1. Calls `_auditRepository.buildSnapshot()` to get all records and totals
    2. Groups records by operationType into `Map<AuditOperationType, int>` (sum of totalTokens per type)
    3. Groups records by chapterId into `Map<String, int>` (sum of totalTokens per chapter)
    4. Computes actualWordCount from `_chapterRepository.getAll()` -- sums `.content.length` for all chapters (content is the markdown string; use `content.replaceAll(RegExp(r'\s'), '').length` for Chinese character count approximation)
    5. Computes projection: multiplier = 500000.0 / max(actualWordCount, 1). estimatedInputTokens = (totalInputTokens * multiplier).round(), estimatedOutputTokens = (totalOutputTokens * multiplier).round(), estimatedCalls = (totalCalls * multiplier).round(). lowEstimateMultiplier = multiplier * 0.8, highEstimateMultiplier = multiplier * 1.2 (per RESEARCH.md Pitfall 2: provide range, not single number)
    6. Generates optimization suggestions from template rules (e.g., "批量操作减少API调用开销" if totalCalls > actualWordCount / 500, "使用更便宜的模型进行草稿生成" if costByType[synthesis] > totalCost * 0.5, "减少知识库注入上下文长度以降低输入Token" if inputTokens > outputTokens * 3, "当前万字短篇Token消耗在合理范围内" as baseline suggestion)
    7. Returns TokenCostReport with all computed fields

    Create `PainPointReportService` in `lib/features/reports/application/pain_point_report_service.dart`. Constructor takes no dependencies (data is hardcoded from Phase 14/15 ISSUE-LOGs per RESEARCH.md -- this is NOT automated issue detection). The `generate()` method:
    1. Creates 6 PainPointIssue objects from Phase 14 ISSUE-LOG data (IDs: P14-04-GLM-01, P14-04-AUTO-01, P14-04-AI-01, P14-07-UI-01, P14-07-HUMAN-01, P14-07-HUMAN-02)
    2. Each issue has correct category, severity, requirement, title, description (from ISSUE-LOG title column), status (closed/open/deferred)
    3. Sorts by severity (高 first, then 中, then 低)
    4. Counts totalHigh, totalMedium, totalLow
    5. Returns PainPointReport

    Create `lib/features/reports/providers.dart` barrel file with:
    - `tokenCostReportServiceProvider` -- Provider<TokenCostReportService> injecting TokenAuditRepository and ChapterRepository
    - `tokenCostReportProvider` -- AsyncNotifierProvider<TokenCostReportNotifier, TokenCostReport> that calls service.generate() in build()
    - `painPointReportServiceProvider` -- Provider<PainPointReportService>
    - `painPointReportProvider` -- AsyncNotifierProvider<PainPointReportNotifier, PainPointReport> that calls service.generate() in build()
    Follow the @riverpod annotation pattern used in existing notifiers (AsyncNotifier from riverpod_annotation). The notifier classes should be private (_TokenCostReportNotifier, _PainPointReportNotifier) with generated providers.

    If @riverpod code gen adds complexity, use manual Riverpod AsyncNotifier pattern instead (same as TokenAuditNotifier in stats module).

    Tests: Mock TokenAuditRepository and ChapterRepository for TokenCostReportService tests. PainPointReportService tests verify hardcoded data correctness.
  </action>
  <verify>
    <automated>flutter test test/features/reports/application/token_cost_report_service_test.dart test/features/reports/application/pain_point_report_service_test.dart -x --timeout 60s</automated>
  </verify>
  <done>
    TokenCostReportService aggregates audit data with projection and suggestions. PainPointReportService catalogs 6 known issues. Both providers registered in barrel file. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TokenCostReportPage, PainPointReportPage, CostProjectionChart, and SeverityIndicator</name>
  <files>
    lib/features/reports/presentation/token_cost_report_page.dart
    lib/features/reports/presentation/pain_point_report_page.dart
    lib/features/reports/presentation/charts/cost_projection_chart.dart
    lib/features/reports/presentation/severity_indicator.dart
    lib/features/reports/presentation/report_card.dart
    test/features/reports/presentation/token_cost_report_page_test.dart
    test/features/reports/presentation/pain_point_report_page_test.dart
  </files>
  <read_first>
    lib/features/stats/presentation/writing_stats_page.dart
    lib/features/stats/presentation/stats_summary_card.dart
    lib/features/stats/presentation/charts/operation_type_pie_chart.dart
    lib/features/stats/presentation/charts/chapter_token_bar_chart.dart
    lib/features/reports/domain/token_cost_report.dart
    lib/features/reports/domain/pain_point_report.dart
    lib/features/reports/presentation/report_card.dart
    lib/features/reports/providers.dart
    .planning/phases/16-analysis-reports/16-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: TokenCostReportPage renders 4 StatsSummaryCards with "输入 Token", "输出 Token", "API 调用次数", "实际字数" labels
    - Test: TokenCostReportPage renders "按操作类型分布" chart section
    - Test: TokenCostReportPage renders "50万字长篇推算" section with projection values
    - Test: TokenCostReportPage renders "优化建议" section with lightbulb icons
    - Test: TokenCostReportPage has export IconButton in AppBar
    - Test: CostProjectionChart renders 2 grouped bars (actual + projected) for 3 categories
    - Test: PainPointReportPage renders categorized issue sections (功能缺陷, 体验摩擦, 缺失需求)
    - Test: PainPointReportPage shows SeverityIndicator with correct color per severity (高=error, 中=tertiary, 低=onSurfaceVariant)
    - Test: PainPointReportPage has export IconButton in AppBar
    - Test: SeverityIndicator renders colored container dot + text label
  </behavior>
  <action>
    Create `CostProjectionChart` in `lib/features/reports/presentation/charts/cost_projection_chart.dart`: StatelessWidget taking `TokenCostProjection` and `TokenCostReport` (for actual values). Uses fl_chart BarChart with height 220px, 2 bar groups (actual=primary color, projected=tertiary color), 3 categories on X-axis ("输入 Token", "输出 Token", "API 调用"). Each category has a grouped pair. Follow the BarChart pattern from chapter_token_bar_chart.dart: maxY * 1.2, gridData show:false, borderData show:false.

    Create `SeverityIndicator` in `lib/features/reports/presentation/severity_indicator.dart`: StatelessWidget taking `String severity` (高/中/低). Renders a small colored Container (6x6 circle) + Text label. Color mapping: 高 = colorScheme.error, 中 = colorScheme.tertiary, 低 = colorScheme.onSurfaceVariant.

    Create `TokenCostReportPage` in `lib/features/reports/presentation/token_cost_report_page.dart`: ConsumerWidget matching the UI-SPEC layout. AppBar with title "Token 消耗分析" and export IconButton. Body uses `ref.watch(tokenCostReportProvider)` with AsyncValue.when pattern (loading/error/data). Data state renders:
    - Page title "Token 消耗分析" (headlineMedium) + subtitle "实际成本核算与长篇推算。" (bodyMedium)
    - _SummaryCards row: 4 StatsSummaryCards in Wrap (input tokens formatted with comma separator, output tokens formatted, total calls, actual word count formatted as integer)
    - _ChartSection "按操作类型分布" containing OperationTypePieChart (reuse existing from stats/charts/ -- import and pass costByType map)
    - Card "50万字长篇推算" containing 3 _ProjectionRow list tiles: "预估总 Token" with formatted projection.estimatedInputTokens + projected.estimatedOutputTokens, "预估 API 调用" with projection.estimatedCalls, "估算范围" with "${(projection.lowEstimateMultiplier * totalTokens).round()} ~ ${(projection.highEstimateMultiplier * totalTokens).round()}"
    - Card "优化建议" containing ListTile per suggestion with Icons.lightbulb_outline leading
    Export button calls ReportExportService.buildTokenCostMarkdown() then ExportService.dartFileWriter() then shows SnackBar "报告已导出至: {path}".

    Create `PainPointReportPage` in `lib/features/reports/presentation/pain_point_report_page.dart`: ConsumerWidget matching UI-SPEC. AppBar with title "痛点报告" and export IconButton. Body uses `ref.watch(painPointReportProvider)` with AsyncValue.when. Data state renders:
    - Page title "痛点报告" (headlineMedium) + subtitle "功能缺陷、体验摩擦与缺失需求汇总。" (bodyMedium)
    - Summary Wrap: 3 StatsSummaryCards showing totalHigh, totalMedium, totalLow counts
    - 3 sections: "功能缺陷", "体验摩擦", "缺失需求" -- each with titleMedium heading + filtered issue cards
    - Each issue card: Card > ListTile with SeverityIndicator leading, title, subtitle (requirement if non-null), trailing status text
    - Empty state when issues list is empty: Card with "创作验证过程中未发现痛点。"
    Export button calls ReportExportService.buildPainPointMarkdown() then ExportService.dartFileWriter() then SnackBar.

    Replace placeholder SizedBox builders in app.dart routes with the actual page widgets (import from reports/presentation).

    Widget tests: Use ProviderContainer overrides with mock report data to test rendering without real repositories.
  </action>
  <verify>
    <automated>flutter test test/features/reports/presentation/token_cost_report_page_test.dart test/features/reports/presentation/pain_point_report_page_test.dart -x --timeout 60s && flutter analyze lib/features/reports/ lib/app.dart</automated>
  </verify>
  <done>
    TokenCostReportPage renders summary cards, pie chart, projection section, and optimization suggestions. PainPointReportPage renders categorized issues with severity indicators. Both pages have working export buttons. app.dart routes serve actual pages. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit data aggregation | Read-only access to existing Hive token_audit box |
| Pain point data | Hardcoded from ISSUE-LOG files, no user input |
| Report export | File write to app directory via ExportService |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | ExportService file path | mitigate | Delegates to ExportService.dartFileWriter which uses dart:io File API. No path traversal from user input -- export filename is hardcoded per UI-SPEC (token-cost-report.md, pain-point-report.md). |
| T-16-05 | Information Disclosure | Token cost data | accept | Token counts are not sensitive -- they are already visible on the existing TokenAuditPage. |
| T-16-06 | Spoofing | Pain point data integrity | accept | Hardcoded from ISSUE-LOG -- no user input involved. |
</threat_model>

<verification>
## Overall Plan Verification

1. TokenCostReportService.generate() returns correct aggregation from audit records
2. 50万字 extrapolation produces multiplier, low/high range, estimated totals
3. PainPointReportService catalogs all 6 Phase 14 issues with correct categories/severities
4. TokenCostReportPage renders 4 summary cards + pie chart + projection + suggestions
5. PainPointReportPage renders categorized issues with severity colors
6. Both pages have export buttons producing Markdown files
7. CostProjectionChart renders 2 grouped bars
8. SeverityIndicator maps 高/中/低 to correct colors
9. app.dart routes serve actual page widgets (not placeholders)
10. All tests pass: `flutter test test/features/reports/ -x --timeout 120s`
11. `flutter analyze` zero errors
</verification>

<success_criteria>
- REPORT-01: Token cost report computes actual costs + 50万字 projection with variance range + optimization suggestions
- REPORT-02: Pain point report catalogs 6 known issues from Phase 14/15, classified by category and severity
- Both report pages render correctly with export functionality
- All tests pass, flutter analyze zero errors
</success_criteria>

<output>
Create `.planning/phases/16-analysis-reports/16-02-SUMMARY.md` when done
</output>
