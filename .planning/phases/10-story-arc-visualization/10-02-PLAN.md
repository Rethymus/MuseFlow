---
phase: 10-story-arc-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - lib/features/story_structure/presentation/story_arc/story_arc_graph.dart
  - lib/features/story_structure/presentation/story_arc/story_arc_node.dart
  - lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart
  - lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart
  - lib/features/story_structure/presentation/story_structure_page.dart
  - test/features/story_structure/presentation/story_arc_graph_test.dart
  - test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart
autonomous: true
requirements:
  - VIZO-01
  - VIZO-02
  - VIZO-03
  - VIZO-04
user_setup: []
must_haves:
  truths:
    - "StoryStructurePage has 5 tabs with '弧线图' as third tab per UI-SPEC layout"
    - "Graph renders PlotNode data as color-coded rectangular nodes with chapter badges"
    - "Edges visually distinguish three relationship types per D-09/D-10/D-11"
    - "Causal edges have gradient + arrow, association edges are thin gray, foreshadowing edges are dashed amber"
    - "Tapping a node opens NodeEditBottomSheet with five editable fields per D-14"
    - "Empty state shows '暂无剧情节点' with icon and '创建第一个节点' button per D-16"
    - "Graph supports zoom and pan via InteractiveViewer integration"
    - "Node sizes vary by title length: <=8 chars = 120px, >8 chars = 160px per D-02"
  artifacts:
    - path: "lib/features/story_structure/presentation/story_arc/story_arc_graph.dart"
      provides: "Main ConsumerStatefulWidget wrapping GraphView.builder"
      min_lines: 80
    - path: "lib/features/story_structure/presentation/story_arc/story_arc_node.dart"
      provides: "Node builder widget with color-coded Container and badge layout"
      min_lines: 60
    - path: "lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart"
      provides: "Custom ArrowEdgeRenderer subclass for 3 edge types"
      min_lines: 80
    - path: "lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart"
      provides: "Bottom sheet form for editing title, chapter, summary, role, status"
      min_lines: 100
    - path: "lib/features/story_structure/presentation/story_structure_page.dart"
      provides: "Updated with 5th tab for graph view"
      contains: "弧线图"
  key_links:
    - from: "story_arc_graph.dart"
      to: "providers.dart"
      via: "ref.watch(plotNodeNotifierProvider)"
      pattern: "plotNodeNotifierProvider"
    - from: "story_arc_graph.dart"
      to: "story_arc_node.dart"
      via: "GraphView.builder node callback"
      pattern: "StoryArcNode"
    - from: "story_arc_graph.dart"
      to: "story_arc_edge_renderer.dart"
      via: "FruchtermanReingoldAlgorithm renderer parameter"
      pattern: "StoryArcEdgeRenderer"
    - from: "story_arc_node.dart"
      to: "graph_colors.dart"
      via: "GraphColor.forRole() and GraphStatus.borderColor()"
      pattern: "GraphColor\\.forRole"
    - from: "node_edit_bottom_sheet.dart"
      to: "plot_node_notifier.dart"
      via: "ref.read(plotNodeNotifierProvider.notifier).save()"
      pattern: "plotNodeNotifierProvider"
    - from: "story_structure_page.dart"
      to: "story_arc_graph.dart"
      via: "TabBarView children list and StoryArcGraph import"
      pattern: "StoryArcGraph"
---

<objective>
Build the interactive graph visualization UI: GraphView.builder with FruchtermanReingold layout, color-coded StoryArcNode widgets, custom StoryArcEdgeRenderer for three edge types, NodeEditBottomSheet for inline editing, empty state, and integrate as a new tab in StoryStructurePage.

Purpose: Deliver the core visual graph experience where users see their story structure as an interactive node-and-edge diagram with distinct visual styling per relationship type and node role.
Output: Functional graph view with styled nodes/edges, inline editing via bottom sheet, and tab integration.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-story-arc-visualization/10-CONTEXT.md
@.planning/phases/10-story-arc-visualization/10-RESEARCH.md
@.planning/phases/10-story-arc-visualization/10-PATTERNS.md
@.planning/phases/10-story-arc-visualization/10-UI-SPEC.md
@.planning/phases/10-story-arc-visualization/10-01-PLAN.md
</context>

<interfaces>
<!-- From Plan 01 -- newly created types -->

From lib/features/story_structure/presentation/story_arc/graph_colors.dart:
```dart
class GraphColor {
  static Color forRole(PlotNodeStructuralRole role, {bool isDark = true});
}
class GraphStatus {
  static Color borderColor(PlotNodeWritingStatus status);
  static String borderPattern(PlotNodeWritingStatus status); // 'dashed'/'solid'/'dotted'
  static IconData statusIcon(PlotNodeWritingStatus status);
}
class GraphTheme {
  GraphTheme({required Brightness brightness});
  Color roleColor(PlotNodeStructuralRole role);
  Color statusBorderColor(PlotNodeWritingStatus status);
}
```

<!-- From Plan 01 -- new providers -->
From lib/core/presentation/providers.dart:
```dart
final nodePositionRepositoryProvider = FutureProvider<NodePositionRepository>((ref) async {
  final box = await Hive.openBox<dynamic>('graph_positions');
  return NodePositionRepository(box);
});
final nodePositionNotifierProvider =
    AsyncNotifierProvider<NodePositionNotifier, Map<String, Offset>>(
      NodePositionNotifier.new,
    );
```

<!-- Existing types -->

From lib/features/story_structure/presentation/story_structure_page.dart:
```dart
class StoryStructurePage extends ConsumerStatefulWidget { ... }
class _StoryStructurePageState extends ConsumerState<StoryStructurePage>
    with SingleTickerProviderStateMixin {
  late final TabController _tabController; // currently length: 4
  // tabs: [伏笔, 剧情线, 守护, 整理与导出]
  // children: [_ForeshadowingSection, PlotTimeline, GuardianPanel, _FinishExportSection]
  // FAB: index 0 -> foreshadowing form, index 1 -> plot node form, _ -> null
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StoryArcNode widget and StoryArcEdgeRenderer</name>
  <files>
    - lib/features/story_structure/presentation/story_arc/story_arc_node.dart
    - lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart
  </files>
  <read_first>
    - lib/features/story_structure/domain/plot_node.dart (PlotNode fields: id, title, chapter, structuralRole, writingStatus)
    - lib/features/story_structure/presentation/story_arc/graph_colors.dart (GraphColor.forRole, GraphStatus.borderColor, GraphStatus.borderPattern, GraphStatus.statusIcon -- created in Plan 01)
    - lib/features/story_structure/presentation/plot_timeline.dart (_PlotNodeCard pattern, lines 128-243)
    - .planning/phases/10-story-arc-visualization/10-PATTERNS.md (story_arc_node.dart and story_arc_edge_renderer.dart patterns)
    - .planning/phases/10-story-arc-visualization/10-UI-SPEC.md (StoryArcNode layout spec and edge color table)
  </read_first>
  <behavior>
    - StoryArcNode renders a Container with width 120px for title.length <= 8, 160px for longer titles per D-02
    - StoryArcNode Container has 4px borderRadius per D-03
    - StoryArcNode background color comes from GraphColor.forRole(plotNode.structuralRole)
    - StoryArcNode border: 2px width, color from GraphStatus.borderColor(plotNode.writingStatus), pattern from GraphStatus.borderPattern (dashed/solid/dotted)
    - StoryArcNode uses Stack layout: title text (14px, maxLines 2, ellipsis) at top-left, chapter badge "Ch{N}" (12px w600) at top-right per D-04
    - StoryArcNode has status icon (from GraphStatus.statusIcon) positioned at top-right offset per D-06/UI-SPEC (top: 4, right: 24)
    - EdgeType enum has three values: causal, association, foreshadowing
    - StoryArcEdgeRenderer.renderEdge dispatches to type-specific render methods
    - Causal edges: use LinearGradient shader on paint (source=Colors.blue.shade700, dest=Colors.blue.shade300), stroke width 2.0px, arrow at destination per D-09/D-12
    - Association edges: Colors.grey.shade600 (dark) or Colors.grey.shade400 (light), stroke width 1.0px, no arrow, solid line per D-10/D-12
    - Foreshadowing edges: Colors.amber.shade500, stroke width 1.5px, dashed line, dot markers at intervals per D-11/D-12
  </behavior>
  <action>
    Create StoryArcNode in `lib/features/story_structure/presentation/story_arc/story_arc_node.dart` as a StatelessWidget. Takes `PlotNode plotNode` and `VoidCallback? onTap` as constructor parameters. Builds a GestureDetector wrapping a Container with the Stack layout described above. The Container decoration uses BoxDecoration with color from GraphColor.forRole(), borderRadius BorderRadius.circular(4), and border via Border.all. For dashed/dotted border patterns, use a CustomPaint border approach or an inner decoration since Border.all does not support dashed borders -- implement via a second Container with a DashedBorder CustomPainter overlay for non-solid patterns. Include Semantics wrapper with label "编辑节点: {plotNode.title}".

    Create EdgeType enum and StoryArcEdgeRenderer in `lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart`. The renderer extends ArrowEdgeRenderer from graphview and takes `Map<Edge, EdgeType> edgeTypes` in its constructor. Override `renderEdge(Canvas canvas, Edge edge, Paint paint)` to dispatch based on edgeTypes map lookup. For causal edges: create a new Paint with LinearGradient shader from source to destination positions (blue.shade700 to blue.shade300), set strokeWidth to 2.0, then draw the line and arrow using parent class methods or direct canvas drawing. For association edges: draw a simple line with grey color, 1.0 stroke width, no arrow. For foreshadowing edges: draw a dashed line in amber with 1.5 stroke width using canvas.drawRawPoints or dash pattern, plus small circle markers at regular intervals.

    IMPORTANT -- Anti-patterns from RESEARCH.md:
    - Always set paint.style = PaintingStyle.stroke for edge paints (Pitfall 5)
    - Edge deduplication: when building the edgeTypes map in the graph widget, only create edges from ONE direction of each relationship to avoid duplicates (Pitfall 1). Use only consequenceNodeIds for causal edges (source->dest), only relatedNodeIds for association, only linkedForeshadowingIds for foreshadowing. Do NOT also create edges from causeNodeIds.
    - Maintain a Set<String> of added edge keys ("{sourceId}->{destId}") to prevent duplicates.
  </action>
  <verify>
    <automated>flutter analyze lib/features/story_structure/presentation/story_arc/story_arc_node.dart lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart</automated>
  </verify>
  <done>
    StoryArcNode renders color-coded rectangular nodes with chapter badges and status icons. StoryArcEdgeRenderer provides three visually distinct edge styles. `flutter analyze` shows zero issues on both files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StoryArcGraph main widget and NodeEditBottomSheet</name>
  <files>
    - lib/features/story_structure/presentation/story_arc/story_arc_graph.dart
    - lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart
  </files>
  <read_first>
    - lib/features/story_structure/presentation/plot_timeline.dart (AsyncValue.when pattern for loading/error/data states, empty state pattern)
    - lib/features/story_structure/presentation/plot_node_form.dart (form field setup: TextEditingControllers, _isSaving flag, _save pattern, _roleLabel, _statusLabel)
    - lib/features/story_structure/presentation/story_structure_page.dart (current 4-tab structure, _buildFAB pattern)
    - lib/features/story_structure/presentation/story_arc/story_arc_node.dart (created in Task 1)
    - lib/features/story_structure/presentation/story_arc/story_arc_edge_renderer.dart (created in Task 1)
    - lib/features/story_structure/presentation/story_arc/graph_colors.dart (GraphTheme for brightness-aware colors)
    - .planning/phases/10-story-arc-visualization/10-PATTERNS.md (story_arc_graph.dart and node_edit_bottom_sheet.dart patterns)
    - .planning/phases/10-story-arc-visualization/10-UI-SPEC.md (layout specs, interaction contracts, copywriting)
  </read_first>
  <behavior>
    - StoryArcGraph watches plotNodeNotifierProvider and nodePositionNotifierProvider
    - When loading: shows CircularProgressIndicator
    - When error: shows error message + "重试" button per UI-SPEC
    - When data is empty: shows empty state with Icons.account_tree_outlined (48px), "暂无剧情节点" heading, "创建情节点来可视化你的故事弧线结构" body, and "创建第一个节点" FilledButton per D-16/UI-SPEC
    - When data exists: builds graphview Graph from PlotNode list, creates Node.Id for each, adds edges with per-edge Paint and edgeTypes map, runs FruchtermanReingoldAlgorithm with shuffleNodes:false when saved positions exist, uses GraphView.builder
    - Graph uses autoZoomToFit: true, animated: true
    - Node builder returns StoryArcNode with onTap callback that shows NodeEditBottomSheet
    - NodeEditBottomSheet takes optional PlotNode (null = create mode) and onSave callback
    - Bottom sheet has drag handle, "编辑节点" or "新建节点" heading per UI-SPEC
    - Five form fields: title (TextField, required), chapter (TextField numeric keyboard), summary (TextField maxLines 3), structural role (DropdownButtonFormField with _roleLabel options), writing status (DropdownButtonFormField with _statusLabel options)
    - Save button calls notifier.save() with node.copyWith(...) for edit mode, or notifier.add() for create mode
    - Title validation: empty title shows SnackBar "请输入标题"
    - _isSaving flag disables button during async save
  </behavior>
  <action>
    Create StoryArcGraph in `lib/features/story_structure/presentation/story_arc/story_arc_graph.dart` as a ConsumerStatefulWidget (needs TransformationController state for Plan 03). The build method watches `plotNodeNotifierProvider` and uses `.when(loading:, error:, data:)` pattern from PlotTimeline. In the data callback: if nodes.isEmpty return empty state widget; otherwise return the graph content.

    For the graph content, create a `_buildGraph(List<PlotNode> nodes, Map<String, Offset> positions)` method that:
    1. Creates a `Graph()..isTree = false`
    2. Creates Node.Id for each PlotNode, stores in `Map<String, Node>` cache (Pitfall 2: reuse Node objects)
    3. Restores saved positions from the positions map onto Node.position
    4. Iterates nodes to create edges, building an `edgeTypes` map. ONLY iterate consequenceNodeIds for causal (A->B), relatedNodeIds for association (A->B, deduplicated via Set), linkedForeshadowingIds for foreshadowing. Set per-edge Paint with appropriate color, strokeWidth (2.0/1.0/1.5), and style=PaintingStyle.stroke.
    5. Creates FruchtermanReingoldAlgorithm with FruchtermanReingoldConfiguration(iterations: 50, shuffleNodes: positions.isEmpty). Passes StoryArcEdgeRenderer(edgeTypes: edgeTypesMap) as renderer.
    6. Returns GraphView.builder with the graph, algorithm, controller, animated: true, autoZoomToFit: true.

    Node builder callback: looks up PlotNode by `node.key.value as String`, returns StoryArcNode(plotNode: foundNode, onTap: () => _showEditSheet(context, foundNode)).

    The empty state follows the UI-SPEC exactly: Center, Padding(32), Column with Icons.account_tree_outlined (48px, colorScheme.outline), SizedBox(16), Text "暂无剧情节点" (20px w600), SizedBox(8), Text body (14px w400, maxWidth 280), SizedBox(24), FilledButton "创建第一个节点".

    Create NodeEditBottomSheet in `lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart` as a ConsumerStatefulWidget. Takes `PlotNode? node` (null = create mode) and `ValueChanged<PlotNode>? onSave`. Uses TextEditingControllers for title/chapter/summary initialized from node in initState. DropdownButtonFormFields for structuralRole and writingStatus with Chinese labels (reuse _roleLabel/_statusLabel values from plot_node_form.dart). Padding `EdgeInsets.fromLTRB(16, 8, 16, 16)` + `MediaQuery.of(context).viewInsets.bottom` for keyboard. Bottom action bar: TextButton "放弃修改" + FilledButton "保存节点" (create) or "保存修改" (edit). Save validates title non-empty, creates PlotNode with copyWith (edit) or new PlotNode (create), calls onSave callback.

    The showEditSheet helper uses `showModalBottomSheet(isScrollControlled: true, shape: RoundedRectangleBorder(borderRadius: BorderRadius.vertical(top: Radius.circular(16))))`.
  </action>
  <verify>
    <automated>flutter test test/features/story_structure/presentation/story_arc_graph_test.dart test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart</automated>
  </verify>
  <done>
    StoryArcGraph renders interactive graph from PlotNode data with proper AsyncValue handling and empty state. NodeEditBottomSheet provides inline editing of 5 fields with validation. Both widgets follow existing codebase patterns.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate graph tab into StoryStructurePage</name>
  <files>
    - lib/features/story_structure/presentation/story_structure_page.dart
    - test/features/story_structure/presentation/story_arc_graph_test.dart
  </files>
  <read_first>
    - lib/features/story_structure/presentation/story_structure_page.dart (full file -- current 4-tab structure)
    - lib/features/story_structure/presentation/story_arc/story_arc_graph.dart (created in Task 2)
    - .planning/phases/10-story-arc-visualization/10-UI-SPEC.md (updated layout spec with 5 tabs)
    - .planning/phases/10-story-arc-visualization/10-PATTERNS.md (story_structure_page.dart modification pattern)
  </read_first>
  <behavior>
    - TabController length changes from 4 to 5
    - TabBar has 5 tabs: 伏笔, 剧情线, 弧线图, 守护, 整理与导出 per UI-SPEC layout
    - TabBarView has 5 children: _ForeshadowingSection, PlotTimeline, StoryArcGraph, GuardianPanel, _FinishExportSection
    - FAB: index 0 -> foreshadowing, index 1 || 2 -> plot node form (graph tab also gets create FAB), _ -> null
    - Import for StoryArcGraph added
  </behavior>
  <action>
    Modify `lib/features/story_structure/presentation/story_structure_page.dart`:
    1. Add import for story_arc_graph.dart
    2. Change TabController length from 4 to 5 (line 33)
    3. Add Tab(text: '弧线图') as third tab in the tabs list (after Tab(text: '剧情线'), before Tab(text: '守护'))
    4. Add StoryArcGraph() as third child in TabBarView.children (after PlotTimeline(), before GuardianPanel())
    5. Update _buildFAB switch: change `1 =>` to `1 || 2 =>` so both 剧情线 and 弧线图 tabs show the "新建情节点" FAB
  </action>
  <verify>
    <automated>flutter test test/features/story_structure/presentation/story_arc_graph_test.dart</automated>
  </verify>
  <done>
    StoryStructurePage now has 5 tabs with graph view accessible via "弧线图" tab. Graph tab shares the create-node FAB with the timeline tab. `flutter analyze` shows zero issues.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node edit form input | User enters title, chapter, summary -- validated before persistence |
| GraphView.builder gesture input | Zoom/pan/tap gestures handled by InteractiveViewer + GestureDetector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Input Validation | NodeEditBottomSheet title field | mitigate | Empty title validation with SnackBar error; chapter field uses numeric keyboard per UI-SPEC; summary no validation needed |
| T-10-05 | Denial of Service | Large graph (100+ nodes) causing UI freeze | mitigate | FruchtermanReingoldConfiguration.iterations = 50 (not 100) per RESEARCH Pitfall 4; autoZoomToFit for viewport management |
| T-10-06 | Tampering | Node title containing script injection | mitigate | Flutter Text widget renders strings as text, not HTML -- no XSS possible. Semantics label uses node title but is not executable. |

</threat_model>

<verification>
## Per-Task Verification

1. `flutter analyze lib/features/story_structure/presentation/story_arc/` -- zero issues
2. `flutter test test/features/story_structure/presentation/story_arc_graph_test.dart` -- graph widget tests pass
3. `flutter test test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart` -- bottom sheet tests pass

## Full Phase Gate

`flutter test` -- full suite green
</verification>

<success_criteria>
- Graph renders from PlotNode data with FruchtermanReingold layout and InteractiveViewer zoom/pan
- Three edge types visually distinguishable (causal=gradient+arrow, association=gray thin, foreshadowing=dashed amber)
- Nodes color-coded by structural role with border by writing status
- Tapping node opens bottom sheet with 5 editable fields
- Empty state shows illustration + "创建第一个节点" button
- StoryStructurePage has 5 tabs with graph accessible
- `flutter analyze` zero issues
</success_criteria>

<output>
Create `.planning/phases/10-story-arc-visualization/10-02-SUMMARY.md` when done
</output>
