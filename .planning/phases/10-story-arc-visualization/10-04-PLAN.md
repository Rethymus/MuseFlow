---
phase: 10-story-arc-visualization
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - lib/features/story_structure/infrastructure/node_position_repository.dart
  - lib/features/story_structure/presentation/story_arc/story_arc_graph.dart
  - lib/features/story_structure/presentation/story_structure_page.dart
  - lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart
  - test/features/story_structure/infrastructure/node_position_repository_test.dart
  - test/features/story_structure/presentation/story_arc_graph_test.dart
  - test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart
autonomous: true
gap_closure: true
requirements: [VIZO-04, VIZO-05]
user_setup: []
must_haves:
  truths:
    - "VIZO-05 gap closed: saved graph node positions reload after Hive returns Map<dynamic, dynamic> values."
    - "VIZO-05 gap closed: dragging node B before node A's pending debounce fires does not cancel node A's persisted save."
    - "VIZO-04 gap closed: StoryStructurePage updates the floating action button immediately after stable tab index changes, including the graph tab."
    - "VIZO-04 gap closed: NodeEditBottomSheet rejects non-numeric, zero, and negative chapter input before saving."
  artifacts:
    - path: "lib/features/story_structure/infrastructure/node_position_repository.dart"
      provides: "Hive map normalization before NodePosition.fromJson"
      contains: "Map<String, dynamic>.from"
    - path: "lib/features/story_structure/presentation/story_arc/story_arc_graph.dart"
      provides: "per-node pending save isolation or immediate drag-end persistence"
      contains: "nodeId"
    - path: "lib/features/story_structure/presentation/story_structure_page.dart"
      provides: "TabController listener-driven FAB rebuild"
      contains: "addListener"
    - path: "lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart"
      provides: "positive-integer chapter validation"
      contains: "请输入有效的章节号"
  key_links:
    - from: "lib/features/story_structure/infrastructure/node_position_repository.dart"
      to: "lib/features/story_structure/domain/node_position.dart"
      via: "NodePosition.fromJson(Map<String, dynamic>.from(...))"
      pattern: "Map<String, dynamic>\\.from"
    - from: "lib/features/story_structure/presentation/story_arc/story_arc_graph.dart"
      to: "nodePositionNotifierProvider"
      via: "savePosition for every dragged node ID"
      pattern: "savePosition\\(nodeId, position\\)"
    - from: "lib/features/story_structure/presentation/story_structure_page.dart"
      to: "floatingActionButton"
      via: "TabController listener calls setState on stable index changes"
      pattern: "_tabController\\.addListener"
    - from: "lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart"
      to: "plotNodeNotifierProvider"
      via: "validation gate before notifier.save/add"
      pattern: "int\\.tryParse"
---

<objective>
Close Phase 10 verification blockers without replanning completed graph visualization work.

Purpose: The graph already renders, edits, drags, and shows a minimap, but verification found reliability and validation gaps that block the phase from being user-safe: persisted positions can fail to reload, drag saves can be dropped, graph-tab create actions can be stale after tab changes, and invalid chapter input can be saved from the inline edit sheet.

Output: Targeted fixes and regression tests for VIZO-04 inline editing/FAB behavior and VIZO-05 drag-position persistence.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-story-arc-visualization/10-CONTEXT.md
@.planning/phases/10-story-arc-visualization/10-RESEARCH.md
@.planning/phases/10-story-arc-visualization/10-PATTERNS.md
@.planning/phases/10-story-arc-visualization/10-VERIFICATION.md
@.planning/phases/10-story-arc-visualization/10-01-SUMMARY.md
@.planning/phases/10-story-arc-visualization/10-02-SUMMARY.md
@.planning/phases/10-story-arc-visualization/10-03-SUMMARY.md

Key locked decisions still in force:
- D-13: node tap opens a BottomSheet for editing.
- D-14: BottomSheet allows editing title, structural role, writing status, chapter number, and summary.
- D-15: node positions stay in an independent NodePosition mapping table; do not add position fields to PlotNode.

Verification gaps to close exactly:
- `NodePositionRepository.getPosition/getAllPositions` must normalize Hive `Map<dynamic, dynamic>` values with `Map<String, dynamic>.from(value as Map)` before `NodePosition.fromJson`.
- `StoryArcGraph` must not let one dragged node cancel another node's pending save; use immediate save on drag end or maintain a debounce timer per node ID.
- `StoryStructurePage` must listen to `TabController` stable index changes and call `setState` so `_buildFAB()` rebuilds after tab switches.
- `NodeEditBottomSheet` must validate chapter text as a positive integer; invalid values block save and show a clear SnackBar.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make persisted node positions reload-safe and save every dragged node</name>
  <files>lib/features/story_structure/infrastructure/node_position_repository.dart, lib/features/story_structure/presentation/story_arc/story_arc_graph.dart, test/features/story_structure/infrastructure/node_position_repository_test.dart, test/features/story_structure/presentation/story_arc_graph_test.dart</files>
  <read_first>
    <file>.planning/phases/10-story-arc-visualization/10-VERIFICATION.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-CONTEXT.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-RESEARCH.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-03-SUMMARY.md</file>
    <file>lib/features/story_structure/infrastructure/node_position_repository.dart</file>
    <file>lib/features/story_structure/domain/node_position.dart</file>
    <file>lib/features/story_structure/application/node_position_notifier.dart</file>
    <file>lib/features/story_structure/presentation/story_arc/story_arc_graph.dart</file>
    <file>test/features/story_structure/infrastructure/node_position_repository_test.dart</file>
    <file>test/features/story_structure/presentation/story_arc_graph_test.dart</file>
  </read_first>
  <behavior>
    - Repository regression: storing `{'plotNodeId': 'pn-1', 'x': 100, 'y': 200}` as a `Map<dynamic, dynamic>` in the Hive box returns `Offset(100, 200)` from `getPosition('pn-1')` without throwing.
    - Repository regression: `getAllPositions()` returns both `pn-1` and `pn-2` when the box contains `Map<dynamic, dynamic>` values.
    - Drag persistence regression: two drag-end save schedules for different node IDs persist both IDs; saving node B must not cancel node A.
  </behavior>
  <action>First add failing regression coverage. In `node_position_repository_test.dart`, add tests named `should read Hive dynamic map values after reload` and `should read all Hive dynamic map values after reload`; insert values into the Hive box using a `Map<dynamic, dynamic>` variable, not `NodePositionRepository.save`, so the test exercises the reload shape. In `node_position_repository.dart`, replace both `json as Map<String, dynamic>` casts with `Map<String, dynamic>.from(json as Map)` before calling `NodePosition.fromJson`; preserve existing StateError wrapping and do not change the `NodePosition` entity per D-15. In `story_arc_graph.dart`, replace the single `_positionSaveTimer` field with either `Map<String, Timer> _positionSaveTimers` keyed by node ID or an immediate `savePosition(nodeId, position)` call in `onPanEnd`; if choosing timers, `dispose()` must cancel every timer in `_positionSaveTimers.values`, `_debouncedSavePosition` must cancel only the timer for the same `nodeId`, and the timer callback must remove its own node ID after calling `savePosition(nodeId, position)`. Add a regression test in `story_arc_graph_test.dart` only if the private save scheduling can be observed through the existing fake notifier without brittle reflection; otherwise rely on the repository tests plus the source assertion in acceptance criteria.</action>
  <acceptance_criteria>
    - Source assertion: `lib/features/story_structure/infrastructure/node_position_repository.dart` contains `Map<String, dynamic>.from(json as Map)` or an equivalent helper used by both `getPosition` and `getAllPositions`.
    - Source assertion: `lib/features/story_structure/presentation/story_arc/story_arc_graph.dart` no longer contains the field declaration `Timer? _positionSaveTimer;`.
    - Source assertion: if debounce remains, `story_arc_graph.dart` contains `Map<String, Timer>` and cancels timers by `nodeId`, not through one shared timer; if debounce is removed, `onPanEnd` calls `savePosition` for the dragged `id` directly.
    - Behavior assertion: `flutter test test/features/story_structure/infrastructure/node_position_repository_test.dart` passes, including `should read Hive dynamic map values after reload` and `should read all Hive dynamic map values after reload`.
    - Behavior assertion: existing graph widget tests still pass after the drag-save change.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter test test/features/story_structure/infrastructure/node_position_repository_test.dart test/features/story_structure/presentation/story_arc_graph_test.dart</automated>
  </verify>
  <done>VIZO-05 persistence blockers are closed: Hive reload does not break saved positions, and saving a later dragged node cannot drop an earlier dragged node's pending position save.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild the graph-tab FAB on stable tab changes</name>
  <files>lib/features/story_structure/presentation/story_structure_page.dart, test/features/story_structure/presentation/story_arc_graph_test.dart</files>
  <read_first>
    <file>.planning/phases/10-story-arc-visualization/10-VERIFICATION.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-02-SUMMARY.md</file>
    <file>lib/features/story_structure/presentation/story_structure_page.dart</file>
    <file>test/features/story_structure/presentation/story_arc_graph_test.dart</file>
  </read_first>
  <behavior>
    - Starting on the `伏笔` tab shows FAB tooltip `新建伏笔`.
    - Switching to the `弧线图` tab rebuilds the Scaffold FAB and shows tooltip `新建情节点`.
    - Switching from the `弧线图` tab to `守护` rebuilds the Scaffold FAB and removes the add FAB because `_buildFAB()` returns null for index 3.
  </behavior>
  <action>Add a widget regression test for `StoryStructurePage` tab/FAB behavior in `story_arc_graph_test.dart` or a new test file only if that file's imports become crowded; keep the plan's file ownership to the listed files if possible. In `story_structure_page.dart`, add a private listener method named `_handleTabChange` that returns immediately when `_tabController.indexIsChanging` is true and otherwise calls `setState(() {})` only when `mounted` is true. Register it in `initState()` with `_tabController.addListener(_handleTabChange)`. In `dispose()`, call `_tabController.removeListener(_handleTabChange)` before `_tabController.dispose()`. Keep `_buildFAB()` target state exactly: index `0` uses tooltip `新建伏笔`, indexes `1 || 2` use tooltip `新建情节点`, all other tabs return `null`. Do not change tab order: `伏笔`, `剧情线`, `弧线图`, `守护`, `整理与导出`.</action>
  <acceptance_criteria>
    - Source assertion: `story_structure_page.dart` contains `_tabController.addListener(_handleTabChange)` in `initState()`.
    - Source assertion: `story_structure_page.dart` contains `_tabController.removeListener(_handleTabChange)` in `dispose()` before `_tabController.dispose()`.
    - Source assertion: `_handleTabChange` checks `_tabController.indexIsChanging` and `mounted` before calling `setState`.
    - Behavior assertion: a widget test taps tab text `弧线图`, pumps until settled, and finds a FAB with tooltip `新建情节点`.
    - Behavior assertion: the same or another widget test taps tab text `守护`, pumps until settled, and verifies tooltip `新建情节点` is absent.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter test test/features/story_structure/presentation/story_arc_graph_test.dart</automated>
  </verify>
  <done>VIZO-04 graph-tab management action gap is closed: the visible FAB always matches the active stable tab after user tab switches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Validate chapter input before inline node save</name>
  <files>lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart, test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart</files>
  <read_first>
    <file>.planning/phases/10-story-arc-visualization/10-VERIFICATION.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-CONTEXT.md</file>
    <file>.planning/phases/10-story-arc-visualization/10-PATTERNS.md</file>
    <file>lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart</file>
    <file>lib/features/story_structure/domain/plot_node.dart</file>
    <file>lib/features/story_structure/presentation/plot_node_form.dart</file>
    <file>test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart</file>
  </read_first>
  <behavior>
    - Empty or whitespace chapter text blocks save and shows SnackBar text `请输入有效的章节号`.
    - Non-numeric chapter text such as `abc` blocks save and shows SnackBar text `请输入有效的章节号`.
    - Zero and negative chapter text (`0`, `-1`) block save and show SnackBar text `章节号必须大于0`.
    - Valid positive chapter text such as `12` saves `chapter == 12` through `onSave`.
  </behavior>
  <action>In `node_edit_bottom_sheet_test.dart`, add regression tests for invalid chapter input that prove `onSave` is not called for `abc`, `0`, and `-1`, and add a positive test that entering `12` saves chapter 12. In `node_edit_bottom_sheet.dart`, before setting `_isSaving = true`, parse `final chapterText = _chapterController.text.trim()` and `final chapter = int.tryParse(chapterText)`. If `chapter == null`, show `SnackBar(content: Text('请输入有效的章节号'))` and return. If `chapter &lt;= 0`, show `SnackBar(content: Text('章节号必须大于0'))` and return. Use the validated non-null `chapter` in both edit and create branches. Do not keep the fallback `int.tryParse(...) ?? 1`; that exact masking behavior is the verification blocker. Preserve D-14's five editable fields and existing title validation.</action>
  <acceptance_criteria>
    - Source assertion: `node_edit_bottom_sheet.dart` does not contain `int.tryParse(_chapterController.text.trim()) ?? 1` or any `?? 1` fallback for chapter parsing.
    - Source assertion: `node_edit_bottom_sheet.dart` contains SnackBar text `请输入有效的章节号` and `章节号必须大于0`.
    - Behavior assertion: `node_edit_bottom_sheet_test.dart` includes tests for non-numeric, zero, negative, and valid positive chapter input.
    - Behavior assertion: invalid chapter tests verify `onSave` remains unset/null and the bottom sheet remains mounted after tapping `保存修改` or `保存节点`.
    - Behavior assertion: valid positive chapter input saves the exact entered integer value, not default chapter 1.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/re/code/MuseFlow &amp;&amp; flutter test test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart</automated>
  </verify>
  <done>VIZO-04 inline editing validation gap is closed: user-entered chapter values must be valid positive integers before any PlotNode save/add occurs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> NodeEditBottomSheet -> PlotNodeNotifier | Untrusted title/chapter/summary text crosses from UI text controllers into local domain persistence. |
| Hive graph_positions box -> NodePositionRepository -> graph rendering | Locally persisted dynamic data crosses from storage into graph layout code. |
| gesture events -> StoryArcGraph -> NodePositionNotifier | User drag gestures update persisted layout coordinates. |
| TabController state -> StoryStructurePage Scaffold FAB | UI navigation state controls which creation action is exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | `node_edit_bottom_sheet.dart` chapter field | mitigate | Validate chapter text before save: reject null parse with `请输入有效的章节号`, reject `&lt;= 0` with `章节号必须大于0`, and call notifier only after validation. |
| T-10-04-02 | Denial of Service | `node_position_repository.dart` Hive reload path | mitigate | Normalize Hive dynamic maps with `Map<String, dynamic>.from(json as Map)` before `NodePosition.fromJson`, preserving StateError handling for genuinely corrupted data. |
| T-10-04-03 | Tampering | `story_arc_graph.dart` drag persistence | mitigate | Isolate pending saves per node ID or save immediately on drag end so a second drag cannot cancel a first node's pending persisted coordinate. |
| T-10-04-04 | Repudiation | `story_structure_page.dart` tab-dependent FAB | accept | Local-only UI action with no audit trail requirement; mitigation is correctness-focused by rebuilding the FAB on stable tab index changes. |
| T-10-04-SC | Tampering | package-manager installs | accept | No npm/pip/cargo/pub package install occurs in this gap-closure plan; graphview already exists from completed Plan 10-01. |
</threat_model>

<verification>
Run targeted checks first, then the phase verification slice:

<automated>cd /home/re/code/MuseFlow &amp;&amp; flutter test test/features/story_structure/infrastructure/node_position_repository_test.dart test/features/story_structure/presentation/story_arc_graph_test.dart test/features/story_structure/presentation/node_edit_bottom_sheet_test.dart</automated>
<automated>cd /home/re/code/MuseFlow &amp;&amp; flutter analyze lib/features/story_structure/infrastructure/node_position_repository.dart lib/features/story_structure/presentation/story_arc/story_arc_graph.dart lib/features/story_structure/presentation/story_structure_page.dart lib/features/story_structure/presentation/story_arc/node_edit_bottom_sheet.dart</automated>
</verification>

<success_criteria>
- 10-VERIFICATION.md failed truth for VIZO-05 can be rechecked as passing because Hive dynamic-map reload works and multi-node drag saves cannot cancel each other.
- 10-VERIFICATION.md failed truth for graph-tab create/manage actions can be rechecked as passing because the FAB rebuilds on stable tab changes.
- 10-VERIFICATION.md failed truth for inline edit validation can be rechecked as passing because invalid chapter input is blocked before save.
- No task implements deferred v2 visualization items: no layout algorithm switching, no PNG export, no animation playback, no 3D visualization.
</success_criteria>

<output>
Create `.planning/phases/10-story-arc-visualization/10-04-SUMMARY.md` when done.
</output>
