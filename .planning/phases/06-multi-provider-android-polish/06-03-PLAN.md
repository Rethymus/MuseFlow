---
phase: 06-multi-provider-android-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-02"]
files_modified:
  - lib/features/ai/presentation/provider_management_page.dart
  - lib/features/ai/presentation/provider_management_notifier.dart
  - integration_test/app_test.dart
  - integration_test/test_driver/integration_test.dart
autonomous: true
requirements: [MODL-03, MODL-04]

must_haves:
  truths:
    - "Provider management page renders correctly on narrow (phone-width) screens without overflow"
    - "Core app flow can be verified via integration tests (launch, navigate, settings)"
    - "New Phase 6 UI (parameter inputs, model dropdown) is usable on narrow screens"
  artifacts:
    - path: "lib/features/ai/presentation/provider_management_page.dart"
      provides: "Responsive layout switching between Row and Column based on screen width"
      contains: "LayoutBuilder"
    - path: "integration_test/app_test.dart"
      provides: "Integration test covering core app flow"
      contains: "testWidgets"
    - path: "integration_test/test_driver/integration_test.dart"
      provides: "Test driver for integration test execution"
      contains: "integrationTest"
  key_links:
    - from: "lib/features/ai/presentation/provider_management_page.dart"
      to: "AppConstants.sidebarCollapsedBreakpoint"
      via: "LayoutBuilder width check against 600px breakpoint"
      pattern: "sidebarCollapsedBreakpoint"
    - from: "integration_test/app_test.dart"
      to: "lib/main.dart"
      via: "app startup and navigation"
      pattern: "pumpWidget"
---

## Phase Goal

**As a** writer using an Android device, **I want to** configure AI providers with a responsive layout that works on my phone screen, **so that** I can manage model settings without horizontal scrolling or overflow errors.

<objective>
Make the provider management page responsive for narrow screens (phones) using LayoutBuilder and AppConstants breakpoints. Add integration tests for core app flow verification. This covers D-10 (Android scope: Phase 6 new UI + core flow testing) and D-11 (integration tests, not manual emulator testing).
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-provider-android-polish/06-CONTEXT.md
@.planning/phases/06-multi-provider-android-polish/06-RESEARCH.md
</context>

<interfaces>
<!-- Executor needs these types/signatures. -->

From lib/shared/constants/app_constants.dart:
```dart
abstract class AppConstants {
  static const double sidebarExtendedBreakpoint = 1000.0;
  static const double sidebarCollapsedBreakpoint = 600.0;
  // ...
}
```

From lib/features/ai/presentation/provider_management_page.dart (current state):
```dart
// build() returns Scaffold with body: Row(children: [SizedBox(width:300, child: leftPanel), VerticalDivider, Expanded(child: rightPanel)])
// This fixed Row layout overflows on screens < 600px
```

From lib/features/ai/presentation/provider_management_notifier.dart:
```dart
class ProviderManagementState {
  final List<AIProvider> providers;
  final AIProvider? selectedProvider;
  // ...
}
final providerManagementProvider = NotifierProvider<...>(...);
final presetProvidersProvider = Provider<List<AIProvider>>((ref) { ... });
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Responsive provider management layout for narrow screens</name>
  <files>
    lib/features/ai/presentation/provider_management_page.dart
  </files>
  <read_first>
    lib/features/ai/presentation/provider_management_page.dart,
    lib/shared/constants/app_constants.dart
  </read_first>
  <action>
    Per D-10/D-12: Replace the fixed `Row` layout in the `build` method's body with a `LayoutBuilder` that checks `constraints.maxWidth` against `AppConstants.sidebarCollapsedBreakpoint` (600px).

    When width >= 600: Use the existing `Row` layout with `SizedBox(width: 300)` for left panel + `VerticalDivider` + `Expanded` right panel (desktop/tablet).

    When width < 600: Use a `Column` layout. Show only the right panel (configuration form) by default. Add a top bar with a "返回列表" text button or icon button that toggles to show the left panel (presets + saved providers list). Use an `AnimatedSwitcher` or a simple boolean `_showList` state to toggle between the list view and the form view. When the user taps a preset or saved provider, populate the form and switch back to the form view. This avoids the "two panels crammed into narrow screen" problem.

    The parameter input rows and model combo input added in 06-02 are inside `_buildRightPanel` and will be inside the scrollable `SingleChildScrollView`, so they should fit within the narrow width. Verify that:
    - SegmentedButton wraps (may need to reduce segment labels or use icons-only on narrow screens)
    - TextField rows use full width (they already do since they are in a Column inside SingleChildScrollView)
    - The model fetch dropdown/chips don't overflow

    Import `AppConstants` from `package:museflow/shared/constants/app_constants.dart`.

    Add a `_showList` boolean state field. On narrow screens, toggling between list and form views sets `_showList` to true/false. Default to showing the form if a provider is selected, or the list if no provider is selected. Tapping a preset or saved provider sets the selection and switches to form view.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter analyze lib/features/ai/presentation/provider_management_page.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "LayoutBuilder" lib/features/ai/presentation/provider_management_page.dart` returns at least 1
    - `grep "sidebarCollapsedBreakpoint" lib/features/ai/presentation/provider_management_page.dart` returns at least 1
    - `flutter analyze lib/features/ai/presentation/provider_management_page.dart` produces zero errors
    - The existing desktop layout (Row with 300px left panel) is preserved for width >= 600
    - A narrow-screen layout (Column, toggleable list/form) is used for width < 600
  </acceptance_criteria>
  <done>
    Provider management page uses LayoutBuilder to switch between desktop Row layout and mobile Column layout at the 600px breakpoint. On narrow screens, user toggles between provider list and configuration form. All existing desktop functionality preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration tests for core app flow</name>
  <files>
    integration_test/app_test.dart,
    integration_test/test_driver/integration_test.dart,
    pubspec.yaml
  </files>
  <read_first>
    lib/main.dart,
    lib/shared/constants/app_constants.dart,
    pubspec.yaml
  </read_first>
  <action>
    Per D-11: Add `integration_test` SDK package to `pubspec.yaml` under `dev_dependencies`:
    ```
    integration_test:
      sdk: flutter
    ```

    Create `integration_test/test_driver/integration_test.dart` with the standard Flutter integration test driver:
    ```
    import 'package:integration_test/integration_test_driver.dart';
    Future<void> main() => integrationDriver();
    ```

    Create `integration_test/app_test.dart` with integration tests covering the core flow per D-10:

    Test 1: "App launches and shows main shell" -- pump the app, verify the scaffold renders. Check that the bottom navigation or sidebar is visible.

    Test 2: "Navigate to settings" -- tap the settings navigation item, verify the settings page loads.

    Test 3: "Navigate to AI provider settings" -- from settings, navigate to the AI providers page, verify it renders with preset cards visible.

    Test 4: "Preset providers are displayed" -- verify that the preset provider cards (OpenAI, DeepSeek, Ollama, Claude) are visible on the provider management page.

    These tests use `integration_test` package's `testWidgets` and `IntegrationTestWidgetsFlutterBinding`. The app's main entry point is `lib/main.dart` -- import and pump `MuseFlowApp` (or whatever the root widget is named). If the main widget requires Hive initialization, wrap the pump with the appropriate setup.

    Run `flutter pub get` after modifying pubspec.yaml.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test integration_test/app_test.dart -d windows 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "integration_test" pubspec.yaml` returns 1
    - File `integration_test/app_test.dart` exists with at least 3 testWidgets blocks
    - File `integration_test/test_driver/integration_test.dart` exists
    - `flutter pub get` completes without errors
    - Integration tests compile without errors (actual run may require desktop target)
  </acceptance_criteria>
  <done>
    integration_test package added to dev_dependencies. Test driver created. app_test.dart contains integration tests for: app launch, navigation to settings, navigation to AI provider page, preset provider display. Tests compile and can run on Windows desktop target.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test -> App runtime | Test controls app lifecycle, no external trust boundary |
| Responsive layout -> Screen size | Layout reads constraints, no data crosses trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | Integration test logs | accept | Integration tests run in dev environment only, not in production. No sensitive data in test assertions. |
| T-06-09 | Tampering | Layout breakpoint logic | accept | Breakpoint values are constants, not user-configurable. No security impact from layout changes. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | integration_test is a Flutter SDK package (not pub.dev). No third-party risk. |
</threat_model>

<verification>
1. `flutter test integration_test/app_test.dart -d windows` -- integration tests compile and run (or gracefully skip if no desktop target)
2. `flutter analyze lib/features/ai/presentation/provider_management_page.dart` -- zero errors
3. `flutter test test/features/ai/` -- all existing tests still pass
4. Manual: resize app window below 600px, verify provider management page switches to stacked layout
</verification>

<success_criteria>
- Provider management page uses LayoutBuilder for responsive layout at 600px breakpoint
- Desktop layout (Row) preserved for width >= 600
- Mobile layout (Column with toggleable list/form) for width < 600
- integration_test package in dev_dependencies
- integration_test/app_test.dart covers app launch, navigation, settings, and provider display
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/06-multi-provider-android-polish/06-03-SUMMARY.md` when done
</output>
