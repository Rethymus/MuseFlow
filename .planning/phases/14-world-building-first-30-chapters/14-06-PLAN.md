---
phase: 14-world-building-first-30-chapters
plan: 06
type: execute
wave: 6
depends_on: [14-05]
files_modified:
  - lib/features/ai/infrastructure/openai_adapter.dart
  - test/journey/serial_generation_test.dart
  - test/journey/full_journey_test.dart
  - test/journey/helpers/journey_container.dart
  - .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
autonomous: true
gap_closure: true
requirements: [JOURNEY-05]
user_setup:
  - service: GLM API
    why: "D-02 requires real GLM API evidence for sustained 30-chapter generation"
    env_vars:
      - name: GLM_API_KEY
        source: "User-provided GLM/BigModel API key; export in shell only, never commit or paste"
      - name: GLM_BASE_URL
        source: "Optional; default remains https://open.bigmodel.cn/api/paas/v4"
      - name: GLM_MODEL
        source: "Optional; default remains glm-4-flash unless user specifies another GLM model"
must_haves:
  truths:
    - "Real GLM serial generation completes 30/30 chapters with token audit and all-30 deviation checks before JOURNEY-05 can close."
    - "Live GLM generated chapters satisfy D-11 300-500 character bounds through product/test enforcement, or live validation fails and the plan remains failed."
    - "A classified GLM failure may be logged with safe evidence, but it routes to debug/replan and does not count as plan completion or P14-04-GLM-01 closure."
    - "A deterministic local journey path supplements, but never replaces, the required D-02 real GLM evidence."
  artifacts:
    - path: "lib/features/ai/infrastructure/openai_adapter.dart"
      provides: "Classified stream error evidence for GLM debugging"
      contains: "classifyException"
    - path: "test/journey/serial_generation_test.dart"
      provides: "Live GLM sustained generation with D-11 bounds, all-30 deviation detection, and deterministic local orchestration coverage"
      contains: "should generate 30 chapters with knowledge injection and Skill guardian"
    - path: "test/journey/full_journey_test.dart"
      provides: "Full journey deterministic and live validation paths"
      contains: "should complete full xianxia journey from world-building to 30 chapters"
    - path: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      provides: "P14-04-GLM-01 closure evidence"
      contains: "P14-04-GLM-01"
  key_links:
    - from: "test/journey/serial_generation_test.dart"
      to: "OpenAIAdapter.createStream"
      via: "real GLM serial loop with D-03 delay and D-04 stop-on-error"
      pattern: "Future\.delayed\(const Duration\(seconds: 3\)\)"
    - from: "test/journey/serial_generation_test.dart"
      to: "DeviationDetectionService.detectDeviations"
      via: "all 30 generated live chapters checked for Skill deviation"
      pattern: "detectDeviations"
    - from: "test/journey/serial_generation_test.dart"
      to: "TokenAuditService"
      via: "recordAudit then flush before snapshot"
      pattern: "recordAudit[\s\S]*flush"
    - from: "test/journey/full_journey_test.dart"
      to: "FakeAdapter or deterministic adapter"
      via: "local full journey coverage without GLM_API_KEY"
      pattern: "FakeAdapter|journey-local-test-key"
---

<objective>
Close the JOURNEY-05 sustained-generation blocker with better diagnostics, deterministic supplemental coverage, live D-11 enforcement, and mandatory successful real GLM evidence.

Purpose: Phase 14 cannot be verified while the real GLM run stops at chapter 2 with only `AIStreamException`. This plan preserves D-02 real-GLM evidence as non-negotiable, makes failures diagnosable, proves local orchestration separately, and requires the live run to pass 30/30 before `P14-04-GLM-01` can close.

Output: Improved stream error classification/logging, deterministic 30-chapter local journey path, live GLM 30/30 evidence with D-11 bounds, all-30 deviation detection, token audit evidence, and closed `P14-04-GLM-01` in `14-ISSUE-LOG.md`.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md
@.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md
@.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md
@.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md
@.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
@lib/features/ai/infrastructure/openai_adapter.dart
@test/journey/serial_generation_test.dart
@test/journey/full_journey_test.dart
@test/journey/helpers/journey_container.dart
@test/automation/helpers/fake_adapter.dart

<interfaces>
Use these existing identifiers exactly:
- `OpenAIAdapter.createStream(...)`
- `OpenAIAdapter.classifyException(Object error)`
- `AIStreamException`, `AIRateLimitException`, `AINetworkException`, `AIAuthException`
- `PromptPipeline`, `PromptContext`
- `tokenAuditServiceProvider.future`, `recordAudit(...)`, `flush()`
- `tokenAuditRepositoryProvider.future`, `buildSnapshot()`
- `DeviationDetectionService.detectDeviations(...)`
- `GLM_API_KEY`, `GLM_BASE_URL`, `GLM_MODEL`
- `FakeAdapter` may be used only for deterministic local orchestration coverage, not as D-02 real GLM evidence.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 用户可以写出前30章并验证核心创作循环可靠性 | 14-06 | COVERED | Requires successful 30/30 real GLM run for JOURNEY-05 closure.
REQ | JOURNEY-05 | 逐章 AI 内容生成、知识库自动注入、Skill 守护连续性、token audit | 14-06 Tasks 1-3 | COVERED | Real GLM rerun is mandatory; deterministic coverage is supplemental.
RESEARCH | Pitfall 4 | GLM API rate limiting must stop and log safely | 14-06 Tasks 1,3 | COVERED | Classification and issue-log evidence distinguish 429/network/stream errors without marking failures complete.
RESEARCH | Pitfall 5 | Token audit flush before verification | 14-06 Tasks 2,3 | COVERED | Both deterministic and live paths flush before snapshot; live closure requires token totals.
RESEARCH | WR-04 | Live LLM output length assertions unstable | 14-06 Task 2 | COVERED | Addressed by enforcing/validating D-11 in the live path, not by downgrading D-11 to deterministic-only proof.
RESEARCH | WR-05 | Long live journey lacks fake adapter quick path | 14-06 Task 2 | COVERED | Adds deterministic full journey path as fast supplemental coverage.
CONTEXT | D-02 | Real GLM API | 14-06 Task 3 | COVERED | Live GLM 30/30 rerun with `GLM_API_KEY` is required for plan success.
CONTEXT | D-03 | Serial 2-3s calls | 14-06 Task 3 | COVERED | Keep 3s inter-call delay for live GLM.
CONTEXT | D-04 | Stop on first failure | 14-06 Tasks 1,3 | COVERED | Do not retry/skip chapters; classify, log, fail, and route to debug/replan.
CONTEXT | D-10 | Coherent 30-chapter xianxia growth line | 14-06 Tasks 2,3 | COVERED | Uses existing StoryOutline.
CONTEXT | D-11 | 300-500 characters per chapter | 14-06 Tasks 2,3 | COVERED | Live GLM outputs must satisfy bounds through generation constraints and validation, or the live test fails.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make GLM stream failures diagnosable without exposing secrets</name>
  <files>lib/features/ai/infrastructure/openai_adapter.dart, test/journey/serial_generation_test.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` GAP-01.
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` issue `P14-04-GLM-01`.
    - `.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md` Pitfall 4 and Security Domain.
    - `lib/features/ai/infrastructure/openai_adapter.dart` current `createStream` and `classifyException`.
    - `test/journey/serial_generation_test.dart` current smoke and chapter loop error logging.
  </read_first>
  <behavior>
    - Test 1: `OpenAIAdapter.classifyException` preserves or returns typed `AIRateLimitException` for rate-limit errors and `AINetworkException` for connection/timeout errors.
    - Test 2: Generic OpenAI stream errors still classify as `AIStreamException`, but logs include exception runtime type and sanitized message if available.
    - Test 3: Serial generation logs `[ERROR] Chapter N/30 failed:` with exception class, no `GLM_API_KEY`, no `Authorization`, and no bearer token.
  </behavior>
  <action>
    Improve diagnostics around stream failures while preserving existing public behavior. In `OpenAIAdapter.classifyException`, include safe diagnostic message text in returned `AIStreamException`, `AIRateLimitException`, or `AINetworkException` when the source exception exposes status code or message, but do not include request headers, API keys, or full prompt text. In `test/journey/serial_generation_test.dart`, update the catch block for each chapter to log exception runtime type and sanitized message, then rethrow per D-04. Add or update unit-level assertions in the closest existing adapter test if one exists; if no adapter test file exists, add assertions in a focused journey/helper test only if it does not require GLM credentials. Update `14-ISSUE-LOG.md` with the new diagnostic format expected for `P14-04-GLM-01` reruns; this diagnostic evidence is for debugging only and must not close `P14-04-GLM-01` unless Task 3 later passes 30/30 live GLM.
  </action>
  <verify>
    <automated>dart analyze lib/features/ai/infrastructure/openai_adapter.dart test/journey/serial_generation_test.dart</automated>
    <automated>flutter test test/journey/serial_generation_test.dart -j 1 --plain-name "should pass GLM streaming smoke test" --timeout 120s</automated>
    <automated>! grep -R "GLM_API_KEY\|Authorization: Bearer\|Bearer " -n test/journey/serial_generation_test.dart .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - Stream failure logging includes exception type and sanitized message, not just `Instance of 'AIStreamException'`.
    - API key, authorization header, and bearer token strings are not written to tests or issue log.
    - Existing smoke test behavior remains unchanged: it passes with real credentials or skips only when credentials are absent.
    - Classified failure evidence alone does not mark `P14-04-GLM-01`, this plan, or Phase 14 complete.
  </acceptance_criteria>
  <done>Future GLM sustained-generation failures are actionable and secret-safe, but failures still block completion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic full 30-chapter orchestration coverage and live D-11 bounds validation</name>
  <files>test/journey/serial_generation_test.dart, test/journey/full_journey_test.dart, test/journey/helpers/journey_container.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md` WR-04 and WR-05.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` D-01, D-02, D-10, D-11.
    - `test/journey/serial_generation_test.dart` current GLM-only serial generation path.
    - `test/journey/full_journey_test.dart` current GLM-only full journey path.
    - `test/automation/helpers/fake_adapter.dart` deterministic adapter behavior.
    - `test/journey/helpers/story_outline.dart` 30-chapter outline and character names.
  </read_first>
  <behavior>
    - Test 1: A no-credential deterministic serial journey creates 30 chapters, writes 300-500 character xianxia content to all 30, checks at least 2 sampled chapters contain expected character names, flushes audit, and verifies `snapshot.totalCalls >= 30`.
    - Test 2: A no-credential deterministic full journey chains world-building, fragment synthesis/opening surrogate, 30 chapter generation, persistence, and token audit without `GLM_API_KEY`.
    - Test 3: Live GLM serial generation validates every live chapter is 300-500 characters per D-11 after any product-level trimming/regeneration/enforcement that already exists or is added by this task.
    - Test 4: If any live GLM chapter is outside 300-500 characters, the live validation fails and `P14-04-GLM-01` remains open; deterministic coverage cannot override this failure.
  </behavior>
  <action>
    Add deterministic local tests that use `createJourneyContainer(apiKey: 'journey-local-test-key', baseUrl: 'https://example.com/v1', model: 'fake-model', aiAdapter: FakeAdapter())` or an equivalent deterministic adapter. The deterministic serial test must run without `skip` and without `GLM_API_KEY`. It must validate local orchestration of 30 chapters, PromptPipeline construction, chapter persistence, sampled character-name presence, deviation-check invocation if feasible without real AI, `TokenAuditService.recordAudit`, `flush()`, and `TokenAuditRepository.buildSnapshot()`. Keep live GLM tests as required D-02 evidence and make D-11 enforceable in the live path: either add/verify product or test harness enforcement that constrains live generated chapter content to 300-500 characters before persistence, or assert live validation fails when a generated chapter violates the 300-500 range. Do not replace live D-11 checks with nonempty-only or deterministic-only assertions. Record actual min/max/average lengths in debug output and issue log only as supporting evidence; the pass/fail gate is that every live chapter is within 300-500 characters.
  </action>
  <verify>
    <automated>dart analyze test/journey/serial_generation_test.dart test/journey/full_journey_test.dart test/journey/helpers/journey_container.dart</automated>
    <automated>flutter test test/journey/serial_generation_test.dart -j 1 --plain-name "deterministic" --timeout 300s</automated>
    <automated>flutter test test/journey/full_journey_test.dart -j 1 --plain-name "deterministic" --timeout 300s</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/serial_generation_test.dart test/journey/full_journey_test.dart | grep -Eq "FakeAdapter|journey-local-test-key"</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/serial_generation_test.dart | grep -Eq "greaterThanOrEqualTo\(300\)|inInclusiveRange\(300, 500\)|lessThanOrEqualTo\(500\)"</automated>
  </verify>
  <acceptance_criteria>
    - At least one 30-chapter serial orchestration test runs without `GLM_API_KEY` and is not skipped.
    - Full journey local orchestration coverage runs without `GLM_API_KEY` and is not skipped.
    - Live GLM path still exists and still uses real `OpenAIAdapter` per D-02.
    - Live GLM validation fails if any generated/persisted chapter violates D-11 300-500 character bounds.
    - Deterministic evidence is clearly labeled supplemental and never used to close live GLM D-02/D-11 evidence.
    - `14-ISSUE-LOG.md` distinguishes deterministic local orchestration evidence from real GLM evidence.
  </acceptance_criteria>
  <done>JOURNEY-05 local orchestration is reliably covered and live D-11 validation remains a required pass/fail gate.</done>
</task>

<task type="auto">
  <name>Task 3: Rerun real GLM sustained validation and close P14-04-GLM-01 only on 30/30 success</name>
  <files>.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` current `P14-04-GLM-01` and blocked checklist.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` D-02, D-03, D-04, D-06, D-11.
    - `test/journey/serial_generation_test.dart` updated live GLM command names.
    - `test/journey/full_journey_test.dart` updated live GLM command names.
  </read_first>
  <action>
    With `GLM_API_KEY` exported, run the updated live GLM tests sequentially with `-j 1`: smoke, full serial generation, and full journey. Preserve D-03 3-second spacing and D-04 stop-on-error. This task and plan can complete only if real GLM evidence shows: 30/30 chapters generated, every live generated/persisted chapter is 300-500 characters per D-11, all 30 chapters ran deviation detection, token audit flushed, token audit `totalCalls >= 30`, aggregate input/output token totals recorded, and `P14-04-GLM-01` is marked closed/resolved. If the run fails for any reason, keep `P14-04-GLM-01` open, update its evidence with sanitized exception class/message, chapter number, last successful chapter count, D-11 status if applicable, and whether audit records flushed, then stop with a failed plan outcome and route to `/gsd:debug` or another gap replan. Do not auto-retry failed chapters, do not skip the failing chapter, and do not count a classified failure as completion.
  </action>
  <verify>
    <automated>test -n "$GLM_API_KEY"</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/serial_generation_test.dart -j 1 --plain-name "should pass GLM streaming smoke test" --timeout 120s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/serial_generation_test.dart -j 1 --timeout 1200s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/full_journey_test.dart -j 1 --plain-name "should complete full xianxia journey from world-building to 30 chapters" --timeout 1200s</automated>
    <automated>grep -Eiq "P14-04-GLM-01.*(closed|resolved|已关闭|已解决)" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <automated>grep -Eiq "30/30" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md && grep -Eiq "all[- ]?30.*deviation|30.*deviation.*checks|30/30.*deviation" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md && grep -Eiq "totalCalls[^0-9]*(3[0-9]|[4-9][0-9]|[1-9][0-9]{2,})" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <automated>grep -Eiq "300-500|300.*500|min/max/avg|length" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <automated>! grep -q "$GLM_API_KEY" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - GLM tests are not counted as evidence if skipped due to missing `GLM_API_KEY`.
    - `14-ISSUE-LOG.md` marks `P14-04-GLM-01` closed/resolved only after real GLM 30/30 success.
    - Real GLM evidence records 30/30 chapters, D-11 300-500 compliance, all-30 deviation detection, token audit `totalCalls >= 30`, and input/output token totals.
    - If real GLM still fails, `P14-04-GLM-01` stays open, the plan/phase remains failed, and the summary routes to debug/replan instead of claiming completion.
    - No secrets are written to `14-ISSUE-LOG.md`.
  </acceptance_criteria>
  <done>JOURNEY-05 real GLM blocker is closed only by 30/30 real GLM generation with D-11, all-30 deviation detection, and token audit evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell environment -> journey tests | `GLM_API_KEY` enters test process through environment variables. |
| Journey tests -> GLM external API | Prompt content leaves local machine for real GLM generation and deviation checks. |
| Test output -> issue log | Error diagnostics and generation counts are persisted in planning artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Information Disclosure | GLM credentials | mitigate | Never print or persist API keys; verify issue log does not contain `$GLM_API_KEY`, `Authorization`, or bearer tokens. |
| T-14-06-02 | Denial of Service | GLM rate limits/quota | mitigate | Run serial tests with `-j 1`, preserve 3s delay, and stop on first failure per D-04. |
| T-14-06-03 | Repudiation | Generic stream failure evidence | mitigate | Log sanitized exception class/message and chapter number for every live failure; do not allow classified failures to count as successful closure. |
| T-14-06-04 | Tampering | D-11 validation | mitigate | Live GLM validation must enforce or fail on 300-500 character bounds; deterministic-only proof is insufficient. |
| T-14-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Overall checks:
- `dart analyze lib/features/ai/infrastructure/openai_adapter.dart test/journey/serial_generation_test.dart test/journey/full_journey_test.dart test/journey/helpers/journey_container.dart`
- No-credential deterministic serial and full journey tests pass as supplemental coverage.
- With `GLM_API_KEY`, live smoke, serial, and full journey commands pass sequentially.
- `14-ISSUE-LOG.md` closes `P14-04-GLM-01` only with 30/30 real GLM generation, D-11 300-500 compliance, all-30 deviation detection, token audit `totalCalls >= 30`, and input/output token totals.
</verification>

<success_criteria>
- `P14-04-GLM-01` is closed/resolved with latest successful real GLM evidence.
- Deterministic local 30-chapter orchestration coverage passes without GLM credentials and is labeled supplemental.
- Live GLM validation honors D-02/D-03/D-04/D-11 and fails if any condition is unmet.
- No secrets are written to `14-ISSUE-LOG.md`.
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-06-SUMMARY.md` when done. If Task 3 does not achieve 30/30 real GLM success with D-11, all-30 deviation detection, and token audit evidence, the summary must state plan failed/blocked and route to debug/replan.
</output>
