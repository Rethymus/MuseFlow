---
phase: 14-world-building-first-30-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/automation/helpers/journey_container.dart
  - test/automation/helpers/xianxia_fixtures.dart
  - test/automation/helpers/story_outline.dart
  - test/automation/world_building_test.dart
autonomous: false
requirements:
  - JOURNEY-01

user_setup:
  - service: glm
    why: "Real GLM API for journey validation scripts"
    env_vars:
      - name: GLM_API_KEY
        source: "User will provide at execution time via script parameter"
      - name: GLM_BASE_URL
        source: "Default: https://open.bigmodel.cn/api/paas/v4, user may override"

must_haves:
  truths:
    - "Journey scripts can create a ProviderContainer with real OpenAIAdapter configured for GLM API" (D-02: real GLM API, not FakeAdapter)
    - "All 15+ Hive boxes required by knowledge/stats/story features are opened in test context"
    - "Xianxia character cards, world settings, and Skill documents can be created programmatically via repositories" (D-01: automation-driven creation)
    - "NameIndexService.refresh() builds an index containing character names after entity creation"
    - "World template instantiation produces a WorldSetting and CharacterCards from the xianxia preset" (D-07: xianxia template + custom supplement)
    - "4-5 Skill guardian rules are created with isActive: true and cover realm constraints, sect hierarchy, world taboos"
  artifacts:
    - path: "test/automation/helpers/journey_container.dart"
      provides: "ProviderContainer factory with real GLM API overrides"
      exports: ["createJourneyContainer", "cleanupJourneyContainer"]
    - path: "test/automation/helpers/xianxia_fixtures.dart"
      provides: "Character card, world setting, Skill document fixture data"
      exports: ["XianxiaFixtures"]
    - path: "test/automation/helpers/story_outline.dart"
      provides: "30-chapter plot outline data"
      exports: ["StoryOutline"]
    - path: "test/automation/world_building_test.dart"
      provides: "JOURNEY-01 world-building integration test"
      contains: "group.*World Building"
  key_links:
    - from: "test/automation/helpers/journey_container.dart"
      to: "lib/core/presentation/providers.dart"
      via: "openaiAdapterProvider.overrideWithValue(OpenAIAdapter())"
      pattern: "overrideWithValue.*OpenAIAdapter"
    - from: "test/automation/helpers/journey_container.dart"
      to: "lib/core/presentation/providers.dart"
      via: "activeProviderProvider.overrideWithValue(AIProvider(...))"
      pattern: "activeProviderProvider"
    - from: "test/automation/world_building_test.dart"
      to: "test/automation/helpers/xianxia_fixtures.dart"
      via: "XianxiaFixtures.protagonist, .master, .rival, .sectHierarch, .realmConstraints, .worldTaboos"
      pattern: "XianxiaFixtures\\."
---

<objective>
Create journey validation test infrastructure: ProviderContainer factory with real GLM API, xianxia domain fixtures, 30-chapter story outline, and world-building integration test covering JOURNEY-01.

Purpose: Provide the shared foundation that all downstream journey tests depend on, while delivering the first requirement (world-building with template instantiation, character cards, Skill guardian rules, and NameIndex verification).

Output: 4 new files in `test/automation/` (helpers + world_building_test), runnable via `flutter test test/automation/world_building_test.dart`
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md
@.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md
@.planning/phases/14-world-building-first-30-chapters/14-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/core/presentation/providers.dart (lines 163-185):
```dart
final activeProviderProvider = Provider<AIProvider?>((ref) {
  final serviceAsync = ref.watch(providerServiceProvider);
  return serviceAsync.asData?.value.getActiveProvider();
});

final activeApiKeyProvider = Provider<String?>((ref) {
  final apiKeyAsync = ref.watch(apiKeyFutureProvider);
  return apiKeyAsync.asData?.value;
});

final openaiAdapterProvider = Provider<AIAdapter>((ref) {
  return OpenAIAdapter();
});
```

From lib/features/ai/domain/ai_provider.dart (lines 38-65):
```dart
class AIProvider {
  final String id;
  final String name;
  final String baseUrl;
  final AiProviderType type;
  final String model;
  final bool isActive;
  final DateTime createdAt;
  final double? temperature;
  final double? topP;
  final int? maxTokens;
  // constructor: requires id, name, baseUrl, type, model, isActive, createdAt
}
```

From lib/features/knowledge/domain/character_card.dart (line 11):
```dart
class CharacterCard implements KnowledgeEntity {
  // fields: id, name, personality, appearance, backstory, aliases, createdAt, updatedAt
}
```

From lib/features/knowledge/domain/world_setting.dart (line 11):
```dart
class WorldSetting implements KnowledgeEntity {
  // fields: id, name, description, rules, factions, geography, techLevel, aliases, createdAt, updatedAt
}
```

From lib/features/knowledge/domain/skill_document.dart (lines 6, 143):
```dart
class SkillSections {
  final String? powerHierarchy;
  final String? rules;
  final String? taboos;
  // constructor: SkillSections({this.powerHierarchy, this.rules, this.taboos})
}
class SkillDocument implements KnowledgeEntity {
  // fields: id, name, description, content, sections, isActive, createdAt, updatedAt
}
```

From lib/features/knowledge/application/name_index_service.dart (lines 7, 36):
```dart
class NameIndexService extends Notifier<NameIndex> {
  NameIndex build() { ... }
  void refresh() { ... }
}
```

Key repository providers (from providers.dart):
- `characterCardRepositoryProvider` -> `FutureProvider<CharacterCardRepository>`
- `worldSettingRepositoryProvider` -> `FutureProvider<WorldSettingRepository>`
- `skillRepositoryProvider` -> `FutureProvider<SkillRepository>`
- `nameIndexServiceProvider` -> `NotifierProvider<NameIndexService, NameIndex>`
- `worldTemplateRepositoryProvider` -> `Provider<WorldTemplateRepository>`
- `templateInstantiationServiceProvider` -> `FutureProvider<TemplateInstantiationService>`

From test/automation/helpers/test_container.dart (Phase 13 reference):
```dart
Future<ProviderContainer> createTestContainer() async {
  TestWidgetsFlutterBinding.ensureInitialized();
  final tempDir = Directory.systemTemp.createTempSync('automation_test_');
  Hive.init(tempDir.path);
  // Opens 5 boxes: manuscripts, chapters, token_audit, ai_providers, fragments
  return ProviderContainer(
    overrides: [openaiAdapterProvider.overrideWithValue(FakeAdapter())],
  );
}
```
</interfaces>
</context>

<research_notes>
## RESEARCH.md Open Questions — Resolution Strategy for This Plan

**Open Question 1: GLM API Streaming Compatibility** (RESEARCH.md "Open Questions" section)
- Relevance: This plan creates the container factory that will be used for all streaming calls in later plans. The container itself does not make API calls, but it wires OpenAIAdapter for GLM.
- Resolution strategy: The container factory is correct regardless of streaming compatibility — if streaming fails, the error surfaces in plans 02/03. The executor should note this as a known risk during the human-verify checkpoint.

**Open Question 2: Provider Graph Depth with Real API Credentials** (RESEARCH.md "Open Questions" section)
- Relevance: HIGH — this plan creates the ProviderContainer factory and must determine which providers to override. If overriding activeProviderProvider + activeApiKeyProvider is insufficient (intermediate providers like providerServiceProvider also need overrides), the container factory will fail at resolution time.
- Resolution strategy: Override the minimal set first (activeProviderProvider, activeApiKeyProvider, openaiAdapterProvider). If StateError "未配置可用的 AI 模型" appears during Task 2 execution, add providerServiceProvider override as fallback. The human-verify checkpoint validates this works end-to-end.

**Open Question 3: Manual Spot-Check Scope Definition** (RESEARCH.md "Open Questions" section)
- Relevance: LOW — this plan is purely automated (world-building test). Manual spot-check belongs to JOURNEY-06 (Plan 03).
- Resolution strategy: Not applicable to this plan.
</research_notes>

<tasks>

<task type="auto">
  <name>Task 1: Create journey helper utilities (container, fixtures, story outline)</name>
  <files>test/automation/helpers/journey_container.dart, test/automation/helpers/xianxia_fixtures.dart, test/automation/helpers/story_outline.dart</files>

  <read_first>
    - test/automation/helpers/test_container.dart — Phase 13 container factory pattern to adapt
    - lib/core/presentation/providers.dart — all provider IDs and override targets
    - lib/features/ai/domain/ai_provider.dart — AIProvider constructor fields
    - lib/features/knowledge/domain/character_card.dart — CharacterCard constructor
    - lib/features/knowledge/domain/world_setting.dart — WorldSetting constructor
    - lib/features/knowledge/domain/skill_document.dart — SkillDocument + SkillSections constructors
    - test/automation/fixtures/manuscript_fixtures.dart — fixture class pattern to replicate
    - test/automation/fixtures/xianxia_content.dart — xianxia content pattern
  </read_first>

  <action>
Create three helper files in `test/automation/helpers/`:

**journey_container.dart**: Adapt Phase 13's `createTestContainer()` pattern. Key differences per D-01 and RESEARCH.md:
- Replace `FakeAdapter()` with `OpenAIAdapter()` (import from `package:museflow/features/ai/infrastructure/openai_adapter.dart`)
- Open 15 Hive boxes: `manuscripts`, `chapters`, `token_audit`, `ai_providers`, `fragments`, `character_cards`, `world_settings`, `skill_documents`, `writing_stats`, `daily_writing_stats`, `achievement_badges`, `plot_nodes`, `foreshadowing_entries`, `graph_positions`, `guardian_annotations`
- Override `openaiAdapterProvider` with `OpenAIAdapter()`
- Override `activeProviderProvider` with an `AIProvider(id: 'glm-journey', name: 'GLM', baseUrl: baseUrl, type: AiProviderType.openai, model: model, isActive: true, createdAt: DateTime.now(), temperature: 0.8, topP: 0.9)` — values from RESEARCH.md code example
- Override `activeApiKeyProvider` with the apiKey parameter
- Accept `apiKey`, `baseUrl`, and `model` as named required parameters
- Do NOT open the `settings` box or depend on `settingsRepositoryProvider`/`secureStorageServiceProvider` (Pitfall 6: SecureStorage fails in test context)
- Export `createJourneyContainer()` and `cleanupJourneyContainer()` functions
- Add `@Tag('journey')` annotation to skip in normal test runs (journey tests require real API key)

**xianxia_fixtures.dart**: Create `XianxiaFixtures` class with static factory methods per D-08 and D-09:
- `static CharacterCard protagonist()` — name "林风", personality "坚韧隐忍", backstory "凡人少年，因意外踏入修仙之路"
- `static CharacterCard master()` — name "清虚真人", personality "严厉但慈爱", backstory "青云宗长老，炼气九层巅峰"
- `static CharacterCard senior()` — name "苏雪晴", personality "聪慧温柔", backstory "大师姐，筑基期，对主角关怀备至"
- `static CharacterCard rival()` — name "赵天磊", personality "傲慢好胜", backstory "外门天才，出身世家，视主角为对手"
- `static WorldSetting sectWorld()` — name "青云宗修仙界", description covering realm system
- `static List<SkillDocument> skillRules()` — returns 4 SkillDocument instances with `isActive: true`:
  1. "境界体系约束" — powerHierarchy: "凡人 < 练气(1-9层) < 筑基 < 金丹 < 元婴 < 化神", taboos: "练气期不可使用筑基以上法术"
  2. "门派等级森严" — rules: "外门弟子不得擅入内门禁地; 杂役弟子不可直接面见长老"
  3. "世界观禁忌" — taboos: "不存在火器、枪械、现代电子设备; 不存在'科学'概念"
  4. "能力限制" — rules: "凡人不可御剑飞行; 练气期不可施展神识"
- Use `DateTime.now()` for createdAt fields, `uuid` for id fields (generate via import)
- Follow Phase 13's `ManuscriptFixtures` class pattern

**story_outline.dart**: Create `StoryOutline` class with `static const List<String> chapters` containing 30 chapter plot points per D-10:
- Each entry is 300-500 characters describing the chapter's theme and key plot events
- Follow coherent xianxia growth line: mortal youth enters sect (ch 1-5) -> Qi Refining early stages (ch 6-10) -> sect trials and rivalries (ch 11-15) -> Qi Refining breakthrough to peak (ch 16-20) -> Foundation Establishment attempt (ch 21-25) -> Foundation Establishment success and new challenges (ch 26-30)
- Include character names (林风, 清虚真人, 苏雪晴, 赵天磊) and setting details in plot points so knowledge injection can be verified
- Also export `static const List<String> characterNames = ['林风', '清虚真人', '苏雪晴', '赵天磊']` for assertion convenience
- Follow `XianxiaContent` static const pattern from Phase 13
  </action>

  <verify>
    <automated>dart analyze test/automation/helpers/journey_container.dart test/automation/helpers/xianxia_fixtures.dart test/automation/helpers/story_outline.dart</automated>
  </verify>

  <done>
    - journey_container.dart exports createJourneyContainer and cleanupJourneyContainer, accepts apiKey/baseUrl/model params
    - journey_container.dart opens 15 Hive boxes and overrides openaiAdapterProvider, activeProviderProvider, activeApiKeyProvider
    - journey_container.dart does NOT reference settingsRepositoryProvider or secureStorageServiceProvider
    - xianxia_fixtures.dart exports XianxiaFixtures with 4 character card factories, 1 world setting factory, 1 skillRules() returning 4 SkillDocuments
    - All SkillDocuments have isActive: true
    - story_outline.dart exports StoryOutline.chapters with exactly 30 entries and StoryOutline.characterNames
  </done>
</task>

<task type="auto">
  <name>Task 2: Create world-building integration test (JOURNEY-01)</name>
  <files>test/automation/world_building_test.dart</files>

  <read_first>
    - test/automation/core_flow_test.dart — test structure pattern (setUp/tearDown with container, group/test nesting)
    - test/automation/helpers/journey_container.dart — the container factory created in Task 1
    - test/automation/helpers/xianxia_fixtures.dart — the fixture data created in Task 1
    - lib/core/presentation/providers.dart — repository provider IDs to use
    - lib/features/knowledge/application/name_index_service.dart — refresh() method
    - lib/features/templates/application/template_instantiation_service.dart — createDraft/saveDraft pattern (if available)
  </read_first>

  <action>
Create `test/automation/world_building_test.dart` implementing JOURNEY-01 validation.

**Test structure**: Follow Phase 13's `core_flow_test.dart` pattern with setUp/tearDown using `createJourneyContainer()` and `cleanupJourneyContainer()`.

**Environment variable for API key**: Read GLM_API_KEY from `Platform.environment['GLM_API_KEY']`. If missing, skip the entire test group with `skip: 'GLM_API_KEY not set'`. Similarly read GLM_BASE_URL (default to `'https://open.bigmodel.cn/api/paas/v4'`) and GLM_MODEL (default to `'glm-4-flash'`).

**Test group 1: Character Card Creation**
- Create protagonist, master, senior, rival via `XianxiaFixtures`
- Add each to `characterCardRepositoryProvider` via `repo.add()`
- Assert `repo.getAll()` returns 4 cards
- Assert each card's name matches fixture name

**Test group 2: World Setting Creation**
- Create world setting via `XianxiaFixtures.sectWorld()`
- Add to `worldSettingRepositoryProvider` via `repo.add()`
- Assert world setting is retrievable by ID

**Test group 3: Skill Document Creation**
- Create 4 Skill documents via `XianxiaFixtures.skillRules()`
- Add each to `skillRepositoryProvider` via `repo.add()`
- Assert `repo.getAll()` returns 4 documents, all with `isActive: true`
- Assert document names match expected Skill rule names

**Test group 4: NameIndex Refresh and Verification**
- After all entities created in groups 1-3, call `container.read(nameIndexServiceProvider.notifier).refresh()` per RESEARCH.md Pitfall 2
- Read the rebuilt NameIndex: `container.read(nameIndexServiceProvider)`
- Assert that `findMatches('林风踏入青云峰')` returns matches containing the character name (verifies KnowledgeInjectionMiddleware can find entities)
- Assert that `findMatches('清虚真人在殿中讲道')` returns matches

**Test group 5: Template Instantiation (if WorldTemplateRepository works in test context)**
- Try loading xianxia template via `worldTemplateRepositoryProvider`
- If it succeeds (Pitfall 7: may fail with asset bundle issues), create a draft via `templateInstantiationServiceProvider` and assert it produces entities
- If template loading fails, skip this group with a message explaining asset bundle unavailability in test context
  </action>

  <verify>
    <automated>flutter test test/automation/world_building_test.dart --timeout 120s</automated>
  </verify>

  <done>
    - Test file has 5 groups covering character cards, world settings, skill documents, NameIndex refresh, and template instantiation
    - GLM_API_KEY read from environment, test skipped if absent
    - 4 character cards created and verified
    - 1 world setting created and verified
    - 4 Skill documents created with isActive: true
    - NameIndex.refresh() called after all entity creation
    - NameIndex.findMatches() returns results for character names
    - All assertions pass when GLM_API_KEY is set
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Journey container factory, xianxia fixtures, story outline, and world-building integration test</what-built>
  <how-to-verify>
    1. Set GLM_API_KEY environment variable: `export GLM_API_KEY=your_key_here`
    2. Run: `flutter test test/automation/world_building_test.dart --timeout 120s`
    3. Verify all 5 test groups pass (character cards, world setting, skill documents, NameIndex, template instantiation or skipped)
    4. Run: `dart analyze test/automation/helpers/` to confirm zero analyzer errors
    5. Verify that test/automation/helpers/ directory structure extends Phase 13's existing layout
    6. Confirm the test does NOT attempt to open the `settings` Hive box (check for SecureStorage errors)
    7. Note: RESEARCH.md Open Question 2 (provider graph depth) is implicitly validated here — if StateError appears, the container factory needs additional overrides
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script->GLM_API | Journey script sends prompts to GLM API via HTTPS; API key in parameter |
| env_var->script | GLM_API_KEY read from environment variable into script memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | journey_container.dart | mitigate | API key passed as parameter, never written to disk, never printed to stdout. journey_container.dart must not contain hardcoded keys. |
| T-14-02 | Information Disclosure | test output | mitigate | Test output (print statements) must never include apiKey value. Log API call counts but not credentials. |
| T-14-03 | Tampering | API calls (MITM) | accept | OpenAIAdapter._validateBaseUrl() enforces HTTPS (except localhost). GLM API uses HTTPS by default. |
| T-14-04 | Information Disclosure | Hive temp directory | accept | Temp directory deleted on cleanup. No API keys written to Hive boxes in journey tests. |
| T-14-SC | Tampering | npm/pub installs | N/A | No new packages installed this phase. All dependencies pre-approved in Phase 1-13. |
</threat_model>

<verification>
- `dart analyze test/automation/helpers/` passes with zero errors
- `flutter test test/automation/world_building_test.dart --timeout 120s` passes all groups (when GLM_API_KEY set)
- Test skips gracefully when GLM_API_KEY is not set
- No SecureStorage/PlatformException errors in test output
- NameIndex contains character name matches after entity creation
</verification>

<success_criteria>
- JOURNEY-01 fully covered: xianxia template world with character cards, world settings, and Skill guardian configuration created programmatically
- Journey container factory works with real GLM API overrides
- NameIndex builds successfully after entity creation
- Shared fixtures and story outline ready for downstream plans (02, 03)
- Zero analyzer errors in test/automation/helpers/
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-01-SUMMARY.md` when done
</output>
