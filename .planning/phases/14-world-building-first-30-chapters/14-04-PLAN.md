---
phase: 14-world-building-first-30-chapters
plan: 04
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - test/journey/world_building_test.dart
  - test/journey/helpers/xianxia_fixtures.dart
  - .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
autonomous: false
gap_closure: true
requirements: [JOURNEY-01, JOURNEY-02, JOURNEY-03, JOURNEY-04, JOURNEY-05, JOURNEY-06]
user_setup:
  - service: GLM API
    why: "Real journey validation requires external GLM generation evidence per D-02"
    env_vars:
      - name: GLM_API_KEY
        source: "User-provided GLM/BigModel API key; export in shell only, never commit or paste"
      - name: GLM_BASE_URL
        source: "Optional; default should remain https://open.bigmodel.cn/api/paas/v4 if tests already default it"
      - name: GLM_MODEL
        source: "Optional; use existing journey test default if unset"
must_haves:
  truths:
    - "JOURNEY-01 world-building validation uses Phase 7 xianxia template instantiation, not a manual world fixture"
    - "GLM-dependent journey tests execute with GLM_API_KEY set and produce non-secret evidence for synthesis, opening, 30-chapter generation, deviation detection, and token audit"
    - "Manual Flutter UI validation evidence exists for toolbar rewrite/polish/free-input, anti-AI-scent review, knowledge/Skill UI, opening guide styles, and chapter operations"
  artifacts:
    - path: "test/journey/world_building_test.dart"
      provides: "Template-instantiation JOURNEY-01 validation"
      contains: "worldTemplateRepositoryProvider"
    - path: "test/journey/world_building_test.dart"
      provides: "Template save path validation"
      contains: "templateInstantiationServiceProvider"
    - path: "test/journey/helpers/xianxia_fixtures.dart"
      provides: "Character and Skill fixtures only; no manual xianxia world skeleton used for D-07"
      excludes: "sectWorld"
    - path: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      provides: "Real GLM and manual UI evidence log without secrets"
      contains: "OQ-01: GLM API Streaming Compatibility"
  key_links:
    - from: "test/journey/world_building_test.dart"
      to: "Phase 7 template repository"
      via: "worldTemplateRepositoryProvider.getById('male-xianxia-sect')"
      pattern: "getById\\('male-xianxia-sect'\\)"
    - from: "test/journey/world_building_test.dart"
      to: "TemplateInstantiationService"
      via: "createDraft() then saveDraft()"
      pattern: "createDraft\\(.*saveDraft\\("
    - from: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      to: "GLM test evidence"
      via: "OQ-01/OQ-02 statuses and automated checklist"
      pattern: "Status:\\s*(resolved|verified)"
    - from: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      to: "Manual UI evidence"
      via: "Manual-only checklist evidence blocks"
      pattern: "Evidence:.*(screenshot|log|observation|observed)"
---

<objective>
Close Phase 14 verification gaps without adding new user-facing product features.

Purpose: Convert Phase 14 from partially evidenced validation into verified journey evidence by fixing the JOURNEY-01 template path, running real GLM/API validation, and recording manual UI observations required by D-05/D-06.

Output: Updated JOURNEY-01 test/fixtures, completed GLM evidence in `14-ISSUE-LOG.md`, completed manual UI evidence in `14-ISSUE-LOG.md`, and no committed secrets or private manuscript content.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md
@.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md
@.planning/phases/14-world-building-first-30-chapters/14-PATTERNS.md
@.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md
@.planning/phases/14-world-building-first-30-chapters/14-UI-SPEC.md
@.planning/phases/14-world-building-first-30-chapters/14-01-SUMMARY.md
@.planning/phases/14-world-building-first-30-chapters/14-02-SUMMARY.md
@.planning/phases/14-world-building-first-30-chapters/14-03-SUMMARY.md
@test/journey/world_building_test.dart
@test/journey/helpers/xianxia_fixtures.dart
@test/journey/fragment_synthesis_test.dart
@test/journey/opening_guide_test.dart
@test/journey/serial_generation_test.dart
@test/journey/full_journey_test.dart
@.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md

<interfaces>
Executor must use these exact existing identifiers for the JOURNEY-01 fix:
- `worldTemplateRepositoryProvider`
- `templateInstantiationServiceProvider`
- `getById('male-xianxia-sect')`
- `createDraft(...)`
- `saveDraft(...)`
- `nameIndexServiceProvider.notifier.refresh()`
- `XianxiaFixtures.characters()` and `XianxiaFixtures.skills()` may remain for custom D-08/D-09 supplements.
- `XianxiaFixtures.sectWorld()` must be removed or made unreachable for D-07/JOURNEY-01 validation.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 用户可以使用 MuseFlow 搭建完整修仙世界观并写出前30章，验证核心创作循环的可靠性 | 14-04 | COVERED | Gap closure verifies blocked/partial evidence paths only.
REQ | JOURNEY-01 | 修仙模板搭建世界观，角色卡、设定集、Skill 守护配置 | 14-04 Task 1 | COVERED | Fixes Phase 7 template instantiation path per D-07.
REQ | JOURNEY-02 | 碎片捕捉到 AI 整理 | 14-04 Task 2 | COVERED | Runs real GLM fragment synthesis and records evidence.
REQ | JOURNEY-03 | 开篇引导三种风格 | 14-04 Task 2, Task 3 | COVERED | Runs real GLM opening test and manual style review.
REQ | JOURNEY-04 | 30章 CRUD/排序/拆分/合并/复制/删除 | 14-04 Task 3 | COVERED | Local automated evidence already passed; manual UI operations evidence is added.
REQ | JOURNEY-05 | 逐章 AI 生成、知识库注入、Skill 守护、token audit | 14-04 Task 2 | COVERED | Runs smoke, serial, full journey tests with GLM_API_KEY.
REQ | JOURNEY-06 | 编辑器浮窗操作与反AI味效果 | 14-04 Task 3 | COVERED | Manual-only UI evidence recorded.
RESEARCH | D-02/D-03/D-04 | Real GLM API, 2-3s serial interval, stop-on-error | 14-04 Task 2 | COVERED | Commands run `-j 1`; failures stop and are recorded.
RESEARCH | Pitfall 2 | Refresh NameIndex after entity creation | 14-04 Task 1 | COVERED | JOURNEY-01 keeps refresh after template save + custom supplements.
RESEARCH | Pitfall 5 | Flush TokenAuditService before snapshot | 14-04 Task 2 | COVERED | Serial/full tests must show audit results after flush.
CONTEXT | D-01 | Automated-first with manual spot-checks | 14-04 Tasks 1-3 | COVERED | Automated fixes/runs first, manual UI evidence last.
CONTEXT | D-02 | Use real GLM API | 14-04 Task 2 | COVERED | Requires `GLM_API_KEY`; skips are not accepted as evidence.
CONTEXT | D-03 | Serial 2-3s calls | 14-04 Task 2 | COVERED | Serial tests run with existing delay behavior.
CONTEXT | D-04 | Stop on first failure | 14-04 Task 2 | COVERED | Test failure blocks completion and must be logged.
CONTEXT | D-05 | Full manual spot-check of 4 areas | 14-04 Task 3 | COVERED | Checklist covers toolbar, knowledge/Skill, opening guide, chapter operations.
CONTEXT | D-06 | Structured issue log | 14-04 Tasks 2-3 | COVERED | Updates `14-ISSUE-LOG.md` with findings and evidence.
CONTEXT | D-07 | Phase 7 xianxia template + custom supplement | 14-04 Task 1 | COVERED | Template instantiation path is mandatory.
CONTEXT | D-08 | 3-4 character cards | 14-04 Task 1 | COVERED | Custom character supplements remain after template save.
CONTEXT | D-09 | 4-5 Skill rules | 14-04 Task 1 | COVERED | Custom active Skill supplements remain after template save.
CONTEXT | D-10 | Coherent 30-chapter xianxia growth line | 14-04 Task 2 | COVERED | Serial/full tests use story outline.
CONTEXT | D-11 | 300-500 characters per chapter | 14-04 Task 2 | COVERED | Real serial results must prove bounds.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix JOURNEY-01 to instantiate the Phase 7 xianxia template</name>
  <files>test/journey/world_building_test.dart, test/journey/helpers/xianxia_fixtures.dart</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` gaps truth 1.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` decisions D-07, D-08, D-09.
    - `.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md` Pattern 2 and Pitfall 7.
    - `test/journey/world_building_test.dart` current manual fixture flow.
    - `test/journey/helpers/xianxia_fixtures.dart` current `sectWorld()` definition and custom fixture helpers.
    - `lib/features/templates/application/template_instantiation_service.dart` for exact `createDraft()` and `saveDraft()` signatures.
    - `lib/features/templates/infrastructure/world_template_repository.dart` for `getById('male-xianxia-sect')` behavior.
    - `lib/core/presentation/providers.dart` for `worldTemplateRepositoryProvider` and `templateInstantiationServiceProvider` provider names.
  </read_first>
  <behavior>
    - Test 1: `world_building_test.dart` loads the template via `container.read(worldTemplateRepositoryProvider).getById('male-xianxia-sect')` and fails if the template is absent.
    - Test 2: `world_building_test.dart` creates a draft through `templateInstantiationServiceProvider`, calls `createDraft(...)`, then persists through `saveDraft(...)`.
    - Test 3: Saved template artifacts plus D-08/D-09 custom supplements result in at least one world setting, at least 3 character cards, and 4 active Skill documents available to repositories.
    - Test 4: `NameIndexService.refresh()` after template save + custom supplements finds template/custom character names and setting keywords for later prompt injection.
    - Test 5: `XianxiaFixtures.sectWorld()` is not defined or not referenced anywhere in the JOURNEY-01 validation path.
  </behavior>
  <action>
    Rewrite the JOURNEY-01 setup as one template-instantiation flow per D-07. In `test/journey/world_building_test.dart`, replace manual `XianxiaFixtures.sectWorld()` world creation with `worldTemplateRepositoryProvider`, `getById('male-xianxia-sect')`, `templateInstantiationServiceProvider`, `createDraft(...)`, and `saveDraft(...)`. Keep custom D-08 character supplements and D-09 active Skill supplements only as additions after the Phase 7 template save. Refresh `nameIndexServiceProvider.notifier` after all template and supplement entities are saved. In `test/journey/helpers/xianxia_fixtures.dart`, remove `sectWorld()` if no longer needed; otherwise ensure JOURNEY-01 never imports or calls it and add no alternate manual world skeleton for D-07. Do not add product features and do not replace the template path with file-parsed JSON unless `WorldTemplateRepository` asset loading fails; if fallback is required, record the asset-loading error and the fallback in the task summary and keep `getById('male-xianxia-sect')` attempted first.
  </action>
  <verify>
    <automated>dart analyze test/journey/world_building_test.dart test/journey/helpers/xianxia_fixtures.dart</automated>
    <automated>flutter test test/journey/world_building_test.dart --timeout 120s</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/world_building_test.dart | grep -q "worldTemplateRepositoryProvider" && grep -v '^[[:space:]]*//' test/journey/world_building_test.dart | grep -q "templateInstantiationServiceProvider" && grep -v '^[[:space:]]*//' test/journey/world_building_test.dart | grep -q "getById('male-xianxia-sect')" && grep -v '^[[:space:]]*//' test/journey/world_building_test.dart | grep -q "createDraft" && grep -v '^[[:space:]]*//' test/journey/world_building_test.dart | grep -q "saveDraft"</automated>
    <automated>! grep -v '^[[:space:]]*//' test/journey/world_building_test.dart test/journey/helpers/xianxia_fixtures.dart | grep -q "sectWorld"</automated>
  </verify>
  <acceptance_criteria>
    - `world_building_test.dart` contains the exact provider/template identifiers required by the verifier: `worldTemplateRepositoryProvider`, `templateInstantiationServiceProvider`, `getById('male-xianxia-sect')`, `createDraft`, and `saveDraft`.
    - `world_building_test.dart` no longer uses `XianxiaFixtures.sectWorld()`.
    - `xianxia_fixtures.dart` no longer defines or exposes `sectWorld()` unless a non-JOURNEY-01 legacy reference requires it; if retained, it must not be referenced by any `test/journey/*` test.
    - The local world-building test passes without `GLM_API_KEY`; JOURNEY-01 must not be skipped just because GLM credentials are absent.
  </acceptance_criteria>
  <done>JOURNEY-01 blocker is closed: the test proves Phase 7 template instantiation plus custom character/Skill supplements and NameIndex refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Run real GLM journey tests and record non-secret evidence</name>
  <files>.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` gaps truth 2.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` decisions D-02, D-03, D-04, D-06, D-11.
    - `.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md` Open Questions OQ-01/OQ-02, Pitfalls 4 and 5, Security Domain.
    - `test/journey/helpers/journey_container.dart` for env/provider defaults and API key handling.
    - `test/journey/fragment_synthesis_test.dart` for JOURNEY-02 test names and output assertions.
    - `test/journey/opening_guide_test.dart` for JOURNEY-03 test names and output assertions.
    - `test/journey/serial_generation_test.dart` for GLM smoke, 30-chapter generation, deviation detection, delay, and token audit assertions.
    - `test/journey/full_journey_test.dart` for E2E journey command and expected completion.
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` current OQ/checklist fields.
  </read_first>
  <action>
    With `GLM_API_KEY` exported in the shell, run the real GLM/API validation commands sequentially with `-j 1`: the serial smoke test named `should pass GLM streaming smoke test`, the complete fragment synthesis test, the complete opening guide test, the complete serial generation test, and the full journey test named `should complete full xianxia journey from world-building to 30 chapters`. If `GLM_API_KEY` is missing, stop before running tests and request the user to provide it as an environment variable; do not paste or store the key in any file. Treat skipped GLM tests as failure for this gap closure. For each command, capture only safe evidence: command name, exit code, pass/fail/skip status, generated output lengths/counts, opening variant count/styles, 30 chapter count, character-bound summary, deviation detection count/summary, token audit totalCalls and token totals. Update `14-ISSUE-LOG.md` OQ-01 and OQ-02 from unresolved to resolved or failed with concise findings. Check the automated checklist items that are proven by the command outputs. If any test fails because of a product bug required to complete the validation, make the smallest code fix necessary and re-run the affected command; do not add new user-facing functionality.
  </action>
  <verify>
    <automated>test -n "$GLM_API_KEY"</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/serial_generation_test.dart -j 1 --plain-name "should pass GLM streaming smoke test" --timeout 120s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/fragment_synthesis_test.dart -j 1 --timeout 300s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/opening_guide_test.dart -j 1 --timeout 300s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/serial_generation_test.dart -j 1 --timeout 1200s</automated>
    <automated>GLM_API_KEY="$GLM_API_KEY" flutter test test/journey/full_journey_test.dart -j 1 --plain-name "should complete full xianxia journey from world-building to 30 chapters" --timeout 1200s</automated>
    <automated>grep -v '^[[:space:]]*-' .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md | grep -q "OQ-01: GLM API Streaming Compatibility" && grep -v '^[[:space:]]*-' .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md | grep -Eiq "Status:[[:space:]]*(resolved|verified|passed)"</automated>
    <automated>! grep -q "$GLM_API_KEY" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - GLM smoke, fragment synthesis, opening guide, serial generation, and full journey tests were executed with `GLM_API_KEY` present; a skip caused by missing credentials is not accepted.
    - `14-ISSUE-LOG.md` OQ-01 records real streaming compatibility results without secrets.
    - `14-ISSUE-LOG.md` OQ-02 records provider graph results and whether additional overrides were needed.
    - Automated checklist entries for fragment synthesis, opening guide variants, 30 chapter bounds, deviation detection, character-name presence, and token audit accuracy are checked only if supported by real command output.
    - The issue log contains no API key value, bearer token, raw credential, or full private manuscript dump.
  </acceptance_criteria>
  <done>Real GLM/API-dependent Phase 14 behavior is proven or a concrete blocking issue is logged with safe evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Perform manual Flutter UI validation and complete evidence fields</name>
  <files>.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` gaps truth 3.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` decisions D-05 and D-06.
    - `.planning/phases/14-world-building-first-30-chapters/14-UI-SPEC.md` Manual spot-check sections for JOURNEY-01 through JOURNEY-06.
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` current Manual Spot-Check Checklist.
  </read_first>
  <what-built>Automated journey scripts and GLM evidence are prepared; remaining evidence requires actual Flutter app interaction and visual/subjective verification.</what-built>
  <action>
    Run the Flutter app in the available target environment and complete the manual-only checklist in `14-ISSUE-LOG.md` per D-05/D-06. Validate editor floating toolbar appearance and operations for `语气改写`, `文段润色`, and `自由输入` with instruction `让这段更悬疑`; record whether streaming, diff/application, undo, bottom-viewport flip, and IME composition behavior work. Review anti-AI-scent output for obvious phrases such as `值得注意的是`, `总而言之`, and `需要指出的是`. Validate knowledge/Skill UI by checking generated chapters reference character names, consistency holds, Skill violations are absent or DeviationWarningWidget appears when applicable. Validate opening guide UI by checking scene, character, and suspense variants are visually distinct and coherent. Validate chapter operations via sidebar/context menu: reorder, split, merge, copy with `(副本)`, delete with confirmation, and final sort order. Record concise observations and evidence paths only; screenshots/logs must avoid API keys and avoid private manuscript content beyond intended xianxia test data. Add structured issue rows for any discovered bug/UX friction/missing need using the existing issue table categories and severity definitions.
  </action>
  <how-to-verify>
    1. Start the app with the same local data or recreate the Phase 14 xianxia test manuscript.
    2. Open a generated chapter and select a paragraph.
    3. Verify FloatingToolbar appears below selection, then run `语气改写`, undo, run `文段润色`, then run `自由输入` with `让这段更悬疑`.
    4. Select text in the bottom 40% of the viewport and verify toolbar flips above selection.
    5. Start Chinese IME composition and verify toolbar hides during composition and reappears afterward.
    6. Open knowledge/Skill surfaces and verify character cards, world setting, active Skill rules, and any deviation warning behavior.
    7. Open the opening guide and verify the three styles: 场景切入, 人物切入, 悬念切入.
    8. Use chapter UI to reorder, split, merge, copy, delete, and verify sidebar sort order.
    9. Fill every `**Evidence:**` block in `14-ISSUE-LOG.md` with observations and safe evidence paths.
  </how-to-verify>
  <verify>
    <automated>grep -q "- \[x\] Select text in any generated chapter" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md && grep -q "- \[x\] Reorder chapters via drag-and-drop" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <automated>grep -Eiq "Evidence:.*(screenshot|log|observation|observed|截图|日志|观察)" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <human-check>Human confirms every manual-only checkbox in `14-ISSUE-LOG.md` is either checked with evidence or has a structured issue row explaining the failure.</human-check>
  </verify>
  <acceptance_criteria>
    - Editor Floating Toolbar section has checked items or structured issue rows for rewrite, polish, free input, anti-AI-scent review, bottom-viewport flip, and IME composition.
    - Knowledge Injection + Skill Guardian section has checked items or structured issue rows for generated character references, consistency, Skill violations, and DeviationWarningWidget behavior.
    - Opening Guide 3 Styles section has checked items or structured issue rows for scene, character, and suspense styles.
    - Chapter Operations section has checked items or structured issue rows for reorder, split, merge, copy, delete, and final sort order.
    - Evidence paths/observations contain no secrets and no private manuscript content outside the intended test data.
  </acceptance_criteria>
  <resume-signal>Type `approved` when `14-ISSUE-LOG.md` contains completed manual evidence, or describe exact failed checklist items so executor can record structured issues.</resume-signal>
  <done>JOURNEY-06 and manual UI portions of JOURNEY-03/JOURNEY-04 have human evidence recorded in the structured issue log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell environment -> journey tests | `GLM_API_KEY` enters test process through environment variables. |
| Journey tests -> GLM external API | Prompt content leaves local machine for real GLM generation and deviation checks. |
| Test/app output -> issue log / screenshots | Evidence is persisted in planning artifacts and may be committed. |
| Human UI validation -> issue log | Manual observations may include generated prose or screenshot paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Information Disclosure | `GLM_API_KEY`, shell logs, `14-ISSUE-LOG.md` | mitigate | Never print, paste, or write the API key; verify the issue log does not contain `$GLM_API_KEY`; redact bearer tokens and headers from any output excerpt. |
| T-14-04-02 | Denial of Service | GLM external API quota/rate limits | mitigate | Run GLM tests sequentially with `-j 1`; preserve existing 2-3s serial delays per D-03; stop on first API failure per D-04 and log safe error class/status only. |
| T-14-04-03 | Tampering | GLM API endpoint / network path | mitigate | Use existing `OpenAIAdapter` HTTPS validation; do not downgrade to non-HTTPS endpoint except localhost test endpoints, which are not acceptable for D-02 evidence. |
| T-14-04-04 | Information Disclosure | Manual screenshots/evidence | mitigate | Store only paths and concise observations; screenshots must avoid secrets, provider settings screens, and private manuscript content beyond Phase 14 intended xianxia test data. |
| T-14-04-05 | Repudiation | Manual UI observations | mitigate | Every manual checklist item must have either checked evidence or a structured issue row with reproduce steps and actual behavior. |
| T-14-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; if an executor attempts an install, stop and return for planning review. |
</threat_model>

<verification>
Overall checks after all tasks:
- `dart analyze test/journey/`
- `flutter test test/journey/world_building_test.dart --timeout 120s`
- With `GLM_API_KEY` set: run GLM smoke, fragment synthesis, opening guide, serial generation, and full journey commands from Task 2.
- Confirm `14-ISSUE-LOG.md` has OQ-01/OQ-02 resolved or failed with safe evidence, automated checklist updated from actual results, and manual evidence filled.
- Confirm no secret was committed: `! grep -q "$GLM_API_KEY" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md`.
</verification>

<success_criteria>
- The verifier's JOURNEY-01 blocker is closed: no manual `sectWorld()` validation path remains, and Phase 7 template instantiation is exercised.
- Real GLM/API evidence exists for fragment synthesis, opening guide, serial generation, full journey, deviation detection, and token audit; skipped tests are not accepted.
- Manual UI validation evidence exists for toolbar operations, anti-AI-scent review, knowledge/Skill UI, opening guide styles, and chapter operations.
- `14-ISSUE-LOG.md` contains structured issues for any failed manual or API validation, with severity and reproduction steps.
- No new user-facing product features are added unless a small bug fix is required to make the validation path work.
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-04-SUMMARY.md` when done.
</output>
