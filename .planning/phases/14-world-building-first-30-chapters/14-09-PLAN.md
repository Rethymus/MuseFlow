---
phase: 14-world-building-first-30-chapters
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ai/infrastructure/openai_adapter.dart
  - test/journey/deviation_warning_widget_test.dart
  - .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
autonomous: true
gap_closure: true
requirements: [JOURNEY-06]
must_haves:
  truths:
    - "fetchModelList validates HTTPS before creating an OpenAIClient, preventing API key leakage over plaintext HTTP (CR-01 closed)."
    - "fetchModelList closes OpenAIClient in a finally block, preventing resource leaks on exception (CR-02 closed)."
    - "DeviationWarningWidget renders correctly with pre-loaded test warnings, proving all four fields (severity, skillName, description, suggestedFix) are visible (P14-07-HUMAN-02 closed)."
  artifacts:
    - path: "lib/features/ai/infrastructure/openai_adapter.dart"
      provides: "fetchModelList with HTTPS validation and resource-safe client lifecycle"
      contains: "fetchModelList"
    - path: "test/journey/deviation_warning_widget_test.dart"
      provides: "Widget test for DeviationWarningWidget with mock deviation state"
      contains: "DeviationWarningWidget"
    - path: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      provides: "Closure evidence for CR-01, CR-02, P14-07-HUMAN-02"
      contains: "CR-01"
  key_links:
    - from: "lib/features/ai/infrastructure/openai_adapter.dart"
      to: "OpenAIClient"
      via: "fetchModelList validates HTTPS then uses try/finally for client lifecycle"
      pattern: "fetchModelList.*_validateBaseUrl|fetchModelList.*finally"
    - from: "test/journey/deviation_warning_widget_test.dart"
      to: "lib/features/knowledge/presentation/deviation_warning_widget.dart"
      via: "pumpWidget with overridden deviationNotifierProvider containing test DeviationWarnings"
      pattern: "deviationNotifierProvider.*overrideWithValue"
---

<objective>
Close security debt CR-01/CR-02 in fetchModelList and create a widget test for DeviationWarningWidget to close P14-07-HUMAN-02.

Purpose: Code review identified two security issues in `fetchModelList`: (1) it bypasses HTTPS validation, potentially leaking the API key over plaintext HTTP, and (2) it leaks the `OpenAIClient` TCP connection on exception. Separately, P14-07-HUMAN-02 blocks JOURNEY-06 verification because DeviationWarningWidget visual rendering was untested. A widget test with pre-loaded mock warnings closes this gap without requiring AI generation or IME.

Output: Fixed `fetchModelList` with HTTPS enforcement and resource-safe lifecycle, new `deviation_warning_widget_test.dart` with passing widget tests, and closed issues in issue log.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md
@.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
@lib/features/ai/infrastructure/openai_adapter.dart
@lib/features/knowledge/presentation/deviation_warning_widget.dart
@lib/features/knowledge/application/deviation_detection_service.dart
@lib/core/presentation/providers.dart

<interfaces>
From lib/features/ai/infrastructure/openai_adapter.dart:
- `fetchModelList({required String apiKey, required String baseUrl})` at line 170 -- currently creates `OpenAIClient.withApiKey(apiKey, baseUrl: baseUrl)` without calling `_validateBaseUrl(baseUrl)`.
- `_validateBaseUrl(String baseUrl)` at line 233 -- existing method that throws `AIStreamException('baseUrl 必须使用 HTTPS 协议')` for non-HTTPS, non-localhost URLs.
- `OpenAIClient.withApiKey(apiKey, baseUrl: baseUrl)` -- creates client from openai_dart.
- `client.close()` -- closes the HTTP client and releases TCP connection.

From lib/features/knowledge/presentation/deviation_warning_widget.dart:
- `DeviationWarningWidget extends ConsumerWidget` -- watches `deviationNotifierProvider`, renders warning list.
- `_WarningTile(index, warning)` -- renders individual warning with `warning.severity`, `warning.skillName`, `warning.description`, `warning.suggestedFix`.
- `DeviationWarning` data class has fields: `severity` (DeviationSeverity enum), `skillName` (String), `description` (String), `suggestedFix` (String?).

From lib/core/presentation/providers.dart:
- `deviationNotifierProvider` at line 394 is `AsyncNotifierProvider<DeviationNotifier, DeviationResult>`.

From lib/features/knowledge/application/deviation_detection_service.dart:
- `DeviationSeverity.clear` and other severity levels exist.
- `DeviationResult({required this.warnings})` constructor.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 用户可以使用 MuseFlow 写前30章并验证核心创作循环可靠性 | 14-09 | COVERED | Security fixes and widget test close remaining JOURNEY-06 gaps.
REQ | JOURNEY-06 | 编辑器浮窗操作验证与反AI味效果 | 14-09 Task 2 | COVERED | DeviationWarningWidget widget test closes P14-07-HUMAN-02.
RESEARCH | Code review CR-01 | fetchModelList bypasses HTTPS validation | 14-09 Task 1 | COVERED | Add `_validateBaseUrl(baseUrl)` call before client creation.
RESEARCH | Code review CR-02 | fetchModelList leaks OpenAIClient on exception | 14-09 Task 1 | COVERED | Use try/finally to ensure `client.close()`.
CONTEXT | D-06 | Structured issue log | 14-09 Tasks 1-2 | COVERED | Update issue log with closure evidence for CR-01, CR-02, P14-07-HUMAN-02.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix fetchModelList HTTPS bypass (CR-01) and resource leak (CR-02)</name>
  <files>lib/features/ai/infrastructure/openai_adapter.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `lib/features/ai/infrastructure/openai_adapter.dart` -- the `fetchModelList` method (lines 170-186) and the existing `_validateBaseUrl` method (lines 233-241).
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` anti-patterns section listing CR-01 and CR-02.
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` current CR-01/P14-05-HIVE resolved items.
  </read_first>
  <behavior>
    - Test 1: `fetchModelList` with a non-HTTPS, non-localhost `baseUrl` (e.g., `http://evil.com`) throws `AIStreamException` with message containing `HTTPS` before any network call.
    - Test 2: `fetchModelList` with empty `apiKey` returns empty list without calling `_validateBaseUrl` or creating a client (existing behavior preserved).
    - Test 3: When `fetchModelList` catches an exception (timeout, network), the `OpenAIClient` is closed -- verified by mocking or by ensuring no resource leak assertion.
  </behavior>
  <action>
    In `lib/features/ai/infrastructure/openai_adapter.dart`, modify the `fetchModelList` method (lines 170-186) with two changes:

    **CR-01 fix:** Add `_validateBaseUrl(baseUrl)` as the first statement inside the method body, after the empty-apiKey early return. This reuses the existing `_validateBaseUrl` method (line 233) that enforces HTTPS with a localhost exception. If validation fails, the `AIStreamException` propagates to the caller before any `OpenAIClient` is created, preventing API key transmission over plaintext.

    **CR-02 fix:** Change the try/catch structure to use try/finally. Move `client.close()` into a `finally` block so the TCP connection is released even when `client.models.list().timeout(...)` throws. The current code has `client.close()` only in the success path (line 180); if the timeout or network call throws, the client leaks. Restructure to:
    ```
    try {
      final modelList = await client.models.list().timeout(...);
      return modelList.data.map((m) => m.id).toList();
    } catch (_) {
      return [];
    } finally {
      client.close();
    }
    ```

    After verifying the changes pass analysis, update `14-ISSUE-LOG.md` with closure notes for CR-01 and CR-02.
  </action>
  <verify>
    <automated>dart analyze lib/features/ai/infrastructure/openai_adapter.dart</automated>
    <automated>grep -A 15 "fetchModelList" lib/features/ai/infrastructure/openai_adapter.dart | grep -q "_validateBaseUrl"</automated>
    <automated>grep -A 20 "fetchModelList" lib/features/ai/infrastructure/openai_adapter.dart | grep -q "finally"</automated>
    <automated>grep -Eiq "CR-01.*(closed|resolved|fixed|已修复)" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
    <automated>grep -Eiq "CR-02.*(closed|resolved|fixed|已修复)" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `fetchModelList` calls `_validateBaseUrl(baseUrl)` before creating `OpenAIClient`.
    - `fetchModelList` uses `try/finally` with `client.close()` in the `finally` block.
    - Empty apiKey early return still works (returns empty list).
    - `dart analyze` passes with no warnings.
    - `14-ISSUE-LOG.md` records CR-01 and CR-02 as fixed/closed.
  </acceptance_criteria>
  <done>CR-01 and CR-02 security debt is resolved in production code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create widget test for DeviationWarningWidget to close P14-07-HUMAN-02</name>
  <files>test/journey/deviation_warning_widget_test.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `lib/features/knowledge/presentation/deviation_warning_widget.dart` -- full widget implementation (68 lines). Watch `deviationNotifierProvider`, renders `_WarningTile` for each warning.
    - `lib/features/knowledge/application/deviation_detection_service.dart` -- `DeviationWarning` constructor (severity, skillName, description, suggestedFix) and `DeviationResult` constructor.
    - `lib/core/presentation/providers.dart` -- `deviationNotifierProvider` at line 394: `AsyncNotifierProvider<DeviationNotifier, DeviationResult>`.
    - `test/journey/helpers/journey_container.dart` -- existing test container pattern for provider overrides.
    - `.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md` P14-07-HUMAN-02 row.
  </read_first>
  <behavior>
    - Test 1: When `deviationNotifierProvider` contains a `DeviationResult` with two warnings, `DeviationWarningWidget` renders both warning tiles. Each tile contains the `skillName` and `description` text.
    - Test 2: When a warning has `suggestedFix` non-null, the subtitle text contains `建议修复` and the suggested fix content.
    - Test 3: When a warning has `DeviationSeverity.clear`, the leading icon color matches `Theme.of(context).colorScheme.error`.
    - Test 4: When `deviationNotifierProvider` has empty warnings list, the widget renders `SizedBox.shrink()` (no visible content).
    - Test 5: Tapping the `全部忽略` button calls `clearAll()` on the notifier.
  </behavior>
  <action>
    Create `test/journey/deviation_warning_widget_test.dart` as a new Flutter widget test file.

    The test must:
    - Use `ProviderContainer` with an override for `deviationNotifierProvider`. Since it is an `AsyncNotifierProvider<DeviationNotifier, DeviationResult>`, override it with a `AsyncValue.data(DeviationResult(warnings: testWarnings))` using the `overrideWithValue` pattern.
    - Construct two `DeviationWarning` fixtures: one with `severity: DeviationSeverity.clear`, `skillName: '境界体系约束'`, `description: '主角使用了筑基期法术'`, `suggestedFix: '将法术改为练气期级别'`; another with `severity: DeviationSeverity.warning` (or the non-clear variant), `skillName: '门派等级森严'`, `description: '外门弟子进入内门禁地'`, `suggestedFix: null`.
    - Wrap `DeviationWarningWidget` in `MaterialApp` and `ProviderScope(overrides: [...], container: container)`.
    - Pump the widget with `tester.pumpWidget(...)`.
    - Assert `find.text` for both `skillName` values, both `description` values, the `建议修复` text, the `全部忽略` text, and the `检测到 2 条设定偏离提醒` text.
    - For the empty warnings case, override with `AsyncValue.data(DeviationResult(warnings: []))` and assert `find.byType(SizedBox)` finds the shrink widget (or alternatively assert no warning-related text exists).
    - Tap the `全部忽略` button and verify via a mock notifier or by checking the notifier state change.

    Update `14-ISSUE-LOG.md` P14-07-HUMAN-02 row to note that automated widget test evidence now covers visual rendering, closing the gap.
  </action>
  <verify>
    <automated>dart analyze test/journey/deviation_warning_widget_test.dart</automated>
    <automated>flutter test test/journey/deviation_warning_widget_test.dart --timeout 60s</automated>
    <automated>grep -Eiq "P14-07-HUMAN-02.*(closed|widget test|automated evidence|已验证)" .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test/journey/deviation_warning_widget_test.dart` exists and contains at least 5 test cases covering populated warnings, empty warnings, suggested fix display, severity coloring, and clear-all action.
    - All tests pass.
    - The test uses `deviationNotifierProvider` override with pre-loaded `DeviationWarning` fixtures.
    - No AI generation or IME is required to run the test.
    - `14-ISSUE-LOG.md` P14-07-HUMAN-02 is marked as closed with widget test evidence.
  </acceptance_criteria>
  <done>P14-07-HUMAN-02 is closed by automated widget test proving DeviationWarningWidget renders all four fields correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetchModelList -> external API | API key transmitted to external server. HTTPS enforcement prevents plaintext leakage. |
| Test process -> Hive temp directories | Widget tests create temporary containers with isolated state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09-01 | Information Disclosure | fetchModelList API key over HTTP | mitigate | `_validateBaseUrl(baseUrl)` now enforced before `OpenAIClient` creation. |
| T-14-09-02 | Denial of Service | fetchModelList leaked TCP connections | mitigate | `try/finally` ensures `client.close()` on all exit paths. |
| T-14-09-03 | Tampering | DeviationWarningWidget test isolation | mitigate | Test uses ProviderContainer override with mock data, no real AI or network. |
| T-14-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs are planned. |
</threat_model>

<verification>
Overall checks:
- `dart analyze lib/features/ai/infrastructure/openai_adapter.dart test/journey/deviation_warning_widget_test.dart`
- `flutter test test/journey/deviation_warning_widget_test.dart --timeout 60s`
- `fetchModelList` calls `_validateBaseUrl` and uses `try/finally`.
- DeviationWarningWidget test covers populated, empty, severity, suggestedFix, and clearAll cases.
- `14-ISSUE-LOG.md` records CR-01, CR-02, and P14-07-HUMAN-02 as closed.
</verification>

<success_criteria>
- CR-01 (HTTPS bypass in fetchModelList) is fixed in production code.
- CR-02 (resource leak in fetchModelList) is fixed in production code.
- P14-07-HUMAN-02 (DeviationWarningWidget visual) is closed by widget test.
- All tests pass.
- Issue log updated with closure evidence.
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-09-SUMMARY.md` when done.
</output>
