---
phase: 14-world-building-first-30-chapters
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - test/automation/fragment_synthesis_test.dart
  - test/automation/opening_guide_test.dart
  - test/automation/chapter_management_test.dart
autonomous: true
requirements:
  - JOURNEY-02
  - JOURNEY-03
  - JOURNEY-04

user_setup: []

must_haves:
  truths:
    - "User can input灵感碎片 via bullet-note mode and AI organizes them into logical story paragraphs (JOURNEY-02)"
    - "Opening guide generates 3 style variants (scene/character/suspense) using real GLM API (JOURNEY-03)"
    - "30 chapters can be created, read, updated, reordered, split, merged, copied, and deleted via repository operations (JOURNEY-04)"
    - "Fragment capture creates Fragment entities that are persisted and retrievable"
    - "AI synthesis produces non-empty text output containing expected story elements"
    - "Chapter management operations (sort, split, merge, copy, delete) correctly modify chapter data"
  artifacts:
    - path: "test/automation/fragment_synthesis_test.dart"
      provides: "JOURNEY-02 fragment capture and AI synthesis validation"
      contains: "group.*Fragment.*Synthesis"
    - path: "test/automation/opening_guide_test.dart"
      provides: "JOURNEY-03 opening guide 3-style validation"
      contains: "group.*Opening.*Guide"
    - path: "test/automation/chapter_management_test.dart"
      provides: "JOURNEY-04 chapter CRUD and operations validation"
      contains: "group.*Chapter.*Management"
  key_links:
    - from: "test/automation/fragment_synthesis_test.dart"
      to: "test/automation/helpers/journey_container.dart"
      via: "createJourneyContainer() for real API access"
      pattern: "createJourneyContainer"
    - from: "test/automation/opening_guide_test.dart"
      to: "lib/features/onboarding/application/opening_generator_service.dart"
      via: "openingGeneratorServiceProvider.generateOpenings()"
      pattern: "generateOpenings"
    - from: "test/automation/chapter_management_test.dart"
      to: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      via: "chapterRepositoryProvider for CRUD operations"
      pattern: "chapterRepositoryProvider"
---

<objective>
Create three integration tests covering JOURNEY-02 (fragment capture + AI synthesis), JOURNEY-03 (opening guide 3 styles), and JOURNEY-04 (30 chapter CRUD + reorder/split/merge/copy/delete).

Purpose: Validate the core creation pipeline (fragments -> AI synthesis -> chapters) and all chapter management operations at the 30-chapter scale, using real GLM API for AI-dependent tests.

Output: 3 new test files in `test/automation/`, each runnable independently via `flutter test test/automation/<file>.dart`
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md
@.planning/phases/14-world-building-first-30-chapters/14-RESEARCH.md
@.planning/phases/14-world-building-first-30-chapters/14-PATTERNS.md

<interfaces>
From test/automation/core_flow_test.dart — test structure (setUp/tearDown, group nesting, repository access pattern):
```dart
final repository = await container.read(someRepositoryProvider.future);
final entity = await repository.add(Entity(...));
expect(repository.getById(entity.id), isNotNull);
```

From lib/core/presentation/providers.dart — key providers:
- `fragmentRepositoryProvider` -> `FutureProvider<FragmentRepository>` (line 86)
- `promptPipelineProvider` -> `FutureProvider<PromptPipeline>` (line 190)
- `openingGeneratorServiceProvider` -> `FutureProvider<OpeningGeneratorService>` (line 436)
- `chapterRepositoryProvider` -> `FutureProvider<ChapterRepository>` (line 658)
- `manuscriptRepositoryProvider` -> `FutureProvider<ManuscriptRepository>` (line 650)

From lib/features/ai/application/prompt_pipeline.dart:
```dart
class PromptContext {
  final List<Fragment> fragments;
  final List<String> bannedPhrases;
  // constructor: PromptContext({this.fragments = const [], this.bannedPhrases = const []})
  // methods: addMessage, withMessages, replaceSystemMessage
}
class PromptPipeline {
  List<ChatMessage> build(PromptContext context);
}
```

From lib/features/onboarding/application/opening_generator_service.dart (from PATTERNS.md):
```dart
class OpeningGeneratorService {
  Future<List<OpeningVariant>> generateOpenings({
    required String genreName,
    required String worldDescription,
    required String characterDescription,
    required String storyConcept,
    required String manuscriptId,
  });
}
```
Note: openingGeneratorServiceProvider requires activeProviderProvider and activeApiKeyProvider to be non-null (throws StateError "未配置可用的 AI 模型" if null).

From test/automation/fixtures/manuscript_fixtures.dart — chapter fixture pattern:
```dart
static Chapter chapter({required String manuscriptId, required int number, String? content}) {
  return Chapter(id: 'ch-$number', manuscriptId: manuscriptId, title: '第$number章',
    sortOrder: number, status: '草稿', documentContent: content ?? '', ...);
}
```

From test/automation/helpers/story_outline.dart (created in Plan 01):
```dart
class StoryOutline {
  static const List<String> chapters; // 30 entries
  static const List<String> characterNames; // ['林风', '清虚真人', '苏雪晴', '赵天磊']
}
```

From test/automation/helpers/xianxia_fixtures.dart (created in Plan 01):
```dart
class XianxiaFixtures {
  static CharacterCard protagonist();
  static WorldSetting sectWorld();
  static List<SkillDocument> skillRules();
}
```
</interfaces>
</context>

<research_notes>
## RESEARCH.md Open Questions — Resolution Strategy for This Plan

**Open Question 1: GLM API Streaming Compatibility**
- Relevance: HIGH — Tasks 1 and 2 make real GLM API streaming calls via OpenAIAdapter.createStream(). If GLM's SSE format is incompatible with openai_dart's parser, these tests will fail at the streaming step.
- Resolution strategy: The tests wrap streaming calls in try/catch. If SSE parsing fails, the error will be caught and printed with the raw error message. The executor should inspect the error to determine if it is a format mismatch (not a network/auth issue). If SSE parsing fails, report as a blocking finding in the issue log for Phase 16 (REPORT-02). This cannot be resolved during planning — only real API testing will confirm compatibility.

**Open Question 2: Provider Graph Depth with Real API Credentials**
- Relevance: MEDIUM — Plan 01 already validated the container factory works. If Plan 01's checkpoint passed, this plan inherits that validation. If Plan 01 required additional overrides (e.g., providerServiceProvider), those overrides are already in journey_container.dart and will be inherited here.
- Resolution strategy: Inherited from Plan 01. No additional action needed unless Plan 01's checkpoint revealed issues.

**Open Question 3: Manual Spot-Check Scope Definition**
- Relevance: LOW — Task 3 (chapter_management_test) is purely local CRUD. Tasks 1-2 include AI calls but their quality assertions are automated (non-empty, differentiated, character name presence). Manual spot-check for JOURNEY-02/03 creative quality belongs to the issue log checklist in Plan 03.
- Resolution strategy: Not applicable to this plan's automated tests.
</research_notes>

<tasks>

<task type="auto">
  <name>Task 1: Create fragment capture and AI synthesis test (JOURNEY-02)</name>
  <files>test/automation/fragment_synthesis_test.dart</files>

  <read_first>
    - test/automation/core_flow_test.dart — Segment 4 (AI generation single chapter) pattern for adapter.createStream()
    - test/automation/helpers/journey_container.dart — createJourneyContainer() API
    - lib/core/presentation/providers.dart — fragmentRepositoryProvider (line 86), promptPipelineProvider (line 190)
    - lib/features/ai/application/prompt_pipeline.dart — PromptContext and PromptPipeline.build() API
    - lib/features/ai/domain/ai_adapter.dart — AIAdapter.createStream() signature
  </read_first>

  <action>
Create `test/automation/fragment_synthesis_test.dart` implementing JOURNEY-02 validation.

**Test structure**: setUp calls `createJourneyContainer(apiKey: apiKey, baseUrl: baseUrl, model: model)`, tearDown calls `cleanupJourneyContainer()`. Skip entire test if GLM_API_KEY env var missing.

**Test group 1: Fragment Creation (Bullet-Note Mode)**
- Create 5 Fragment entities via `fragmentRepositoryProvider` -> `repo.addFragment()` — each containing a xianxia-themed灵感碎片 (e.g., "林风在青云峰采集灵草时发现一块古玉", "苏雪晴暗中帮助林风通过入门考核", "赵天磊在比武中使出禁术", "清虚真人传授林风玄天功第一层", "外门禁地传来异响，有弟子失踪")
- Assert `repo.getAll()` returns 5 fragments
- Assert each fragment's text is non-empty

**Test group 2: AI Synthesis via PromptPipeline**
- Read `promptPipelineProvider` from container
- Create `PromptContext` with the 5 fragments as `fragments` parameter and empty `bannedPhrases`
- Call `pipeline.build(context)` to assemble messages (includes KnowledgeInjection, SkillEnforcement, AntiAIScent, TokenAudit middlewares per RESEARCH.md architecture)
- Assert messages list is non-empty and contains system message and user message
- Assert the user message contains fragment text content (verify pipeline assembles context)

**Test group 3: Real AI Synthesis Call**
- Read `openaiAdapterProvider` from container
- Get apiKey, baseUrl, model from container overrides (read activeProviderProvider and activeApiKeyProvider)
- Build messages via PromptPipeline as in group 2
- Call `adapter.createStream()` with pipeline-built messages and an onUsage callback
- Collect stream output via `.join()` into a string
- Assert output is non-empty (real GLM API returned content)
- Assert output length > 50 characters (meaningful synthesis, not empty)
- Assert onUsage callback was invoked with non-null Usage
- NOTE: If streaming fails with SSE parsing errors (RESEARCH.md Open Question 1), log the raw error message with `print('[STREAM_ERROR] ...')` and rethrow. Do NOT swallow streaming errors.

**Test group 4: Synthesis Quality Check**
- From the synthesis output in group 3, check that it contains at least one character name from `StoryOutline.characterNames` (verifies knowledge injection is working — the pipeline should have injected character context)
- If no character name found, log a warning but do not fail (knowledge injection depends on NameIndex being populated, which may not be set up in this test)
  </action>

  <verify>
    <automated>flutter test test/automation/fragment_synthesis_test.dart --timeout 120s</automated>
  </verify>

  <done>
    - 4 test groups: fragment creation, pipeline assembly, real AI synthesis, quality check
    - 5 fragments created and persisted
    - PromptPipeline.build() assembles messages with fragment content
    - Real GLM API call produces non-empty output > 50 chars
    - onUsage callback records token usage
    - Quality check verifies character name presence in output (or logs warning)
    - Streaming errors logged with full error context (not swallowed)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create opening guide 3-style test (JOURNEY-03)</name>
  <files>test/automation/opening_guide_test.dart</files>

  <read_first>
    - test/automation/helpers/journey_container.dart — createJourneyContainer() API
    - lib/core/presentation/providers.dart — openingGeneratorServiceProvider (line 436), requires activeProviderProvider + activeApiKeyProvider non-null
    - lib/features/onboarding/application/opening_generator_service.dart — generateOpenings() signature and return type
    - test/automation/helpers/xianxia_fixtures.dart — character and world fixture data
  </read_first>

  <action>
Create `test/automation/opening_guide_test.dart` implementing JOURNEY-03 validation.

**Test structure**: setUp calls `createJourneyContainer(apiKey: apiKey, baseUrl: baseUrl, model: model)`, tearDown calls `cleanupJourneyContainer()`. Skip if GLM_API_KEY missing.

**Test group 1: Opening Generator Service Resolution**
- Read `openingGeneratorServiceProvider.future` from container
- Assert it resolves without throwing StateError "未配置可用的 AI 模型" (proves activeProviderProvider and activeApiKeyProvider overrides are working)
- The service depends on activeProvider + activeApiKey + openaiAdapter, all of which are overridden in journey_container

**Test group 2: Generate 3 Opening Styles**
- Create a manuscript via `manuscriptRepositoryProvider` for the manuscriptId parameter
- Call `service.generateOpenings()` with parameters:
  - genreName: `'修仙'`
  - worldDescription: use `XianxiaFixtures.sectWorld().description` (or a hardcoded fallback)
  - characterDescription: combine protagonist and master descriptions from `XianxiaFixtures`
  - storyConcept: `'凡人少年林风入门修仙，经历炼气筑基的成长之路'`
  - manuscriptId: created manuscript's id
- Assert result is a List with length 3 (per RESEARCH.md: 3 variants)
- Assert result maps contain styles: verify each variant has a `style` field

**Test group 3: Style Differentiation**
- Extract the 3 variants from group 2
- Assert each variant's content is non-empty (each style produced real text)
- Assert the 3 variants are NOT identical (each style should produce different opening text)
- Log each variant's style label and first 50 characters for manual review

**Note**: If `OpeningGeneratorService` or `OpeningVariant` class structure differs from what PATTERNS.md describes (the executor should read the actual source files), adapt assertions accordingly. The key invariant is: 3 non-identical, non-empty text outputs with different style identifiers.
  </action>

  <verify>
    <automated>flutter test test/automation/opening_guide_test.dart --timeout 120s</automated>
  </verify>

  <done>
    - openingGeneratorServiceProvider resolves without StateError (proves container overrides work for dependent services)
    - generateOpenings() returns exactly 3 variants
    - Each variant is non-empty and they are not all identical
    - Variant styles are identifiable (scene/character/suspense or equivalent)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create chapter management test (JOURNEY-04)</name>
  <files>test/automation/chapter_management_test.dart</files>

  <read_first>
    - test/automation/core_flow_test.dart — Segments 2-3 (Chapter CRUD + sorting) pattern
    - test/automation/helpers/journey_container.dart — createJourneyContainer() API
    - test/automation/fixtures/manuscript_fixtures.dart — chapter fixture pattern
    - lib/features/manuscript/infrastructure/chapter_repository.dart — add, update, delete, getByManuscriptId, updateDocumentContent methods
  </read_first>

  <action>
Create `test/automation/chapter_management_test.dart` implementing JOURNEY-04 validation.

**Test structure**: setUp creates container (GLM API not needed for CRUD — but still use journey_container for consistency), tearDown cleans up. This test does NOT require GLM_API_KEY since chapter CRUD is local-only. Use `@Tags(const ['journey'])` but do NOT skip if API key missing — chapter operations are purely local.

**Test group 1: Create 30 Chapters**
- Create a manuscript via `manuscriptRepositoryProvider`
- Loop i=1..30: create chapter via `chapterRepositoryProvider.add()` with manuscriptId, title "第i章", sortOrder i
- Assert `chapterRepository.getByManuscriptId()` returns 30 chapters
- Assert chapters are ordered by sortOrder 1-30

**Test group 2: Chapter Content Update**
- Get first chapter from the 30 created
- Call `chapterRepository.updateDocumentContent(chapter.id, '第一章正文内容测试')` per D-11 (300-500 chars, but shorter for test speed)
- Assert updated chapter's documentContent matches
- Assert updatedAt timestamp changed

**Test group 3: Chapter Reorder**
- Take chapters 5, 10, 15 — swap their sortOrder values (5->15, 10->5, 15->10) via `chapterRepository.update()` with copyWith(sortOrder: newValue)
- Assert chapters retrieved in new order (chapter formerly at position 10 is now at position 5)

**Test group 4: Chapter Split**
- Take a chapter with content "第一段内容\n---\n第二段内容"
- Create two new chapters: first half with "第一段内容", second half with "第二段内容" at adjacent sortOrders
- Delete the original chapter
- Assert chapter count remains 30 (1 deleted, 2 added)
- Assert new chapters have correct content

**Test group 5: Chapter Merge**
- Take two adjacent chapters (e.g., chapters 1 and 2)
- Create a merged chapter with content = chapter1.content + "\n" + chapter2.content at chapter1's sortOrder
- Delete both original chapters
- Assert chapter count decreased by 1

**Test group 6: Chapter Copy**
- Take chapter 3
- Create a new chapter with same content but title "第3章（副本）" and sortOrder after chapter 30
- Assert copied chapter exists with identical documentContent but different id

**Test group 7: Chapter Delete**
- Delete chapter 25 via `chapterRepository.delete()`
- Assert chapter count is now 29 (or appropriate count after merge operations)
- Assert deleted chapter ID returns null from getById

Note: Groups 4-7 modify chapter state sequentially (split changes count, merge changes count, etc.). Either use separate setUp for each group or carefully track the expected count. Recommended approach: each group creates its own manuscript + 30 chapters in setUp, so group state is independent.
  </action>

  <verify>
    <automated>flutter test test/automation/chapter_management_test.dart --timeout 60s</automated>
  </verify>

  <done>
    - 7 test groups covering create-30, content-update, reorder, split, merge, copy, delete
    - 30 chapters created successfully in group 1
    - Content update persists correctly
    - Reorder produces chapters in new sortOrder sequence
    - Split replaces 1 chapter with 2, preserving content halves
    - Merge replaces 2 chapters with 1, concatenating content
    - Copy creates new chapter with identical content but unique ID
    - Delete removes chapter from repository
    - Each group is independent (own manuscript + chapters)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script->GLM_API | fragment_synthesis_test.dart and opening_guide_test.dart make real API calls |
| script->Hive | chapter_management_test.dart is local-only, no API calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | fragment_synthesis_test.dart output | mitigate | Test print statements must not include apiKey. Print AI output text and usage counts only. |
| T-14-06 | Information Disclosure | opening_guide_test.dart output | mitigate | Same as T-14-05: no credential logging. |
| T-14-07 | Tampering | GLM API responses | accept | Responses are used for validation only (non-empty, differentiated). Content stored only in Hive temp dir, deleted on cleanup. |
</threat_model>

<verification>
- `flutter test test/automation/fragment_synthesis_test.dart --timeout 120s` passes (with GLM_API_KEY)
- `flutter test test/automation/opening_guide_test.dart --timeout 120s` passes (with GLM_API_KEY)
- `flutter test test/automation/chapter_management_test.dart --timeout 60s` passes (no API key needed)
- All 3 tests skip gracefully when GLM_API_KEY is not set (fragment + opening tests only)
- Chapter management test runs without GLM_API_KEY (pure local operations)
- dart analyze test/automation/ passes with zero errors
</verification>

<success_criteria>
- JOURNEY-02: Fragment creation + AI synthesis pipeline validated end-to-end with real GLM API
- JOURNEY-03: Opening guide generates 3 differentiated style variants via real GLM API
- JOURNEY-04: All chapter operations (CRUD, reorder, split, merge, copy, delete) verified at 30-chapter scale
- Zero analyzer errors
- All tests pass independently
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-02-SUMMARY.md` when done
</output>
