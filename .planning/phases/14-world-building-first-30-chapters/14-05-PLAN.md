---
phase: 14-world-building-first-30-chapters
plan: 05
type: execute
wave: 5
depends_on: [14-04]
files_modified:
  - test/journey/helpers/journey_container.dart
  - lib/features/ai/application/anti_ai_scent_processor.dart
  - test/journey/automated_ui_evidence_test.dart
  - test/journey/chapter_management_test.dart
  - .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
autonomous: true
gap_closure: true
requirements: [JOURNEY-04, JOURNEY-06]
must_haves:
  truths:
    - "Journey tests can run without global Hive cleanup deleting another journey test's boxes."
    - "Anti-AI-scent processing removes the verifier-listed phrases 值得注意的是, 总而言之, and 需要指出的是 instead of encoding their presence as passing evidence."
    - "Chapter management validation exercises production ChapterNotifier operations for reorder, split, merge, duplicate, and delete."
  artifacts:
    - path: "test/journey/helpers/journey_container.dart"
      provides: "Per-test Hive directory ownership and cleanup"
      contains: "JourneyTestContainer"
    - path: "lib/features/ai/application/anti_ai_scent_processor.dart"
      provides: "Built-in AI-scent phrase removal"
      contains: "总而言之"
    - path: "test/journey/automated_ui_evidence_test.dart"
      provides: "Anti-AI-scent regression gate"
      contains: "isNot(contains('总而言之'))"
    - path: "test/journey/chapter_management_test.dart"
      provides: "Production-path chapter operation validation"
      contains: "chapterNotifierProvider"
    - path: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      provides: "Updated closure evidence for P14-04-AI-01 and CR-01"
      contains: "P14-05"
  key_links:
    - from: "test/journey/helpers/journey_container.dart"
      to: "Hive temp directory"
      via: "cleanup closes boxes and deletes only the owning tempDir"
      pattern: "Directory.*delete\(recursive: true\)"
    - from: "test/journey/automated_ui_evidence_test.dart"
      to: "lib/features/ai/application/anti_ai_scent_processor.dart"
      via: "AntiAIScentProcessor().process"
      pattern: "AntiAIScentProcessor\(\)\.process"
    - from: "test/journey/chapter_management_test.dart"
      to: "lib/features/manuscript/application/chapter_notifier.dart"
      via: "chapterNotifierProvider.notifier production operations"
      pattern: "chapterNotifierProvider\.notifier"
---

<objective>
Close Phase 14 deterministic validation blockers before rerunning long GLM validation.

Purpose: Remove the code-review critical Hive isolation risk, convert the anti-AI-scent known limitation into a real regression gate, and strengthen chapter operation evidence so JOURNEY-04/JOURNEY-06 no longer rely on brittle or self-fulfilling tests.

Output: Hardened journey test container, expanded anti-AI-scent phrase coverage, production-path chapter management tests, and safe issue-log closure notes.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md
@.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md
@.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md
@.planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
@test/journey/helpers/journey_container.dart
@lib/features/ai/application/anti_ai_scent_processor.dart
@test/journey/automated_ui_evidence_test.dart
@test/journey/chapter_management_test.dart
@lib/features/manuscript/application/chapter_notifier.dart

<interfaces>
Use these existing identifiers exactly:
- `createJourneyContainer(...)`
- `cleanupJourneyContainer(...)`
- `ProviderContainer`
- `Hive.close()`
- `AntiAIScentProcessor().process(...)`
- `chapterNotifierProvider.notifier`
- `loadChapters(manuscript.id)`
- `reorder(manuscript.id, oldIndex, newIndex)`
- `splitChapter(chapterId, beforeContent, afterContent)`
- `mergeChapters(chapterId1, chapterId2)`
- `duplicateChapter(chapterId)`
- `delete(chapterId)`
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | 用户可以使用 MuseFlow 搭建完整修仙世界观并写出前30章，验证核心创作循环的可靠性 | 14-05..14-07 | COVERED | This plan closes deterministic blockers before live GLM rerun and final human review.
REQ | JOURNEY-04 | 章节 CRUD/排序/拆分/合并/复制/删除可靠性 | 14-05 Task 3 | COVERED | Uses production `ChapterNotifier` operations rather than handwritten test logic.
REQ | JOURNEY-06 | 编辑器浮窗和反AI味效果验证 | 14-05 Task 2 | COVERED | Removes all verifier-listed obvious AI-scent phrases.
RESEARCH | Code review CR-01 | Journey Hive tests share global state and cleanup | 14-05 Task 1 | COVERED | Per-test cleanup owns its temp directory; no global delete during concurrent tests.
RESEARCH | Code review WR-02 | Anti-AI-scent test encodes limitation as pass | 14-05 Task 2 | COVERED | Test asserts phrases are absent.
RESEARCH | Code review WR-03 | Chapter tests duplicate business logic | 14-05 Task 3 | COVERED | Tests call production notifier methods.
CONTEXT | D-01 | Automated-first validation | 14-05 Tasks 1-3 | COVERED | All tasks are automated tests/fixes.
CONTEXT | D-05 | Validate chapter operations and editor anti-AI-scent | 14-05 Tasks 2-3 | COVERED | Automated evidence supports final manual review.
CONTEXT | D-06 | Structured issue log | 14-05 Tasks 1-3 | COVERED | Update existing issue rows or add P14-05 closure rows.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Isolate journey Hive cleanup to the owning test container</name>
  <files>test/journey/helpers/journey_container.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` code-review section about CR-01.
    - `.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md` CR-01 exact finding.
    - `test/journey/helpers/journey_container.dart` current `createJourneyContainer` and `cleanupJourneyContainer` implementation.
    - `test/journey/world_building_test.dart`, `test/journey/opening_guide_test.dart`, `test/journey/fragment_synthesis_test.dart`, `test/journey/serial_generation_test.dart`, `test/journey/full_journey_test.dart`, `test/journey/automated_ui_evidence_test.dart`, `test/journey/chapter_management_test.dart` current setUp/tearDown call sites.
  </read_first>
  <behavior>
    - Test 1: Each journey test container owns a unique `Directory.systemTemp.createTempSync('journey_test_')` directory that is retained until cleanup.
    - Test 2: `cleanupJourneyContainer` closes Hive boxes and deletes only the temp directory created for that container; it does not call global `Hive.deleteFromDisk()`.
    - Test 3: Existing test call sites can still call `cleanupJourneyContainer(container)` or are updated to an explicit owner object without leaving compile errors.
  </behavior>
  <action>
    Introduce a concrete ownership wrapper in `test/journey/helpers/journey_container.dart` named `JourneyTestContainer` with fields `ProviderContainer container` and `Directory tempDir`. Keep backwards compatibility if practical by exposing `ProviderContainer` access through `.container`, or update all journey setUp/tearDown call sites touched by this task to store the wrapper. Replace global `Hive.deleteFromDisk()` with cleanup that disposes the provider container, waits 100ms, calls `Hive.close()`, then deletes `tempDir` using `delete(recursive: true)` only if it exists. Do not delete arbitrary Hive global state. Update `14-ISSUE-LOG.md` with a safe note that CR-01/P14-05-HIVE isolation was fixed; do not paste full command logs.
  </action>
  <verify>
    <automated>dart analyze test/journey/helpers/journey_container.dart test/journey/world_building_test.dart test/journey/opening_guide_test.dart test/journey/fragment_synthesis_test.dart test/journey/serial_generation_test.dart test/journey/full_journey_test.dart test/journey/automated_ui_evidence_test.dart test/journey/chapter_management_test.dart</automated>
    <automated>! grep -v '^[[:space:]]*//' test/journey/helpers/journey_container.dart | grep -q "Hive.deleteFromDisk"</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/helpers/journey_container.dart | grep -q "class JourneyTestContainer" && grep -v '^[[:space:]]*//' test/journey/helpers/journey_container.dart | grep -q "Hive.close" && grep -v '^[[:space:]]*//' test/journey/helpers/journey_container.dart | grep -q "delete(recursive: true)"</automated>
  </verify>
  <acceptance_criteria>
    - `journey_container.dart` contains `class JourneyTestContainer` with a `Directory tempDir` field.
    - `cleanupJourneyContainer` or its replacement deletes only the container-owned `tempDir`, not all Hive data on disk.
    - No uncommented `Hive.deleteFromDisk()` remains in `test/journey/helpers/journey_container.dart`.
    - All journey tests that import `journey_container.dart` pass Dart analysis after the helper signature change.
  </acceptance_criteria>
  <done>Journey test cleanup is isolated and cannot delete another concurrently running journey test's Hive directory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert anti-AI-scent limitation into a real removal gate</name>
  <files>lib/features/ai/application/anti_ai_scent_processor.dart, test/journey/automated_ui_evidence_test.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` GAP-02.
    - `.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md` WR-02.
    - `.planning/phases/14-world-building-first-30-chapters/14-CONTEXT.md` D-06 and core value notes.
    - `lib/features/ai/application/anti_ai_scent_processor.dart` current `_synonymMap` and boundary-aware replacement behavior.
    - `test/journey/automated_ui_evidence_test.dart` current anti-AI-scent test.
  </read_first>
  <behavior>
    - Test 1: Processing `值得注意的是，林风没有退。总而言之，需要指出的是，他选择入山。` leaves none of `值得注意的是`, `总而言之`, or `需要指出的是` in `processedText`.
    - Test 2: Highlights include banned-word entries for all removed phrases so the UI can still report that removals happened.
    - Test 3: Existing synonym behavior for `然而` remains `但是`.
  </behavior>
  <action>
    Update `test/journey/automated_ui_evidence_test.dart` first so the anti-AI-scent test asserts `isNot(contains('值得注意的是'))`, `isNot(contains('总而言之'))`, and `isNot(contains('需要指出的是'))`. Then update `AntiAIScentProcessor._synonymMap` in `lib/features/ai/application/anti_ai_scent_processor.dart` to include exact keys `总而言之` and `需要指出的是` with empty-string replacements while preserving the existing key `值得注意的是` and the existing `然而` to `但是` replacement. Replace the current debug text that says uncovered phrases remain documented with text indicating all verifier-listed phrases were removed. Update `14-ISSUE-LOG.md` row `P14-04-AI-01` or add `P14-05-AI-01` to record closure with command evidence only.
  </action>
  <verify>
    <automated>dart analyze lib/features/ai/application/anti_ai_scent_processor.dart test/journey/automated_ui_evidence_test.dart</automated>
    <automated>flutter test test/journey/automated_ui_evidence_test.dart --plain-name "should remove obvious AI-scent phrases from editor output" --timeout 180s</automated>
    <automated>grep -v '^[[:space:]]*//' lib/features/ai/application/anti_ai_scent_processor.dart | grep -q "'总而言之': ''" && grep -v '^[[:space:]]*//' lib/features/ai/application/anti_ai_scent_processor.dart | grep -q "'需要指出的是': ''"</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/automated_ui_evidence_test.dart | grep -q "isNot(contains('总而言之'))" && grep -v '^[[:space:]]*//' test/journey/automated_ui_evidence_test.dart | grep -q "isNot(contains('需要指出的是'))"</automated>
  </verify>
  <acceptance_criteria>
    - `AntiAIScentProcessor._synonymMap` contains exact entries for `总而言之` and `需要指出的是` with empty-string replacements.
    - The automated evidence test fails if any of `值得注意的是`, `总而言之`, or `需要指出的是` remains in processed text.
    - The test no longer uses `anyOf(contains('总而言之'), contains('需要指出的是'))` as a passing condition.
    - `14-ISSUE-LOG.md` no longer describes `总而言之` or `需要指出的是` as accepted remaining limitations after this task.
  </acceptance_criteria>
  <done>JOURNEY-06 anti-AI-scent blocker is closed for the verifier-listed obvious phrases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strengthen chapter operation tests through production ChapterNotifier APIs</name>
  <files>test/journey/chapter_management_test.dart, .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md</files>
  <read_first>
    - `.planning/phases/14-world-building-first-30-chapters/14-REVIEW.md` WR-01 and WR-03.
    - `.planning/phases/14-world-building-first-30-chapters/14-VERIFICATION.md` JOURNEY-04 warning notes.
    - `test/journey/chapter_management_test.dart` current handwritten split/merge/copy test groups.
    - `test/journey/automated_ui_evidence_test.dart` current production notifier example for chapter operations.
    - `lib/features/manuscript/application/chapter_notifier.dart` method signatures and sorting behavior.
  </read_first>
  <behavior>
    - Test 1: Reorder validation calls `chapterNotifierProvider.notifier.reorder(manuscript.id, oldIndex, newIndex)` and verifies final sequential sortOrder.
    - Test 2: Split validation calls `splitChapter(chapterId, '第一段内容', '第二段内容')` and verifies two persisted chapters with those exact contents.
    - Test 3: Merge validation calls `mergeChapters(chapterId1, chapterId2)` and verifies one persisted chapter contains `第一章正文内容\n\n第二章正文内容`.
    - Test 4: Copy validation calls `duplicateChapter(chapterId)` after re-reading updated repository state and verifies exact copied content `第三章正文内容` and title suffix `(副本)` or `（副本）` as implemented by production code.
    - Test 5: Delete validation calls `chapterNotifierProvider.notifier.delete(chapterId)` and verifies repository `getById` returns null.
  </behavior>
  <action>
    Rewrite the `Reorder`, `Split`, `Merge`, `Copy`, and `Delete` groups in `test/journey/chapter_management_test.dart` to use the production `ChapterNotifier` methods named in the behavior block. Keep the `Create 30 Chapters` and `Content Update` repository tests if they still prove basic persistence. For copy, after `updateDocumentContent(ch3.id, '第三章正文内容')`, re-read the updated chapter from `chapterRepo.getById(ch3.id)!`, call `duplicateChapter(updatedCh3.id)`, and assert the duplicate persisted content equals `第三章正文内容`. Remove local test-only creation/deletion logic that simulates split/merge/copy instead of invoking production code. Update `14-ISSUE-LOG.md` with a safe closure note for WR-01/WR-03 and do not claim platform UI proof.
  </action>
  <verify>
    <automated>dart analyze test/journey/chapter_management_test.dart</automated>
    <automated>flutter test test/journey/chapter_management_test.dart --timeout 240s</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/chapter_management_test.dart | grep -q "chapterNotifierProvider.notifier" && grep -v '^[[:space:]]*//' test/journey/chapter_management_test.dart | grep -q "splitChapter" && grep -v '^[[:space:]]*//' test/journey/chapter_management_test.dart | grep -q "mergeChapters" && grep -v '^[[:space:]]*//' test/journey/chapter_management_test.dart | grep -q "duplicateChapter"</automated>
    <automated>grep -v '^[[:space:]]*//' test/journey/chapter_management_test.dart | grep -q "第三章正文内容"</automated>
  </verify>
  <acceptance_criteria>
    - `chapter_management_test.dart` production operation groups use `chapterNotifierProvider.notifier` rather than local split/merge/copy implementations.
    - The copy test re-reads the updated chapter before duplicating and asserts the duplicate content equals `第三章正文内容`.
    - The test suite proves reorder, split, merge, duplicate, delete, and final sort order through production methods.
    - No user-facing product code is changed for this task unless a production method fails its intended behavior.
  </acceptance_criteria>
  <done>JOURNEY-04 chapter operation evidence is production-path evidence, not duplicated test logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> Hive temp directories | Journey tests create and delete local data under system temp. |
| Generated/processed text -> issue log | Evidence about anti-AI-scent processing is persisted in planning artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | Hive test cleanup | mitigate | Delete only the container-owned temp directory after `Hive.close()`, never global `Hive.deleteFromDisk()` in journey helper. |
| T-14-05-02 | Repudiation | Anti-AI-scent evidence | mitigate | Replace limitation-as-pass with exact absence assertions for `值得注意的是`, `总而言之`, `需要指出的是`. |
| T-14-05-03 | Tampering | Chapter operation tests | mitigate | Call production `ChapterNotifier` APIs rather than duplicating operation logic in test code. |
| T-14-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; if an executor attempts an install, stop and return for planning review. |
</threat_model>

<verification>
Overall checks:
- `dart analyze test/journey/helpers/journey_container.dart lib/features/ai/application/anti_ai_scent_processor.dart test/journey/automated_ui_evidence_test.dart test/journey/chapter_management_test.dart`
- `flutter test test/journey/automated_ui_evidence_test.dart test/journey/chapter_management_test.dart --timeout 300s`
- Source gates from each task pass without using unfiltered bare `grep -c` checks.
</verification>

<success_criteria>
- CR-01 Hive cleanup risk is resolved in code and issue-log evidence.
- GAP-02 anti-AI-scent blocker is resolved for all verifier-listed phrases.
- JOURNEY-04 chapter operation evidence uses production paths.
- `14-ISSUE-LOG.md` records closure notes without secrets or private manuscript dumps.
</success_criteria>

<output>
Create `.planning/phases/14-world-building-first-30-chapters/14-05-SUMMARY.md` when done.
</output>
