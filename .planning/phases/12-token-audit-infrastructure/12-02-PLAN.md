---
phase: 12-token-audit-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/features/ai/presentation/synthesis_notifier.dart
  - lib/features/editor/application/editor_ai_notifier.dart
  - lib/features/onboarding/application/opening_generator_service.dart
  - lib/features/knowledge/application/skill_generation_service.dart
  - lib/features/knowledge/application/deviation_detection_service.dart
  - lib/features/templates/application/template_completion_service.dart
  - lib/features/stats/presentation/writing_stats_page.dart
  - lib/shared/constants/app_constants.dart
  - lib/app.dart
  - test/features/stats/presentation/writing_stats_page_test.dart
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-03
user_setup: []

must_haves:
  truths:
    - "All 6 AI call sites pass audit context (operationType, manuscriptId, chapterId?) to the adapter and record token usage after stream completion per D-01, D-05"
    - "WritingStatsPage bottom section shows 3 summary cards: total input tokens, total output tokens, API call count, using existing StatsSummaryCard widget per D-02"
    - "WritingStatsPage AppBar has a 'Token 消耗' navigation button linking to the TokenAuditPage per D-02"
    - "Route '/stats/tokens' registered in app.dart and constant added to AppConstants"
  artifacts:
    - path: "lib/features/stats/presentation/writing_stats_page.dart"
      provides: "WritingStatsPage with token summary section and navigation"
      contains: "TokenSummarySection|tokenAuditNotifierProvider|statsTokens"
      min_lines: 190
    - path: "lib/shared/constants/app_constants.dart"
      provides: "Route constant for token audit page"
      contains: "statsTokens|stats/tokens"
  key_links:
    - from: "synthesis_notifier.dart"
      to: "token_audit_service.recordAudit"
      via: "onUsage callback from adapter.createStream"
      pattern: "onUsage.*recordAudit|recordAudit"
    - from: "editor_ai_notifier.dart"
      to: "token_audit_service.recordAudit"
      via: "onUsage callback from adapter.createStream"
      pattern: "onUsage.*recordAudit|recordAudit"
    - from: "writing_stats_page.dart"
      to: "token_audit_notifier_provider"
      via: "ref.watch(tokenAuditNotifierProvider)"
      pattern: "ref.watch.*tokenAuditNotifierProvider"
---

<objective>
Wire all 6 AI call sites to capture and record token usage after every stream completion. Embed a token consumption summary section in the existing WritingStatsPage with navigation to the detailed TokenAuditPage.

Purpose: This makes AUDIT-01 real -- every AI call now produces an audit record. AUDIT-03 gets its summary surface.
Output: All AI operations produce audit records; WritingStatsPage shows token summary cards.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-token-audit-infrastructure/12-CONTEXT.md
@.planning/phases/12-token-audit-infrastructure/12-RESEARCH.md
@.planning/phases/12-token-audit-infrastructure/12-PATTERNS.md
@.planning/phases/12-token-audit-infrastructure/12-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire 6 AI call sites to record token audit data</name>
  <files>lib/features/ai/presentation/synthesis_notifier.dart, lib/features/editor/application/editor_ai_notifier.dart, lib/features/onboarding/application/opening_generator_service.dart, lib/features/knowledge/application/skill_generation_service.dart, lib/features/knowledge/application/deviation_detection_service.dart, lib/features/templates/application/template_completion_service.dart</files>
  <read_first>
    - lib/features/ai/presentation/synthesis_notifier.dart (full file -- stream consumption at lines 210-241, needs onUsage + recordAudit after stream)
    - lib/features/editor/application/editor_ai_notifier.dart (full file -- stream at lines 136-157, needs onUsage + recordAudit after stream)
    - lib/features/onboarding/application/opening_generator_service.dart (createStream call at line 84)
    - lib/features/knowledge/application/skill_generation_service.dart (createStream call at line 29)
    - lib/features/knowledge/application/deviation_detection_service.dart (createStream call at line 72)
    - lib/features/templates/application/template_completion_service.dart (createStream call at line 44)
    - lib/features/stats/domain/audit_operation_type.dart (enum values for operationType)
    - lib/features/stats/application/token_audit_service.dart (recordAudit method signature)
    - lib/core/presentation/providers.dart (tokenAuditServiceProvider to read the service)
  </read_first>
  <behavior>
    - Test: SynthesisNotifier calls recordAudit with operationType=AuditOperationType.synthesis after stream completes successfully
    - Test: SynthesisNotifier does not call recordAudit when stream throws AIException
    - Test: EditorAINotifier calls recordAudit with correct operationType (rewrite/polish/freeInput) mapping from EditorAIOperation after stream completes
    - Test: EditorAINotifier does not call recordAudit when stream is cancelled
  </behavior>
  <action>
    Modify 6 files to pass `onUsage` callback to `adapter.createStream()` and record audit data on stream completion. Each call site follows this pattern:

    **Pattern for each call site:**
    1. Before `adapter.createStream()`, resolve the service: `final auditService = await ref.read(tokenAuditServiceProvider.future);` (for notifiers) or accept as constructor param (for services).
    2. Capture input text before streaming (the messages list joined as text, or the prompt content).
    3. Add `onUsage:` parameter to `createStream()` call.
    4. In the `onUsage` callback, call `auditService.recordAudit(usage: usage, modelName: provider.model, operationType: ..., manuscriptId: ..., chapterId: ..., inputText: capturedInput, outputText: state.accumulatedText)`.

    **Specific mappings per call site:**

    1. **synthesis_notifier.dart** (line ~212): operationType = `AuditOperationType.synthesis`. manuscriptId comes from `ref.read(activeManuscriptProvider)?.id` (or null). chapterId = null. inputText = synthesized prompt messages content. outputText = `state.accumulatedText` after stream completes (access via closure).

    2. **editor_ai_notifier.dart** (line ~136): operationType maps from `EditorAIOperation`: `EditorAIOperation.tone` -> `AuditOperationType.rewrite`, `EditorAIOperation.polish` -> `AuditOperationType.polish`, `EditorAIOperation.freeInput` -> `AuditOperationType.freeInput`. manuscriptId and chapterId must be passed to `startOperation()` and `_fetchKeyAndStream()` as new parameters -- the callers (EditorWithSidebar) already have `widget.manuscriptId` and `_currentChapterId`. inputText = selectedText. outputText = `state.progressText` after stream completes.

    3. **opening_generator_service.dart** (line ~84): operationType = `AuditOperationType.opening`. This service takes `OpenAIAdapter?` as constructor param. Add `TokenAuditService?` as optional constructor param (null in tests). manuscriptId = passed via new method param or constructor. chapterId = null.

    4. **skill_generation_service.dart** (line ~29): operationType = `AuditOperationType.skillGen`. Same pattern -- add `TokenAuditService?` as optional constructor param.

    5. **deviation_detection_service.dart** (line ~72): operationType = `AuditOperationType.deviationDetect`. Same pattern.

    6. **template_completion_service.dart** (line ~44): operationType = `AuditOperationType.templateComplete`. Same pattern.

    **Important:** For services (3-6), the `onUsage` callback should only call `recordAudit` if the `TokenAuditService` is non-null. This allows tests to continue working without mock audit service. The callers of these services (notifiers/providers) are responsible for passing the audit service when available.

    For `EditorAINotifier`, update the `startOperation` method signature to accept `manuscriptId` and `chapterId`:
    ```dart
    void startOperation(
      EditorAIOperation operation,
      String selectedText,
      String selectionNodeId,
      int selectionStartOffset,
      int selectionEndOffset, {
      String? userInstruction,
      String? manuscriptId,
      String? chapterId,
    })
    ```
    Thread these through to `_fetchKeyAndStream`. Update all callers of `startOperation` in the codebase to pass `manuscriptId` and `chapterId` (search for `startOperation(` calls).
  </action>
  <verify>
    <automated>flutter test test/features/stats/application/token_audit_service_test.dart</automated>
  </verify>
  <done>
    - All 6 AI call sites pass onUsage callback to createStream
    - recordAudit called after successful stream completion with correct operationType
    - recordAudit NOT called on stream error or cancel
    - EditorAINotifier.startOperation accepts manuscriptId and chapterId
    - All callers updated to pass new params
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed token summary in WritingStatsPage and register route</name>
  <files>lib/features/stats/presentation/writing_stats_page.dart, lib/shared/constants/app_constants.dart, lib/app.dart</files>
  <read_first>
    - lib/features/stats/presentation/writing_stats_page.dart (full file -- _StatsContent ListView children, _SummaryWrap pattern, AppBar actions)
    - lib/features/stats/presentation/stats_summary_card.dart (reuse directly for token cards)
    - lib/shared/constants/app_constants.dart (route constants, add statsTokens)
    - lib/app.dart lines 175-186 (stats branch route structure)
    - lib/features/stats/application/token_audit_notifier.dart (tokenAuditNotifierProvider, TokenAuditSnapshot with totalInputTokens, totalOutputTokens, totalCalls)
  </read_first>
  <behavior>
    - Test: WritingStatsPage renders without error when token audit data is empty (0 tokens, 0 calls)
    - Test: WritingStatsPage displays totalInputTokens, totalOutputTokens, and totalCalls from tokenAuditNotifierProvider
    - Test: WritingStatsPage AppBar has a TextButton.icon that navigates to AppConstants.statsTokens on tap
  </behavior>
  <action>
    Modify 3 files:

    1. **writing_stats_page.dart** -- Add two changes:

       a. In `_StatsContent.build()`, after `AchievementBadgeSection` (line ~100), add a new section:
       ```dart
       const SizedBox(height: 24),
       const Divider(),
       const SizedBox(height: 16),
       _TokenSummarySection(),
       ```
       The `_TokenSummarySection` is a new private `ConsumerWidget` that watches `tokenAuditNotifierProvider` and renders 3 `StatsSummaryCard` widgets in the existing `_SummaryWrap` responsive layout pattern (LayoutBuilder + Wrap, 2-column on wide screens). Cards:
       - Icon: `Icons.arrow_downward_outlined`, title: '输入 Token', value: formatted number (e.g., "12,450")
       - Icon: `Icons.arrow_upward_outlined`, title: '输出 Token', value: formatted number
       - Icon: `Icons.swap_calls_outlined`, title: 'API 调用次数', value: formatted number
       The entire section is wrapped in `InkWell` that navigates to `AppConstants.statsTokens` on tap.
       When data is loading or error, show empty/default (0 values). When tokenSnapshot.totalCalls is 0, show a placeholder card "暂无 Token 记录" instead of the 3 cards.

       b. In `WritingStatsPage.build()` AppBar actions, add a new `TextButton.icon` after the existing "当前作品" button:
       ```dart
       TextButton.icon(
         onPressed: () => context.go(AppConstants.statsTokens),
         icon: const Icon(Icons.token_outlined),
         label: const Text('Token 消耗'),
       ),
       ```

    2. **app_constants.dart** -- Add route constant:
       `static const String statsTokens = '/stats/tokens';`

    3. **app.dart** -- Add sub-route under the existing `/stats` route (after the 'project' sub-route at line ~183):
       ```dart
       GoRoute(
         path: 'tokens',
         builder: (context, state) => const TokenAuditPage(),
       ),
       ```
       Import the TokenAuditPage (will be created in Plan 03 -- use a placeholder `const SizedBox()` if needed for compilation, or rely on Plan 03 running in parallel and completing before execution). Since Plan 03 creates this file, add the import but the class will exist by execution time.
  </action>
  <verify>
    <automated>flutter test test/features/stats/presentation/writing_stats_page_test.dart</automated>
  </verify>
  <done>
    - WritingStatsPage shows token summary section with 3 metric cards
    - WritingStatsPage AppBar has "Token 消耗" navigation button
    - Route constant statsTokens added to AppConstants
    - Route registered in app.dart under /stats branch
    - Empty state handled when no token records exist
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI call site -> audit service | Token counts from API responses flow into audit records |
| UI display -> user | Summary cards show aggregate token counts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | WritingStatsPage token summary | accept | Token counts are not sensitive data. Local-only display. No network transmission. |
| T-12-06 | Tampering | Audit record input via onUsage callback | accept | API response values are used as-is. Worst case: inaccurate statistics. No security impact. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase. |
</threat_model>

<verification>
1. `flutter test test/features/stats/presentation/writing_stats_page_test.dart` -- summary display tests pass
2. `flutter test` -- full suite green (no regressions from call site wiring)
3. `flutter analyze` -- zero errors
4. Verify all 6 call sites have onUsage: `grep -rn 'onUsage' lib/features/ --include="*.dart" | grep -v test | grep -v token_audit_service | wc -l` returns >= 6
5. Verify route registered: `grep -c 'statsTokens' lib/shared/constants/app_constants.dart` returns >= 1
6. Verify route in app.dart: `grep -c 'tokens' lib/app.dart` returns >= 1
</verification>

<success_criteria>
- All 6 AI call sites record token usage after successful stream completion
- WritingStatsPage displays token consumption summary (input tokens, output tokens, call count)
- Navigation from WritingStatsPage to TokenAuditPage works
- No regressions in existing tests
- `flutter analyze` zero errors
</success_criteria>

<output>
Create `.planning/phases/12-token-audit-infrastructure/12-02-SUMMARY.md` when done
</output>
