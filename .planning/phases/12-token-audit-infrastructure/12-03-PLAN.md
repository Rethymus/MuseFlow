---
phase: 12-token-audit-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - lib/features/stats/presentation/token_audit_page.dart
  - lib/features/stats/presentation/charts/operation_type_pie_chart.dart
  - lib/features/stats/presentation/charts/chapter_token_bar_chart.dart
  - lib/features/stats/presentation/charts/token_trend_line_chart.dart
  - test/features/stats/presentation/token_audit_page_test.dart
  - test/features/stats/presentation/charts/operation_type_pie_chart_test.dart
  - test/features/stats/presentation/charts/chapter_token_bar_chart_test.dart
  - test/features/stats/presentation/charts/token_trend_line_chart_test.dart
autonomous: true
requirements:
  - AUDIT-03
user_setup: []

must_haves:
  truths:
    - "TokenAuditPage displays page title 'Token 消耗总览' with subtitle"
    - "TokenAuditPage shows 4 summary cards in responsive 2-column layout: input tokens, output tokens, API call count, and a placeholder for future cost (showing token count instead per D-03)"
    - "TokenAuditPage shows per-chapter bar chart with chapter numbers on X-axis and token totals on Y-axis"
    - "TokenAuditPage shows per-operation-type pie chart with 4 group sections (organize, edit, worldview, template) per D-04"
    - "TokenAuditPage shows token consumption trend line chart with cumulative usage over time"
    - "TokenAuditPage handles empty state: Card with message '开始使用 AI 功能后，这里会出现消耗统计。'"
    - "TokenAuditPage handles loading state: CircularProgressIndicator"
    - "TokenAuditPage handles error state: error message + retry button"
  artifacts:
    - path: "lib/features/stats/presentation/token_audit_page.dart"
      provides: "Full audit detail page with summary + 3 charts"
      contains: "class TokenAuditPage|ConsumerWidget|TokenAuditSnapshot"
      min_lines: 150
    - path: "lib/features/stats/presentation/charts/operation_type_pie_chart.dart"
      provides: "Pie chart for operation type distribution"
      contains: "class OperationTypePieChart|PieChart"
      min_lines: 60
    - path: "lib/features/stats/presentation/charts/chapter_token_bar_chart.dart"
      provides: "Bar chart for per-chapter token distribution"
      contains: "class ChapterTokenBarChart|BarChart"
      min_lines: 60
    - path: "lib/features/stats/presentation/charts/token_trend_line_chart.dart"
      provides: "Line chart for token consumption trend"
      contains: "class TokenTrendLineChart|LineChart"
      min_lines: 60
  key_links:
    - from: "token_audit_page.dart"
      to: "tokenAuditNotifierProvider"
      via: "ref.watch(tokenAuditNotifierProvider)"
      pattern: "ref.watch.*tokenAuditNotifierProvider"
    - from: "token_audit_page.dart"
      to: "operation_type_pie_chart.dart"
      via: "OperationTypePieChart widget instantiation"
      pattern: "OperationTypePieChart"
    - from: "token_audit_page.dart"
      to: "chapter_token_bar_chart.dart"
      via: "ChapterTokenBarChart widget instantiation"
      pattern: "ChapterTokenBarChart"
    - from: "token_audit_page.dart"
      to: "token_trend_line_chart.dart"
      via: "TokenTrendLineChart widget instantiation"
      pattern: "TokenTrendLineChart"
---

<objective>
Create the TokenAuditPage with three fl_chart visualizations: per-chapter bar chart, per-operation-type pie chart, and cumulative token trend line chart. This is the detailed view that users navigate to from the WritingStatsPage summary.

Purpose: Fulfills AUDIT-03 requirement for detailed token consumption breakdowns by chapter and operation type.
Output: TokenAuditPage with summary cards and 3 charts, fully tested.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-token-audit-infrastructure/12-CONTEXT.md
@.planning/phases/12-token-audit-infrastructure/12-RESEARCH.md
@.planning/phases/12-token-audit-infrastructure/12-PATTERNS.md
@.planning/phases/12-token-audit-infrastructure/12-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 3 chart widgets (pie, bar, line)</name>
  <files>lib/features/stats/presentation/charts/operation_type_pie_chart.dart, lib/features/stats/presentation/charts/chapter_token_bar_chart.dart, lib/features/stats/presentation/charts/token_trend_line_chart.dart</files>
  <read_first>
    - lib/features/stats/presentation/charts/ai_usage_pie_chart.dart (pie chart pattern: PieChart, PieChartData, PieChartSectionData, empty state, colorScheme)
    - lib/features/stats/presentation/charts/daily_words_bar_chart.dart (bar chart pattern: BarChart, BarChartData, BarChartGroupData, BarChartRodData, empty state, maxY calculation)
    - lib/features/stats/presentation/charts/speed_trend_line_chart.dart (line chart pattern: LineChart, LineChartData, FlSpot, empty state)
    - lib/features/stats/domain/audit_operation_type.dart (enum with group field for pie chart grouping)
    - lib/features/stats/domain/token_audit_record.dart (entity fields for chart data mapping)
  </read_first>
  <behavior>
    - Test: OperationTypePieChart renders empty state text when no records provided
    - Test: OperationTypePieChart groups records by AuditOperationType.group into 4 pie sections
    - Test: ChapterTokenBarChart renders empty state text when no records provided
    - Test: ChapterTokenBarChart aggregates totalTokens per chapterId and renders one bar per chapter
    - Test: ChapterTokenBarChart uses chapter number as X-axis label (extracted from chapterId or sequential index)
    - Test: TokenTrendLineChart renders empty state text when no records provided
    - Test: TokenTrendLineChart plots cumulative totalTokens over timestamp as a line
  </behavior>
  <action>
    Create 3 chart widget files, each following the exact pattern of its analog:

    1. **operation_type_pie_chart.dart** -- `OperationTypePieChart extends StatelessWidget`:
    - Constructor: `const OperationTypePieChart({super.key, required this.records})`.
    - Field: `final List<TokenAuditRecord> records`.
    - Empty state: if `records.isEmpty`, return `SizedBox(height: 220, child: Center(child: Text('还没有 Token 使用记录')))`.
    - Aggregate: group records by `record.operationType.group` (4 groups: organize, edit, worldview, template). Sum `totalTokens` per group.
    - Build `PieChart(PieChartData(sectionsSpace: 2, centerSpaceRadius: 44, sections: [...]))` with one `PieChartSectionData` per group. Each section has: `value: groupTotal.toDouble()`, `title: groupLabel`, `radius: 58`, `color` from colorScheme (primary, tertiary, secondary, primaryContainer for the 4 groups).
    - Group display labels: `organize` -> '整理类', `edit` -> '编辑类', `worldview` -> '世界观类', `template` -> '模板类'.

    2. **chapter_token_bar_chart.dart** -- `ChapterTokenBarChart extends StatelessWidget`:
    - Constructor: `const ChapterTokenBarChart({super.key, required this.records})`.
    - Field: `final List<TokenAuditRecord> records`.
    - Empty state: if no records with non-null chapterId, return empty state text.
    - Aggregate: filter records where `chapterId != null`, group by chapterId, sum `totalTokens` per chapter. Sort by chapterId.
    - If > 15 chapters, wrap in `SingleChildScrollView(scrollDirection: Axis.horizontal, child: SizedBox(width: chapters.length * 40.0, height: 220, child: BarChart(...)))`.
    - Build `BarChart(BarChartData(maxY: maxY * 1.2, gridData: const FlGridData(show: false), borderData: const FlBorderData(show: false), titlesData: FlTitlesData(bottomTitles: AxisTitles(sideTitles: SideTitles(showTitles: true, getTitlesWidget: (value, meta) => Text('Ch${value + 1}')), ...), topTitles/show: false, rightTitles/show: false), barGroups: [...]))` per DailyWordsBarChart pattern.
    - Bar width: 16, borderRadius: 4, color: `colorScheme.primary`.

    3. **token_trend_line_chart.dart** -- `TokenTrendLineChart extends StatelessWidget`:
    - Constructor: `const TokenTrendLineChart({super.key, required this.records})`.
    - Field: `final List<TokenAuditRecord> records`.
    - Empty state: if records empty, return empty state text.
    - Sort records by timestamp ascending. Compute cumulative sum of totalTokens. Map to `FlSpot(x: index.toDouble(), y: cumulativeTotal.toDouble())`.
    - Build `LineChart(LineChartData(maxY: maxY * 1.2, gridData: const FlGridData(show: false), borderData: const FlBorderData(show: false), titlesData: FlTitlesData(show: false on top/right/left, bottom: SideTitles with getTitlesWidget showing date), lineBarsData: [LineChartBarData(spots: spots, isCurved: true, color: colorScheme.primary, barWidth: 3, dotData: const FlDotData(show: false))]))` per SpeedTrendLineChart pattern.
    - Height: 220. Bottom titles show abbreviated date (MM/dd).

    **All 3 charts:** Use `Theme.of(context).colorScheme` for colors. Wrap in `SizedBox(height: 220)` as the container. Handle empty state with centered text in the same height container.
  </action>
  <verify>
    <automated>flutter test test/features/stats/presentation/charts/operation_type_pie_chart_test.dart test/features/stats/presentation/charts/chapter_token_bar_chart_test.dart test/features/stats/presentation/charts/token_trend_line_chart_test.dart</automated>
  </verify>
  <done>
    - OperationTypePieChart renders 4-group pie chart with empty state
    - ChapterTokenBarChart renders per-chapter bars with empty state, scrollable for many chapters
    - TokenTrendLineChart renders cumulative trend line with empty state
    - All chart tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TokenAuditPage with summary cards, charts, and state handling</name>
  <files>lib/features/stats/presentation/token_audit_page.dart, test/features/stats/presentation/token_audit_page_test.dart</files>
  <read_first>
    - lib/features/stats/presentation/writing_stats_page.dart (page pattern: ConsumerWidget, Scaffold, AppBar, statsAsync.when, _StatsContent ListView, _SummaryWrap, _ChartSection)
    - lib/features/stats/presentation/stats_summary_card.dart (reuse for 4 summary cards)
    - lib/features/stats/domain/token_audit_record.dart (TokenAuditRecord fields for passing to charts)
    - lib/features/stats/application/token_audit_notifier.dart (TokenAuditSnapshot with totalInputTokens, totalOutputTokens, totalCalls, records)
    - lib/core/presentation/providers.dart (tokenAuditNotifierProvider)
    - lib/features/stats/presentation/charts/operation_type_pie_chart.dart (import from Task 1)
    - lib/features/stats/presentation/charts/chapter_token_bar_chart.dart (import from Task 1)
    - lib/features/stats/presentation/charts/token_trend_line_chart.dart (import from Task 1)
  </read_first>
  <behavior>
    - Test: TokenAuditPage renders loading state (CircularProgressIndicator) when snapshot is AsyncLoading
    - Test: TokenAuditPage renders error state with message and retry button when snapshot is AsyncError
    - Test: TokenAuditPage renders empty state Card when totalCalls is 0
    - Test: TokenAuditPage renders 4 summary cards when data exists
    - Test: TokenAuditPage renders 3 chart sections (chapter bar, operation pie, trend line) when data exists
    - Test: TokenAuditPage AppBar title is 'Token 消耗总览'
  </behavior>
  <action>
    Create the full TokenAuditPage following WritingStatsPage pattern exactly:

    **File: `lib/features/stats/presentation/token_audit_page.dart`**

    `TokenAuditPage extends ConsumerWidget`:
    - `ref.watch(tokenAuditNotifierProvider)` for async state.
    - Scaffold with AppBar(title: const Text('Token 消耗总览')).
    - Body: `statsAsync.when(loading: CircularProgressIndicator, error: error message + retry button calling `refresh()`, data: _TokenAuditContent)`.

    **Private `_TokenAuditContent extends StatelessWidget`**:
    - Takes `TokenAuditSnapshot snapshot`.
    - ListView with padding 24, containing:
      1. Page title: Text('Token 消耗总览', style: headlineMedium)
      2. SizedBox(8)
      3. Subtitle: Text('追踪每次 AI 调用的 Token 用量与成本分布。', style: bodyMedium)
      4. SizedBox(24)
      5. Empty state check: if `snapshot.totalCalls == 0`, show Card with padding 20, child: Text('开始使用 AI 功能后，这里会出现消耗统计。')
      6. `_SummaryCards(snapshot: snapshot)` -- private widget with responsive Wrap layout (same as _SummaryWrap pattern from writing_stats_page.dart). 4 StatsSummaryCard widgets:
         - Icons.arrow_downward_outlined, title: '输入 Token', value: formatted number
         - Icons.arrow_upward_outlined, title: '输出 Token', value: formatted number
         - Icons.swap_calls_outlined, title: 'API 调用次数', value: formatted number
         - Icons.confirmation_number_outlined, title: '总 Token', value: formatted total (inputTokens + outputTokens)
      7. SizedBox(24)
      8. _ChartSection(title: '每章 Token 分布', child: ChapterTokenBarChart(records: snapshot.records))
      9. _ChartSection(title: '按操作类型分布', child: OperationTypePieChart(records: snapshot.records))
      10. _ChartSection(title: 'Token 消耗趋势', child: TokenTrendLineChart(records: snapshot.records))

    **Private `_ChartSection extends StatelessWidget`**: Same as WritingStatsPage._ChartSection (Card + title + child). Since it is private, duplicate the pattern here (cannot import private class from another file).

    **Private `_SummaryCards extends StatelessWidget`**: Same LayoutBuilder + Wrap responsive pattern as _SummaryWrap. Uses StatsSummaryCard directly (imported from stats_summary_card.dart). 4 cards in 2-column responsive grid.

    **Number formatting**: Use a helper or inline: `snapshot.totalInputTokens.toString().replaceAllMapped(RegExp(r'\B(?=(\d{3})+(?!\d))'), (m) => ',')` for comma-separated numbers. Or simpler: `NumberFormat.compact().format(value)` from `intl` package -- but since intl may not be in pubspec, use manual comma formatting.

    **Important:** Per D-03, the 4th card shows "总 Token" (total token count), NOT monetary cost. Cost is deferred to Phase 16.
  </action>
  <verify>
    <automated>flutter test test/features/stats/presentation/token_audit_page_test.dart</automated>
  </verify>
  <done>
    - TokenAuditPage renders with loading, error, empty, and data states
    - 4 summary cards displayed in responsive layout
    - 3 chart sections render with correct chart widgets
    - AppBar title is 'Token 消耗总览'
    - Page follows WritingStatsPage visual patterns exactly
    - All page tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive audit data -> UI | Local audit records displayed in charts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | TokenAuditPage charts | accept | All data is local-only, user's own token usage statistics. No sensitive content (no prompt text, no API keys). |
| T-12-08 | Denial of Service | Chart rendering with many records | mitigate | Charts aggregate data before rendering (not rendering raw records). Max 10000 records per D-06. Pie chart renders 4 groups, bar chart renders max chapters (~100). Performance safe. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase. |
</threat_model>

<verification>
1. `flutter test test/features/stats/presentation/charts/operation_type_pie_chart_test.dart` -- pie chart tests pass
2. `flutter test test/features/stats/presentation/charts/chapter_token_bar_chart_test.dart` -- bar chart tests pass
3. `flutter test test/features/stats/presentation/charts/token_trend_line_chart_test.dart` -- line chart tests pass
4. `flutter test test/features/stats/presentation/token_audit_page_test.dart` -- page rendering tests pass
5. `flutter test` -- full suite green
6. `flutter analyze` -- zero errors
7. Verify page class exists: `grep -c 'class TokenAuditPage' lib/features/stats/presentation/token_audit_page.dart` returns >= 1
8. Verify 3 chart classes exist: `grep -c 'class.*Chart.*StatelessWidget' lib/features/stats/presentation/charts/*.dart` returns >= 3
</verification>

<success_criteria>
- TokenAuditPage renders with all 4 states (loading, error, empty, data)
- 4 summary cards show formatted token counts and call count
- Per-chapter bar chart aggregates and displays correctly
- Per-operation-type pie chart shows 4 groups per D-04
- Cumulative trend line chart plots over time
- All chart and page tests pass
- `flutter analyze` zero errors
</success_criteria>

<output>
Create `.planning/phases/12-token-audit-infrastructure/12-03-SUMMARY.md` when done
</output>
