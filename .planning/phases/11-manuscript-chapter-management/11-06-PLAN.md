---
phase: 11-manuscript-chapter-management
plan: 06
type: execute
wave: 6
depends_on: [11-05]
files_modified:
  - lib/features/manuscript/presentation/editor_with_sidebar.dart
  - lib/features/manuscript/application/chapter_auto_save.dart
  - test/features/manuscript/presentation/editor_with_sidebar_test.dart
  - test/features/manuscript/application/chapter_auto_save_test.dart
autonomous: true
gap_closure: true
requirements: [SC-2, SC-3, SC-4]
requirements_addressed: [SC-2, SC-3, SC-4]

must_haves:
  truths:
    - "SC-2/SC-3: Entering EditorWithSidebar for a manuscript with existing chapters loads those chapters from ChapterRepository via ChapterNotifier.loadChapters(manuscriptId)."
    - "SC-2/SC-3: After chapter loading completes, the first loaded chapter is selected and its persisted documentContent is loaded into the editor route."
    - "SC-4: Chapter switch and back-to-library navigation await forced saves before changing chapter or route."
    - "SC-4: App lifecycle pause/inactive triggers a best-effort awaited-save path where Flutter lifecycle permits it, and ChapterAutoSave.dispose no longer relies on unawaited async flush for the must-have guarantee."
  artifacts:
    - path: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      provides: "Editor route chapter-loading data flow and awaited forced-save transitions"
      contains: "loadChapters(widget.manuscriptId)"
    - path: "lib/features/manuscript/application/chapter_auto_save.dart"
      provides: "Debounced and forced chapter content persistence without unawaited dispose flush"
      contains: "Future<void> forceSave()"
    - path: "test/features/manuscript/presentation/editor_with_sidebar_test.dart"
      provides: "Regression coverage for initial chapter loading and first chapter selection"
      contains: "loadChapters"
    - path: "test/features/manuscript/application/chapter_auto_save_test.dart"
      provides: "Regression coverage for forced-save/dispose semantics"
      contains: "forceSave"
  key_links:
    - from: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      to: "lib/features/manuscript/application/chapter_notifier.dart"
      via: "ref.read(chapterNotifierProvider.notifier).loadChapters(widget.manuscriptId) during initialization"
      pattern: "loadChapters\\(widget\\.manuscriptId\\)"
    - from: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      to: "lib/features/manuscript/application/chapter_auto_save.dart"
      via: "await _forceSaveAsync() on chapter switch and navigation"
      pattern: "await _forceSaveAsync\\(\\)"
    - from: "lib/features/manuscript/application/chapter_auto_save.dart"
      to: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      via: "await _repository.updateDocumentContent(chapterId, markdown) in flush path"
      pattern: "await _repository\\.updateDocumentContent"
---

<objective>
Close Phase 11 verification gaps for existing-chapter loading and forced-save guarantees.

Purpose: Phase 11 is not goal-complete until the manuscript editor route loads persisted chapters on entry and auto-save guarantees are enforced on chapter switch, back navigation, and lifecycle transitions per D-19.
Output: Focused fixes and regression tests for `EditorWithSidebar` initialization and `ChapterAutoSave` forced-save/dispose behavior.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/re/code/MuseFlow/.planning/PROJECT.md
@/home/re/code/MuseFlow/.planning/ROADMAP.md
@/home/re/code/MuseFlow/.planning/REQUIREMENTS.md
@/home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@/home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-VERIFICATION.md
@/home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md
@/home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-05-SUMMARY.md
@/home/re/code/MuseFlow/lib/features/manuscript/presentation/editor_with_sidebar.dart
@/home/re/code/MuseFlow/lib/features/manuscript/application/chapter_auto_save.dart
@/home/re/code/MuseFlow/lib/features/manuscript/application/chapter_notifier.dart
@/home/re/code/MuseFlow/lib/features/manuscript/presentation/chapter_sidebar.dart
@/home/re/code/MuseFlow/test/features/manuscript/presentation/editor_with_sidebar_test.dart
@/home/re/code/MuseFlow/test/features/manuscript/application/chapter_auto_save_test.dart

<interfaces>
Key existing contracts the executor must use:

- `ChapterNotifier.build()` intentionally returns an empty list. The editor route must explicitly call `await ref.read(chapterNotifierProvider.notifier).loadChapters(widget.manuscriptId)` before reading loaded chapters.
- `ChapterNotifier.loadChapters(String manuscriptId)` loads `repository.getByManuscriptId(manuscriptId)` and sets `state = AsyncData(chapters)`.
- `ChapterAutoSave.onDocumentChanged(String chapterId, String markdown)` marks dirty content and schedules debounce.
- `ChapterAutoSave.forceSave()` is asynchronous and already cancels the debounce timer before flushing.
- `EditorWithSidebar._switchChapter(String newChapterId)` is already async and can await `_forceSaveAsync()` before loading the next chapter.
- `EditorWithSidebar._navigateBack()` is already async and can await `_forceSaveAsync()` before `context.go(AppConstants.editor)`.
</interfaces>
</context>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Status | Planned In |
|--------|------|--------|------------|
| GOAL | Phase 11 supports chapter entity/navigation and editor chapter-level switching | COVERED | Task 1 |
| GOAL | Chapter content auto-saves with debounced + forced-save guarantees | COVERED | Task 2 |
| REQ | SC-2: User can manage existing chapters within a manuscript | COVERED | Task 1 |
| REQ | SC-3: Editor switches chapter documents when user selects a chapter | COVERED | Task 1 |
| REQ | SC-4: Auto-save forced-save guarantee | COVERED | Task 2 |
| RESEARCH | `ChapterNotifier.build()` can be empty but editor entry must call `loadChapters(manuscriptId)` | COVERED | Task 1 |
| RESEARCH | Auto-save race mitigation: cancel debounce timer, use one awaited flush path, avoid concurrent writes | COVERED | Task 2 |
| CONTEXT | D-01: Each Chapter owns its own persisted SuperEditor document | COVERED | Task 1 |
| CONTEXT | D-13/D-14: Sidebar displays loaded chapters and active chapter highlight | COVERED | Task 1 |
| CONTEXT | D-19: Debounced save plus forced save on switch, navigation, lifecycle pause | COVERED | Task 2 |
| VERIFICATION | Gap 1: EditorWithSidebar never loads existing chapters | COVERED | Task 1 |
| VERIFICATION | Gap 2: Forced save not guaranteed due to unawaited async flushes | COVERED | Task 2 |

Deferred ideas: none in Phase 11 CONTEXT. No out-of-phase items are planned here.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load existing manuscript chapters on editor entry and select first chapter</name>
  <files>lib/features/manuscript/presentation/editor_with_sidebar.dart, test/features/manuscript/presentation/editor_with_sidebar_test.dart</files>
  <read_first>
    - /home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-VERIFICATION.md
    - /home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
    - /home/re/code/MuseFlow/lib/features/manuscript/presentation/editor_with_sidebar.dart
    - /home/re/code/MuseFlow/lib/features/manuscript/application/chapter_notifier.dart
    - /home/re/code/MuseFlow/lib/features/manuscript/presentation/chapter_sidebar.dart
    - /home/re/code/MuseFlow/test/features/manuscript/presentation/editor_with_sidebar_test.dart
  </read_first>
  <behavior>
    - Test 1: When `EditorWithSidebar(manuscriptId: 'm1')` is mounted and `chapterNotifierProvider.build()` starts empty, `loadChapters('m1')` is called during initialization.
    - Test 2: After `loadChapters('m1')` completes with persisted chapters, the sidebar renders those chapters and the first chapter becomes active/loaded without requiring a manual tap.
    - Test 3: If `loadChapters('m1')` completes with an empty list, the editor remains in the existing empty state and does not throw.
  </behavior>
  <action>Update `EditorWithSidebar` initialization to make `_loadInitialChapter` asynchronous and data-source-driven, per SC-2/SC-3 and D-01/D-13/D-14. In the post-frame initialization path, call `await ref.read(chapterNotifierProvider.notifier).loadChapters(widget.manuscriptId)`, then read `ref.read(chapterNotifierProvider).asData?.value ?? []`, and if non-empty load/select `chapters.first` with `_loadChapter(chapters.first)` inside a mounted check. Do not rely on the provider's pre-existing `asData` state because `ChapterNotifier.build()` deliberately returns `[]`. Guard against duplicate initialization if the widget rebuilds or post-frame callback races by adding a private boolean such as `_initialChapterLoaded` or by making the post-frame callback one-shot from `initState`. Update `editor_with_sidebar_test.dart` so its fake chapter notifier starts empty, records `loadChapters` calls, populates state only inside `loadChapters`, and asserts that entering the editor route loads visible chapters and selects the first persisted chapter. Keep the existing empty-state test, but make it prove `loadChapters('m1')` was still called even when no chapters exist.</action>
  <verify>
    <automated>flutter test test/features/manuscript/presentation/editor_with_sidebar_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/features/manuscript/presentation/editor_with_sidebar.dart` contains a call equivalent to `await ref.read(chapterNotifierProvider.notifier).loadChapters(widget.manuscriptId)` in the initialization path.
    - Source assertion: `_loadInitialChapter` or its replacement reads chapters only after `loadChapters` completes and performs `if (!mounted) return` checks after awaits/post-frame callbacks.
    - Behavior assertion: a widget test fails before the fix because fake `loadChapters` is not called, and passes after the fix.
    - Behavior assertion: a widget test verifies first loaded chapter content/path is active enough for the editor area/status bar/sidebar to reflect the first loaded chapter instead of showing only the empty state.
    - CLI output: `flutter test test/features/manuscript/presentation/editor_with_sidebar_test.dart` exits 0.
  </acceptance_criteria>
  <done>Entering `/manuscript/:id/editor` for a manuscript with existing persisted chapters loads those chapters into `chapterNotifierProvider`, displays them in `ChapterSidebar`, and selects the first chapter without user intervention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make forced saves awaited where Flutter lifecycle permits</name>
  <files>lib/features/manuscript/presentation/editor_with_sidebar.dart, lib/features/manuscript/application/chapter_auto_save.dart, test/features/manuscript/application/chapter_auto_save_test.dart, test/features/manuscript/presentation/editor_with_sidebar_test.dart</files>
  <read_first>
    - /home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-VERIFICATION.md
    - /home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
    - /home/re/code/MuseFlow/lib/features/manuscript/presentation/editor_with_sidebar.dart
    - /home/re/code/MuseFlow/lib/features/manuscript/application/chapter_auto_save.dart
    - /home/re/code/MuseFlow/test/features/manuscript/application/chapter_auto_save_test.dart
    - /home/re/code/MuseFlow/test/features/manuscript/presentation/editor_with_sidebar_test.dart
  </read_first>
  <behavior>
    - Test 1: `ChapterAutoSave.forceSave()` persists pending content immediately and can be awaited by callers before route/chapter changes.
    - Test 2: `ChapterAutoSave.dispose()` cancels timers and does not start an unawaited async flush; pending content is persisted only through explicit awaited `forceSave()` paths.
    - Test 3: `EditorWithSidebar` awaits forced saves on chapter switch and back-to-library navigation before loading the next chapter or navigating away.
    - Test 4: App lifecycle pause/inactive triggers the same force-save request path, with errors caught/logged rather than causing an unhandled async exception.
  </behavior>
  <action>Refactor forced-save handling per SC-4 and D-19. In `ChapterAutoSave.dispose`, remove `unawaited(_flush())`; dispose must only cancel the debounce timer and release resources because Dart/Flutter `dispose()` cannot be awaited. If helpful, expose a synchronous `bool get hasPendingChanges` or `Future<void> close()` method, but the must-have guarantee must come from explicit awaited `forceSave()` calls before transitions, not unawaited dispose. In `EditorWithSidebar`, replace `_forceSaveSync()` with a best-effort lifecycle wrapper that captures the `Future` from `_forceSaveAsync()` and handles errors via `debugPrint`; use awaited `_forceSaveAsync()` in `_switchChapter` before `_loadChapter(newChapter)`, in `_navigateBack` before `context.go(AppConstants.editor)`, and before route changes from AppBar settings if that path leaves the editor. For lifecycle `paused` and `inactive`, call the best-effort wrapper because `didChangeAppLifecycleState` cannot be declared async for framework awaiting; document this limitation in a comment and ensure all controllable transitions are awaited. Ensure `_forceSaveAndCleanup` during `dispose()` does not claim a guarantee through unawaited async work; cleanup listeners/composer only after prior awaited paths have had a chance to run. Avoid using `ref.read` after disposal. Add regression tests that would fail if `dispose()` still contains `unawaited(_flush())`, if pending content can only be saved by dispose, or if chapter switch/back navigation does not await a delayed fake `forceSave()`.</action>
  <verify>
    <automated>flutter test test/features/manuscript/application/chapter_auto_save_test.dart test/features/manuscript/presentation/editor_with_sidebar_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `lib/features/manuscript/application/chapter_auto_save.dart` no longer contains `unawaited(_flush())` in `dispose()`.
    - Source assertion: `EditorWithSidebar._switchChapter` awaits `_forceSaveAsync()` before loading the selected chapter.
    - Source assertion: `EditorWithSidebar._navigateBack` awaits `_forceSaveAsync()` before `context.go(AppConstants.editor)`.
    - Source assertion: lifecycle pause/inactive handling calls a best-effort async save wrapper with error handling and a comment that Flutter does not await lifecycle callbacks.
    - Behavior assertion: tests prove explicit `await autoSave.forceSave()` persists pending content and `dispose()` does not silently perform an unawaited write as the only persistence path.
    - Behavior assertion: widget tests using a delayed fake `ChapterAutoSave.forceSave()` prove chapter switching/back navigation does not proceed until the save future completes where the framework exposes an awaitable callback.
    - CLI output: `flutter test test/features/manuscript/application/chapter_auto_save_test.dart test/features/manuscript/presentation/editor_with_sidebar_test.dart` exits 0.
  </acceptance_criteria>
  <done>Forced saves are awaited before chapter switch and user-initiated navigation, lifecycle pause/inactive triggers best-effort async save with handled errors, and `ChapterAutoSave.dispose` no longer relies on unawaited async flush for persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Riverpod notifier | Local user actions in `EditorWithSidebar` trigger chapter loading/saving through providers. |
| Editor document → Hive repository | User-authored manuscript content is serialized and persisted locally. |
| Flutter lifecycle → save service | OS/app lifecycle events trigger local persistence attempts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | Tampering | `EditorWithSidebar` manuscript/chapter selection | mitigate | Always load chapters by `widget.manuscriptId` via `ChapterNotifier.loadChapters(widget.manuscriptId)` before selecting the first chapter, preventing stale provider state from another manuscript from being used. |
| T-11-06-02 | Denial of Service | `ChapterAutoSave.forceSave` / Hive write path | mitigate | Await forced saves on switch/navigation and catch/log lifecycle best-effort save errors so persistence failures do not cause unhandled exceptions or silent route changes before save completion. |
| T-11-06-03 | Repudiation | Local save operations | accept | Local-only single-user app has no audit log requirement at ASVS L1; tests verify persistence behavior instead. |
| T-11-06-SC | Tampering | Package installs | accept | No npm/pip/cargo/pub package installs are introduced by this gap-closure plan. |

No HIGH security threats are introduced. This plan touches only local UI state flow and local Hive-backed save timing; it adds no network boundary, auth/session surface, secrets handling, or external input parser.
</threat_model>

<verification>
Run focused checks first, then a manuscript subset if focused checks pass:

<automated>flutter test test/features/manuscript/application/chapter_auto_save_test.dart test/features/manuscript/presentation/editor_with_sidebar_test.dart</automated>
<automated>flutter test test/features/manuscript/</automated>

Optional static source gates after implementation:
- `grep -R "unawaited(_flush())" /home/re/code/MuseFlow/lib/features/manuscript/application/chapter_auto_save.dart` returns no matches.
- `grep -R "loadChapters(widget.manuscriptId)" /home/re/code/MuseFlow/lib/features/manuscript/presentation/editor_with_sidebar.dart` returns at least one match.
</verification>

<success_criteria>
- SC-2/SC-3 gap closed: editor entry loads persisted manuscript chapters and the sidebar/editor can access existing chapter content.
- SC-4 gap closed: awaited forced-save paths exist for controllable transitions, lifecycle save is best-effort with handled errors, and dispose no longer pretends to guarantee persistence through unawaited async work.
- Focused regression tests prevent reintroducing the two verification failures.
- No broad replanning or unrelated Phase 11 feature changes are included.
</success_criteria>

<output>
Create `/home/re/code/MuseFlow/.planning/phases/11-manuscript-chapter-management/11-06-SUMMARY.md` when done.
</output>
