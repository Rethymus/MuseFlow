---
phase: 11-manuscript-chapter-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/features/manuscript/domain/manuscript.dart
  - lib/features/manuscript/domain/chapter.dart
  - lib/features/manuscript/domain/manuscript_genre.dart
  - lib/features/manuscript/domain/chapter_export.dart
  - lib/core/infrastructure/hive_adapters.dart
  - lib/main.dart
autonomous: true
requirements: [SC-1, SC-2, SC-3, SC-5, SC-6]
user_setup:
  - service: super_editor_markdown
    why: "Chapter content serialization to/from Markdown"
    env_vars: []
    dashboard_config: []
    fallback: "Manual Document node serialization (fragile, not recommended)"

must_haves:
  truths:
    - "Manuscript entity exists with all D-03 fields: id, title, description, genre, targetWordCount, status, worldSettingId, characterCardIds, createdAt, updatedAt, deletedAt, coverLetter"
    - "Chapter entity exists with all D-04 fields: id, manuscriptId, title, sortOrder, status, documentContent (Markdown string), createdAt, updatedAt"
    - "Chapter.wordCount is a computed getter from documentContent (Chinese character count excluding whitespace)"
    - "ManuscriptGenre utility maps 14 preset genres to distinct colors with WCAG AA contrast"
    - "ChapterExport model exists with title, sortOrder, content fields"
    - "ManuscriptAdapter uses HiveTypeIds.manuscript (2), ChapterAdapter uses HiveTypeIds.chapter (9)"
    - "Both adapters registered in main.dart Hive.registerAdapter calls"
  artifacts:
    - path: "lib/features/manuscript/domain/manuscript.dart"
      provides: "Manuscript immutable entity with copyWith, fromJson, toJson, equality"
      min_lines: 120
    - path: "lib/features/manuscript/domain/chapter.dart"
      provides: "Chapter immutable entity with copyWith, fromJson, toJson, computed wordCount"
      min_lines: 100
    - path: "lib/features/manuscript/domain/manuscript_genre.dart"
      provides: "Genre preset list, color mapping, genreColor utility"
      min_lines: 40
    - path: "lib/features/manuscript/domain/chapter_export.dart"
      provides: "ChapterExport model for structured export"
      min_lines: 30
    - path: "lib/core/infrastructure/hive_adapters.dart"
      provides: "ManuscriptAdapter and ChapterAdapter added to existing file"
      contains: "class ManuscriptAdapter"
    - path: "lib/main.dart"
      provides: "Adapter registration for ManuscriptAdapter and ChapterAdapter"
      contains: "Hive.registerAdapter(ManuscriptAdapter()"
  key_links:
    - from: "lib/features/manuscript/domain/manuscript.dart"
      to: "lib/core/infrastructure/hive_adapters.dart"
      via: "ManuscriptAdapter delegates to Manuscript.fromJson/toJson"
      pattern: "Manuscript.fromJson"
    - from: "lib/features/manuscript/domain/chapter.dart"
      to: "lib/core/infrastructure/hive_adapters.dart"
      via: "ChapterAdapter delegates to Chapter.fromJson/toJson"
      pattern: "Chapter.fromJson"
    - from: "pubspec.yaml"
      to: "super_editor_markdown"
      via: "flutter pub add super_editor_markdown"
      pattern: "super_editor_markdown"
---

<objective>
Create the domain entities and infrastructure foundation for the manuscript/chapter management feature module. Install super_editor_markdown for chapter content serialization. Register Hive TypeAdapters.

Purpose: All downstream plans depend on these domain models and Hive infrastructure. Without them, no repository, notifier, or UI widget can function.
Output: Manuscript, Chapter, ManuscriptGenre, ChapterExport domain entities. ManuscriptAdapter + ChapterAdapter in hive_adapters.dart. super_editor_markdown package installed.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md
@.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md
@.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md

# Entity pattern reference (the exact pattern to follow):
@lib/core/domain/fragment.dart

# TypeAdapter pattern reference (the exact file to extend):
@lib/core/infrastructure/hive_adapters.dart

# Adapter registration location (the exact file to modify):
@lib/main.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install super_editor_markdown and create Manuscript + ManuscriptGenre entities</name>
  <files>
    pubspec.yaml
    lib/features/manuscript/domain/manuscript.dart
    lib/features/manuscript/domain/manuscript_genre.dart
  </files>
  <read_first>
    @lib/core/domain/fragment.dart -- Immutable entity pattern: const constructor, copyWith with nullable params, fromJson/toJson, operator==, hashCode
    @.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md -- Pattern 1 (Manuscript entity example with all fields). Note: documentContent stores Markdown string (NOT JSON) per RESEARCH correction.
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- Manuscript deviations from Fragment pattern: more fields, deletedAt for soft delete, characterCardIds as List<String>
    @.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md -- Genre color table (14 genres with distinct colors, WCAG AA 4.5:1 contrast with white text), manuscript status colors, chapter status colors
    @pubspec.yaml -- Verify current super_editor version before adding super_editor_markdown
  </read_first>
  <behavior>
    - Test 1: Manuscript.fromJson(toJson()) roundtrip preserves all fields including deletedAt null, characterCardIds empty list, coverLetter default
    - Test 2: ManuscriptGenre.presets contains exactly 14 genres matching Phase 7 template types
    - Test 3: ManuscriptGenre.genreColor returns a non-zero color for all presets and default gray for unknown genres
    - Test 4: Manuscript.copyWith(coverLetter: '修') updates only coverLetter
  </behavior>
  <action>
    Run `flutter pub add super_editor_markdown` to install the serialization package. Verify resolution succeeds against `super_editor: ^0.3.0-dev.20`.

    Create `lib/features/manuscript/domain/manuscript.dart` following the Fragment immutable entity pattern exactly. Include all fields from D-03: `id`, `title`, `description` (nullable), `genre`, `targetWordCount` (default 0), `status` (default '构思中', valid values: '构思中'/'写作中'/'已完成'), `worldSettingId` (nullable), `characterCardIds` (default const []), `createdAt`, `updatedAt`, `deletedAt` (nullable), `coverLetter` (max 2 chars). Constructor uses `const`, all fields required except those noted as nullable or having defaults. Provide `copyWith` with nullable params, `fromJson` factory, `toJson` method, `operator==` and `hashCode`.

    Create `lib/features/manuscript/domain/manuscript_genre.dart` as a utility class with `static const List<String> presets` containing the 14 Phase 7 novel types (玄幻, 仙侠, 都市, 科幻, 奇幻, 武侠, 历史, 军事, 悬疑, 恐怖, 言情, 校园, 游戏, 末世). Add `static const Map<String, int> _genreColors` mapping each genre to a distinct hex color. Colors must be warm/bold for the 8 male-frequency genres and soft/elegant for the 6 female-frequency genres, all with WCAG AA contrast (>=4.5:1) against white text for the cover letter overlay. Add `static int genreColor(String genre)` returning the mapped color or `0xFF49454F` (gray) for unknown genres. Add `static const List<String> statusValues = ['构思中', '写作中', '已完成']` and `static const List<String> chapterStatusValues = ['草稿', '初稿', '精修', '定稿']`.
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/domain/manuscript_test.dart test/features/manuscript/domain/manuscript_genre_test.dart</automated>
  </verify>
  <done>
    pubspec.yaml contains super_editor_markdown dependency. Manuscript entity with all D-03 fields, ManuscriptGenre with 14 presets and WCAG AA color mapping. Domain unit tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Chapter and ChapterExport entities</name>
  <files>
    lib/features/manuscript/domain/chapter.dart
    lib/features/manuscript/domain/chapter_export.dart
  </files>
  <read_first>
    @lib/core/domain/fragment.dart -- Immutable entity pattern reference
    @lib/features/manuscript/domain/manuscript.dart -- Manuscript entity (created in Task 1, for consistency)
    @.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md -- Chapter entity with computed wordCount. Note: documentContent stores serialized Markdown -- NOT JSON
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- Chapter deviations: documentContent field, wordCount computed getter
  </read_first>
  <behavior>
    - Test 1: Chapter.wordCount returns 0 for empty documentContent, returns Chinese character count (excluding whitespace) for non-empty
    - Test 2: Chapter.fromJson(toJson()) roundtrip preserves all fields including default status '草稿' and empty documentContent
    - Test 3: Chapter equality compares all fields including manuscriptId and sortOrder
    - Test 4: ChapterExport.toJson produces map with title, sortOrder, content keys
    - Test 5: ChapterExport equality compares title, sortOrder, content
  </behavior>
  <action>
    Create `lib/features/manuscript/domain/chapter.dart` following the same immutable pattern as Manuscript. Fields from D-04 (with RESEARCH correction): `id`, `manuscriptId`, `title`, `sortOrder` (int), `status` (default '草稿', valid values: '草稿'/'初稿'/'精修'/'定稿'), `documentContent` (String, default '', stores serialized Markdown -- NOT JSON), `createdAt`, `updatedAt`. Add computed getter `wordCount` that counts Chinese characters excluding whitespace: `documentContent.replaceAll(RegExp(r'\s'), '').length`. Provide copyWith, fromJson, toJson, equality.

    Create `lib/features/manuscript/domain/chapter_export.dart` with `ChapterExport` class: fields `title` (String), `sortOrder` (int), `content` (String). Immutable with const constructor, fromJson, toJson, equality.
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/domain/chapter_test.dart test/features/manuscript/domain/chapter_export_test.dart</automated>
  </verify>
  <done>
    Chapter entity with all D-04 fields, computed wordCount from Markdown content. ChapterExport model with title/sortOrder/content. Domain unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Hive TypeAdapters and register in main.dart</name>
  <files>
    lib/core/infrastructure/hive_adapters.dart
    lib/main.dart
  </files>
  <read_first>
    @lib/core/infrastructure/hive_adapters.dart -- Current file with all existing adapters and HiveTypeIds registry. Manuscript type ID already reserved as 2. Chapter needs type ID 9.
    @lib/main.dart -- Current Hive.registerAdapter calls after line 64. New adapters must be added here.
    @lib/features/manuscript/domain/manuscript.dart -- Manuscript entity (created in Task 1)
    @lib/features/manuscript/domain/chapter.dart -- Chapter entity (created in Task 2)
  </read_first>
  <acceptance_criteria>
    - hive_adapters.dart contains `static const int chapter = 9` in HiveTypeIds class
    - hive_adapters.dart contains `class ManuscriptAdapter extends TypeAdapter<Manuscript>` with typeId = HiveTypeIds.manuscript (2)
    - hive_adapters.dart contains `class ChapterAdapter extends TypeAdapter<Chapter>` with typeId = HiveTypeIds.chapter (9)
    - Both adapters delegate read to fromJson and write to toJson (exact same pattern as FragmentAdapter)
    - main.dart contains `Hive.registerAdapter(ManuscriptAdapter())` and `Hive.registerAdapter(ChapterAdapter())` after existing adapter registrations
    - `flutter analyze lib/core/infrastructure/hive_adapters.dart lib/main.dart` exits 0
  </acceptance_criteria>
  <action>
    In `lib/core/infrastructure/hive_adapters.dart`: Add `static const int chapter = 9;` to the `HiveTypeIds` abstract class. Add import for `package:museflow/features/manuscript/domain/manuscript.dart` and `package:museflow/features/manuscript/domain/chapter.dart`. Add `ManuscriptAdapter` class following the exact FragmentAdapter pattern (lines 28-50): `typeId = HiveTypeIds.manuscript`, `read` calls `Manuscript.fromJson(reader.readMap())`, `write` calls `writer.writeMap(obj.toJson())`, include hashCode and operator==. Add `ChapterAdapter` class with identical pattern using `HiveTypeIds.chapter`.

    In `lib/main.dart`: Add import for `package:museflow/core/infrastructure/hive_adapters.dart` (already imported). Add two lines after the existing `Hive.registerAdapter(GuardianAnnotationAdapter())` on line 71:
    `Hive.registerAdapter(ManuscriptAdapter());`
    `Hive.registerAdapter(ChapterAdapter());`
  </action>
  <verify>
    <automated>flutter analyze lib/core/infrastructure/hive_adapters.dart lib/main.dart</automated>
  </verify>
  <done>
    ManuscriptAdapter and ChapterAdapter registered in hive_adapters.dart with correct type IDs (2 and 9). Both registered in main.dart. Static analysis passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain entity deserialization | Untrusted Hive box data flows through fromJson into domain entities |
| Genre string input | User-created custom genres flow into color lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Manuscript.fromJson / Chapter.fromJson | mitigate | Validate field types in fromJson (cast with type checks). Title max length enforced at UI layer (input validation per ASVS V5). |
| T-11-02 | Denial of Service | Hive box corruption | mitigate | Existing HiveAesCipher encryption on settings box. Manuscript/chapter boxes use standard unencrypted Hive (same pattern as fragments, character_cards). Data integrity via fromJson type safety. |
| T-11-SC | Tampering | super_editor_markdown npm/pub.dev install | mitigate | [ASSUMED] official companion package from superlistapp (same publisher as super_editor already in project). Human checkpoint if pub resolution fails. |

## Package Legitimacy Gate

Per RESEARCH.md audit: super_editor_markdown is marked [ASSUMED] (official companion package from superlistapp, matching super_editor ^0.3.0-dev.20). If `flutter pub add super_editor_markdown` fails to resolve, stop and flag for human verification before proceeding.
</threat_model>

<verification>
1. `flutter pub add super_editor_markdown` resolves without version conflict
2. `flutter test test/features/manuscript/domain/` passes
3. `flutter analyze lib/core/infrastructure/hive_adapters.dart lib/main.dart lib/features/manuscript/domain/` exits 0
4. ManuscriptAdapter typeId = 2, ChapterAdapter typeId = 9
5. Manuscript has all 12 fields from D-03, Chapter has all 8 fields from D-04
6. Chapter.wordCount is a computed getter, not a stored field
</verification>

<success_criteria>
- Domain entities exist with full immutability pattern (copyWith, fromJson/toJson, equality)
- Hive TypeAdapters registered for Manuscript (typeId=2) and Chapter (typeId=9)
- super_editor_markdown installed and resolving correctly
- ManuscriptGenre maps 14 preset genres to visually distinct colors
- ChapterExport model ready for export integration
- All domain unit tests pass
- All static analysis passes
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-01-SUMMARY.md` when done
</output>
