---
phase: 11-manuscript-chapter-management
plan: 05
type: execute
wave: 5
depends_on: ["11-04"]
files_modified:
  - lib/features/story_structure/domain/export_bundle.dart
  - lib/features/story_structure/application/export_service.dart
  - lib/features/templates/application/template_instantiation_service.dart
  - lib/features/editor/application/editor_prompt_pipeline.dart
  - lib/features/editor/presentation/status_bar.dart
  - lib/main.dart
autonomous: true
requirements: [SC-5, SC-6]
user_setup: []

must_haves:
  truths:
    - "ExportBundle has chapters field: List<ChapterExport> with title, sortOrder, content"
    - "ExportBundle.manuscriptText is computed from chapters: chapters.map((c) => c.content).join('\\n\\n')"
    - "ExportService.buildContent produces chapter-aware output for Markdown and JSON formats"
    - "TemplateInstantiationService.saveDraft creates chapter skeleton entities alongside WorldSetting + CharacterCards"
    - "EditorPromptPipeline injects adjacent chapter summaries into AI context (D-24)"
    - "StatusBar shows '总字数: {current}/{target} 字' when manuscript context is available"
    - "Keyboard shortcuts registered: Ctrl+Up (prev chapter), Ctrl+Down (next chapter), Ctrl+Shift+N (new chapter) (D-26)"
    - "ManuscriptPurgeService called on app startup in main.dart after Hive initialization"
  artifacts:
    - path: "lib/features/story_structure/domain/export_bundle.dart"
      provides: "ExportBundle extended with chapters field and ChapterExport list"
      contains: "final List<ChapterExport> chapters"
    - path: "lib/features/story_structure/application/export_service.dart"
      provides: "ExportService updated for chapter-aware export"
      contains: "chapters"
    - path: "lib/features/templates/application/template_instantiation_service.dart"
      provides: "TemplateInstantiationService extended with chapter skeleton creation"
      contains: "chapterRepository"
    - path: "lib/features/editor/application/editor_prompt_pipeline.dart"
      provides: "EditorPromptPipeline extended with ChapterContextMiddleware"
      contains: "ChapterContextMiddleware"
    - path: "lib/features/editor/presentation/status_bar.dart"
      provides: "StatusBar extended with manuscript progress display"
      contains: "总字数"
    - path: "lib/main.dart"
      provides: "Purge service called on app startup"
      contains: "purgeExpired"
  key_links:
    - from: "lib/features/story_structure/domain/export_bundle.dart"
      to: "lib/features/manuscript/domain/chapter_export.dart"
      via: "ExportBundle.chapters contains List<ChapterExport>"
      pattern: "ChapterExport"
    - from: "lib/features/templates/application/template_instantiation_service.dart"
      to: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      via: "TemplateInstantiationService injects ChapterRepository to create chapter skeletons"
      pattern: "chapterRepository"
    - from: "lib/features/editor/application/editor_prompt_pipeline.dart"
      to: "lib/features/manuscript/application/chapter_notifier.dart"
      via: "ChapterContextMiddleware reads adjacent chapter content from ChapterNotifier"
      pattern: "ChapterContextMiddleware"
---

<objective>
Complete all remaining integrations for Phase 11: chapter-aware export bundle, template chapter skeleton creation, AI adjacent chapter context injection, manuscript progress in StatusBar, keyboard shortcuts for chapter navigation, and purge service startup wiring.

Purpose: These are the integration touchpoints that connect the new manuscript/chapter system to existing subsystems (export, templates, AI pipeline, stats). Without them, chapters exist in isolation.
Output: ExportBundle with chapters, ExportService updated, TemplateInstantiationService with chapter skeleton, EditorPromptPipeline with ChapterContextMiddleware, StatusBar with progress, keyboard shortcuts, purge service on startup.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md
@.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md

# Files to extend:
@lib/features/story_structure/domain/export_bundle.dart
@lib/features/templates/application/template_instantiation_service.dart
@lib/features/editor/application/editor_prompt_pipeline.dart
@lib/features/editor/presentation/status_bar.dart
@lib/main.dart

# Chapter export model:
@lib/features/manuscript/domain/chapter_export.dart

# Auto-save and notifier services:
@lib/features/manuscript/application/chapter_auto_save.dart
@lib/features/manuscript/application/chapter_notifier.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ExportBundle with chapters, update ExportService, extend TemplateInstantiationService with chapter skeleton</name>
  <files>
    lib/features/story_structure/domain/export_bundle.dart
    lib/features/story_structure/application/export_service.dart
    lib/features/templates/application/template_instantiation_service.dart
  </files>
  <read_first>
    @lib/features/story_structure/domain/export_bundle.dart -- Current ExportBundle with manuscriptText field (line 22), all existing fields, fromJson/toJson, equality. Must add `chapters` field, update fromJson/toJson, update equality, make manuscriptText computed.
    @lib/features/manuscript/domain/chapter_export.dart -- ChapterExport model with title, sortOrder, content
    @lib/features/story_structure/application/export_service.dart -- Current export service that builds content from ExportBundle. Must handle chapters field.
    @lib/features/templates/application/template_instantiation_service.dart -- Current saveDraft that creates WorldSetting + CharacterCards. Constructor takes worldSettingRepository and characterCardRepository. Must add chapterRepository injection and chapter skeleton creation. Also check TemplateCreationResult and TemplateDraft classes.
    @lib/features/templates/application/template_draft.dart -- TemplateDraft structure -- may need chapterTitles field added
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- ExportBundle extension pattern (lines 830-849): add chapters field, manuscriptText becomes computed. TemplateInstantiationService extension (lines 903-933): inject ChapterRepository, create chapter skeleton in saveDraft.
  </read_first>
  <acceptance_criteria>
    - ExportBundle has `final List<ChapterExport> chapters` field with default const []
    - ExportBundle constructor includes chapters parameter
    - ExportBundle.fromJson parses chapters from JSON array
    - ExportBundle.toJson serializes chapters to JSON array
    - ExportBundle.operator== compares chapters list
    - ExportBundle.manuscriptText remains as stored field (NOT computed -- backward compat). A new getter `computedManuscriptText` returns chapters.join('\n\n') for chapter-aware consumers. The existing manuscriptText field continues to work for legacy consumers.
    - ExportService buildContent checks if chapters is non-empty and produces chapter-structured output (each chapter with header)
    - TemplateInstantiationService constructor accepts optional ChapterRepository parameter
    - TemplateInstantiationService.saveDraft creates Chapter entities for chapterTitles in the draft (or a default set of 3 chapters: '第一章', '第二章', '第三章' if no chapterTitles provided)
    - TemplateCreationResult includes chapters field
    - `flutter analyze lib/features/story_structure/domain/export_bundle.dart lib/features/story_structure/application/export_service.dart lib/features/templates/application/template_instantiation_service.dart` exits 0
  </acceptance_criteria>
  <action>
    In `lib/features/story_structure/domain/export_bundle.dart`: Add import for ChapterExport. Add `final List<ChapterExport> chapters` field with default `const []`. Update constructor to include `this.chapters`. Update fromJson: parse `chapters` key as list of maps, map to ChapterExport.fromJson. Update toJson: add `'chapters': chapters.map((c) => c.toJson()).toList()`. Update operator== to compare chapters using _listEquals. Update hashCode to include Object.hashAll(chapters). Add getter `String get computedManuscriptText => chapters.map((c) => c.content).join('\n\n');` for chapter-aware consumers. Keep manuscriptText as-is for backward compatibility.

    In `lib/features/story_structure/application/export_service.dart`: Modify the buildContent method to check if bundle.chapters is non-empty. If chapters present, format output as chapter-structured content: for each chapter, add header line `## ${chapter.title}` followed by chapter content, separated by double newlines. For Markdown format, use `##` headers. For JSON format, include chapters array in output.

    In `lib/features/templates/application/template_instantiation_service.dart`: Add optional `ChapterRepository?` parameter to constructor. In saveDraft method, after creating WorldSetting and CharacterCards, if chapterRepository is not null: create Chapter entities. If draft has chapterTitles, use those; otherwise create default 3 chapters ('第一章', '第二章', '第三章'). Each chapter: id = uuid, manuscriptId = worldSetting.id (or a passed manuscriptId -- need to handle the case where no manuscript exists yet). For the template flow, chapters need a manuscriptId. Since template instantiation currently doesn't create a Manuscript entity, add the chapter creation as optional: only create chapters if a manuscriptId is provided to saveDraft (add optional manuscriptId parameter). Update TemplateCreationResult to include `List<Chapter> chapters = const []`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/story_structure/domain/export_bundle.dart lib/features/story_structure/application/export_service.dart lib/features/templates/application/template_instantiation_service.dart</automated>
  </verify>
  <done>
    ExportBundle has chapters field with full serialization support. ExportService produces chapter-structured output. TemplateInstantiationService creates chapter skeletons when ChapterRepository and manuscriptId are provided. Backward compatible with existing manuscriptText field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AI chapter context middleware, StatusBar progress, keyboard shortcuts, and purge service startup</name>
  <files>
    lib/features/editor/application/editor_prompt_pipeline.dart
    lib/features/editor/presentation/status_bar.dart
    lib/main.dart
  </files>
  <read_first>
    @lib/features/editor/application/editor_prompt_pipeline.dart -- Current middleware ordering (line 34-44): SystemPrompt, PersonaInjection, BannedList, KnowledgeInjection, SkillEnforcement, ContextAnchor, EditorOperation, EditorUserContent. ChapterContextMiddleware should be inserted AFTER KnowledgeInjection but BEFORE ContextAnchor (so it provides context after world/character knowledge but before the anchor). Read PromptMiddleware and PromptContext class signatures.
    @lib/features/editor/presentation/status_bar.dart -- Current StatusBar widget showing AI modification count. Must add manuscript progress display when manuscript context is available.
    @lib/main.dart -- Current Hive initialization and adapter registration. Add purge service call after all adapters registered and before runApp. Need to create purge service instance with repositories.
    @.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md -- D-24: AI operations inject adjacent chapter summaries as context. D-25: Word count progress in library card and editor status bar. D-26: Keyboard shortcuts Ctrl+Up/Down for chapter nav, Ctrl+Shift+N for new chapter.
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- ChapterContextMiddleware pattern (lines 873-897): inject previousChapterSummary and nextChapterSummary into system message. StatusBar extension: prepend manuscript progress.
    @lib/features/manuscript/infrastructure/manuscript_purge_service.dart -- ManuscriptPurgeService constructor taking ManuscriptRepository and ChapterRepository
    @lib/core/presentation/providers.dart -- manuscriptPurgeServiceProvider
  </read_first>
  <acceptance_criteria>
    - EditorPromptPipeline has ChapterContextMiddleware in middleware list (after KnowledgeInjection, before ContextAnchor)
    - ChapterContextMiddleware reads adjacent chapter summaries from provider state and injects as '上一章节摘要：...' and '下一章节摘要：...' in system message
    - StatusBar accepts optional manuscript progress params (currentWordCount, targetWordCount) and displays '总字数: {current}/{target} 字' when both are non-null and target > 0
    - main.dart creates ManuscriptPurgeService and calls purgeExpired() after Hive adapter registration, wrapped in try-catch (non-blocking -- purge failure should not prevent app startup)
    - `flutter analyze lib/features/editor/application/editor_prompt_pipeline.dart lib/features/editor/presentation/status_bar.dart lib/main.dart` exits 0
  </acceptance_criteria>
  <action>
    In `lib/features/editor/application/editor_prompt_pipeline.dart`: Create `ChapterContextMiddleware` class extending `PromptMiddleware`. Its `apply` method checks if PromptContext has adjacent chapter data (previousChapterSummary and nextChapterSummary -- these will be strings injected by the caller before pipeline execution). If present, inject into system message: prepend '上一章节摘要：\n{summary}\n\n' and/or '下一章节摘要：\n{summary}\n\n' before existing system content. Add `if (chapterContextMiddleware != null) chapterContextMiddleware` to the middleware list in EditorPromptPipeline constructor, positioned after skillEnforcementMiddleware and before ContextAnchorMiddleware. Add optional parameter to EditorPromptPipeline constructor.

    In `lib/features/editor/presentation/status_bar.dart`: Add optional named parameters to StatusBar: `int? manuscriptCurrentWords`, `int? manuscriptTargetWords`. In the build method, if manuscriptCurrentWords != null and manuscriptTargetWords != null and manuscriptTargetWords > 0, display a Row with Text '总字数: ${formatted current}/${formatted target} 字' at the leading position of the status bar, before the existing AI diff count. Format numbers with comma separators for thousands.

    In `lib/main.dart`: After the adapter registrations (line 71, after GuardianAnnotationAdapter), add purge service call. Since this runs before ProviderScope, create the repositories directly. Add imports for ManuscriptPurgeService, ManuscriptRepository, ChapterRepository. After all adapter registrations, add: open 'manuscripts' and 'chapters' boxes via Hive.openBox, create ManuscriptPurgeService with both repositories, call purgeExpired() wrapped in try-catch (non-blocking: purge failure must not prevent app startup). Hive.openBox with same name returns the existing box if already open, so this is safe with provider-based box opening later.
  </action>
  <verify>
    <automated>flutter analyze lib/features/editor/application/editor_prompt_pipeline.dart lib/features/editor/presentation/status_bar.dart lib/main.dart</automated>
  </verify>
  <done>
    ChapterContextMiddleware injected into EditorPromptPipeline. StatusBar shows manuscript word count progress. ManuscriptPurgeService called on startup. Static analysis passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chapter context injection | Adjacent chapter content injected into AI prompt |
| Export content | Chapter content serialized for file output |
| Purge service | Cascading delete of manuscript + chapter data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Information Disclosure | Chapter context in AI prompts | accept | Only chapter summaries (not full content) injected. Local-only AI calls. No network interception risk for local LLM. |
| T-11-12 | Denial of Service | Purge service on startup | mitigate | Wrapped in try-catch. Purge failure does not prevent app startup. 30-day retention prevents accidental data loss. |
| T-11-13 | Tampering | Export file content | accept | Export writes to user-selected path. Content is user's own data. No cross-user concern in single-user local app. |

## Package Legitimacy Gate

No new packages installed in this plan. All integrations use existing project dependencies.
</threat_model>

<verification>
1. `flutter analyze lib/features/story_structure/domain/export_bundle.dart lib/features/story_structure/application/export_service.dart lib/features/templates/application/template_instantiation_service.dart lib/features/editor/application/editor_prompt_pipeline.dart lib/features/editor/presentation/status_bar.dart lib/main.dart` exits 0
2. ExportBundle.fromJson handles chapters field
3. ExportBundle.computedManuscriptText joins chapter content
4. TemplateInstantiationService has ChapterRepository in constructor
5. ChapterContextMiddleware is in EditorPromptPipeline middleware list
6. StatusBar has manuscript progress display capability
7. main.dart calls purgeExpired in try-catch
</verification>

<success_criteria>
- ExportBundle extended with chapters field, backward compatible
- ExportService produces chapter-structured output
- Template instantiation creates chapter skeletons
- AI pipeline injects adjacent chapter context
- StatusBar shows manuscript word count progress
- Purge service runs on app startup (non-blocking)
- All integrations wired and static analysis passes
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-05-SUMMARY.md` when done
</output>
