---
phase: 11-manuscript-chapter-management
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - lib/features/manuscript/presentation/manuscript_library_page.dart
  - lib/features/manuscript/presentation/manuscript_card.dart
  - lib/features/manuscript/presentation/manuscript_create_dialog.dart
  - lib/features/manuscript/presentation/manuscript_settings_page.dart
  - lib/shared/constants/app_constants.dart
  - lib/app.dart
autonomous: true
requirements: [SC-1, SC-5]
user_setup: []

must_haves:
  truths:
    - "D-06: ManuscriptLibraryPage renders as Branch 1 default in StatefulShellRoute, replacing EditorPage as home screen"
    - "D-07: User sees a responsive card grid with genre-colored covers (ManuscriptGenre.genreColor), title, word count/target progress bar, status badge, and last edited timestamp"
    - "D-17: User can create a manuscript via quick-create dialog (title + genre dropdown from ManuscriptGenre.presets), which auto-creates one empty chapter and navigates to /manuscript/:id/editor"
    - "D-18: User can edit manuscript metadata on ManuscriptSettingsPage (title, genre, description, targetWordCount) accessible from long-press context menu"
    - "D-08: User can sort manuscripts by recentEdit (default), creationDate, or titleAlphabetical using ManuscriptSortMode dropdown in AppBar"
    - "D-21: User can soft-delete a manuscript via long-press context menu with 30-day recovery warning confirmation dialog"
    - "D-10: User sees empty state with Icons.auto_stories, heading '创建你的第一部作品', body '从灵感开始，写下属于你的故事', and '创建文稿' FilledButton when no manuscripts exist"
    - "D-11: Tapping a card navigates to /manuscript/:id/editor via context.go()"
    - "D-12: Bottom navigation is hidden inside manuscript editor routes (top-level GoRoute outside StatefulShellRoute per RESEARCH Pitfall 4)"
  artifacts:
    - path: "lib/features/manuscript/presentation/manuscript_library_page.dart"
      provides: "Library homepage with card grid, sort toggle, empty state, FAB"
      min_lines: 150
    - path: "lib/features/manuscript/presentation/manuscript_card.dart"
      provides: "Genre-colored card with cover letter, title, progress bar, status badge"
      min_lines: 80
    - path: "lib/features/manuscript/presentation/manuscript_create_dialog.dart"
      provides: "Quick-create dialog with title + genre fields"
      min_lines: 80
    - path: "lib/features/manuscript/presentation/manuscript_settings_page.dart"
      provides: "Manuscript metadata editing form"
      min_lines: 100
    - path: "lib/shared/constants/app_constants.dart"
      provides: "Route constants for manuscript editor and settings"
      contains: "manuscriptEditor"
    - path: "lib/app.dart"
      provides: "Updated routing with library as Branch 1, top-level editor routes"
      contains: "ManuscriptLibraryPage"
  key_links:
    - from: "ManuscriptLibraryPage"
      to: "manuscriptNotifierProvider"
      via: "ref.watch(manuscriptNotifierProvider)"
      pattern: "manuscriptNotifierProvider"
    - from: "ManuscriptCard"
      to: "/manuscript/:id/editor"
      via: "context.go(AppConstants.manuscriptEditor with id replacement)"
      pattern: "context.go.*manuscript"
    - from: "ManuscriptCreateDialog"
      to: "ManuscriptNotifier.create"
      via: "ref.read(manuscriptNotifierProvider.notifier).create()"
      pattern: "manuscriptNotifierProvider.notifier.*create"
    - from: "app.dart"
      to: "EditorWithSidebar"
      via: "GoRoute outside StatefulShellRoute"
      pattern: "EditorWithSidebar"
---

<objective>
Build the manuscript library homepage with card grid UI, quick-create dialog, settings page, and routing changes that make the library the new home screen and the editor a full-screen sub-route.

Purpose: Transform the app entry point from a single editor into a multi-manuscript management view, enabling users to browse, create, and navigate to manuscripts.

Output: ManuscriptLibraryPage, ManuscriptCard, ManuscriptCreateDialog, ManuscriptSettingsPage, updated app.dart routing, updated AppConstants.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/features/manuscript/domain/manuscript.dart:
```dart
class Manuscript {
  final String id, title, genre;
  final String? description, worldSettingId, deletedAt;
  final int targetWordCount;
  final String status; // 构思中/写作中/已完成
  final List<String> characterCardIds;
  final DateTime createdAt, updatedAt;
  final String coverLetter; // max 2 chars
  Manuscript copyWith({...});
  factory Manuscript.fromJson(Map<String, dynamic> json);
}
```

From lib/features/manuscript/domain/chapter.dart:
```dart
class Chapter {
  final String id, manuscriptId, title, status; // 草稿/初稿/精修/定稿
  final int sortOrder;
  final String documentContent; // Serialized Markdown
  final DateTime createdAt, updatedAt;
  int get wordCount; // computed, excludes whitespace
}
```

From lib/features/manuscript/domain/manuscript_genre.dart:
```dart
class ManuscriptGenre {
  static const List<String> presets = ['玄幻','仙侠','都市','科幻',...14 items];
  static const List<String> statusValues = ['构思中','写作中','已完成'];
  static int genreColor(String genre); // returns int color value, 0xFF49454F default
}
```

From lib/features/manuscript/application/manuscript_notifier.dart:
```dart
class ManuscriptNotifier extends AsyncNotifier<List<Manuscript>> {
  Future<void> create(Manuscript manuscript); // auto-creates 第一章
  Future<void> save(Manuscript manuscript);
  Future<void> softDelete(String id); // sets deletedAt
  Future<void> purgeDeleted();
  List<Manuscript> searchByTitle(String query);
}
// Provider: manuscriptNotifierProvider
```

From lib/features/manuscript/application/manuscript_sort.dart:
```dart
enum ManuscriptSortMode { recentEdit, creationDate, titleAlphabetical }
int compareManuscripts(Manuscript a, Manuscript b, ManuscriptSortMode mode);
```

From lib/shared/constants/app_constants.dart:
```dart
abstract class AppConstants {
  static const String editor = '/editor';
  static const double editorMaxWidth = 800.0;
  static const double sidebarCollapsedBreakpoint = 600.0;
}
// Add: static const String manuscriptEditor = '/manuscript/:id/editor';
// Add: static const String manuscriptSettings = '/manuscript/:id/settings';
```

From lib/app.dart (current routing structure):
- StatefulShellRoute.indexedStack with 6 branches
- Branch 1 currently renders EditorPage at path '/editor'
- _handleRedirect checks Hive settings box for onboarding
- Top-level onboarding route already exists outside StatefulShellRoute
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ManuscriptLibraryPage, ManuscriptCard, ManuscriptCreateDialog, and ManuscriptSettingsPage</name>
  <files>
    lib/features/manuscript/presentation/manuscript_library_page.dart
    lib/features/manuscript/presentation/manuscript_card.dart
    lib/features/manuscript/presentation/manuscript_create_dialog.dart
    lib/features/manuscript/presentation/manuscript_settings_page.dart
    test/features/manuscript/presentation/manuscript_library_page_test.dart
    test/features/manuscript/presentation/manuscript_card_test.dart
  </files>
  <behavior>
    - ManuscriptLibraryPage uses ConsumerWidget, watches manuscriptNotifierProvider
    - When manuscripts is empty, renders empty state: Icons.auto_stories (48px, colorScheme.outline), heading "创建你的第一部作品" (20px w600), body "从灵感开始，写下属于你的故事" (14px w400), FilledButton "创建文稿" that opens ManuscriptCreateDialog
    - When manuscripts exist, renders GridView.builder with SliverGridDelegateWithFixedCrossAxisCount: 3 columns for >=1000px, 2 for >=600px, 1 for mobile. Spacing 16px both axes
    - AppBar title is "文稿库" (28px w700), trailing has sort dropdown (SegmentedButton or PopupMenuButton with ManuscriptSortMode labels: "最近编辑"/"创建时间"/"标题") and FAB "创建文稿" (FloatingActionButton.extended with Icons.add)
    - ManuscriptCard: Card(elevation: 0, color: colorScheme.surfaceContainerHighest, shape: RoundedRectangleBorder(borderRadius: 12)). Cover area: 80px height with ManuscriptGenre.genreColor(manuscript.genre) background, cover letter centered (36px w700 white, max 2 chars, default = manuscript.title.substring(0, min(2, title.length))). Content area: title (14px w400, max 1 line, ellipsis), word count row "2,400 / 50,000 字" (12px w500) + LinearProgressIndicator (4px, primary fill, surfaceContainerLow track, value = totalChapterWordCount / targetWordCount clamped 0-1), bottom row = status badge (pill shape, colored per status: 构思中=gray, 写作中=blue, 已完成=green) + last edited time (12px, colorScheme.onSurfaceVariant, relative format)
    - ManuscriptCard.onTap calls context.go('/manuscript/${manuscript.id}/editor')
    - ManuscriptCard long-press shows PopupMenuButton with: "编辑信息" (navigates to settings), "删除" (red text, shows confirmation dialog with "确定要删除文稿'{title}'吗？文稿将在30天后永久删除，期间可恢复。")
    - ManuscriptCreateDialog: AlertDialog titled "创建文稿", SizedBox(width: 400). TextField for title (required, non-empty validation). DropdownButtonFormField for genre populated from ManuscriptGenre.presets. "自定义" option shows additional TextField. Actions: TextButton "取消" + FilledButton "创建". On confirm, calls manuscriptNotifier.create with Manuscript(coverLetter: title[0..2], status: '构思中') then navigates to /manuscript/:id/editor
    - ManuscriptSettingsPage: ConsumerStatefulWidget accepting manuscriptId. Loads manuscript from manuscriptNotifierProvider. Form fields: title (TextField), genre (DropdownButtonFormField from ManuscriptGenre.presets), description (TextField, max 500), targetWordCount (TextField, numeric, default 50000). AppBar with "返回文稿库" back button. Save button calls manuscriptNotifier.save. Title "文稿设置"
  </behavior>
  <read_first>
    - lib/features/manuscript/domain/manuscript.dart (Manuscript entity fields)
    - lib/features/manuscript/domain/manuscript_genre.dart (presets, genreColor, statusValues)
    - lib/features/manuscript/application/manuscript_notifier.dart (create, save, softDelete, searchByTitle)
    - lib/features/manuscript/application/manuscript_sort.dart (ManuscriptSortMode enum, compareManuscripts)
    - lib/features/knowledge/presentation/knowledge_base_page.dart (ConsumerWidget pattern for grid page)
    - lib/features/templates/presentation/template_gallery_page.dart (GridView + empty state + _PassiveTag pattern)
  </read_first>
  <action>
    Create ManuscriptLibraryPage as a ConsumerStatefulWidget with sort state (ManuscriptSortMode, default recentEdit) stored in local state. Watch manuscriptNotifierProvider, sort the list using compareManuscripts, then render GridView.builder. Implement empty state per UI-SPEC. Add FAB that shows ManuscriptCreateDialog via showDialog. Implement sort toggle as PopupMenuButton<ManuscriptSortMode> in AppBar trailing. Per D-25, compute total word count by watching chapterNotifierProvider (will be empty until editor is entered -- show 0 until chapters load).

    Create ManuscriptCard as a StatelessWidget accepting a Manuscript parameter. Build per UI-SPEC layout: Container with top clip (80px colored cover with letter), content padding (16px) with title, progress bar, status badge, and timestamp. Wrap in InkWell for tap handling and GestureDetector for long-press context menu.

    Create ManuscriptCreateDialog as a ConsumerStatefulWidget. Track title and genre in controllers. Validate title non-empty. On confirm, construct Manuscript with all required fields (id='', title from controller, genre from dropdown, coverLetter = title[0..min(2,title.length)], status='构思中'), call ref.read(manuscriptNotifierProvider.notifier).create(manuscript), then context.go('/manuscript/${createdId}/editor') -- note: since create() invalidates state, the created manuscript's ID comes from the Manuscript that was saved (the notifier's create method returns void, so navigate to library first and let the user tap the card, OR better: the ManuscriptNotifier.create should be extended to return the created manuscript ID -- but since it returns void, after create, invalidate and navigate to library where the new card appears). For the MVP flow: after create, pop dialog, and the user sees the new card and taps it.

    Create ManuscriptSettingsPage as a ConsumerStatefulWidget. Accept manuscriptId as constructor param. Load manuscript from repository in initState. Form fields pre-filled with manuscript values. Save button calls manuscriptNotifier.save with updated manuscript.

    Write widget tests for ManuscriptLibraryPage (renders empty state when no manuscripts, renders grid when manuscripts exist) and ManuscriptCard (renders cover letter, title, progress bar, status badge).
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/presentation/</automated>
  </verify>
  <done>
    - ManuscriptLibraryPage renders card grid for existing manuscripts, empty state when none
    - ManuscriptCard displays genre-colored cover, title, progress bar, status badge
    - ManuscriptCreateDialog creates manuscript and dismisses
    - ManuscriptSettingsPage loads and saves manuscript metadata
    - All widget tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update routing (app.dart) and route constants (app_constants.dart)</name>
  <files>
    lib/app.dart
    lib/shared/constants/app_constants.dart
  </files>
  <read_first>
    - lib/app.dart (current StatefulShellRoute structure, _handleRedirect, onboarding route)
    - lib/shared/constants/app_constants.dart (existing route constants)
    - .planning/phases/11-manuscript-chapter-management/11-RESEARCH.md (Pitfall 4: editor route must be top-level outside shell)
  </read_first>
  <action>
    In app_constants.dart, add two new route constants:
    - `static const String manuscriptEditor = '/manuscript/:id/editor';`
    - `static const String manuscriptSettings = '/manuscript/:id/settings';`

    In app.dart, make three changes:

    1. Add import for ManuscriptLibraryPage and ManuscriptSettingsPage:
    - `import 'package:museflow/features/manuscript/presentation/manuscript_library_page.dart';`
    - `import 'package:museflow/features/manuscript/presentation/manuscript_settings_page.dart';`
    (EditorWithSidebar import will be added in Plan 04 when that file is created)

    2. Change Branch 1 in StatefulShellRoute.indexedStack to render ManuscriptLibraryPage instead of EditorPage:
    - Replace `builder: (context, state) => const EditorPage()` with `builder: (context, state) => const ManuscriptLibraryPage()`
    - Keep the path as AppConstants.editor for compatibility

    3. Add top-level GoRoute entries BEFORE the StatefulShellRoute (per RESEARCH Pitfall 4 -- outside the shell so bottom nav is hidden):
    - `/manuscript/:id/editor` route -- for now, placeholder builder that returns SizedBox() or a simple Scaffold with text "Editor (Plan 04)". This ensures routing works but the full EditorWithSidebar is implemented in Plan 04.
    - `/manuscript/:id/settings` route -- builder returns ManuscriptSettingsPage(manuscriptId: state.pathParameters['id']!)

    4. Keep the _handleRedirect logic unchanged (onboarding detection still works).

    5. The initialLocation stays as AppConstants.editor which now shows the library page.

    IMPORTANT: Do NOT remove the EditorPage import or file -- it is still used by the legacy system and will be refactored into EditorWithSidebar in Plan 04. Just remove it from the Branch 1 route.
  </action>
  <verify>
    <automated>flutter test 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - AppConstants has manuscriptEditor and manuscriptSettings constants
    - Branch 1 renders ManuscriptLibraryPage instead of EditorPage
    - Top-level /manuscript/:id/editor route exists (placeholder for Plan 04)
    - Top-level /manuscript/:id/settings route renders ManuscriptSettingsPage
    - flutter test passes (no regressions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> Manuscript fields | Title, genre, description entered by user flow into domain entity |
| Widget -> GoRouter navigation | Manuscript ID from user tap flows into route path parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | ManuscriptCreateDialog | mitigate | Title field max length validation (TextInputFormatter.limit). Genre restricted to ManuscriptGenre.presets dropdown. |
| T-11-02 | Spoofing | Route parameter :id | accept | Single-user local app, no auth required. Route ID used only for local Hive lookup. |
| T-11-03 | Denial of Service | Manuscript card grid | accept | Card rendering is bounded. No risk of resource exhaustion in local-only app. |
</threat_model>

<verification>
1. flutter test test/features/manuscript/presentation/ passes
2. flutter test passes (no regressions from app.dart changes)
3. ManuscriptLibraryPage file exists with ConsumerStatefulWidget pattern
4. ManuscriptCard file exists with genre color cover, progress bar, status badge
5. ManuscriptCreateDialog file exists with title + genre fields
6. ManuscriptSettingsPage file exists with form fields
7. AppConstants has manuscriptEditor and manuscriptSettings
8. app.dart Branch 1 renders ManuscriptLibraryPage
9. app.dart has top-level /manuscript/:id/editor and /manuscript/:id/settings routes
</verification>

<success_criteria>
- ManuscriptLibraryPage is the new home screen (Branch 1 default)
- User can browse manuscripts in a responsive card grid
- User can create a manuscript via quick-create dialog
- User can soft-delete a manuscript with 30-day warning
- User can edit manuscript metadata via settings page
- User can sort manuscripts by recent edit, creation date, or title
- Empty state shows welcoming message and create CTA
- Tapping a manuscript card navigates to /manuscript/:id/editor
- Routing infrastructure supports full-screen editor (top-level route outside shell)
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-03-SUMMARY.md` when done
</output>
