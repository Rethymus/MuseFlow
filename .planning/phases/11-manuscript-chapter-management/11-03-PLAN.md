---
phase: 11-manuscript-chapter-management
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - lib/features/manuscript/presentation/manuscript_library_page.dart
  - lib/features/manuscript/presentation/manuscript_card.dart
  - lib/features/manuscript/presentation/manuscript_create_dialog.dart
  - lib/features/manuscript/presentation/manuscript_settings_page.dart
  - lib/shared/constants/app_constants.dart
  - lib/app.dart
autonomous: true
requirements: [SC-1, SC-5]
user_setup: []

must_haves:
  truths:
    - "User sees a card grid as the app home screen with genre-colored covers, titles, progress bars, and status badges"
    - "User can create a new manuscript via quick-create dialog (title + genre) that navigates directly to the editor"
    - "User can create a manuscript from a template, which auto-creates linked WorldSetting, CharacterCards, and chapter skeleton"
    - "User can sort manuscripts by recent edit (default), creation date, or title"
    - "User can long-press a card to see context menu with edit info, export, and delete options"
    - "User sees a 30-day recovery warning when deleting a manuscript"
    - "User can tap a card to enter the full-screen editor (bottom nav hidden)"
    - "User can edit manuscript metadata (title, genre, description, target word count) on a dedicated settings page"
    - "User sees a welcoming empty state when no manuscripts exist"
  artifacts:
    - path: "lib/features/manuscript/presentation/manuscript_library_page.dart"
      provides: "Card grid library homepage replacing EditorPage as Branch 1 default"
      min_lines: 150
    - path: "lib/features/manuscript/presentation/manuscript_card.dart"
      provides: "Genre-colored card with cover letter, title, progress, status, timestamp"
      min_lines: 100
    - path: "lib/features/manuscript/presentation/manuscript_create_dialog.dart"
      provides: "Quick create dialog with title + genre dropdown + template creation option"
      min_lines: 120
    - path: "lib/features/manuscript/presentation/manuscript_settings_page.dart"
      provides: "Full manuscript metadata editing form"
      min_lines: 120
    - path: "lib/shared/constants/app_constants.dart"
      provides: "New route constants for manuscript editor and settings"
      contains: "manuscriptEditor"
    - path: "lib/app.dart"
      provides: "Route wiring: top-level manuscript routes + Branch 1 library page"
      contains: "ManuscriptLibraryPage"
  key_links:
    - from: "lib/features/manuscript/presentation/manuscript_library_page.dart"
      to: "lib/core/presentation/providers.dart"
      via: "ref.watch(manuscriptNotifierProvider) for manuscript list"
      pattern: "manuscriptNotifierProvider"
    - from: "lib/features/manuscript/presentation/manuscript_library_page.dart"
      to: "lib/app.dart"
      via: "context.go('/manuscript/${manuscript.id}/editor') on card tap"
      pattern: "/manuscript/"
    - from: "lib/features/manuscript/presentation/manuscript_create_dialog.dart"
      to: "lib/features/templates/application/template_instantiation_service.dart"
      via: "When 'from template' selected, calls TemplateInstantiationService.saveDraft to create WorldSetting + CharacterCards + chapter skeleton"
      pattern: "TemplateInstantiationService"
    - from: "lib/app.dart"
      to: "lib/features/manuscript/presentation/manuscript_library_page.dart"
      via: "Branch 1 GoRoute builder returns ManuscriptLibraryPage"
      pattern: "StatefulShellBranch"
---

<objective>
Build the manuscript library homepage (card grid with genre-colored covers), manuscript creation dialog (with template integration), settings page, and wire all routes including the top-level manuscript editor route outside StatefulShellRoute.

Purpose: This is the new entry point for the app -- the library replaces the editor as the home screen per D-06. All manuscript browsing, creation, and management flows start here. The create dialog includes a template creation path that triggers D-20 (template chapter skeleton creation).
Output: ManuscriptLibraryPage, ManuscriptCard, ManuscriptCreateDialog (with template option), ManuscriptSettingsPage. Route wiring in app.dart. Route constants in app_constants.dart.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md
@.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md

# Route structure to modify:
@lib/app.dart

# Route constants to extend:
@lib/shared/constants/app_constants.dart

# ConsumerWidget pattern reference:
@lib/features/knowledge/presentation/knowledge_base_page.dart

# Card widget pattern reference:
@lib/features/capture/presentation/fragment_card.dart

# Form page pattern reference:
@lib/features/knowledge/presentation/world_setting_form.dart

# Template instantiation service to call for D-20:
@lib/features/templates/application/template_instantiation_service.dart

# Notifier to consume:
@lib/features/manuscript/application/manuscript_notifier.dart

# Domain entities:
@lib/features/manuscript/domain/manuscript.dart
@lib/features/manuscript/domain/manuscript_genre.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ManuscriptLibraryPage, ManuscriptCard, and ManuscriptCreateDialog</name>
  <files>
    lib/features/manuscript/presentation/manuscript_library_page.dart
    lib/features/manuscript/presentation/manuscript_card.dart
    lib/features/manuscript/presentation/manuscript_create_dialog.dart
  </files>
  <read_first>
    @.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md -- Library page layout ASCII diagram, card internal layout, empty state specs, copywriting contract (all Chinese labels), responsive grid breakpoints, card spacing and padding values, status badge colors, ManuscriptCreateDialog with template option
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- KnowledgeBasePage ConsumerWidget pattern (line 496-519): ref.watch, AsyncValue.when, GridView/Card layout. FragmentCard pattern: Card elevation 0, surfaceContainerHighest bg, InkWell tap.
    @lib/features/knowledge/presentation/knowledge_base_page.dart -- ConsumerWidget pattern with ref.watch for async data, loading/error/data states
    @lib/features/manuscript/domain/manuscript.dart -- Manuscript entity fields for card display: title, genre, targetWordCount, status, updatedAt, coverLetter
    @lib/features/manuscript/domain/manuscript_genre.dart -- ManuscriptGenre.genreColor() for card cover background
    @lib/features/manuscript/application/manuscript_sort.dart -- ManuscriptSortMode enum and compareManuscripts function
    @lib/features/editor/presentation/editor_page.dart -- EditorHolderNotifier and editorProvider export (line 66) -- these must remain accessible after library replaces editor as Branch 1
    @lib/features/templates/application/template_instantiation_service.dart -- TemplateInstantiationService.saveDraft API for template-based manuscript creation (D-20)
  </read_first>
  <behavior>
    - Test 1: ManuscriptLibraryPage renders empty state with '创建你的第一部作品' heading when manuscript list is empty
    - Test 2: ManuscriptLibraryPage renders GridView with correct crossAxisCount for different widths (1 for <600, 2 for 600-999, 3 for >=1000)
    - Test 3: ManuscriptLibraryPage shows sort dropdown with three options: '最近编辑', '创建时间', '标题'
    - Test 4: ManuscriptCard displays genre-colored cover area with cover letter, title, word count, progress bar, status badge, and last edited time
    - Test 5: ManuscriptCreateDialog validates that title is non-empty before creation
    - Test 6: ManuscriptCreateDialog has '从模板创建' option that shows template selection
    - Test 7: Delete action shows confirmation dialog with '30天后永久删除' warning text
  </behavior>
  <action>
    Create `lib/features/manuscript/presentation/manuscript_library_page.dart` as a ConsumerStatefulWidget. AppBar title: Text('文稿库'). Trailing actions: PopupMenuButton<ManuscriptSortMode> with sort icon, three items matching sort labels. FAB: FloatingActionButton.extended. Body: ref.watch(manuscriptNotifierProvider).when data: sort manuscripts using compareManuscripts, if empty show empty state, if not empty show GridView.builder. On FAB or empty-state button press: call _showCreateDialog. On card long-press: show context menu. Delete action: show confirmation dialog calling softDelete on confirm.

    Create `lib/features/manuscript/presentation/manuscript_card.dart` as a StatelessWidget. Card layout per UI-SPEC: Container with genre-colored top area (80px height) containing centered cover letter (36px w700, white, max 2 chars from manuscript.coverLetter or title[0]). Below: Padding(16) with title (14px w400, max 1 line ellipsis), word count row (12px w500 "{current} / {target} 字" format), LinearProgressIndicator (4px height, primary fill, surfaceContainerLow track), bottom row with status badge (Colored container with status text using ManuscriptGenre status colors) and last edited time (12px, formatted relative or absolute). Card shape: RoundedRectangleBorder(borderRadius: 12), elevation 0, color: colorScheme.surfaceContainerHighest. InkWell wraps entire card for tap.

    Create `lib/features/manuscript/presentation/manuscript_create_dialog.dart` as a ConsumerStatefulWidget. The dialog supports TWO creation modes toggled by a SegmentedButton at the top: '快速创建' and '从模板创建'. For '快速创建': TextField for title (required, non-empty validation), DropdownButtonFormField for genre (14 presets from ManuscriptGenre.presets + "自定义" option). For '从模板创建': shows a template selection list (reuse existing template gallery data from providers). When template is selected and user confirms, call TemplateInstantiationService.saveDraft with the selected template to create WorldSetting + CharacterCards + chapter skeleton (per D-20). The service returns a Manuscript entity with preset chapters. On create: validate title non-empty, create Manuscript entity with title, genre, coverLetter = title[0], status = '构思中', timestamps = now. If quick create (no template): create one empty Chapter with title '第一章', sortOrder = 0. If from template: TemplateInstantiationService already creates chapters. Call manuscriptNotifier.add then chapterNotifier.add as needed. Navigate to `/manuscript/${manuscript.id}/editor`. Actions: TextButton('取消'), FilledButton('创建').
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/presentation/manuscript_library_page_test.dart test/features/manuscript/presentation/manuscript_card_test.dart</automated>
  </verify>
  <done>
    ManuscriptLibraryPage with responsive card grid, empty state, sort, FAB, long-press context menu. ManuscriptCard with genre-colored cover, progress bar, status badge. ManuscriptCreateDialog with quick create AND template creation (D-20 trigger). Library page and card tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ManuscriptSettingsPage, add route constants, wire routes in app.dart</name>
  <files>
    lib/features/manuscript/presentation/manuscript_settings_page.dart
    lib/shared/constants/app_constants.dart
    lib/app.dart
  </files>
  <read_first>
    @lib/app.dart -- Current GoRouter structure. StatefulShellRoute.indexedStack with 6 branches. Branch 1 currently renders EditorPage at /editor path. Note Pitfall 4: manuscript editor route must be TOP-LEVEL (outside StatefulShellRoute) to hide bottom nav. The redirect guard reads 'onboarding_completed' from Hive settings box.
    @lib/shared/constants/app_constants.dart -- Existing route constants. Add manuscript route constants here.
    @lib/features/knowledge/presentation/world_setting_form.dart -- Form page pattern: ConsumerStatefulWidget with TextEditingController, form fields, save button, loading state
    @lib/features/manuscript/domain/manuscript.dart -- Manuscript entity fields for settings form
    @lib/features/manuscript/domain/manuscript_genre.dart -- Genre presets for dropdown
    @.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md -- Route structure section: top-level /manuscript/:id/editor and /manuscript/:id/settings outside StatefulShellRoute. Branch 1 builder changes from EditorPage to ManuscriptLibraryPage.
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- App.dart extension pattern: add top-level GoRoutes before StatefulShellRoute, change Branch 1 to ManuscriptLibraryPage
  </read_first>
  <acceptance_criteria>
    - app_constants.dart contains `static const String manuscriptEditor = '/manuscript/:id/editor'` and `static const String manuscriptSettings = '/manuscript/:id/settings'`
    - app.dart has two top-level GoRoutes BEFORE StatefulShellRoute: /manuscript/:id/editor and /manuscript/:id/settings
    - /manuscript/:id/editor route extracts manuscriptId from pathParameters and returns EditorWithSidebar (placeholder for now -- will be built in Plan 04)
    - /manuscript/:id/settings route extracts manuscriptId and returns ManuscriptSettingsPage
    - Branch 1 GoRoute builder changed from `EditorPage()` to `ManuscriptLibraryPage()`
    - Branch 1 GoRoute still uses path AppConstants.editor ('/editor') for bottom nav compatibility
    - Initial location remains AppConstants.editor (library is now the default screen)
    - ManuscriptSettingsPage has form fields for title, genre, description, target word count
    - ManuscriptSettingsPage AppBar has title '文稿设置' and back button
    - `flutter analyze lib/app.dart lib/shared/constants/app_constants.dart lib/features/manuscript/presentation/manuscript_settings_page.dart` exits 0
  </acceptance_criteria>
  <action>
    Create `lib/features/manuscript/presentation/manuscript_settings_page.dart` as a ConsumerStatefulWidget accepting `required String manuscriptId`. AppBar: title '文稿设置' with back IconButton (Icons.arrow_back). Body: SingleChildScrollView with Padding(24) containing Form with: TextField for title (pre-filled from manuscript entity), DropdownButtonFormField for genre (14 presets), TextField for description (multiline, maxLines 3), TextField for target word count (number keyboard type). Save button: FilledButton '保存' that calls manuscriptNotifier.save with updated fields. Load manuscript by filtering manuscriptNotifier state by id.

    In `lib/shared/constants/app_constants.dart`: Add two constants after the existing route paths: `static const String manuscriptEditor = '/manuscript/:id/editor';` and `static const String manuscriptSettings = '/manuscript/:id/settings';`.

    In `lib/app.dart`: Add imports for ManuscriptLibraryPage, ManuscriptSettingsPage, and app_constants (already imported). Add two top-level GoRoutes BEFORE the StatefulShellRoute (inside the routes list, after the onboarding route on line 54):
    ```
    GoRoute(
      path: '/manuscript/:id/editor',
      builder: (context, state) {
        final manuscriptId = state.pathParameters['id']!;
        return Scaffold(body: Center(child: Text('Editor placeholder for $manuscriptId')));
      },
    ),
    GoRoute(
      path: '/manuscript/:id/settings',
      builder: (context, state) {
        final manuscriptId = state.pathParameters['id']!;
        return ManuscriptSettingsPage(manuscriptId: manuscriptId);
      },
    ),
    ```
    Change Branch 1 builder (line 75) from `const EditorPage()` to `const ManuscriptLibraryPage()`. Keep the path as `AppConstants.editor`. Add import for ManuscriptLibraryPage. The /manuscript/:id/editor route is a placeholder Scaffold -- it will be replaced with EditorWithSidebar in Plan 04.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart lib/shared/constants/app_constants.dart lib/features/manuscript/presentation/manuscript_settings_page.dart</automated>
  </verify>
  <done>
    ManuscriptSettingsPage with form for all manuscript metadata fields. Route constants added. GoRouter wired with top-level manuscript routes and Branch 1 showing library. Static analysis passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manuscript title input | User-supplied string stored in Hive via repository |
| Genre dropdown | Selection from preset list or custom user input |
| Template selection | User selects a template that triggers automated entity creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | ManuscriptCreateDialog title field | mitigate | Non-empty validation on title TextField. Max length enforced at UI layer. |
| T-11-07 | Denial of Service | Manuscript soft-delete confirmation | mitigate | Confirmation dialog with 30-day recovery warning. softDelete sets timestamp, does not remove data. |
| T-11-14 | Tampering | Template instantiation creating entities | mitigate | TemplateInstantiationService validates template data before creating WorldSetting/CharacterCards/Chapters. All entities use uuid for IDs. |
</threat_model>

<verification>
1. `flutter test test/features/manuscript/presentation/manuscript_library_page_test.dart test/features/manuscript/presentation/manuscript_card_test.dart` passes
2. `flutter analyze lib/features/manuscript/presentation/ lib/app.dart lib/shared/constants/app_constants.dart` exits 0
3. Branch 1 renders ManuscriptLibraryPage at /editor path
4. Top-level /manuscript/:id/editor route exists outside StatefulShellRoute
5. ManuscriptCreateDialog validates non-empty title
6. ManuscriptCreateDialog has '从模板创建' option that triggers TemplateInstantiationService (D-20)
7. Delete shows 30-day recovery confirmation dialog
8. Responsive grid: 3 columns at 1000px+, 2 at 600-999px, 1 below 600px
</verification>

<success_criteria>
- Library page replaces editor as Branch 1 default home screen
- Card grid displays genre-colored covers with all required card elements
- Quick create dialog creates manuscript + empty chapter
- Template create dialog triggers D-20: creates manuscript + WorldSetting + CharacterCards + chapter skeleton via TemplateInstantiationService
- Long-press context menu with edit/export/delete options
- Sort dropdown with 3 modes
- Empty state with illustration and CTA
- Route wiring complete: library at /editor, manuscript editor and settings at top-level routes
- Library page and card widget tests pass
- Static analysis passes on all modified files
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-03-SUMMARY.md` when done
</output>
