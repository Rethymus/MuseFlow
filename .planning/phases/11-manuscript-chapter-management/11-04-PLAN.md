---
phase: 11-manuscript-chapter-management
plan: 04
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - lib/features/manuscript/presentation/editor_with_sidebar.dart
  - lib/features/manuscript/presentation/chapter_sidebar.dart
  - lib/features/manuscript/presentation/chapter_sidebar_row.dart
  - lib/features/manuscript/presentation/chapter_create_dialog.dart
  - lib/features/manuscript/presentation/chapter_rename_dialog.dart
  - lib/features/manuscript/presentation/chapter_context_menu.dart
  - lib/features/editor/presentation/editor_provider.dart
  - lib/features/editor/presentation/status_bar.dart
  - lib/app.dart
autonomous: true
requirements: [SC-2, SC-3, SC-4]
user_setup: []

must_haves:
  truths:
    - "D-13: User sees a left sidebar (260px fixed width, colorScheme.surface background) with manuscript title header, ReorderableListView of chapter rows, and '新建章节' OutlinedButton"
    - "D-14: Each chapter row displays title (14px w400) left-aligned + word count (12px w500, colorScheme.onSurfaceVariant) right-aligned. Active chapter has primary.withOpacity(0.15) background highlight and w600 title"
    - "D-15: User can drag chapters in sidebar to reorder. After drop, ChapterNotifier.reorder is called and sortOrder values are recalculated to sequential"
    - "D-16: User can create (ChapterCreateDialog), rename (ChapterRenameDialog), split (at cursor), merge (with adjacent), duplicate (with '(副本)' suffix), and delete (with confirmation) chapters via context menu and sidebar interactions"
    - "SC-3: Editor switches SuperEditor Document when user taps a different chapter. Forced save of current chapter content happens before loading the new chapter. Document is swapped using ValueKey(chapterId) pattern per RESEARCH.md Open Question 1"
    - "SC-4: Chapter content auto-saves 2-3 seconds after last edit via ChapterAutoSave.onDocumentChanged. Force save on chapter switch, back-to-library navigation (PopScope), and app lifecycle pause (WidgetsBindingObserver)"
    - "D-26: Keyboard shortcuts Ctrl+Up (previous chapter), Ctrl+Down (next chapter), Ctrl+Shift+N (new chapter) work in EditorWithSidebar"
    - "D-25: StatusBar shows '总字数: {current}/{target} 字' manuscript progress when manuscript context is available, alongside existing AI diff count"
    - "Switching chapters does not cause editor toolbar flickering or layout reset"
  artifacts:
    - path: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      provides: "Full-screen editor with chapter sidebar, auto-save, document switching, lifecycle management"
      min_lines: 200
    - path: "lib/features/manuscript/presentation/chapter_sidebar.dart"
      provides: "Fixed-width sidebar with ReorderableListView, chapter rows, new chapter button"
      min_lines: 100
    - path: "lib/features/manuscript/presentation/chapter_sidebar_row.dart"
      provides: "Chapter row with title, word count, active highlight, drag handle"
      min_lines: 50
    - path: "lib/features/manuscript/presentation/chapter_create_dialog.dart"
      provides: "Dialog for creating new chapter with title input"
      min_lines: 40
    - path: "lib/features/manuscript/presentation/chapter_rename_dialog.dart"
      provides: "Dialog for renaming chapter with pre-filled title"
      min_lines: 40
    - path: "lib/features/manuscript/presentation/chapter_context_menu.dart"
      provides: "PopupMenuButton with rename, split, merge, duplicate, delete options"
      min_lines: 50
    - path: "lib/features/editor/presentation/editor_provider.dart"
      provides: "createEditorWithDocument function for chapter document loading"
      contains: "createEditorWithDocument"
    - path: "lib/features/editor/presentation/status_bar.dart"
      provides: "Extended status bar with manuscript word count progress"
      contains: "总字数"
  key_links:
    - from: "EditorWithSidebar"
      to: "ChapterAutoSave"
      via: "ref.read(chapterAutoSaveProvider.future)"
      pattern: "chapterAutoSaveProvider"
    - from: "EditorWithSidebar"
      to: "SuperEditor Document"
      via: "onDocumentChanged listener serializes to Markdown via serializeDocumentToMarkdown"
      pattern: "serializeDocumentToMarkdown"
    - from: "ChapterSidebar"
      to: "ChapterNotifier"
      via: "ref.watch(chapterNotifierProvider)"
      pattern: "chapterNotifierProvider"
    - from: "EditorWithSidebar"
      to: "EditorHolderNotifier"
      via: "ref.read(editorProvider.notifier).setEditor(_editor)"
      pattern: "editorProvider.notifier.*setEditor"
    - from: "EditorWithSidebar"
      to: "/manuscript/:id/editor"
      via: "PopScope with forced save before context.go"
      pattern: "forceSave"
---

<objective>
Build the EditorWithSidebar that wraps SuperEditor with a chapter navigation sidebar, implementing document switching with forced-save guarantees, auto-save, chapter operations (create/rename/reorder/split/merge/duplicate/delete), keyboard shortcuts, and status bar progress.

Purpose: Enable per-chapter editing with seamless switching, so users can manage and edit chapters within a manuscript context.

Output: EditorWithSidebar, ChapterSidebar, ChapterSidebarRow, ChapterCreateDialog, ChapterRenameDialog, ChapterContextMenu, updated editor_provider.dart, updated status_bar.dart.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-UI-SPEC.md
@.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/features/editor/presentation/editor_page.dart (existing editor lifecycle):
```dart
class EditorPage extends ConsumerStatefulWidget {
  // Creates Editor in initState, sets on editorProvider in postFrameCallback
  // Disposes composer, clears editorProvider in dispose
  // Has Shortcuts widget with Ctrl+B, Ctrl+I, Ctrl+Shift+Z, Ctrl+K
  // Has PopScope with unresolved diff check
  // Layout: Column[EditorToolbar, DeviationWarningWidget, Divider, Expanded(Center(SuperEditor)), StatusBar]
}

class EditorHolderNotifier extends Notifier<Editor?> {
  void setEditor(Editor? editor);
}
final editorProvider = NotifierProvider<EditorHolderNotifier, Editor?>;
```

From lib/features/editor/presentation/editor_provider.dart:
```dart
Editor createDefaultEditor(); // Creates MutableDocument with placeholder text
// ADD: createEditorWithDocument(MutableDocument document) for chapter switching
```

From lib/features/manuscript/application/chapter_auto_save.dart:
```dart
class ChapterAutoSave {
  void onDocumentChanged(String chapterId, String markdown);
  Future<void> forceSave();
  void dispose();
}
// Provider: chapterAutoSaveProvider (FutureProvider)
```

From lib/features/manuscript/application/chapter_notifier.dart:
```dart
class ChapterNotifier extends AsyncNotifier<List<Chapter>> {
  Future<void> loadChapters(String manuscriptId);
  Future<void> add(Chapter chapter);
  Future<void> save(Chapter chapter);
  Future<void> delete(String id);
  Future<void> reorder(String manuscriptId, int oldIndex, int newIndex);
  Future<void> duplicateChapter(String chapterId);
  Future<void> splitChapter(String chapterId, String beforeContent, String afterContent);
  Future<void> mergeChapters(String chapterId1, String chapterId2);
}
// Provider: chapterNotifierProvider
```

From lib/features/manuscript/application/manuscript_notifier.dart:
```dart
class ManuscriptNotifier extends AsyncNotifier<List<Manuscript>> {
  Future<void> create(Manuscript manuscript);
  Future<void> save(Manuscript manuscript);
  Future<void> softDelete(String id);
  List<Manuscript> searchByTitle(String query);
}
```

From lib/features/manuscript/domain/chapter.dart:
```dart
class Chapter {
  final String id, manuscriptId, title, status; // 草稿/初稿/精修/定稿
  final int sortOrder;
  final String documentContent; // Serialized Markdown
  int get wordCount; // computed
  Chapter copyWith({...});
}
```

From lib/features/manuscript/domain/manuscript_genre.dart:
```dart
class ManuscriptGenre {
  static const List<String> chapterStatusValues = ['草稿','初稿','精修','定稿'];
}
```

From lib/features/editor/presentation/status_bar.dart:
```dart
class StatusBar extends ConsumerWidget {
  // Watches editorAINotifierProvider, shows pending diff count
  // Returns SizedBox.shrink() when no diffs
}
// EXTEND: Add manuscript progress display
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChapterSidebar, ChapterSidebarRow, ChapterCreateDialog, ChapterRenameDialog, ChapterContextMenu</name>
  <files>
    lib/features/manuscript/presentation/chapter_sidebar.dart
    lib/features/manuscript/presentation/chapter_sidebar_row.dart
    lib/features/manuscript/presentation/chapter_create_dialog.dart
    lib/features/manuscript/presentation/chapter_rename_dialog.dart
    lib/features/manuscript/presentation/chapter_context_menu.dart
    test/features/manuscript/presentation/chapter_sidebar_test.dart
  </files>
  <behavior>
    - ChapterSidebar is a ConsumerWidget accepting manuscriptId. Watches chapterNotifierProvider. Column layout: manuscript title header (20px w600, padding 16px top/sides, 0 bottom), SizedBox(height: 16), Expanded(ReorderableListView.builder) with chapter rows, SizedBox(height: 16), Padding(16px) wrapping OutlinedButton.icon "新建章节". Container width is 260px, background colorScheme.surface
    - ReorderableListView.builder: onReorder calls chapterNotifier.reorder(manuscriptId, oldIndex, newIndex). proxyDecorator wraps drag preview with Material elevation
    - ChapterSidebarRow: StatelessWidget accepting Chapter, isActive bool, VoidCallback onTap, VoidCallback onLongPress. Layout: Container with rounded decoration (8px radius), padding 16px horizontal 12px vertical. Active state: colorScheme.primary.withOpacity(0.15) background. Row with Expanded(Text(chapter.title, 14px, w600 if active else w400, max 1 line ellipsis)) and Text('${chapter.wordCount}', 12px w500, colorScheme.onSurfaceVariant)
    - ChapterCreateDialog: showDialog with AlertDialog title "新建章节". Single TextField for chapter title with placeholder "章节标题". FilledButton "创建". On confirm, creates Chapter(id='', manuscriptId, title, sortOrder=currentChapter.sortOrder+1, status='草稿') and calls chapterNotifier.add
    - ChapterRenameDialog: showDialog with AlertDialog title "重命名章节". TextField pre-filled with current title. FilledButton "保存". On confirm, calls chapterNotifier.save(chapter.copyWith(title: newTitle))
    - ChapterContextMenu: PopupMenuButton with items: PopupMenuItem "重命名" (Icons.edit), PopupMenuItem "在光标处拆分" (Icons.cut, disabled if no cursor), PopupMenuItem "与下一章合并" (Icons.merge, disabled if last chapter), PopupMenuItem "复制章节" (Icons.copy), PopupMenuDivider, PopupMenuItem "删除" (Icons.delete, destructive red style)
  </behavior>
  <read_first>
    - lib/features/manuscript/domain/chapter.dart (Chapter entity with wordCount getter)
    - lib/features/manuscript/domain/manuscript_genre.dart (chapterStatusValues)
    - lib/features/manuscript/application/chapter_notifier.dart (all CRUD + split/merge/duplicate/reorder methods)
    - lib/features/templates/presentation/template_gallery_page.dart (_PassiveTag pill badge pattern for reuse)
  </read_first>
  <action>
    Create ChapterSidebar as ConsumerWidget. Accept manuscriptId. In initState equivalent (build method), call ref.read(chapterNotifierProvider.notifier).loadChapters(manuscriptId) if state is empty. Watch chapterNotifierProvider for the chapter list. Build Column with manuscript title (read from manuscriptNotifierProvider to get the manuscript title), SizedBox(height: 16), Expanded containing ReorderableListView.builder. The list builder maps chapters to ChapterSidebarRow widgets. Below the list, OutlinedButton "新建章节" that shows ChapterCreateDialog.

    ChapterCreateDialog needs the current chapter's sortOrder to insert after it. Pass a callback that receives the new title, and the sidebar handler creates the chapter with sortOrder = currentActiveChapter.sortOrder + 1 (or 0 if no active chapter).

    Create ChapterSidebarRow as StatelessWidget per UI-SPEC.

    Create ChapterCreateDialog and ChapterRenameDialog as simple ConsumerStatefulWidget dialogs with single TextField.

    Create ChapterContextMenu as a function (showChapterContextMenu) that takes BuildContext, required callbacks for each action, and shows a PopupMenuButton.

    Write test for ChapterSidebar (renders chapter list, renders new chapter button).
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/presentation/chapter_sidebar_test.dart</automated>
  </verify>
  <done>
    - ChapterSidebar renders manuscript title, reorderable chapter list, and new chapter button
    - ChapterSidebarRow shows title + word count, highlights active chapter
    - ChapterCreateDialog creates chapter after current position
    - ChapterRenameDialog updates chapter title
    - ChapterContextMenu shows all operations with correct enable/disable state
    - Chapter sidebar test passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create EditorWithSidebar with document switching, auto-save integration, keyboard shortcuts, and extend StatusBar</name>
  <files>
    lib/features/manuscript/presentation/editor_with_sidebar.dart
    lib/features/editor/presentation/editor_provider.dart
    lib/features/editor/presentation/status_bar.dart
    test/features/manuscript/presentation/editor_with_sidebar_test.dart
  </files>
  <behavior>
    - EditorWithSidebar is a ConsumerStatefulWidget accepting String manuscriptId. Scaffold with AppBar (back button "返回文稿库" that calls context.go('/editor') after forced save, title from manuscript, trailing: settings icon navigating to /manuscript/:id/settings) and body as Row[ChapterSidebar(260px), VerticalDivider(1px), Expanded(editor area)]
    - Editor area reproduces the EditorPage layout: Column[EditorToolbar, Divider, Expanded(Center(SuperEditor))]. Uses the same SuperEditor configuration (stylesheet, selection styles, document overlay builders) from EditorPage
    - initState: load chapters via chapterNotifier.loadChapters(manuscriptId). If chapters exist, load the first chapter's document. Track _currentChapterId as local state. Create Editor with ValueKey(_currentChapterId) so SuperEditor rebuilds on chapter switch
    - Document loading: deserialize chapter.documentContent via deserializeMarkdownToDocument (from super_editor_markdown). If content is empty, create MutableDocument with single empty ParagraphNode
    - Document change listener: FunctionalEditListener that serializes current document to Markdown via serializeDocumentToMarkdown, then calls ChapterAutoSave.onDocumentChanged(chapterId, markdown). Debounce is 2-3 seconds per D-19
    - Chapter switch: when user taps a different chapter row (detected via callback from ChapterSidebar), call chapterAutoSave.forceSave(), then update _currentChapterId, deserialize new chapter's content, rebuild SuperEditor with new ValueKey. Update editorProvider with new Editor instance
    - PopScope (back navigation): call chapterAutoSave.forceSave() before context.go('/editor')
    - WidgetsBindingObserver: register in initState, in didChangeAppLifecycleState(Paused or Inactive) call chapterAutoSave.forceSave()
    - Keyboard shortcuts: Shortcuts widget wrapping Scaffold with: Ctrl+Up -> previous chapter, Ctrl+Down -> next chapter, Ctrl+Shift+N -> new chapter dialog
    - editor_provider.dart: add `Editor createEditorWithDocument(MutableDocument document)` function that calls `createDefaultDocumentEditor(document: document)`. This is used by EditorWithSidebar to create an Editor with a specific chapter's document
    - status_bar.dart: extend StatusBar to accept optional manuscriptId or totalWordCount/targetWordCount params. When provided, show "总字数: {current}/{target} 字" prefix before existing AI diff count. Current and target are computed from all chapters in the manuscript
  </behavior>
  <read_first>
    - lib/features/editor/presentation/editor_page.dart (complete Editor lifecycle, Shortcuts, PopScope, SuperEditor config, overlay builders, stylesheet)
    - lib/features/editor/presentation/editor_provider.dart (createDefaultEditor function)
    - lib/features/editor/presentation/status_bar.dart
  - lib/app.dart (current StatusBar with AI diff count)
    - lib/features/editor/presentation/editor_toolbar.dart (EditorToolbar widget interface)
    - lib/features/editor/presentation/floating_toolbar.dart (FloatingToolbar needs editor + selectionLinks)
    - lib/features/editor/presentation/diff_display.dart (DiffOverlayBuilder, AcceptRejectBar)
    - lib/features/editor/presentation/context_anchor_indicator.dart (ContextAnchorOverlayBuilder)
    - lib/features/manuscript/application/chapter_auto_save.dart (onDocumentChanged, forceSave, dispose)
    - lib/features/manuscript/application/chapter_notifier.dart (loadChapters, add, save)
    - lib/features/manuscript/infrastructure/chapter_repository.dart (getById, updateDocumentContent)
  </read_first>
  <action>
    In editor_provider.dart, add a new function:
    `Editor createEditorWithDocument(MutableDocument document) => createDefaultDocumentEditor(document: document);`
    This enables creating an Editor pre-loaded with a specific chapter's document content.

    In status_bar.dart, modify the StatusBar widget to optionally display manuscript progress. Add optional parameters (or use a provider) for manuscript context. When manuscript context is active, show a Row with "总字数: {formatted current}/{formatted target} 字" followed by the existing AI diff indicator. Use 12px w500 text, colorScheme.onSurfaceVariant. The progress is computed from the chapter list: sum all chapter.wordCount values vs manuscript.targetWordCount.

    Create EditorWithSidebar as ConsumerStatefulWidget. This is the core of this task. The widget manages:
    1. Editor lifecycle (create, switch, dispose)
    2. Chapter state (_currentChapterId, _currentDocument)
    3. Auto-save (ChapterAutoSave listener)
    4. App lifecycle (WidgetsBindingObserver)
    5. Keyboard shortcuts
    6. Document serialization/deserialization

    Build the widget step by step:
    - StatefulWidget with _currentChapterId (String?) and _editor (Editor?) local state
    - initState: add WidgetsBindingObserver, load chapters, select first chapter
    - _loadChapter(Chapter chapter): deserialize chapter.documentContent using super_editor_markdown's deserializeMarkdownToDocument, create Editor via createEditorWithDocument, set on editorProvider, attach edit listener that calls chapterAutoSave.onDocumentChanged
    - _switchChapter(String newChapterId): forceSave current, find chapter from list, _loadChapter it
    - dispose: remove WidgetsBindingObserver, forceSave, clear editorProvider, dispose editor
    - build: Row[ChapterSidebar(manuscriptId, onChapterTap: _switchChapter, onNewChapter: _showCreateDialog), VerticalDivider, Expanded(Column[EditorToolbar, Divider, Expanded(SuperEditor with ValueKey(_currentChapterId)), StatusBar])]

    The SuperEditor widget configuration must mirror EditorPage's configuration: same stylesheet (_buildThemedStylesheet), same selectionStyle, same selectionLayerLinks, same documentOverlayBuilders (SelectionLeaders, ContextAnchorOverlay, DiffOverlay, FloatingToolbar, AcceptRejectBar, DefaultCaretOverlay).

    Wrap everything in Shortcuts+Actions for keyboard shortcuts and PopScope for back navigation.

    NOTE on document switching: Per RESEARCH.md Open Question 1 (RESOLVED): "Use ValueKey(chapterId) on the SuperEditor widget to force a clean rebuild when switching chapters. SuperEditor 0.3.0 separates Editor/Document/Composer, but the Editor is tightly coupled to its Document at the widget level. Key-based rebuild clears undo history per-document (acceptable at chapter boundaries)."

    Write a basic widget test that verifies EditorWithSidebar can be constructed with a manuscriptId.

    Finally, update app.dart to import EditorWithSidebar and replace the placeholder in the /manuscript/:id/editor route with the real EditorWithSidebar widget.
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/presentation/ 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - EditorWithSidebar renders chapter sidebar alongside SuperEditor
    - Chapter switching triggers forced save, document deserialization, and editor rebuild
    - Auto-save fires 2-3s after edits via ChapterAutoSave
    - Force save on chapter switch, back navigation, and app pause
    - Keyboard shortcuts Ctrl+Up/Down/N work
    - StatusBar shows manuscript word count progress when in manuscript context
    - editor_provider.dart has createEditorWithDocument function
    - app.dart /manuscript/:id/editor route renders EditorWithSidebar
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document content -> Markdown serialization | SuperEditor Document traversed for serialization, user-controlled content |
| Chapter title input -> Chapter entity | Title from user input flows into domain entity and Hive storage |
| Route parameter -> manuscriptId | Manuscript ID from URL used for all chapter queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Denial of Service | serializeDocumentToMarkdown | accept | Serialization is synchronous but bounded by document size. Debounce prevents excessive calls. |
| T-11-05 | Tampering | ChapterRenameDialog | mitigate | Chapter title validation: non-empty, max 100 chars via TextInputFormatter. |
| T-11-06 | Information Disclosure | Auto-save markdown | accept | All data is local-only, user's own content. No external transmission. |
| T-11-07 | Tampering | ChapterCreateDialog | mitigate | Title field non-empty validation, max 100 chars. |
</threat_model>

<verification>
1. flutter test test/features/manuscript/presentation/ passes
2. flutter test passes (no regressions)
3. EditorWithSidebar file exists with ConsumerStatefulWidget, WidgetsBindingObserver, Shortcuts
4. ChapterSidebar file exists with ReorderableListView
5. All dialog files exist (create, rename, context menu)
6. editor_provider.dart has createEditorWithDocument function
7. status_bar.dart shows manuscript progress when context available
8. app.dart /manuscript/:id/editor route renders EditorWithSidebar (not placeholder)
</verification>

<success_criteria>
- Editor displays chapter sidebar (260px) with manuscript title and chapter list
- Active chapter is highlighted with primary color background
- Tapping a chapter switches the editor document with forced save
- Auto-save fires 2-3s after content changes
- Force save on chapter switch, back navigation, and app lifecycle pause
- All chapter operations work: create, rename, reorder, split, merge, duplicate, delete
- Keyboard shortcuts Ctrl+Up/Down/N navigate chapters and create new ones
- StatusBar shows manuscript word count progress alongside AI diff count
- No toolbar flickering on chapter switch (ValueKey pattern)
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-04-SUMMARY.md` when done
</output>
