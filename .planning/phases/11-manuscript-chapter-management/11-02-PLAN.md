---
phase: 11-manuscript-chapter-management
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - lib/features/manuscript/infrastructure/manuscript_repository.dart
  - lib/features/manuscript/infrastructure/chapter_repository.dart
  - lib/features/manuscript/infrastructure/manuscript_purge_service.dart
  - lib/features/manuscript/application/manuscript_notifier.dart
  - lib/features/manuscript/application/chapter_notifier.dart
  - lib/features/manuscript/application/chapter_auto_save.dart
  - lib/features/manuscript/application/manuscript_sort.dart
  - lib/core/presentation/providers.dart
autonomous: true
requirements: [SC-1, SC-2, SC-4]
user_setup: []

must_haves:
  truths:
    - "ManuscriptRepository provides getAll() that filters out soft-deleted manuscripts (deletedAt != null)"
    - "D-21: ManuscriptRepository provides softDelete(String id) that sets deletedAt timestamp; getAll() filters out soft-deleted manuscripts (deletedAt != null)"
    - "ManuscriptRepository provides getAllIncludingDeleted() for purge service"
    - "ChapterRepository provides getByManuscriptId(String manuscriptId) returning chapters ordered by sortOrder"
    - "ChapterRepository provides updateDocumentContent(String chapterId, String markdown) for auto-save"
    - "D-22: ChapterRepository provides deleteByManuscriptId(String manuscriptId) for cascade delete; chapter deletion is immediate hard delete with confirmation dialog"
    - "ManuscriptNotifier extends AsyncNotifier<List<Manuscript>> with create, save, softDelete, purgeDeleted methods"
    - "D-16: ChapterNotifier extends AsyncNotifier<List<Chapter>> with add, save, delete, reorder, duplicate, splitChapter, mergeChapters methods"
    - "ChapterNotifier.reorder recalculates sortOrder values to sequential (0,1,2,...) after drop"
    - "D-19: ChapterAutoSave has debounced save (2-3 seconds) and forceSave method"
    - "ChapterAutoSave.forceSave cancels debounce timer before flushing"
    - "ManuscriptPurgeService.purgeExpired deletes manuscripts with deletedAt older than 30 days and their chapters"
    - "All new providers registered in providers.dart"
  artifacts:
    - path: "lib/features/manuscript/infrastructure/manuscript_repository.dart"
      provides: "Hive box wrapper for manuscripts CRUD + soft delete + purge queries"
      min_lines: 80
    - path: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      provides: "Hive box wrapper for chapters CRUD + manuscript-scoped queries + document content update"
      min_lines: 90
    - path: "lib/features/manuscript/infrastructure/manuscript_purge_service.dart"
      provides: "Soft-delete auto-purge job (30-day retention)"
      min_lines: 40
    - path: "lib/features/manuscript/application/manuscript_notifier.dart"
      provides: "AsyncNotifier for manuscript list CRUD, soft delete, sort-aware queries"
      min_lines: 80
    - path: "lib/features/manuscript/application/chapter_notifier.dart"
      provides: "AsyncNotifier for chapter list CRUD, reorder, split, merge, duplicate"
      min_lines: 120
    - path: "lib/features/manuscript/application/chapter_auto_save.dart"
      provides: "Debounced + forced auto-save service for chapter documents"
      min_lines: 60
    - path: "lib/features/manuscript/application/manuscript_sort.dart"
      provides: "ManuscriptSortMode enum + comparator function"
      min_lines: 25
    - path: "lib/core/presentation/providers.dart"
      provides: "New provider declarations for manuscript/chapter infrastructure"
      contains: "manuscriptRepositoryProvider"
  key_links:
    - from: "lib/features/manuscript/infrastructure/manuscript_repository.dart"
      to: "lib/features/manuscript/domain/manuscript.dart"
      via: "Repository stores/retrieves Manuscript entities via toJson/fromJson"
      pattern: "Manuscript.fromJson"
    - from: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      to: "lib/features/manuscript/domain/chapter.dart"
      via: "Repository stores/retrieves Chapter entities via toJson/fromJson"
      pattern: "Chapter.fromJson"
    - from: "lib/features/manuscript/application/chapter_auto_save.dart"
      to: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      via: "Auto-save flushes pending Markdown through updateDocumentContent"
      pattern: "updateDocumentContent"
    - from: "lib/features/manuscript/infrastructure/manuscript_purge_service.dart"
      to: "lib/features/manuscript/infrastructure/chapter_repository.dart"
      via: "Purge cascades: deleteByManuscriptId before hardDelete manuscript"
      pattern: "deleteByManuscriptId"
---

<objective>
Build the application and infrastructure layers for manuscript and chapter management: Hive repositories, Riverpod notifiers, auto-save service, purge service, and sort utility. Register all providers.

Purpose: These services power all UI interactions in later plans. The repository pattern follows established codebase conventions, ensuring consistency and testability.
Output: ManuscriptRepository, ChapterRepository, ManuscriptPurgeService, ManuscriptNotifier, ChapterNotifier, ChapterAutoSave, ManuscriptSort utility. All providers registered in providers.dart.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-manuscript-chapter-management/11-CONTEXT.md
@.planning/phases/11-manuscript-chapter-management/11-RESEARCH.md
@.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md

# Repository pattern to follow exactly:
@lib/features/knowledge/infrastructure/character_card_repository.dart

# AsyncNotifier pattern to follow exactly:
@lib/features/knowledge/application/character_card_notifier.dart

# Debounced write pattern to follow:
@lib/features/stats/application/writing_stats_collector.dart

# Provider registration pattern to follow:
@lib/core/presentation/providers.dart

# Domain entities created in Plan 01:
@lib/features/manuscript/domain/manuscript.dart
@lib/features/manuscript/domain/chapter.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create repositories, purge service, and sort utility</name>
  <files>
    lib/features/manuscript/infrastructure/manuscript_repository.dart
    lib/features/manuscript/infrastructure/chapter_repository.dart
    lib/features/manuscript/infrastructure/manuscript_purge_service.dart
    lib/features/manuscript/application/manuscript_sort.dart
  </files>
  <read_first>
    @lib/features/knowledge/infrastructure/character_card_repository.dart -- Exact repository pattern: Box<dynamic> constructor, add with uuid, getAll, getById, update with DateTime.now() on updatedAt, delete, try-catch with StateError
    @lib/features/manuscript/domain/manuscript.dart -- Manuscript entity with soft delete (deletedAt field)
    @lib/features/manuscript/domain/chapter.dart -- Chapter entity with manuscriptId, sortOrder, documentContent
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- Repository deviations: ManuscriptRepository needs getAll() filtering out deletedAt != null, getAllIncludingDeleted(), softDelete(), purgeOlderThan(), hardDelete(). ChapterRepository needs getByManuscriptId(), updateDocumentContent(), deleteByManuscriptId()
  </read_first>
  <behavior>
    - Test 1: ManuscriptRepository.add creates a manuscript with uuid if id empty, sets createdAt/updatedAt, stores via toJson
    - Test 2: ManuscriptRepository.getAll returns only manuscripts where deletedAt is null
    - Test 3: ManuscriptRepository.softDelete sets deletedAt to DateTime.now() and updates the entity
    - Test 4: ManuscriptRepository.getAllIncludingDeleted returns all manuscripts including soft-deleted
    - Test 5: ChapterRepository.getByManuscriptId returns chapters filtered by manuscriptId, sorted by sortOrder ascending
    - Test 6: ChapterRepository.updateDocumentContent updates only the documentContent and updatedAt fields
    - Test 7: ChapterRepository.deleteByManuscriptId deletes all chapters with matching manuscriptId
    - Test 8: ManuscriptPurgeService.purgeExpired deletes chapters then manuscripts for items with deletedAt older than 30 days
    - Test 9: ManuscriptSortMode.compareManuscripts sorts by updatedAt descending for recentEdit
    - Test 10: ManuscriptSortMode.compareManuscripts sorts alphabetically for titleAlphabetical
  </behavior>
  <action>
    Create `lib/features/manuscript/infrastructure/manuscript_repository.dart` following the CharacterCardRepository pattern exactly. Box name: `'manuscripts'`. Include standard methods: add, getAll (filters out deletedAt != null), getById, update (sets updatedAt = DateTime.now()), delete (hard delete). Add soft delete methods: `softDelete(String id)` that calls update with deletedAt = DateTime.now(), `getAllIncludingDeleted()` that returns all without filtering, `hardDelete(String id)` that calls box.delete, `purgeOlderThan(Duration age)` that gets all including deleted and hard-deletes those with deletedAt before cutoff.

    Create `lib/features/manuscript/infrastructure/chapter_repository.dart` following the same pattern. Box name: `'chapters'`. Standard methods: add, getAll, getById, update, delete. Add manuscript-scoped methods: `getByManuscriptId(String manuscriptId)` returning chapters filtered by manuscriptId and sorted by sortOrder, `updateDocumentContent(String chapterId, String markdown)` that updates only documentContent and updatedAt fields (not full entity rewrite -- use getById, copyWith, then put), `deleteByManuscriptId(String manuscriptId)` that finds and deletes all matching chapters.

    Create `lib/features/manuscript/infrastructure/manuscript_purge_service.dart` taking ManuscriptRepository and ChapterRepository in constructor. Method `purgeExpired({Duration retention = const Duration(days: 30)})`: iterate getAllIncludingDeleted, for each with deletedAt before cutoff, call chapterRepository.deleteByManuscriptId then manuscriptRepository.hardDelete.

    Create `lib/features/manuscript/application/manuscript_sort.dart` with `enum ManuscriptSortMode { recentEdit, creationDate, titleAlphabetical }` and function `int compareManuscripts(Manuscript a, Manuscript b, ManuscriptSortMode mode)` using Dart switch expression.
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/infrastructure/ test/features/manuscript/application/manuscript_sort_test.dart</automated>
  </verify>
  <done>
    ManuscriptRepository with CRUD + soft delete + purge queries. ChapterRepository with CRUD + manuscript-scoped queries + document content update. ManuscriptPurgeService with 30-day purge. ManuscriptSort utility with enum + comparator. All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ManuscriptNotifier and ChapterNotifier</name>
  <files>
    lib/features/manuscript/application/manuscript_notifier.dart
    lib/features/manuscript/application/chapter_notifier.dart
  </files>
  <read_first>
    @lib/features/knowledge/application/character_card_notifier.dart -- Exact AsyncNotifier pattern: extends AsyncNotifier<List<Entity>>, build() returns repository.getAll(), add/save/delete call ref.invalidateSelf()
    @lib/features/manuscript/infrastructure/manuscript_repository.dart -- Repository created in Task 1
    @lib/features/manuscript/infrastructure/chapter_repository.dart -- Repository created in Task 1
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- Notifier deviations: ManuscriptNotifier needs softDelete, purgeDeleted, sort-aware getter. ChapterNotifier needs reorder with sortOrder recalculation, splitChapter, mergeChapters, duplicateChapter.
  </read_first>
  <behavior>
    - Test 1: ManuscriptNotifier.build returns manuscripts filtered (no soft-deleted)
    - Test 2: ManuscriptNotifier.create adds manuscript with one empty chapter
    - Test 3: ManuscriptNotifier.softDelete sets deletedAt and refreshes list
    - Test 4: ChapterNotifier.build filters by manuscriptId and orders by sortOrder
    - Test 5: ChapterNotifier.reorder recalculates sortOrder to sequential 0,1,2,... after reordering
    - Test 6: ChapterNotifier.duplicateChapter creates copy with title suffix "(副本)" and next sortOrder
    - Test 7: ChapterNotifier.splitChapter updates current chapter with beforeContent and creates new chapter with afterContent
    - Test 8: ChapterNotifier.mergeChapters combines content and deletes second chapter
  </behavior>
  <action>
    Create `lib/features/manuscript/application/manuscript_notifier.dart` following CharacterCardNotifier pattern. `manuscriptNotifierProvider` type: `AsyncNotifierProvider<ManuscriptNotifier, List<Manuscript>>`. build() watches manuscriptRepositoryProvider.future and calls getAll(). Add methods: create(Manuscript) calls add + ref.invalidateSelf. save(Manuscript) calls update + ref.invalidateSelf. softDelete(String id) calls repository.softDelete + ref.invalidateSelf. purgeDeleted() calls repository.purgeOlderThan + ref.invalidateSelf. searchByTitle(String query) filters state by title.toLowerCase().contains(query).

    Create `lib/features/manuscript/application/chapter_notifier.dart`. `chapterNotifierProvider` type: `AsyncNotifierProvider<ChapterNotifier, List<Chapter>>`. This notifier needs a manuscriptId parameter: use a FamilyAsyncNotifier or pass manuscriptId via a provider override. The simpler approach: make build() return empty list by default, add `loadChapters(String manuscriptId)` method that sets state to AsyncData with repository.getByManuscriptId. Add methods: add(Chapter), save(Chapter), delete(String id), reorder(int oldIndex, int newIndex) that swaps chapters in list and recalculates all sortOrder values sequentially (0,1,2,...), duplicateChapter(String chapterId) that clones with "(副本)" suffix, splitChapter(String chapterId, String beforeContent, String afterContent) that updates current chapter with beforeContent and creates new chapter with afterContent at next sortOrder, mergeChapters(String chapterId1, String chapterId2) that combines content and deletes chapterId2.
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/application/manuscript_notifier_test.dart test/features/manuscript/application/chapter_notifier_test.dart</automated>
  </verify>
  <done>
    ManuscriptNotifier with CRUD + soft delete + search. ChapterNotifier with CRUD + reorder + split + merge + duplicate. Notifier tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ChapterAutoSave and register all providers</name>
  <files>
    lib/features/manuscript/application/chapter_auto_save.dart
    lib/core/presentation/providers.dart
  </files>
  <read_first>
    @lib/features/stats/application/writing_stats_collector.dart -- Debounced write pattern: Timer-based _scheduleFlush, flush cancels timer, dispose cancels + unawaited flush
    @lib/core/presentation/providers.dart -- Provider registration pattern: FutureProvider for repositories, AsyncNotifierProvider for notifiers. Lines 271-300 for repository + notifier pattern, lines 448-455 for collector provider pattern
    @lib/features/manuscript/infrastructure/manuscript_repository.dart -- Repository created in Task 1
    @lib/features/manuscript/infrastructure/chapter_repository.dart -- Repository created in Task 1
    @.planning/phases/11-manuscript-chapter-management/11-PATTERNS.md -- ChapterAutoSave needs debounce 2-3s, forceSave, _isDirty flag
  </read_first>
  <behavior>
    - Test 1: ChapterAutoSave.onDocumentChanged sets dirty and starts debounce timer
    - Test 2: ChapterAutoSave.forceSave cancels timer and flushes immediately
    - Test 3: ChapterAutoSave does not flush when not dirty
    - Test 4: All providers are registered in providers.dart (manuscriptRepositoryProvider, chapterRepositoryProvider, manuscriptNotifierProvider, chapterNotifierProvider, chapterAutoSaveProvider, manuscriptPurgeServiceProvider)
  </behavior>
  <action>
    Create `lib/features/manuscript/application/chapter_auto_save.dart` following WritingStatsCollector debounced pattern. Class `ChapterAutoSave` takes ChapterRepository. Fields: debounceDuration (default 2 seconds), Timer? _debounceTimer, String? _currentChapterId, String? _pendingMarkdown, bool _isDirty. Methods: onDocumentChanged(String chapterId, String markdown) sets dirty, cancels timer, starts new timer, forceSave() cancels timer and calls _flush(), _flush() checks _isDirty and writes via repository.updateDocumentContent, dispose() cancels timer.

    In `lib/core/presentation/providers.dart`: Add imports for all new manuscript files. Add providers following existing patterns:
    - `manuscriptRepositoryProvider` = FutureProvider opening 'manuscripts' box (line ~277 after worldSettingRepositoryProvider)
    - `chapterRepositoryProvider` = FutureProvider opening 'chapters' box
    - `manuscriptNotifierProvider` = AsyncNotifierProvider<ManuscriptNotifier, List<Manuscript>>
    - `chapterNotifierProvider` = AsyncNotifierProvider<ChapterNotifier, List<Chapter>>
    - `chapterAutoSaveProvider` = FutureProvider<ChapterAutoSave> that creates instance, ref.onDispose(autoSave.dispose)
    - `manuscriptPurgeServiceProvider` = FutureProvider<ManuscriptPurgeService> injecting both repositories
  </action>
  <verify>
    <automated>flutter test test/features/manuscript/application/chapter_auto_save_test.dart && flutter analyze lib/core/presentation/providers.dart</automated>
  </verify>
  <done>
    ChapterAutoSave with 2s debounce + forceSave. All 6 providers registered in providers.dart. Auto-save tests pass, static analysis passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hive write operations | Repository writes pass through toJson serialization |
| Auto-save flush | Pending Markdown content written to Hive on timer or force trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Denial of Service | ChapterAutoSave race condition | mitigate | forceSave cancels debounce timer before flushing. _isDirty flag prevents redundant writes. Single _flush() entry point prevents concurrent writes. |
| T-11-04 | Information Disclosure | ManuscriptRepository.getAllIncludingDeleted | accept | Only used by purge service, never exposed to UI. Soft-deleted manuscripts have recovery period. |
| T-11-05 | Tampering | ChapterNotifier.reorder sortOrder manipulation | mitigate | Recalculate all sortOrder values to sequential after every reorder/delete/split operation (Pitfall 5 prevention). |
</threat_model>

<verification>
1. `flutter test test/features/manuscript/infrastructure/ test/features/manuscript/application/` passes
2. `flutter analyze lib/features/manuscript/ lib/core/presentation/providers.dart` exits 0
3. ManuscriptRepository.softDelete sets deletedAt and filters from getAll
4. ChapterNotifier.reorder produces sequential sortOrder (0,1,2,...)
5. ChapterAutoSave.forceSave cancels timer before flush
6. All 6 new providers registered in providers.dart
</verification>

<success_criteria>
- Repository layer follows established Hive CRUD pattern exactly
- Notifiers extend AsyncNotifier with all required operations
- Auto-save service has debounce + force-save with no race conditions
- Purge service cascades chapter deletion before manuscript hard delete
- All providers registered and resolving correctly
- All tests pass
- Static analysis passes
</success_criteria>

<output>
Create `.planning/phases/11-manuscript-chapter-management/11-02-SUMMARY.md` when done
</output>
