---
phase: 07-preset-world-template-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - assets/templates/world_presets/templates_zh.json
  - lib/features/templates/domain/world_template.dart
  - lib/features/templates/infrastructure/world_template_repository.dart
  - lib/core/presentation/providers.dart
  - test/features/templates/domain/world_template_test.dart
  - test/features/templates/infrastructure/world_template_repository_test.dart
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-04, TMPL-06]

must_haves:
  truths:
    - "Bundled template data contains exactly 14 Chinese templates: 8 male-channel and 6 female-channel"
    - "Each template has one world skeleton, exactly three character prototypes, 3-5 foreshadowing arcs, and three opening samples"
    - "Opening sample styles are scene, character, and suspense, matching Phase 8 categories"
    - "Template JSON includes independent schema version, language, channel, curated sort order, review metadata, and reviewed quality checks"
    - "Template data can instantiate valid WorldSetting and CharacterCard domain entities without exceeding field limits"
    - "No online template update or network dependency is introduced"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Flutter asset registration for bundled template JSON"
      contains: "assets/templates/world_presets/templates_zh.json"
    - path: "assets/templates/world_presets/templates_zh.json"
      provides: "14 reviewed Chinese preset world templates"
      contains: "templateSchemaVersion"
    - path: "lib/features/templates/domain/world_template.dart"
      provides: "Template domain model and JSON parsing"
      contains: "class WorldTemplate"
    - path: "lib/features/templates/infrastructure/world_template_repository.dart"
      provides: "Local asset-loading repository with search/filter helpers"
      contains: "rootBundle.loadString"
    - path: "lib/core/presentation/providers.dart"
      provides: "Riverpod provider for template repository"
      contains: "worldTemplateRepositoryProvider"
  key_links:
    - from: "assets/templates/world_presets/templates_zh.json"
      to: "lib/features/templates/domain/world_template.dart"
      via: "WorldTemplateLibrary.fromJson parses bundled asset schema"
      pattern: "WorldTemplateLibrary"
    - from: "lib/features/templates/domain/world_template.dart"
      to: "lib/features/knowledge/domain/world_setting.dart"
      via: "Template world draft converts into valid WorldSetting"
      pattern: "toWorldSetting"
    - from: "lib/features/templates/domain/world_template.dart"
      to: "lib/features/knowledge/domain/character_card.dart"
      via: "Template character draft converts into valid CharacterCard"
      pattern: "toCharacterCard"
---

## Phase Goal

**As a** writer starting a new story, **I want to** load a reviewed local library of genre templates, **so that** later UI and draft flows can create useful world and character starting points without relying on network template updates.

<objective>
Create the template domain model, bundled Chinese JSON asset, local repository, provider wiring, and validation tests for Phase 7. This plan is the data foundation for gallery, preview, draft creation, and AI completion.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-preset-world-template-library/07-CONTEXT.md
@.planning/phases/07-preset-world-template-library/07-RESEARCH.md
@.planning/phases/07-preset-world-template-library/07-VALIDATION.md
</context>

<interfaces>

From `lib/features/knowledge/domain/world_setting.dart`:
```dart
class WorldSetting {
  final String id;
  final String name;
  final String description;
  final String rules;
  final String factions;
  final String geography;
  final String techLevel;
  final List<String> aliases;
  final DateTime createdAt;
  final DateTime? updatedAt;
}
```

From `lib/features/knowledge/domain/character_card.dart`:
```dart
class CharacterCard {
  final String id;
  final String name;
  final String personality;
  final String appearance;
  final String backstory;
  final List<String> aliases;
  final DateTime createdAt;
  final DateTime? updatedAt;
}
```

From `pubspec.yaml`:
```yaml
flutter:
  uses-material-design: true
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create template domain model and JSON parser</name>
  <files>
    lib/features/templates/domain/world_template.dart,
    test/features/templates/domain/world_template_test.dart
  </files>
  <read_first>
    lib/features/knowledge/domain/world_setting.dart,
    lib/features/knowledge/domain/character_card.dart
  </read_first>
  <behavior>
    - Test: WorldTemplateLibrary.fromJson parses templateSchemaVersion, language, and templates
    - Test: TemplateChannel parses `male` and `female`; invalid values throw ArgumentError
    - Test: each WorldTemplate exposes searchableText containing genreName, subtitle, description, and tags
    - Test: each template can convert its world source into a WorldSetting with empty id and valid createdAt placeholder
    - Test: each template character can convert into a CharacterCard with empty id and valid createdAt placeholder
    - Test: opening samples are constrained to scene, character, suspense
  </behavior>
  <action>
    Create `lib/features/templates/domain/world_template.dart` with immutable plain Dart classes, not code generation. Keep this feature lightweight and consistent with existing manually written domain entities.

    Required model shape:
    - `WorldTemplateLibrary`: `templateSchemaVersion`, `language`, `templates`
    - `TemplateChannel`: enum values `male`, `female`
    - `WorldTemplate`: `id`, `channel`, `sortOrder`, `genreName`, `subtitle`, `description`, `iconName`, `tags`, `review`, `world`, `characters`, `foreshadowingArcs`, `openingSamples`
    - `TemplateReviewMetadata`: `sourceNote`, `reviewedAt`, `qualityChecks`
    - `WorldTemplateWorld`: maps to WorldSetting fields
    - `WorldTemplateCharacter`: maps to CharacterCard fields
    - `ForeshadowingArc`: `setup`, `development`, `payoff`
    - `OpeningSample`: `style`, `text`

    Per D-30 through D-33, include maintenance metadata in the model but do not design any UI exposure here. Per D-16, keep the model JSON-oriented so later AI completion can assign fields structurally.

    Add helper methods where useful:
    - `WorldTemplate.matchesQuery(String query)` for lowercase search over genreName, subtitle, description, and tags.
    - `WorldTemplateWorld.toWorldSetting()` returning a `WorldSetting` with `id: ''` and `createdAt: DateTime.now()`.
    - `WorldTemplateCharacter.toCharacterCard()` returning a `CharacterCard` with `id: ''` and `createdAt: DateTime.now()`.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/templates/domain/world_template_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class WorldTemplate" lib/features/templates/domain/world_template.dart` returns at least 1 match
    - `grep "enum TemplateChannel" lib/features/templates/domain/world_template.dart` returns 1 match
    - `grep "toWorldSetting" lib/features/templates/domain/world_template.dart` returns 1 match
    - `grep "toCharacterCard" lib/features/templates/domain/world_template.dart` returns 1 match
    - `flutter test test/features/templates/domain/world_template_test.dart` passes
  </acceptance_criteria>
  <done>
    Template domain classes parse JSON, validate channels/opening sample styles, support search matching, and convert template sources into valid existing knowledge entities.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add bundled template asset and repository</name>
  <files>
    pubspec.yaml,
    assets/templates/world_presets/templates_zh.json,
    lib/features/templates/infrastructure/world_template_repository.dart,
    lib/core/presentation/providers.dart,
    test/features/templates/infrastructure/world_template_repository_test.dart
  </files>
  <read_first>
    pubspec.yaml,
    lib/core/presentation/providers.dart,
    lib/features/templates/domain/world_template.dart
  </read_first>
  <behavior>
    - Test: repository loads templates from `assets/templates/world_presets/templates_zh.json`
    - Test: loaded library contains exactly 14 templates
    - Test: channel counts are exactly 8 male and 6 female
    - Test: every template has 5-8 tags, exactly 3 characters, 3-5 foreshadowing arcs, and exactly 3 opening samples
    - Test: each template has all three opening styles: scene, character, suspense
    - Test: every bundled world and character source instantiates existing domain entities without validation errors
    - Test: repository returns templates in curated `sortOrder` order
    - Test: repository search matches name, subtitle, description, and tags
  </behavior>
  <action>
    Add an assets section to `pubspec.yaml` under `flutter:`:
    ```yaml
    assets:
      - assets/templates/world_presets/templates_zh.json
    ```

    Create `assets/templates/world_presets/templates_zh.json` with exactly 14 Chinese templates. Per D-01 through D-04, use 8 male-channel and 6 female-channel main templates. Each title should follow `主类｜默认热门方向` through `genreName` plus `subtitle` fields.

    Suggested curated order and channels:
    1. 男频: 玄幻｜血脉觉醒
    2. 男频: 仙侠｜宗门修真
    3. 男频: 都市｜异能逆袭
    4. 男频: 科幻｜近未来危机
    5. 男频: 历史｜乱世权谋
    6. 男频: 游戏｜系统竞技
    7. 男频: 悬疑｜诡案追踪
    8. 男频: 末世｜废土求生
    9. 女频: 现言｜职场成长
    10. 女频: 古言｜宅斗权谋
    11. 女频: 幻言｜契约灵族
    12. 女频: 青春｜校园暗恋
    13. 女频: 悬疑情感｜双线追凶
    14. 女频: 种田｜家业经营

    Per D-18 through D-20, content may be AI-drafted but must be manually reviewable. Include `review.qualityChecks` with four strings equivalent to: no obvious AI-scent prose, no genre-common-sense violation, schema/field length valid, opening samples non-duplicative. Avoid generic AI prose and do not over-bind content to a single external platform.

    Create `WorldTemplateRepository` with a default constructor taking an optional asset path and optional asset loader function for tests. Production path must use `rootBundle.loadString`. Provide methods:
    - `Future<WorldTemplateLibrary> loadLibrary()`
    - `Future<List<WorldTemplate>> getAll()` sorted by `sortOrder`
    - `Future<WorldTemplate?> getById(String id)`
    - `Future<List<WorldTemplate>> search(String query)`
    - `Future<List<WorldTemplate>> filterByChannel(TemplateChannel? channel)` where `null` means all

    Add `worldTemplateRepositoryProvider` to `lib/core/presentation/providers.dart` as a normal `Provider<WorldTemplateRepository>`.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/templates/domain test/features/templates/infrastructure</automated>
  </verify>
  <acceptance_criteria>
    - `grep "assets/templates/world_presets/templates_zh.json" pubspec.yaml` returns 1 match
    - `grep '"templateSchemaVersion"' assets/templates/world_presets/templates_zh.json` returns at least 1 match
    - `grep "rootBundle.loadString" lib/features/templates/infrastructure/world_template_repository.dart` returns 1 match
    - `grep "worldTemplateRepositoryProvider" lib/core/presentation/providers.dart` returns at least 1 match
    - `flutter test test/features/templates/domain test/features/templates/infrastructure` passes
  </acceptance_criteria>
  <done>
    Bundled template JSON is registered as a Flutter asset, loads through WorldTemplateRepository, has exactly 14 validated templates, and is exposed through Riverpod provider wiring.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled JSON asset -> Template domain parser | Static app asset is decoded into domain objects |
| Template domain objects -> Knowledge entities | Template strings pass into existing field validators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Template asset data | mitigate | Tests assert schema version, counts, required fields, channel distribution, opening styles, field length validity, and entity instantiation. Asset updates only ship with app releases per D-21. |
| T-07-02 | Information Disclosure | Template repository | accept | Repository reads only bundled public template data. No user data or API keys are accessed. |
| T-07-SC | Tampering | Third-party supply chain | mitigate | No new packages are installed. Uses Flutter asset loading and existing domain classes only. |

</threat_model>

<verification>
1. `flutter test test/features/templates/domain test/features/templates/infrastructure`
2. `flutter analyze lib/features/templates test/features/templates`
3. Manual content review: read all 14 templates for genre accuracy and no obvious AI-scent prose before Phase 7 completion
</verification>

<success_criteria>
- Exactly 14 bundled Chinese templates exist: 8 male-channel, 6 female-channel
- Every template has 5-8 searchable tags, one world source, three character sources, 3-5 foreshadowing arcs, and three opening samples
- Template schema includes version, language, channel, sort order, source/review notes, and review timestamp
- Template repository loads, sorts, searches, filters, and finds by id without network dependency
- All bundled template worlds and characters instantiate existing WorldSetting and CharacterCard classes successfully
</success_criteria>

<output>
Create `.planning/phases/07-preset-world-template-library/07-01-SUMMARY.md` when done
</output>
