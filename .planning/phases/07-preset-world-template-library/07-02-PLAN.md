---
phase: 07-preset-world-template-library
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/shared/constants/app_constants.dart
  - lib/app.dart
  - lib/features/knowledge/presentation/knowledge_base_page.dart
  - lib/features/templates/presentation/template_gallery_page.dart
  - lib/features/templates/presentation/template_preview_page.dart
  - test/features/templates/presentation/template_gallery_page_test.dart
  - test/features/templates/presentation/template_preview_page_test.dart
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-04, TMPL-06]

must_haves:
  truths:
    - "User can reach a template gallery from the knowledge base surface"
    - "Gallery shows all 14 templates with icon, genre name, subtitle, description, and 5-8 popular tags"
    - "Gallery has a top segmented channel control: 全部 / 男频 / 女频"
    - "Text search matches template name, subtitle, description, and tags"
    - "Tags are displayed and searchable but are not active filter chips in Phase 7"
    - "Template preview shows full world, characters, foreshadowing arcs, and opening samples in collapsed sections"
    - "Story concept input exists on preview page and is passed toward the draft route"
  artifacts:
    - path: "lib/shared/constants/app_constants.dart"
      provides: "Template route constants"
      contains: "knowledgeTemplates"
    - path: "lib/app.dart"
      provides: "Nested knowledge routes for template gallery and preview"
      contains: "TemplateGalleryPage"
    - path: "lib/features/knowledge/presentation/knowledge_base_page.dart"
      provides: "Knowledge base entry point to template gallery"
      contains: "模板库"
    - path: "lib/features/templates/presentation/template_gallery_page.dart"
      provides: "Segmented channel filter, search, curated cards"
      contains: "SegmentedButton"
    - path: "lib/features/templates/presentation/template_preview_page.dart"
      provides: "Collapsed full template preview and concept input"
      contains: "ExpansionTile"
  key_links:
    - from: "lib/features/templates/presentation/template_gallery_page.dart"
      to: "lib/features/templates/infrastructure/world_template_repository.dart"
      via: "worldTemplateRepositoryProvider"
      pattern: "worldTemplateRepositoryProvider"
    - from: "lib/app.dart"
      to: "lib/shared/constants/app_constants.dart"
      via: "AppConstants.knowledgeTemplates route"
      pattern: "knowledgeTemplates"
---

## Phase Goal

**As a** writer, **I want to** browse, filter, search, and preview preset genre templates before creating anything, **so that** I can choose a starting point with confidence and without committing data too early.

<objective>
Build the user-facing gallery and preview flow on top of the Phase 7 template repository. This plan intentionally stops before draft persistence and AI completion; `Use Template` should navigate to the draft route that 07-03 implements.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-preset-world-template-library/07-CONTEXT.md
@.planning/phases/07-preset-world-template-library/07-RESEARCH.md
@.planning/phases/07-preset-world-template-library/07-VALIDATION.md
@.planning/phases/07-preset-world-template-library/07-01-SUMMARY.md
</context>

<interfaces>

From `lib/app.dart`:
```dart
GoRoute(
  path: AppConstants.knowledge,
  builder: (context, state) => const KnowledgeBasePage(),
  routes: [ ... ],
)
```

From `lib/shared/constants/app_constants.dart`:
```dart
static const String knowledge = '/knowledge';
static const String knowledgeCharacterNew = '/knowledge/character/new';
static const String knowledgeSettingNew = '/knowledge/setting/new';
```

From 07-01 expected provider:
```dart
final worldTemplateRepositoryProvider = Provider<WorldTemplateRepository>((ref) {
  return WorldTemplateRepository();
});
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add template routes and knowledge-base entry point</name>
  <files>
    lib/shared/constants/app_constants.dart,
    lib/app.dart,
    lib/features/knowledge/presentation/knowledge_base_page.dart,
    test/features/templates/presentation/template_gallery_page_test.dart
  </files>
  <read_first>
    lib/app.dart,
    lib/shared/constants/app_constants.dart,
    lib/features/knowledge/presentation/knowledge_base_page.dart
  </read_first>
  <behavior>
    - Test: routing constants include `/knowledge/templates`, `/knowledge/templates/:id`, and draft route helper or constant
    - Test: knowledge base page exposes a visible `模板库` entry action
    - Test: tapping template entry navigates to `/knowledge/templates`
  </behavior>
  <action>
    Add route constants to `AppConstants`:
    - `knowledgeTemplates = '/knowledge/templates'`
    - `knowledgeTemplatePreviewBase = '/knowledge/templates'`
    - a helper method or explicit route-building helper for preview and draft paths if that matches existing style minimally

    In `lib/app.dart`, import the new template pages and add nested routes under the existing knowledge branch:
    - `templates` -> `TemplateGalleryPage`
    - `templates/:id` -> `TemplatePreviewPage(templateId: id)`
    - Do not add draft route in this task unless a placeholder is needed for navigation compile safety; 07-03 owns the real draft page.

    Add a non-intrusive template-library entry to `KnowledgeBasePage`. Keep existing two-tab knowledge list behavior. A simple `OutlinedButton.icon` or compact card above the search field is sufficient. Label it `模板库` and route to `AppConstants.knowledgeTemplates`.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/templates/presentation/template_gallery_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "knowledgeTemplates" lib/shared/constants/app_constants.dart` returns at least 1 match
    - `grep "TemplateGalleryPage" lib/app.dart` returns at least 1 match
    - `grep "TemplatePreviewPage" lib/app.dart` returns at least 1 match
    - `grep "模板库" lib/features/knowledge/presentation/knowledge_base_page.dart` returns at least 1 match
    - Relevant widget/routing tests pass
  </acceptance_criteria>
  <done>
    Template gallery and preview routes are nested under `/knowledge`, and the knowledge base page exposes a clear entry point to the template library without disrupting existing tabs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build genre gallery with channel filter and search</name>
  <files>
    lib/features/templates/presentation/template_gallery_page.dart,
    test/features/templates/presentation/template_gallery_page_test.dart
  </files>
  <read_first>
    lib/features/templates/domain/world_template.dart,
    lib/features/templates/infrastructure/world_template_repository.dart,
    lib/core/presentation/providers.dart,
    lib/features/knowledge/presentation/knowledge_base_page.dart
  </read_first>
  <behavior>
    - Test: gallery loading state shows a progress indicator
    - Test: gallery displays all templates in curated order after repository load
    - Test: segmented control has labels 全部, 男频, 女频
    - Test: choosing 男频 shows 8 templates; choosing 女频 shows 6 templates
    - Test: search by a tag filters to matching cards
    - Test: tag labels are visible but not selectable filter chips
    - Test: tapping a template card navigates to `/knowledge/templates/<id>`
  </behavior>
  <action>
    Create `TemplateGalleryPage` as a `ConsumerStatefulWidget`. Use repository provider from 07-01 and local state for selected channel plus search query.

    UI requirements:
    - AppBar title: `世界观模板库`
    - Top segmented control with `全部 / 男频 / 女频` per D-02, D-22
    - Search `TextField` matching current knowledge base search style
    - Template cards showing icon, `genreName｜subtitle`, description, tags, and light trust cue such as `内置已审核`
    - Cards sorted by `sortOrder`
    - Tags displayed using passive `ActionChip` is not allowed if it implies filtering; use `InputChip` disabled, `Chip`, or small Text containers with no `onTap`
    - Empty states for no search match and load error with retry

    Keep the visual language consistent with current Material app. Do not add new packages.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/templates/presentation/template_gallery_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SegmentedButton" lib/features/templates/presentation/template_gallery_page.dart` returns at least 1 match
    - `grep "全部" lib/features/templates/presentation/template_gallery_page.dart` returns at least 1 match
    - `grep "男频" lib/features/templates/presentation/template_gallery_page.dart` returns at least 1 match
    - `grep "女频" lib/features/templates/presentation/template_gallery_page.dart` returns at least 1 match
    - `flutter test test/features/templates/presentation/template_gallery_page_test.dart` passes
  </acceptance_criteria>
  <done>
    Template gallery renders curated cards, supports all/male/female segmented filtering, performs text search over template metadata, and opens preview pages from cards.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build full collapsed template preview</name>
  <files>
    lib/features/templates/presentation/template_preview_page.dart,
    test/features/templates/presentation/template_preview_page_test.dart
  </files>
  <read_first>
    lib/features/templates/domain/world_template.dart,
    lib/features/templates/infrastructure/world_template_repository.dart,
    lib/core/presentation/providers.dart
  </read_first>
  <behavior>
    - Test: preview loads template by id and shows title, subtitle, description, tags, and reviewed cue
    - Test: preview uses collapsed sections for world skeleton, characters, foreshadowing arcs, and opening samples
    - Test: foreshadowing arcs render in 起点 -> 发展 -> 回收 form
    - Test: all three opening styles are visible when opening sample section expands
    - Test: story concept TextField exists and preserves typed text
    - Test: Use Template button navigates to the future draft route with template id and concept query or route state
    - Test: missing template id shows a safe not-found state
  </behavior>
  <action>
    Create `TemplatePreviewPage(templateId: String?)` as a `ConsumerStatefulWidget`.

    UI requirements per D-05 through D-08 and D-15:
    - Load template by id through repository provider
    - Show header with title `genreName｜subtitle`, description, tags, and light `内置已审核` trust cue
    - Add story concept input on preview page, labeled clearly such as `你的故事概念（可选）`
    - Use `ExpansionTile` sections, collapsed by default, for:
      - 世界设定骨架
      - 角色原型（三个）
      - 伏笔模式
      - 开篇示例
    - Render foreshadowing arcs as `setup -> development -> payoff`
    - Render opening samples grouped/labeled as 场景切入, 人物切入, 悬念切入
    - Add primary action `使用模板` that routes to `/knowledge/templates/<id>/draft`. Include story concept in a query parameter or go_router extra if supported. Prefer query parameter for testability.

    If 07-03 has not yet added the draft route, add only navigation code; route registration can be completed in 07-03. Tests may assert the intended location via a fake router wrapper or link builder.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/templates/presentation/template_preview_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ExpansionTile" lib/features/templates/presentation/template_preview_page.dart` returns at least 4 matches
    - `grep "使用模板" lib/features/templates/presentation/template_preview_page.dart` returns at least 1 match
    - `grep "故事概念" lib/features/templates/presentation/template_preview_page.dart` returns at least 1 match
    - `flutter test test/features/templates/presentation/template_preview_page_test.dart` passes
  </acceptance_criteria>
  <done>
    Template preview page shows complete collapsed template content, lets the user enter a story concept, and routes to the draft flow without persisting anything.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled template data -> Presentation widgets | Local asset content is rendered in UI |
| User search/concept text -> Widget state and route query | User input affects filtering and future draft route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | Template route id | mitigate | Preview must handle unknown template ids with a safe not-found state instead of throwing. |
| T-07-04 | Information Disclosure | Story concept route query | accept | Story concept stays local in app navigation. Do not log it. 07-03 should avoid writing it to Hive unless user saves draft-derived entities. |
| T-07-SC | Tampering | Third-party supply chain | mitigate | No new packages. Uses Material widgets, go_router, and existing Riverpod. |

</threat_model>

<verification>
1. `flutter test test/features/templates/presentation/template_gallery_page_test.dart test/features/templates/presentation/template_preview_page_test.dart`
2. `flutter analyze lib/features/templates lib/features/knowledge/presentation/knowledge_base_page.dart lib/app.dart`
3. Manual: open knowledge base, enter template library, filter all/male/female, search a tag, open preview, expand each section
</verification>

<success_criteria>
- Knowledge base exposes a template library entry
- Gallery supports channel filter and text search while showing curated template cards
- Preview shows all template material before creation and keeps it collapsed by default
- Story concept can be entered before using a template
- No entities are persisted by gallery or preview pages
</success_criteria>

<output>
Create `.planning/phases/07-preset-world-template-library/07-02-SUMMARY.md` when done
</output>
