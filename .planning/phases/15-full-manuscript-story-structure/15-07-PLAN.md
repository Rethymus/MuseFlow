---
phase: 15-full-manuscript-story-structure
plan: 07
type: execute
wave: 3
depends_on: [15-02, 15-03, 15-04, 15-05]
files_modified:
  - test/journey/automated_ui_evidence_test.dart
autonomous: true
requirements: [JOURNEY-07, JOURNEY-08, JOURNEY-09, JOURNEY-10]

must_haves:
  truths:
    - "automated_ui_evidence_test.dart has new test groups producing [AUTO_UI] evidence output for JOURNEY-07 (foreshadowing service API at 100-chapter scale), JOURNEY-08 (FormatCleaner produces clean output), JOURNEY-09 (ExportService builds valid three-format output), and JOURNEY-10 (WritingStatsCollector and TokenAuditRepository return accurate data at scale)"
    - "Each new evidence group uses FakeAdapter (no GLM key needed), following the existing Phase 14 pattern"
    - "Phase 14 issue recurrence check: enforceD11Bounds verified at 100-chapter scale (P14-04-GLM-01 regression), anti-AI-scent phrases still removed (P14-04-AI-01 regression)"
    - "All existing Phase 14 evidence groups remain unchanged and passing"
  artifacts:
    - path: "test/journey/automated_ui_evidence_test.dart"
      provides: "Automated UI evidence for JOURNEY-07/08/09/10 + Phase 14 regression checks"
      min_lines: 350
      contains: "JOURNEY-07"
  key_links:
    - from: "automated_ui_evidence_test.dart"
      to: "lib/features/story_structure/application/foreshadowing_notifier.dart"
      via: "container.read(foreshadowingNotifierProvider.notifier)"
      pattern: "foreshadowingNotifierProvider"
    - from: "automated_ui_evidence_test.dart"
      to: "lib/features/story_structure/application/format_cleaner.dart"
      via: "const FormatCleaner().clean()"
      pattern: "FormatCleaner"
    - from: "automated_ui_evidence_test.dart"
      to: "lib/features/story_structure/application/export_service.dart"
      via: "ExportService(fileWriter: ...)"
      pattern: "ExportService"
    - from: "automated_ui_evidence_test.dart"
      to: "lib/features/stats/infrastructure/token_audit_repository.dart"
      via: "auditRepository.buildSnapshot()"
      pattern: "buildSnapshot"
---

<objective>
Extend automated_ui_evidence_test.dart with JOURNEY-07/08/09/10 evidence groups and Phase 14 issue regression checks.

Purpose: The automated_ui_evidence_test is the Phase 15 executor's evidence artifact -- it produces [AUTO_UI] debug prints that prove existing services work at 100-chapter scale without requiring manual UI interaction. This file was explicitly listed in CONTEXT.md (line 86), RESEARCH.md recommended project structure, and UI-SPEC test file structure as requiring extension for Phase 15. Additionally, CONTEXT.md line 76 requires checking Phase 14 issues for recurrence at 100-chapter scale.

Output: Extended automated_ui_evidence_test.dart with 4 new evidence groups + 2 regression checks

</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-manuscript-story-structure/15-CONTEXT.md
@.planning/phases/15-full-manuscript-story-structure/15-RESEARCH.md
@.planning/phases/15-full-manuscript-story-structure/15-PATTERNS.md
@.planning/phases/15-full-manuscript-story-structure/15-UI-SPEC.md

<interfaces>
<!-- Key types from existing code -->

From test/journey/automated_ui_evidence_test.dart (242 lines, the file being modified):
- Single group `'Task 3 automated final-review evidence'` containing 5 tests (lines 33-230)
- Test names follow pattern: `'should {verb} {feature}'`
- Each test ends with `debugPrint('[AUTO_UI] {category} passed: {details}')`
- Uses FakeAdapter for all tests (no GLM key needed)
- Setup: `createJourneyContainer(apiKey: 'journey-local-test-key', baseUrl: 'https://example.com/v1', model: 'fake-model', aiAdapter: FakeAdapter())`
- Teardown: `cleanupJourneyContainer(container)`
- Helper: `_pumpAndWait()` for async state updates

From lib/features/story_structure/application/foreshadowing_notifier.dart (89 lines):
```dart
class ForeshadowingNotifier extends AsyncNotifier<List<ForeshadowingEntry>> {
  Future<void> add(ForeshadowingEntry entry);
  Future<void> save(ForeshadowingEntry entry);
  Future<void> markResolved(String id, {required int resolvedChapter});
}
```

From lib/features/story_structure/domain/foreshadowing_entry.dart:
```dart
enum ForeshadowingStatus { planted, developing, resolved, abandoned; }
class ForeshadowingEntry {
  final String id;
  final String title;
  final ForeshadowingStatus status;
  final int plantedChapter;
  final int? resolvedChapter;
  bool get isResolved;
  ForeshadowingEntry copyWith({...});
}
```

From lib/features/story_structure/application/format_cleaner.dart:
```dart
class FormatCleaner {
  const FormatCleaner();
  FormatCleanResult clean(String text, {FormatCleanOptions? options});
}
```

From lib/features/story_structure/domain/format_clean_result.dart:
```dart
class FormatCleanResult {
  final String cleanedText;
  final List<FormatChange> changes;
  bool get hasChanges;
}
```

From lib/features/story_structure/application/export_service.dart:
```dart
class ExportService {
  ExportService({required FileWriter fileWriter});
  String buildMarkdown(ExportBundle bundle);
  String buildTxt(ExportBundle bundle);
  String buildJson(ExportBundle bundle);
}
```

From lib/features/story_structure/domain/export_bundle.dart:
```dart
class ExportBundle {
  const ExportBundle({
    required this.schemaVersion,
    required this.manuscriptText,
    required this.chapters,
    ...
  });
}
```

From lib/features/manuscript/domain/chapter_export.dart:
```dart
class ChapterExport {
  ChapterExport({required this.title, required this.sortOrder, required this.content});
}
```

From lib/features/stats/infrastructure/token_audit_repository.dart:
```dart
class TokenAuditRepository {
  Future<TokenAuditSnapshot> buildSnapshot();
}
class TokenAuditSnapshot {
  final int totalCalls;
  final int totalInputTokens;
  final int totalOutputTokens;
  final List<TokenAuditRecord> records;
}
```

From test/journey/helpers/enforce_d11_bounds.dart:
```dart
String enforceD11Bounds(String rawOutput); // Returns output in 300-500 char range
```

From test/journey/helpers/story_outline.dart (100 entries after Plan 15-01):
```dart
class StoryOutline {
  static const List<String> chapters; // 100 entries after Plan 15-01
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add JOURNEY-07/08/09/10 evidence groups + Phase 14 regression checks to automated_ui_evidence_test.dart</name>
  <files>test/journey/automated_ui_evidence_test.dart</files>
  <read_first>
    test/journey/automated_ui_evidence_test.dart
    lib/features/story_structure/application/foreshadowing_notifier.dart
    lib/features/story_structure/domain/foreshadowing_entry.dart
    lib/features/story_structure/application/format_cleaner.dart
    lib/features/story_structure/domain/format_clean_result.dart
    lib/features/story_structure/application/export_service.dart
    lib/features/story_structure/domain/export_bundle.dart
    lib/features/manuscript/domain/chapter_export.dart
    lib/features/stats/infrastructure/token_audit_repository.dart
    lib/features/ai/application/anti_ai_scent_processor.dart
    test/journey/helpers/d11_bounds.dart
    test/journey/helpers/story_outline.dart
  </read_first>
  <behavior>
    - New group "JOURNEY-07 foreshadowing service evidence": Create 4 foreshadowing entries via notifier, transition to resolved, all 4 isResolved == true
    - New group "JOURNEY-08 format cleaning evidence": FormatCleaner.clean() on 10 representative chapters (1, 15, 30, 45, 60, 75, 90, 100 from StoryOutline), all produce cleaned text with no markdown heading markers
    - New group "JOURNEY-09 three-format export evidence": ExportService builds Markdown (100 ## headers), TXT (no ##), JSON (parseable, 100 chapters), from 100 StoryOutline chapters
    - New group "JOURNEY-10 statistics evidence": Generate 10 chapters via FakeAdapter + enforceD11Bounds, flush audit, buildSnapshot shows totalCalls >= 10
    - New group "Phase 14 regression checks": enforceD11Bounds truncates a 1000-char string to <=500 (P14-04-GLM-01 regression), anti-AI-scent processor removes banned phrases (P14-04-AI-01 regression)
    - All existing 5 Phase 14 tests remain byte-identical
    - All new tests use existing FakeAdapter/container setup
    - Each test prints [AUTO_UI] evidence line
  </behavior>
  <action>
    Add 5 new test groups AFTER the existing `'Task 3 automated final-review evidence'` group (after line 230, before the closing of the outer group). Do NOT modify any existing test code.

    **New imports needed** (add at top of file, after existing imports):
    - `import 'dart:convert';`
    - `import 'package:museflow/features/story_structure/application/export_service.dart';`
    - `import 'package:museflow/features/story_structure/domain/export_bundle.dart';`
    - `import 'package:museflow/features/manuscript/domain/chapter_export.dart';`
    - `import 'package:museflow/features/stats/infrastructure/token_audit_repository.dart';`
    - `import '../features/story_structure/domain/foreshadowing_entry.dart';` (adjust path as needed per existing import pattern)
    - `import 'helpers/d11_bounds.dart';`
    - `import 'helpers/story_outline.dart';`

    **Group: "JOURNEY-07 foreshadowing service evidence"**

    Test: "should prove foreshadowing plant-track-resolve lifecycle at 100-chapter scale"
    1. Read `foreshadowingNotifierProvider.notifier` from container
    2. Create 4 entries with `ForeshadowingEntry` constructor (same IDs/titles as UI-SPEC fixtures: fs-mysterious-origin/神秘身世 planted ch 3, fs-senior-sister-secret/师姐的秘密 planted ch 10, fs-forbidden-zone/门派禁地 planted ch 20, fs-ancient-artifact/远古法器 planted ch 30 -- all with mode=detailed, status=planted)
    3. For each entry: save with status=developing, then markResolved with appropriate resolvedChapter (92, 78, 88, 96 per UI-SPEC)
    4. Read all entries, assert all 4 have isResolved == true
    5. Print: `debugPrint('[AUTO_UI] JOURNEY-07 foreshadowing lifecycle passed: 4/4 threads resolved')`

    **Group: "JOURNEY-08 format cleaning evidence"**

    Test: "should prove FormatCleaner produces clean output at 100-chapter scale"
    1. Create const FormatCleaner
    2. For 8 sampled indices [0, 14, 29, 44, 59, 74, 89, 99] (chapters 1, 15, 30, 45, 60, 75, 90, 100):
       a. Get text from StoryOutline.chapters[index]
       b. Inject markdown artifacts: append "\n**加粗文本**\n" for odd indices
       c. Call cleaner.clean(text)
       d. Assert cleanedText does not contain "**"
    3. Print: `debugPrint('[AUTO_UI] JOURNEY-08 format cleaning passed: 8/8 sampled chapters clean')`

    **Group: "JOURNEY-09 three-format export evidence"**

    Test: "should prove ExportService builds valid three-format output for 100 chapters"
    1. Build 100 ChapterExport items from StoryOutline.chapters: `ChapterExport(title: '第${i+1}章', sortOrder: i+1, content: StoryOutline.chapters[i])`
    2. Build ExportBundle with schemaVersion='1.0', the 100 chapters, empty lists for other fields
    3. Create ExportService with no-op fileWriter
    4. Call buildMarkdown: assert RegExp `^## ` with multiLine matches 100 times
    5. Call buildTxt: assert no "##" in output
    6. Call buildJson: jsonDecode succeeds, decoded['chapters'] has length 100
    7. Print: `debugPrint('[AUTO_UI] JOURNEY-09 export validation passed: md=100 headers, txt=clean, json=100 chapters')`

    **Group: "JOURNEY-10 statistics evidence"**

    Test: "should prove statistics accuracy and token audit at scale"
    1. Create manuscript and 10 chapters (smaller scale for evidence speed)
    2. For each chapter, generate content via PromptPipeline + FakeAdapter, apply enforceD11Bounds, save to repository
    3. Flush audit service
    4. Read tokenAuditRepository.buildSnapshot()
    5. Assert snapshot.totalCalls >= 10, totalInputTokens > 0, totalOutputTokens > 0
    6. Calculate total word count across 10 chapters, assert > 0
    7. Print: `debugPrint('[AUTO_UI] JOURNEY-10 statistics passed: calls=${snapshot.totalCalls}, input=${snapshot.totalInputTokens}, output=${snapshot.totalOutputTokens}')`

    **Group: "Phase 14 issue regression checks"**

    Test: "should verify Phase 14 fixes hold at 100-chapter scale"
    1. **P14-04-GLM-01 regression (enforceD11Bounds):** Create a 1000-character Chinese string (repeat "林风修行的日子一天天过去" 40 times = ~400 chars, then pad to 1000). Call enforceD11Bounds on it. Assert result.length <= 500 and result.length >= 300.
    2. **P14-04-AI-01 regression (anti-AI-scent):** Create text containing "这段话总而言之非常重要，需要指出的是值得注意的", run AntiAIScentProcessor (same pattern as existing test line 83-95). Assert output does NOT contain "总而言之" and does NOT contain "需要指出的是".
    3. Print: `debugPrint('[AUTO_UI] Phase 14 regression passed: enforceD11Bounds<=500, anti-AI-scent phrases removed')`

    The new groups go inside the existing outer `setUp/tearDown` block so they share the same container lifecycle. Place them as siblings to the existing group, not nested inside it.

    Per CONTEXT.md line 76: This group fulfills the "Phase 15 应检查这些问题是否在 100 章规模下复现" requirement.
  </action>
  <verify>
    <automated>dart analyze test/journey/automated_ui_evidence_test.dart && flutter test test/journey/automated_ui_evidence_test.dart -j 1 --timeout 300s</automated>
  </verify>
  <done>
    automated_ui_evidence_test.dart has 6 total groups (5 existing Phase 14 + 1 new Phase 14 regression group + 4 new JOURNEY-07/08/09/10 groups = actually 6 groups total: existing group + 5 new groups). All existing tests unchanged. New groups produce [AUTO_UI] evidence output. enforceD11Bounds regression confirmed. anti-AI-scent regression confirmed. `dart analyze` passes. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test script -> ForeshadowingNotifier API | Test calls CRUD operations on foreshadowing entries |
| test script -> FormatCleaner | Pure Dart function call, no external I/O |
| test script -> ExportService | Pure content building, no file writes |
| test script -> TokenAuditRepository | Reads from Hive box via repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Tampering | Hive temp directory stale data | mitigate | Per-container temp directories with try/finally cleanup in tearDown. Same pattern as existing test. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
1. `dart analyze test/journey/automated_ui_evidence_test.dart` -- zero errors
2. `flutter test test/journey/automated_ui_evidence_test.dart -j 1 --timeout 300s` -- all tests pass (existing + new)
3. Existing 5 Phase 14 evidence tests remain unchanged and passing
4. New [AUTO_UI] evidence lines printed for JOURNEY-07, JOURNEY-08, JOURNEY-09, JOURNEY-10, and Phase 14 regression
5. enforceD11Bounds regression: 1000-char input truncated to <=500
6. anti-AI-scent regression: banned phrases removed
</verification>

<success_criteria>
- automated_ui_evidence_test.dart extended with 5 new groups (4 JOURNEY + 1 regression)
- All 4 JOURNEY-07/08/09/10 requirements have automated evidence output
- Phase 14 issue recurrence checks present (P14-04-GLM-01 enforceD11Bounds, P14-04-AI-01 anti-AI-scent)
- Existing Phase 14 evidence tests unchanged and passing
- File compiles with `dart analyze` zero errors
- All tests pass with FakeAdapter (no GLM key required)
</success_criteria>

<output>
Create `.planning/phases/15-full-manuscript-story-structure/15-07-SUMMARY.md` when done
</output>
