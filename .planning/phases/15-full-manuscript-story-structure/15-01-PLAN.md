---
phase: 15-full-manuscript-story-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/journey/helpers/story_outline.dart
  - test/journey/helpers/stage_prompts.dart
autonomous: true
requirements: [JOURNEY-07, JOURNEY-08, JOURNEY-09, JOURNEY-10]

must_haves:
  truths:
    - "StoryOutline.chapters has exactly 100 entries (30 existing + 70 new)"
    - "StagePrompts class provides stage-specific prompts for Golden Core (31-60), Nascent Soul (61-90), Ascension (91-100)"
    - "StagePrompts.forChapterIndex() returns correct prompt for any index 0-99"
    - "All 70 new chapter entries follow the three-stage story arc from D-01"
    - "D-02: Story outline chapters 31-100 include all six multi-conflict threads (Core Formation failure, Wang Lei schemes, Su Yue captured, sect war, heart demon, heavenly tribulation) -- verifyable by grepping outline entries for conflict keywords"
  artifacts:
    - path: "test/journey/helpers/story_outline.dart"
      provides: "100-chapter plot outline with three-stage arc"
      min_lines: 200
      contains: "static const List<String> chapters"
    - path: "test/journey/helpers/stage_prompts.dart"
      provides: "Three-stage prompt constants and forChapterIndex()"
      min_lines: 30
      contains: "class StagePrompts"
  key_links:
    - from: "test/journey/serial_generation_test.dart"
      to: "test/journey/helpers/story_outline.dart"
      via: "StoryOutline.chapters[index] reference"
      pattern: "StoryOutline\\.chapters\\["
    - from: "test/journey/serial_generation_test.dart"
      to: "test/journey/helpers/stage_prompts.dart"
      via: "StagePrompts.forChapterIndex(index) call"
      pattern: "StagePrompts\\.forChapterIndex"
---

<objective>
Extend test infrastructure with 100-chapter story outline and three-stage prompt definitions.

Purpose: All downstream Phase 15 tests depend on having 100 chapter plot points and stage-specific prompts. This plan provides the foundational data that Plan 15-02 through 15-06 consume.

Output: story_outline.dart (extended to 100 entries), stage_prompts.dart (new)
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-manuscript-story-structure/15-CONTEXT.md
@.planning/phases/15-full-manuscript-story-structure/15-RESEARCH.md
@.planning/phases/15-full-manuscript-story-structure/15-PATTERNS.md
@.planning/phases/15-full-manuscript-story-structure/15-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from existing code that the executor needs -->

From test/journey/helpers/story_outline.dart (current, 50 lines):
```dart
class StoryOutline {
  static const List<String> chapters = [
    '第1章 凡人少年：林风是青云山脚下偏远山村的凡人少年...',
    // ... 30 existing entries ending with ...
    '第30章 筑基：月光如水的夜晚，林风在青云峰顶服下筑基丹...',
  ];

  static const List<String> characterNames = [
    '林风', '清虚真人', '苏雪晴', '赵天磊',
  ];
}
```

From test/journey/helpers/xianxia_fixtures.dart (pattern reference for static const class):
```dart
class XianxiaFixtures {
  static final DateTime _fixedDate = DateTime(2026, 6, 7);
  static const Uuid _uuid = Uuid();
  // Static factory methods return domain entities
}
```

From test/journey/serial_generation_test.dart (consumer of StoryOutline):
- Line 158: `text: StoryOutline.chapters[index]` in generateChapter()
- Line 447: `(index % StoryOutline.chapters.length) + 1` in _DeterministicJourneyAdapter._chapterText()
- Line 449: `StoryOutline.chapters[index % StoryOutline.chapters.length]` same adapter

From test/journey/full_journey_test.dart (consumer of StoryOutline):
- Line 286: `text: StoryOutline.chapters[index]` in _generateChapter()
- Line 407: `(index % StoryOutline.chapters.length) + 1` in _DeterministicFullJourneyAdapter._chapterText()
- Line 409: `StoryOutline.chapters[index % StoryOutline.chapters.length]` same adapter
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend StoryOutline from 30 to 100 chapters with three-stage arc</name>
  <files>test/journey/helpers/story_outline.dart</files>
  <read_first>
    test/journey/helpers/story_outline.dart
  </read_first>
  <behavior>
    - StoryOutline.chapters has exactly 100 entries after extension
    - Entries 1-30 remain unchanged from Phase 14
    - Entries 31-60 (Golden Core stage): Advancing cultivation, Core Formation failure/retry, sect conflict escalates (per D-01, D-02)
    - Entries 61-90 (Nascent Soul stage): New realm abilities, Su Yue rescue arc, sect war, heart demon tribulation, forbidden zone awakening (per D-01, D-02)
    - Entries 91-100 (Ascension stage): Final breakthrough, resolving all threads, heavenly tribulation, jade slip revelation, ascension (per D-01, D-02)
    - Each entry is 200-400 characters describing the chapter's key event
    - All entries feature protagonist Lin Feng and supporting cast (Lin Feng, Elder Chen, Su Yue, Wang Lei)
    - StoryOutline.characterNames remains unchanged (4 names)
    - Multi-conflict threads appear: Core Formation failure (Ch 41-50), Wang Lei's schemes (throughout), Su Yue captured (Ch 55-60), sect war (Ch 75-85), heart demon (Ch 80-88), heavenly tribulation (Ch 96-100) per D-02
    - Foreshadowing threads from UI-SPEC are reflected: mysterious origin hints (Ch 50-60), Su Yue's secret clues (Ch 55-65), forbidden zone ominous signs (Ch 60-70), ancient artifact power awakens (Ch 70-80)
  </behavior>
  <action>
    Extend test/journey/helpers/story_outline.dart by adding 70 new entries (chapters 31-100) to the existing `static const List<String> chapters` array. The first 30 entries remain byte-identical to Phase 14.

    Follow the three-stage story arc per D-01:
    - Ch 31-40: Golden Core early -- advancing cultivation, discovering new abilities, Wang Lei rivalry escalates
    - Ch 41-50: Golden Core mid -- Core Formation attempt fails at Ch 45, setback and recovery, Su Yue's secret begins surfacing
    - Ch 51-60: Golden Core late -- retry Core Formation succeeds at Ch 55, sect conflict escalates, Su Yue captured by hostile faction at Ch 58-60
    - Ch 61-70: Nascent Soul early -- new realm abilities unlock, rescue mission for Su Yue, mysterious origin hints strengthen
    - Ch 71-80: Nascent Soul mid -- sect war erupts, foreshadowing threads start resolving (Su Yue's secret revealed Ch 78-82), ancient artifact awakens, Wang Lei's schemes exposed
    - Ch 81-90: Nascent Soul late -- heart demon tribulation, forbidden zone seal breaks open (Ch 88-90), elder Chen's true purpose revealed
    - Ch 91-95: Ascension build-up -- final preparations, remaining foreshadowing threads converge, mysterious origin confirmed (Ch 92-95)
    - Ch 96-100: Ascension climax -- heavenly tribulation, jade slip artifact's true purpose revealed, all 4 foreshadowing threads resolved, Lin Feng ascends

    Do NOT modify `characterNames` list or the class structure. Keep the same `static const List<String>` pattern. Each entry must start with "第N章" format matching existing entries.
  </action>
  <verify>
    <automated>flutter test test/journey/helpers/story_outline_test.dart -j 1 --timeout 30s (if exists), else: dart run -c 'import "test/journey/helpers/story_outline.dart"; void main() { assert(StoryOutline.chapters.length == 100, "Expected 100 chapters, got ${StoryOutline.chapters.length}"); for (var i = 0; i < 100; i++) { assert(StoryOutline.chapters[i].startsWith("第${i+1}章"), "Ch ${i+1} bad prefix"); } print("OK: 100 chapters verified"); }'</automated>
  </verify>
  <done>
    story_outline.dart has exactly 100 chapter entries. First 30 unchanged. Entries 31-100 cover Golden Core (31-60), Nascent Soul (61-90), Ascension (91-100). All entries start with "第N章" format. `dart analyze` passes with zero errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StagePrompts helper with three-stage prompt constants</name>
  <files>test/journey/helpers/stage_prompts.dart</files>
  <read_first>
    test/journey/helpers/xianxia_fixtures.dart
    test/journey/helpers/story_outline.dart
  </read_first>
  <behavior>
    - StagePrompts.goldenCore returns a non-empty string containing stage description for chapters 31-60
    - StagePrompts.nascentSoul returns a non-empty string containing stage description for chapters 61-90
    - StagePrompts.ascension returns a non-empty string containing stage description for chapters 91-100
    - StagePrompts.forChapterIndex(0) returns empty string (chapters 0-29 have no stage prompt)
    - StagePrompts.forChapterIndex(30) returns goldenCore
    - StagePrompts.forChapterIndex(59) returns goldenCore
    - StagePrompts.forChapterIndex(60) returns nascentSoul
    - StagePrompts.forChapterIndex(89) returns nascentSoul
    - StagePrompts.forChapterIndex(90) returns ascension
    - StagePrompts.forChapterIndex(99) returns ascension
  </behavior>
  <action>
    Create test/journey/helpers/stage_prompts.dart following the static-const-class pattern from xianxia_fixtures.dart.

    Define class StagePrompts with:
    - `static const String goldenCore` -- A 100-200 character prompt setting the Golden Core stage scene/theme. Must mention: Core Formation challenge, sect politics intensifying, the risk of failure and retry. Per CONTEXT.md D-03 this sets scene/theme for Ch 31-60 block.
    - `static const String nascentSoul` -- A 100-200 character prompt setting the Nascent Soul stage scene/theme. Must mention: tribulation dangers, inner demons, the weight of accumulated power and responsibility. Per D-03 for Ch 61-90 block.
    - `static const String ascension` -- A 100-200 character prompt setting the Ascension ending stage. Must mention: final breakthrough, resolving all story threads, ascending to higher realm. Per D-03 for Ch 91-100 block.
    - `static String forChapterIndex(int index)` -- Returns empty string for indices 0-29, goldenCore for 30-59, nascentSoul for 60-89, ascension for 90-99. This is the method that downstream generation scripts call.

    The stage prompts provide thematic direction; individual chapter plot points in StoryOutline.chapters provide per-chapter specifics. Per RESEARCH.md OQ-02: stage prompts set scene/theme for each block, individual plot points provide per-chapter direction.
  </action>
  <verify>
    <automated>dart analyze test/journey/helpers/stage_prompts.dart && dart run -c 'import "test/journey/helpers/stage_prompts.dart"; void main() { assert(StagePrompts.forChapterIndex(0) == "", "index 0 should be empty"); assert(StagePrompts.forChapterIndex(30) == StagePrompts.goldenCore, "index 30 should be goldenCore"); assert(StagePrompts.forChapterIndex(60) == StagePrompts.nascentSoul, "index 60 should be nascentSoul"); assert(StagePrompts.forChapterIndex(90) == StagePrompts.ascension, "index 90 should be ascension"); assert(StagePrompts.forChapterIndex(99) == StagePrompts.ascension, "index 99 should be ascension"); print("OK: StagePrompts verified"); }'</automated>
  </verify>
  <done>
    stage_prompts.dart exists with StagePrompts class. Three static const String fields (goldenCore, nascentSoul, ascension) each 100-200 characters. forChapterIndex() returns correct prompt for all 100 indices. `dart analyze` passes with zero errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test data -> production services | Test fixture data (story outline, stage prompts) is read by downstream tests that invoke production services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | story_outline.dart test data | accept | No secrets, no user input, no mutable state. Static const strings are immutable and test-only. |
| T-15-02 | Information Disclosure | stage_prompts.dart prompt content | accept | Test data only, not user-facing. Contains no API keys or secrets. |
</threat_model>

<verification>
1. `dart analyze test/journey/helpers/story_outline.dart test/journey/helpers/stage_prompts.dart` -- zero errors
2. `StoryOutline.chapters.length == 100` in Dart runtime
3. First 30 entries unchanged from Phase 14
4. `StagePrompts.forChapterIndex(n)` returns correct prompt for all n in 0..99
</verification>

<success_criteria>
- story_outline.dart has exactly 100 entries, first 30 byte-identical to Phase 14
- stage_prompts.dart has 3 stage prompt strings and forChapterIndex() routing method
- Both files pass `dart analyze` with zero errors
- All downstream plans (15-02 through 15-06) can import and use these helpers
</success_criteria>

<output>
Create `.planning/phases/15-full-manuscript-story-structure/15-01-SUMMARY.md` when done
</output>
