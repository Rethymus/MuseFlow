---
phase: 15-full-manuscript-story-structure
plan: 06
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - test/journey/full_journey_test.dart
  - .planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md
autonomous: true
requirements: [JOURNEY-07, JOURNEY-10]

must_haves:
  truths:
    - "full_journey_test.dart deterministic E2E test covers world-building through 100 chapters (not 30)"
    - "Stage prompts injected during chapter generation per D-03"
    - "Previous chapter summary injection during chapter generation per D-04"
    - "Token audit assertion updated from >=31 to >=101 calls (100 chapters + smoke + synthesis)"
    - "15-ISSUE-LOG.md exists with structured issue table format matching Phase 14 issue log"
  artifacts:
    - path: "test/journey/full_journey_test.dart"
      provides: "E2E full journey validation extended to 100 chapters"
      min_lines: 500
      contains: "100"
    - path: ".planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md"
      provides: "Structured issue log for Phase 15 findings"
      min_lines: 50
      contains: "Issue Log"
  key_links:
    - from: "full_journey_test.dart"
      to: "test/journey/helpers/stage_prompts.dart"
      via: "import and StagePrompts.forChapterIndex() in generation loop"
      pattern: "StagePrompts\\.forChapterIndex"
    - from: "full_journey_test.dart"
      to: "test/journey/helpers/story_outline.dart"
      via: "StoryOutline.chapters[index] in generateChapter"
      pattern: "StoryOutline\\.chapters\\["
    - from: "15-ISSUE-LOG.md"
      to: ".planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md"
      via: "Same structured format for issue tracking"
      pattern: "Issue Log"
---

<objective>
Extend full_journey_test.dart E2E test from 30 to 100 chapters and create the Phase 15 structured issue log template.

Purpose: The E2E full journey test chains world-building -> fragment synthesis -> opening guide -> 100-chapter generation -> token audit verification in a single test body. This validates end-to-end integration at the target 100-chapter scale. Per CONTEXT.md: "Phase 15 does NOT add new user-facing features" -- this is integration validation only.

Output: Updated full_journey_test.dart (100 chapters), new 15-ISSUE-LOG.md
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-manuscript-story-structure/15-CONTEXT.md
@.planning/phases/15-full-manuscript-story-structure/15-RESEARCH.md
@.planning/phases/15-full-manuscript-story-structure/15-PATTERNS.md
@.planning/phases/15-full-manuscript-story-structure/15-UI-SPEC.md
@.planning/phases/15-02-PLAN.md

<interfaces>
<!-- Key types from existing full_journey_test.dart (435 lines) -->

From test/journey/full_journey_test.dart (the file being modified):
- `_phaseDSerialGeneration()` at line 214-254: Currently loops 30 chapters. Must extend to 100.
- `_generateChapter()` at line 272-317: Builds PromptContext with StoryOutline.chapters[index]. Must add stage prompt + previous chapter summary.
- `_createThirtyChapters()` at line 319-340: Creates 30 chapters. Must parameterize to accept count.
- `_phaseETokenAudit()` at line 256-270: Asserts `snapshot.totalCalls >= 31`. Must update to `>= 101`.
- `_DeterministicFullJourneyAdapter._chapterText()` at line 406-411: Already fixed by Plan 15-02 (no modulo).
- Phase structure: _phaseAWorldBuilding (unchanged), _phaseBFragmentSynthesis (unchanged), _phaseCOpeningGuide (unchanged), _phaseDSerialGeneration (extend), _phaseETokenAudit (update assertion).

From test/journey/helpers/stage_prompts.dart (created by Plan 15-01):
```dart
class StagePrompts {
  static const String goldenCore;
  static const String nascentSoul;
  static const String ascension;
  static String forChapterIndex(int index);
}
```

From .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md (template reference):
- Issue table format: ID, Category, Severity, Requirement, Title, Reproduction Steps, Expected Behavior, Actual Behavior, Evidence
- Severity classification: 高/中/低 with criteria definitions
- Summary statistics table
- Evidence hygiene notes
- RESEARCH.md open questions tracking section
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend E2E full journey test from 30 to 100 chapters</name>
  <files>test/journey/full_journey_test.dart</files>
  <read_first>
    test/journey/full_journey_test.dart
    test/journey/serial_generation_test.dart
    test/journey/helpers/stage_prompts.dart
    test/journey/helpers/d11_bounds.dart
  </read_first>
  <behavior>
    - Deterministic E2E test creates 100 chapters (not 30) and generates all with stage prompts
    - Previous chapter summary injection applied for chapters 2-100
    - enforceD11Bounds applied to every generated chapter
    - Token audit assertion updated: totalCalls >= 101 (100 generation + 1 synthesis + opening guide calls)
    - GLM E2E test similarly extended to 100 chapters with 3-second delays
    - Phase A (world-building), B (fragment synthesis), C (opening guide) remain unchanged
    - _createThirtyChapters renamed to _createChapters with count parameter
  </behavior>
  <action>
    Make the following targeted changes to full_journey_test.dart:

    **1. Add import for stage_prompts:**
    Add `import 'helpers/stage_prompts.dart';` at the top, alongside existing helper imports.

    **2. Parameterize _createThirtyChapters:**
    Rename `_createThirtyChapters` to `_createChapters` and add `int count` parameter. Change loop from `for (var i = 1; i <= 30; i++)` to `for (var i = 1; i <= count; i++)`. Update all callers.

    **3. Extend _phaseDSerialGeneration:**
    - Change `_createThirtyChapters(chapterRepository, manuscript.id)` to `_createChapters(chapterRepository, manuscript.id, 100)`
    - Change loop from `for (var i = 0; i < 30; i++)` to `for (var i = 0; i < 100; i++)`
    - Change delay guard from `if (useDelay && i < 29)` to `if (useDelay && i < 99)`
    - Change debug print from `Chapter ${i + 1}/30` to `Chapter ${i + 1}/100`
    - Change debug print from `Phase D: 30-chapter generation complete` to `Phase D: 100-chapter generation complete`

    **4. Add stage prompt + previous chapter summary to _generateChapter:**
    Inside _generateChapter, BEFORE building PromptContext:
    a. Get previous chapter summary: If `index > 0`, get previous chapter content from the chapters list parameter (pass `List<Chapter> chapters` as additional parameter to _generateChapter). Use `chapters[index - 1].documentContent`, truncate to first 100 chars.
    b. Get stage prompt: `StagePrompts.forChapterIndex(index)`
    c. Build fragment text: Join non-empty stage prompt, non-empty previous summary (prefixed '上一章概要：'), and `StoryOutline.chapters[index]` with '\n\n' separators.
    d. Use this combined text as the fragment text in PromptContext.
    The _generateChapter function signature needs an additional `List<Chapter> chapters` parameter.

    **5. Update _phaseDSerialGeneration to pass chapters to _generateChapter:**
    Pass the `chapters` list to each _generateChapter call as the new parameter.

    **6. Update _phaseETokenAudit assertion:**
    Change `expect(snapshot.totalCalls, greaterThanOrEqualTo(31))` to `expect(snapshot.totalCalls, greaterThanOrEqualTo(101))`. The 101 accounts for: 1 synthesis call (Phase B) + 100 chapter generation calls (Phase D). Update debug print similarly.

    **7. Update deterministic E2E test name:**
    Change test name from "should complete deterministic full xianxia journey without GLM credentials" to "should complete deterministic full xianxia journey to 100 chapters without GLM credentials" (or similar). The skip logic remains the same.

    **8. Update GLM E2E test name and timeout:**
    Change test name from "should complete full xianxia journey from world-building to 30 chapters" to "should complete full xianxia journey from world-building to 100 chapters". Change timeout from `Duration(minutes: 15)` to `Duration(minutes: 60)` (100 chapters with 3s delay = ~5 minutes of delays alone, plus GLM response time).

    **Keep Phase A, B, C completely unchanged.** Only Phase D (generation count) and Phase E (audit assertion) change.

    Per RESEARCH.md Pitfall 1: "70 sequential GLM calls with 2-3s delay may hit rate limits." Set timeout to 60 minutes for the real GLM test. Keep 3-second delays. Log progress every chapter.

    Per RESEARCH.md Pitfall 4: The audit flush in _phaseETokenAudit is already present (line 258). Keep it.
  </action>
  <verify>
    <automated>dart analyze test/journey/full_journey_test.dart && flutter test test/journey/full_journey_test.dart -j 1 --plain-name "should complete deterministic full xianxia journey to 100 chapters" --timeout 600s</automated>
  </verify>
  <done>
    full_journey_test.dart E2E deterministic test covers 100 chapters. Stage prompts injected. Previous chapter summary injected. enforceD11Bounds applied. Token audit >= 101. GLM timeout 60 minutes. Phase A/B/C unchanged. `dart analyze` passes. Deterministic test passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 15 structured issue log template</name>
  <files>.planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md</files>
  <read_first>
    .planning/phases/14-world-building-first-30-chapters/14-ISSUE-LOG.md
    .planning/phases/15-full-manuscript-story-structure/15-UI-SPEC.md
  </read_first>
  <action>
    Create .planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md following the exact same format as Phase 14's issue log (14-ISSUE-LOG.md).

    Include:
    1. **Header:** Phase 15 issue log title, created date, updated date
    2. **Summary statistics table:** Total issues, high/medium/low severity counts, category counts (功能缺陷/体验摩擦/缺失需求) -- all initially 0
    3. **Issues table:** Same column structure as Phase 14: ID, Category, Severity, Requirement, Title, Reproduction Steps, Expected Behavior, Actual Behavior, Evidence -- initially empty (just the header row, no issues pre-populated)
    4. **Severity classification guide:** Same definitions as Phase 14 (高/中/低 with criteria)
    5. **Evidence hygiene notes:** Same as Phase 14 -- "Never paste GLM_API_KEY or any other secret", "Prefer command output excerpts"
    6. **Verification checklist section:** Placeholder for automated and manual verification results during execution

    Use requirement IDs JOURNEY-07 through JOURNEY-10 in the Requirement column (not JOURNEY-05/06 which belong to Phase 14).

    Issue IDs should follow pattern: P15-XX-YY-ZZ where XX is plan number, YY is category code (FUNC for 功能缺陷, UX for 体验摩擦, NEED for 缺失需求), ZZ is sequential number.

    This is a template -- the issue table body should be empty (no pre-populated issues). Issues are filled during execution by the executor agent when problems are discovered.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md && grep -c "Issue Log" .planning/phases/15-full-manuscript-story-structure/15-ISSUE-LOG.md</automated>
  </verify>
  <done>
    15-ISSUE-LOG.md exists with structured format matching Phase 14 issue log. Contains summary statistics table (all zeros), empty issues table with correct column headers, severity guide, evidence hygiene notes, and verification checklist. Issue IDs use JOURNEY-07~10 requirement references.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test -> GLM API | Real GLM API calls with API key from environment variable (GLM variant only) |
| E2E test -> Hive temp storage | Per-container temp directories with cleanup |
| issue log -> execution artifacts | Markdown document records test findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Information Disclosure | GLM API key in E2E test output | mitigate | _safeExceptionDiagnostic() already present in full_journey_test.dart (line 342-361). All error paths use this function. |
| T-15-09 | Information Disclosure | Issue log may contain sensitive test output | mitigate | Issue log includes evidence hygiene notes: "Never paste GLM_API_KEY or any other secret." Executors must follow this guideline. |
| T-15-10 | Denial of Service | Hive temp directory leak on E2E failure | mitigate | try/finally cleanup in setUp/tearDown. Per-container temp directories deleted in cleanupJourneyContainer(). |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
1. `dart analyze test/journey/full_journey_test.dart` -- zero errors
2. Deterministic E2E test passes (100 chapters generated)
3. GLM E2E test skips correctly without GLM_API_KEY
4. Token audit assertion >= 101 (updated from 31)
5. 15-ISSUE-LOG.md exists with correct format
6. Stage prompts and previous chapter summary injection present in _generateChapter
</verification>

<success_criteria>
- full_journey_test.dart covers 100 chapters in both deterministic and GLM variants
- Stage prompts and previous chapter summary injection implemented in generation loop
- Token audit assertion updated to >= 101
- Phase A/B/C unchanged from Phase 14
- 15-ISSUE-LOG.md created with structured template matching Phase 14 format
- `dart analyze` passes on all modified files
</success_criteria>

<output>
Create `.planning/phases/15-full-manuscript-story-structure/15-06-SUMMARY.md` when done
</output>
