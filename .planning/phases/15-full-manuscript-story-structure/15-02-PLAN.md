---
phase: 15-full-manuscript-story-structure
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - test/journey/serial_generation_test.dart
autonomous: true
requirements: [JOURNEY-07]

must_haves:
  truths:
    - "serial_generation_test.dart has a new test that generates chapters 31-100 (or all 100 in deterministic path) with stage prompts"
    - "Previous chapter summary injection is implemented per D-04 (first 100 chars of previous chapter)"
    - "enforceD11Bounds post-processing applied to every generated chapter (inherited D-11)"
    - "Deviation detection runs for all 100 chapters (or 70 new chapters) per D-07"
    - "Deterministic adapter no longer uses modulo -- uses index directly since StoryOutline has 100 entries"
    - "3-second delay between GLM API calls maintained per inherited D-03"
    - "Stop-on-error pattern maintained per inherited D-04"
    - "D-02: Generation loop processes all 100 chapters including those containing D-02's multi-conflict threads (Core Formation failure ch 41-50, Wang Lei schemes throughout, Su Yue captured ch 55-60, sect war ch 75-85, heart demon ch 80-88, heavenly tribulation ch 96-100) -- verified by sampled character-name checks at conflict-chapter indices"
  artifacts:
    - path: "test/journey/serial_generation_test.dart"
      provides: "70-chapter generation test + updated deterministic adapter"
      min_lines: 550
      contains: "StagePrompts.forChapterIndex"
  key_links:
    - from: "serial_generation_test.dart"
      to: "test/journey/helpers/stage_prompts.dart"
      via: "import and StagePrompts.forChapterIndex() call in generation loop"
      pattern: "import.*stage_prompts"
    - from: "serial_generation_test.dart"
      to: "test/journey/helpers/d11_bounds.dart"
      via: "enforceD11Bounds() call on every generated chapter"
      pattern: "enforceD11Bounds\\(output\\)"
---

<objective>
Extend serial_generation_test.dart with 100-chapter generation support: fix deterministic adapter modulo, add stage prompt injection, add previous-chapter summary injection, and add a new test covering chapters 31-100 generation with deviation detection.

Purpose: This plan implements the core 70-chapter generation capability that all downstream validation tests depend on. Per D-03 (reuse serial_generation_test pattern + stage prompts), D-04 (previous chapter summary injection), and D-02 (multi-conflict threads exercised via 100-chapter generation across all conflict arcs).

Output: Updated serial_generation_test.dart with deterministic adapter fix, new 100-chapter generation test
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-full-manuscript-story-structure/15-CONTEXT.md
@.planning/phases/15-full-manuscript-story-structure/15-RESEARCH.md
@.planning/phases/15-full-manuscript-story-structure/15-PATTERNS.md
@.planning/phases/15-full-manuscript-story-structure/15-UI-SPEC.md
@.planning/phases/15-01-PLAN.md

<interfaces>
<!-- Key types and contracts from existing code -->

From test/journey/serial_generation_test.dart (484 lines, the file being modified):
- `_DeterministicJourneyAdapter._chapterText(int index)` at line 446-452: Currently uses `(index % StoryOutline.chapters.length) + 1` which wraps around after 30 entries. Must change to `index + 1`.
- `_generateThirtyChapterJourney()` at line 127-252: Creates manuscript, 30 chapters, generates via GLM/Fake, runs deviation detection, flushes audit. The pattern for the new 100-chapter function.
- `generateChapter(int index)` inner function at line 154-187: Uses `StoryOutline.chapters[index]` as fragment text. Must add stage prompt prefix and previous-chapter summary injection.
- `_createThirtyChapters()` at line 343-365: Creates chapters in repository. Parameterize to accept count.
- `_runDeviationDetectionForAllChapters()` at line 367-404: Runs deviation detection for all chapters. Works for any chapter count.
- `_safeExceptionDiagnostic()` at line 287-306: Sanitizes error output. Reuse as-is.
- `_JourneyResult` class at line 465-483: Holds generation results. May need field additions.

From test/journey/helpers/stage_prompts.dart (created by Plan 15-01):
```dart
class StagePrompts {
  static const String goldenCore;
  static const String nascentSoul;
  static const String ascension;
  static String forChapterIndex(int index);
}
```

From test/journey/helpers/story_outline.dart (extended by Plan 15-01):
```dart
class StoryOutline {
  static const List<String> chapters; // Now 100 entries
  static const List<String> characterNames;
}
```

From test/journey/helpers/d11_bounds.dart (48 lines, unchanged):
```dart
String enforceD11Bounds(String rawOutput); // Truncates at 500 chars with sentence boundary
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deterministic adapters and parameterize chapter creation</name>
  <files>test/journey/serial_generation_test.dart, test/journey/full_journey_test.dart</files>
  <read_first>
    test/journey/serial_generation_test.dart
    test/journey/full_journey_test.dart
  </read_first>
  <action>
    Make three targeted changes in serial_generation_test.dart:

    1. **Fix _DeterministicJourneyAdapter._chapterText()** (line 447): Change `final chapterNo = (index % StoryOutline.chapters.length) + 1;` to `final chapterNo = index + 1;`. Also change line 449 `StoryOutline.chapters[index % StoryOutline.chapters.length]` to `StoryOutline.chapters[index]`. Change line 448 `StoryOutline.characterNames[index % StoryOutline.characterNames.length]` to `StoryOutline.characterNames[index % StoryOutline.characterNames.length]` (this one can stay modulo since we only have 4 character names).

    2. **Fix _DeterministicFullJourneyAdapter._chapterText()** in full_journey_test.dart (line 407): Same change -- `(index % StoryOutline.chapters.length) + 1` becomes `index + 1`, and `StoryOutline.chapters[index % StoryOutline.chapters.length]` becomes `StoryOutline.chapters[index]`.

    3. **Parameterize _createThirtyChapters** in serial_generation_test.dart: Rename to `_createChapters` and add an `int count` parameter. Replace `for (var i = 1; i <= 30; i++)` with `for (var i = 1; i <= count; i++)`. Replace `StoryOutline.chapters[i - 1]` reference (which is fine since i starts at 1). Update all callers to pass count: the existing 30-chapter test passes `30`.

    Do NOT change the existing 30-chapter test logic or the smoke test. Only fix the adapter modulo bug and parameterize the helper.
  </action>
  <verify>
    <automated>dart analyze test/journey/serial_generation_test.dart test/journey/full_journey_test.dart</automated>
  </verify>
  <done>
    _DeterministicJourneyAdapter._chapterText uses `index + 1` not modulo. _DeterministicFullJourneyAdapter._chapterText uses `index + 1` not modulo. _createChapters accepts count parameter. All callers updated. `dart analyze` passes with zero errors on both files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 100-chapter generation test with stage prompts and previous-chapter summary injection</name>
  <files>test/journey/serial_generation_test.dart</files>
  <read_first>
    test/journey/serial_generation_test.dart
    test/journey/helpers/stage_prompts.dart
    test/journey/helpers/d11_bounds.dart
  </read_first>
  <behavior>
    - New test "deterministic 100-chapter journey should generate chapters 31-100 with stage prompts" runs without GLM credentials
    - 100 chapters created in repository (30 via _createChapters(30) + 70 via _createChapters(70) starting at sortOrder 31)
    - Each generated chapter (31-100) has stage prompt prepended to fragment text when generating via PromptPipeline
    - Each generated chapter (31+) has previous chapter summary injected (first 100 chars of previous chapter content)
    - enforceD11Bounds applied to every generated chapter output
    - All 100 chapters have content length in 300-500 char range after enforceD11Bounds
    - Deviation detection runs for chapters 31-100 (70 chapters)
    - Token audit flushed and verified: totalCalls >= 70 (70 generation calls for ch 31-100)
    - 3-second delay between GLM calls in real GLM variant
    - Stop-on-error: first failure rethrows immediately
  </behavior>
  <action>
    Add two new tests to serial_generation_test.dart and a new generation function.

    **Import:** Add `import 'helpers/stage_prompts.dart';` at the top of serial_generation_test.dart.

    **New function _generateHundredChapterJourney():**
    Based on _generateThirtyChapterJourney() but with these changes:
    - Creates manuscript with title '剑道苍穹'
    - Calls `_createChapters(chapterRepository, manuscript.id, 100)` to create all 100 chapters
    - Runs GLM smoke test (same as existing)
    - Generation loop: `for (var i = 0; i < 100; i++)` -- generates ALL 100 chapters
    - Inside generateChapter inner function, BEFORE building PromptContext:
      a. Get previous chapter summary per D-04: If `i > 0`, read `chapters[i-1].documentContent` from repository (via `chapterRepository.getByManuscriptId(manuscript.id)` sorted by sortOrder, take index i-1). Truncate to first 100 chars: `prevContent.length > 100 ? '${prevContent.substring(0, 100)}...' : prevContent`
      b. Get stage prompt: `StagePrompts.forChapterIndex(i)`
      c. Build fragment text: join non-empty stage prompt, non-empty previous summary (prefixed with '上一章概要：'), and plot point `StoryOutline.chapters[i]` with '\n\n' separators
    - The rest is identical: PromptPipeline build, createStream, enforceD11Bounds, recordAudit, updateDocumentContent
    - 3-second delay: `if (useDelay && i < 99) await Future.delayed(Duration(seconds: 3))`
    - Stop-on-error: `catch (e) { debugPrint(...); rethrow; }` same as existing
    - After generation loop: run deviation detection for ALL 100 chapters via _runDeviationDetectionForAllChapters (already works for any count)
    - Flush audit, build snapshot, return _JourneyResult (extend with `totalChapters: 100` field if needed)
    - Validation: each chapter content length in 300-500 range, sampled character-name checks at conflict-chapter indices per D-02 -- include indices [44, 57, 79, 84, 96] (Core Formation failure, Su Yue captured, sect war, heart demon, heavenly tribulation chapters from D-02)

    **New test 1 (deterministic, always runs):**
    "deterministic 100-chapter journey should generate with stage prompts and summary injection"
    - Creates container with `_DeterministicJourneyAdapter(FakeAdapter())`
    - Calls `_generateHundredChapterJourney()` with `useDelay: false, runDeviationDetection: true`
    - Asserts: 100 chapters, all content non-empty, all in 300-500 range, snapshot.totalCalls >= 100, deviation checks == 100
    - Skip logic: skip if GLM_API_KEY IS set (deterministic path runs only without credentials), matching existing pattern
    - Timeout: 10 minutes

    **New test 2 (real GLM, skip without key):**
    "should generate chapters 31-100 with knowledge injection and stage prompts"
    - Uses existing container (requires GLM_API_KEY)
    - Calls `_generateHundredChapterJourney()` with `useDelay: true, runDeviationDetection: true`
    - Asserts: 100 chapters, character names found in sampled chapters at conflict indices, deviation checks == 100, snapshot.totalCalls >= 100
    - Skip logic: skip if GLM_API_KEY not set
    - Timeout: 60 minutes (70 sequential GLM calls with 3s delay)

    **Keep the existing 30-chapter tests unchanged.** The new tests are additive.

    Per RESEARCH.md Pitfall 1: Set timeout to 60 minutes for real GLM. Per Pitfall 3: The deterministic adapter now has 100 StoryOutline entries (no modulo wrap). Per Pitfall 4: Always flush audit before reading stats.
  </action>
  <verify>
    <automated>dart analyze test/journey/serial_generation_test.dart && flutter test test/journey/serial_generation_test.dart -j 1 --plain-name "deterministic 100-chapter journey" --timeout 600s</automated>
  </verify>
  <done>
    serial_generation_test.dart has 2 new tests (deterministic 100-chapter + real GLM 100-chapter). Deterministic adapter fixed (no modulo). _createChapters parameterized. Stage prompt and previous-chapter summary injection implemented in generation loop. Character-name checks include D-02 conflict-chapter indices. All existing tests unchanged. `dart analyze` passes. Deterministic test passes (skipped if GLM_API_KEY set).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test script -> GLM API | Real GLM API calls with API key from environment variable |
| test script -> Hive temp storage | Per-container temp directories with cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Information Disclosure | GLM API key in test output | mitigate | _safeExceptionDiagnostic() sanitizes bearer tokens and api_key values (inherited from Phase 14). All error paths use this function. |
| T-15-04 | Denial of Service | Hive temp directory leak on test failure | mitigate | try/finally cleanup in tearDown + _generateHundredChapterJourney wraps generation in try/catch with rethrow. Per RESEARCH.md Pitfall 2. |
| T-15-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
1. `dart analyze test/journey/serial_generation_test.dart` -- zero errors
2. Deterministic 100-chapter test passes when GLM_API_KEY is not set
3. Existing 30-chapter tests remain unchanged and pass
4. _DeterministicJourneyAdapter no longer uses modulo
5. `StagePrompts.forChapterIndex` called in generation loop
6. Previous chapter summary injection present in generateChapter inner function
7. Sampled character-name checks include D-02 conflict-chapter indices [44, 57, 79, 84, 96]
</verification>

<success_criteria>
- 2 new tests added: deterministic 100-chapter (always runnable) + real GLM 100-chapter (skipped without key)
- Stage prompts injected for chapters 31-100
- Previous chapter summary injected for chapters 2-100
- enforceD11Bounds applied to every chapter
- Deviation detection runs for all 100 chapters
- Token audit records >= 100 after flush
- Character-name checks exercise D-02 conflict arcs (indices at conflict-chapter positions)
- All existing tests unchanged and passing
</success_criteria>

<output>
Create `.planning/phases/15-full-manuscript-story-structure/15-02-SUMMARY.md` when done
</output>
