---
phase: 08-onboarding-guide
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/features/onboarding/presentation/wizard_steps/world_step_page.dart
  - lib/features/onboarding/presentation/wizard_steps/character_step_page.dart
  - lib/features/onboarding/presentation/onboarding_wizard_page.dart
  - test/features/onboarding/presentation/onboarding_wizard_test.dart
autonomous: true
requirements:
  - ONBD-02
user_setup: []

must_haves:
  truths:
    - "Step 2 allows world name and description entry with validation"
    - "Step 3 allows character name and description entry with validation"
    - "WorldSetting entity created when user completes world step"
    - "CharacterCard entity created when user completes character step"
    - "Both entities persist to Hive and appear in knowledge base after wizard"
  artifacts:
    - path: "lib/features/onboarding/presentation/wizard_steps/world_step_page.dart"
      provides: "World setting creation form (simplified from WorldSettingForm)"
      contains: "TextFormField", "GlobalKey<FormState>"
    - path: "lib/features/onboarding/presentation/wizard_steps/character_step_page.dart"
      provides: "Character card creation form (simplified from CharacterCardForm)"
      contains: "TextFormField", "GlobalKey<FormState>"
    - path: "lib/features/onboarding/presentation/onboarding_wizard_page.dart"
      provides: "Wizard modified to integrate entity creation on step advance"
      contains: "templateInstantiationServiceProvider"
  key_links:
    - from: "onboarding_wizard_page.dart"
      to: "templateInstantiationServiceProvider"
      via: "entity creation service"
      pattern: "ref.read\(templateInstantiationServiceProvider\)"
    - from: "templateInstantiationServiceProvider"
      to: "Hive boxes (world_settings, character_cards)"
      via: "entity persistence"
      pattern: "worldSettingRepository\.add\(|characterCardRepository\.add\("
---

<objective>
Implement the world and character creation step pages with entity creation via TemplateInstantiationService.

Purpose: Enable users to create WorldSetting and CharacterCard entities during the onboarding wizard, delivering tangible value as specified in ROADMAP success criterion #2 ("each step to deliver tangible value"). This plan implements steps 2 and 3 of the wizard with form validation and automatic entity creation.

Output: Working WorldStepPage and CharacterStepPage with form validation, and integration with TemplateInstantiationService to persist created entities to Hive knowledge base storage.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-guide/08-RESEARCH.md
@.planning/phases/08-onboarding-guide/08-PATTERNS.md
@.planning/phases/08-onboarding-guide/08-UI-SPEC.md

@lib/features/knowledge/presentation/world_setting_form.dart (form pattern, field validation, entity creation)
@lib/features/knowledge/presentation/character_card_form.dart (form pattern, field validation, entity creation)
@lib/features/templates/domain/world_template.dart (WorldSetting fields)
@lib/features/knowledge/domain/character_card.dart (CharacterCard fields)
@lib/features/templates/application/template_instantiation_service.dart (entity creation service from Phase 7)
@lib/core/presentation/providers.dart (provider registrations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WorldStepPage with simplified world form</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/world_step_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/world_setting_form.dart (form structure, validation, save pattern)
    lib/features/templates/domain/world_template.dart (WorldSetting fields)
  </read_first>
  <action>
    Create ConsumerStatefulWidget WorldStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - Constructor takes TextEditingController for worldName and worldDescription (passed from OnboardingWizardPage)
    - State field: GlobalKey<FormState>

    Build method:
    - Form with GlobalKey<FormState>
    - TextFormField for world name:
      * controller: worldNameController
      * labelText: '世界观名称 *'
      * hintText: '为你的故事世界命名'
      * validator: required check, max length 100
      * maxLength: 100
    - TextFormField for world description:
      * controller: worldDescriptionController
      * labelText: '世界简介'
      * hintText: '描述你的故事背景、设定、规则等'
      * maxLines: 3
      * maxLength: 500
    - No submit button - parent OnboardingWizardPage handles "下一步"

    Validation:
    - Name is required (non-empty after trim)
    - Description is optional
    - Name length <= 100 characters

    Skip behavior:
    - Form can be skipped even if invalid (parent _skipStep doesn't call validate)
    - Only validate when "下一步" is clicked (parent calls validate before advancing)

    Note: This is a simplified version of WorldSettingForm - only 2 fields, no separate Scaffold. Entity creation happens via TemplateInstantiationService when wizard advances to next step.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests world step validation)</automated>
  </verify>
  <done>
    WorldStepPage has 2 TextFormFields, name validation works, description is optional, form state preserved via AutomaticKeepAliveClientMixin, controllers update parent state
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CharacterStepPage with simplified character form</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/character_step_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/character_card_form.dart (form structure, validation, CharacterCard fields)
    lib/features/knowledge/domain/character_card.dart (CharacterCard fields)
  </read_first>
  <action>
    Create ConsumerStatefulWidget CharacterStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - Constructor takes TextEditingController for characterName and characterDescription
    - State field: GlobalKey<FormState>

    Build method:
    - Form with GlobalKey<FormState>
    - TextFormField for character name:
      * controller: characterNameController
      * labelText: '角色名称 *'
      * hintText: '主角的名字'
      * validator: required check, max length 50
      * maxLength: 50
    - TextFormField for character description:
      * controller: characterDescriptionController
      * labelText: '角色简介'
      * hintText: '描述角色的性格、外貌、背景等'
      * maxLines: 3
      * maxLength: 500
    - No submit button - parent handles navigation

    Validation:
    - Name is required (non-empty after trim)
    - Description is optional
    - Name length <= 50 characters

    Skip behavior:
    - Form can be skipped even if invalid
    - Only validate when "下一步" is clicked

    Note: Simplified CharacterCardForm - only 2 core fields, no separate Scaffold. Entity creation via TemplateInstantiationService on wizard completion.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests character step validation)</automated>
  </verify>
  <done>
    CharacterStepPage has 2 TextFormFields, name validation works, description is optional, form state preserved, controllers update parent state
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate WorldStepPage and CharacterStepPage into OnboardingWizardPage with entity creation</name>
  <files>
    lib/features/onboarding/presentation/onboarding_wizard_page.dart
  </files>
  <read_first>
    lib/features/onboarding/presentation/onboarding_wizard_page.dart (wizard navigation, _nextStep method)
    lib/features/templates/application/template_instantiation_service.dart (instantiateWorld and instantiateCharacter methods)
  </read_first>
  <action>
    Modify OnboardingWizardPage to replace stub step pages with real implementations:
    - Replace WorldStepPage stub with real WorldStepPage implementation
    - Replace CharacterStepPage stub with real CharacterStepPage implementation
    - Pass worldNameController, worldDescriptionController to WorldStepPage
    - Pass characterNameController, characterDescriptionController to CharacterStepPage

    Modify _nextStep method to create entities when advancing from world/character steps:
    - When advancing from step 1 (genre) to step 2 (world): no entity creation
    - When advancing from step 2 (world) to step 3 (character):
      * Validate world form via GlobalKey<FormState>
      * If valid and not skipped, call templateInstantiationServiceProvider.instantiateWorld()
      * Pass worldName and worldDescription from controllers
      * Pass selectedTemplateId from genre step
      * Entity is persisted to Hive via WorldSettingRepository
    - When advancing from step 3 (character) to step 4 (opening):
      * Validate character form via GlobalKey<FormState>
      * If valid and not skipped, call templateInstantiationServiceProvider.instantiateCharacter()
      * Pass characterName and characterDescription from controllers
      * Entity is persisted to Hive via CharacterCardRepository

    Error handling for entity creation:
    - Wrap instantiateWorld/instantiateCharacter calls in try-catch
    - On error, show SnackBar with error message
    - Allow user to retry or skip

    Update imports to include WorldStepPage, CharacterStepPage, and TemplateInstantiationService.

    Note: This modification requires the wizard to read templateInstantiationServiceProvider from ref. The provider is already registered from Phase 7.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests entity creation on step advance)</automated>
  </verify>
  <done>
    OnboardingWizardPage uses real WorldStepPage and CharacterStepPage implementations, entities are created when advancing from steps 2 and 3, created entities persist to Hive and appear in knowledge base, error states show SnackBar, skip behavior works without entity creation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input→form fields | Untrusted user input in world/character name and description fields |
| form→TemplateInstantiationService | User input passed to entity creation service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Input Validation | WorldStepPage/CharacterStepPage text fields | mitigate | Validators enforce maxLength (name: 100/50, description: 500), required field checks on non-skip navigation |
| T-08-06 | Tampering | Entity creation with malicious input | accept | Entities are stored locally only, no network transmission; TemplateInstantiationService applies its own validation and sanitization |
</threat_model>

<verification>
## Per-Task Verification

**Task 1:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test world name validation (required, max length)
- Test world description optional
- Test form state preserved with AutomaticKeepAliveClientMixin

**Task 2:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test character name validation (required, max length)
- Test character description optional
- Test form state preserved

**Task 3:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test WorldSetting entity created when advancing from step 2
- Test CharacterCard entity created when advancing from step 3
- Test entities persist to Hive and appear in knowledge base
- Test skip does not create entities
- Test error handling shows SnackBar on entity creation failure

## Phase Gate

- All widget tests pass: `flutter test test/features/onboarding/presentation/`
- Manual: Walk through wizard steps 2 and 3, verify entities appear in knowledge base, test skip behavior
</verification>

<success_criteria>
1. WorldStepPage and CharacterStepPage validate form input correctly
2. WorldSetting entity created when user completes step 2 with valid input
3. CharacterCard entity created when user completes step 3 with valid input
4. Created entities persist to Hive and appear in knowledge base after wizard
5. Skip button allows advancing without entity creation
6. Form state preserved when swiping back (AutomaticKeepAliveClientMixin)
7. Error states handled gracefully with SnackBar messages
8. All widget tests pass
9. Plan correctly declares depends_on: ["08-01"] for onboardingProgressProvider dependency
</success_criteria>

<output>
Create `.planning/phases/08-onboarding-guide/08-03-SUMMARY.md` when done
</output>
