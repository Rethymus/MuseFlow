---
phase: 08-onboarding-guide
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/features/onboarding/presentation/onboarding_wizard_page.dart
  - lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart
  - test/features/onboarding/presentation/onboarding_wizard_test.dart
autonomous: true
requirements:
  - ONBD-02
  - ONBD-03
user_setup: []

must_haves:
  truths:
    - "Wizard displays 4-step PageView with progress bar and navigation buttons"
    - "Step 1 shows genre grid with 14 template cards from WorldTemplateRepository"
    - "User can select template, skip steps, and close wizard"
    - "Wizard progress persists across app restarts via OnboardingProgressRepository"
  artifacts:
    - path: "lib/features/onboarding/presentation/onboarding_wizard_page.dart"
      provides: "Full-screen wizard host with PageView, progress bar, skip/close actions"
      contains: "PageController", "PageView", "AutomaticKeepAliveClientMixin"
    - path: "lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart"
      provides: "Genre selection grid reusing _TemplateCard pattern"
      contains: "GridView.builder", "WorldTemplateRepository"
  key_links:
    - from: "onboarding_wizard_page.dart"
      to: "onboardingProgressProvider"
      via: "provider read in initState"
      pattern: "ref.read\(onboardingProgressProvider\)"
    - from: "genre_step_page.dart"
      to: "worldTemplateRepositoryProvider"
      via: "provider read for template list"
      pattern: "ref.watch\(worldTemplateRepositoryProvider\)"
    - from: "onboarding_wizard_page.dart"
      to: "wizard_steps/"
      via: "PageView children"
      pattern: "PageView\(children: \[GenreStepPage, WorldStepPage, CharacterStepPage, OpeningStepPage\]\)"
---

<objective>
Implement the onboarding wizard host PageView with navigation controls and the genre selection step page.

Purpose: Provide the structural foundation for the 4-step wizard flow with PageView-based navigation, progress persistence, and skip/resume functionality. This plan creates the wizard shell and the first step (genre selection) to establish the navigation pattern before adding remaining steps.

Output: Working OnboardingWizardPage with PageView, progress bar, navigation buttons, and GenreStepPage displaying 14 template cards from Phase 7 templates.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-guide/08-RESEARCH.md
@.planning/phases/08-onboarding-guide/08-PATTERNS.md
@.planning/phases/08-onboarding-guide/08-UI-SPEC.md

@lib/features/knowledge/presentation/skill_generation_wizard.dart (multi-step wizard pattern)
@lib/features/templates/presentation/template_gallery_page.dart (_TemplateCard, _PassiveTag patterns)
@lib/features/templates/infrastructure/world_template_repository.dart (template loading)
@lib/core/presentation/providers.dart (provider registrations)
@lib/features/onboarding/infrastructure/onboarding_progress_repository.dart (from Plan 08-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingWizardPage with PageView and progress bar</name>
  <files>
    lib/features/onboarding/presentation/onboarding_wizard_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/skill_generation_wizard.dart (wizard state management, step index, controllers)
    lib/app.dart (router structure, for understanding navigation context)
  </read_first>
  <action>
    Create ConsumerStatefulWidget OnboardingWizardPage:
    - State fields: PageController, int currentStep, TextEditingController for world/character names (for future steps)
    - initState: read onboardingProgressProvider (from Plan 08-01), restore currentStep, initialize PageController
    - dispose: dispose PageController and controllers

    Build method structure:
    - Scaffold with transparent AppBar (no title, actions: close icon on right, skip button on right)
    - Body: Column with progress indicator (LinearProgressIndicator or custom dots), step title/subtitle, PageView
    - Bottom navigation bar: "上一步" button (disabled if step 0), "下一步" or "开始创作" button (step 3)

    PageView configuration:
    - 4 children: GenreStepPage (implemented in this plan), WorldStepPage (placeholder from Plan 08-03), CharacterStepPage (placeholder from Plan 08-03), OpeningStepPage (placeholder from Plan 08-05)
    - onPageChange callback updates currentStep state
    - physics: NeverScrollableScrollPhysics (disable swipe, use buttons only) OR allow with ClampingScrollPhysics

    Progress display:
    - Horizontal LinearProgressIndicator with value: (currentStep + 1) / 4
    - Label: "Step X of 4" or progress dots (4 dots, filled up to currentStep)

    Navigation methods:
    - _nextStep(): validate current step, save progress to OnboardingProgressRepository, animateToPage
    - _skipStep(): mark step as completed without saving entities, advance
    - _previousStep(): animateToPage(currentStep - 1)
    - _completeOnboarding(): call markCompleted(), context.go(AppConstants.editor)

    Skip button behavior:
    - Always visible, calls _skipStep()
    - Step 4 (opening) skip means complete wizard without inserting opening

    Close button behavior:
    - Top-right X icon
    - Calls _completeOnboarding() directly (marks complete, navigates to editor)

    State preservation:
    - Pass TextEditingController instances to step pages via constructor
    - Use AutomaticKeepAliveClientMixin on step pages (see Task 2)

    Note: WorldStepPage, CharacterStepPage, and OpeningStepPage are stubs in this plan. They will be implemented in Plans 08-03 and 08-05. Use Container with placeholder text for now.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart</automated>
  </verify>
  <done>
    OnboardingWizardPage builds without errors, PageView has 4 children (GenreStepPage implemented, others stubbed), progress bar updates on step change, navigation buttons advance/retreat correctly, close/skip buttons work, state persists across widget rebuild, depends on onboardingProgressProvider from Plan 08-01
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GenreStepPage with template grid</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart
  </files>
  <read_first>
    lib/features/templates/presentation/template_gallery_page.dart (_TemplateCard layout, _PassiveTag widget, icon mapping)
    lib/features/templates/infrastructure/world_template_repository.dart (getAll method, template structure)
  </read_first>
  <action>
    Create ConsumerStatefulWidget GenreStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - State field: String? selectedTemplateId

    Build method:
    - Read worldTemplateRepositoryProvider (from Phase 7)
    - Call getAll() to get List<WorldTemplate>
    - Display GridView.builder with 3 columns (crossAxisCount: 3)
    - Each item: template card with icon, title, description, tags

    Card design (reuse _TemplateCard pattern):
    - Card with 12px border radius, margin 12px bottom
    - GestureDetector wrapping card
    - Leading: CircleAvatar with icon (use _iconFor mapping from template_gallery_page)
    - Title: template.displayTitle (label style, 12px w500)
    - Subtitle: template.description (body, 14px w400, maxLines: 2, ellipsis)
    - Tags: Wrap with _PassiveTag widgets for channel (男频/女频) and template.tags
    - Selected state: border color changes to colorScheme.primary, width 2px

    _iconFor mapping (copy from template_gallery_page):
    - filter_vintage, location_city, science, fantasy, martial_arts, history, etc.
    - Default: Icons.auto_awesome

    Tap behavior:
    - Update selectedTemplateId in parent OnboardingWizardPage via callback or direct provider access
    - Update card visual selection state

    Empty state:
    - If template list is empty, show "暂无模板" message

    Note: This is a simplified gallery - no search/filter, just the full grid.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests genre step rendering and selection)</automated>
  </verify>
  <done>
    GenreStepPage displays all 14 templates in 3-column grid, card selection highlights border, selectedTemplateId updates on tap, empty state shows message, AutomaticKeepAliveClientMixin preserves state
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input→template selection | User's template choice persisted to OnboardingProgress |
| wizard→OnboardingProgress | Progress state may contain invalid templateId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | Persisted OnboardingProgress with malicious templateId | accept | TemplateId is validated against WorldTemplateRepository lookup during entity creation; invalid IDs result in empty/null, no crash |
</threat_model>

<verification>
## Per-Task Verification

**Task 1:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test wizard builds with 4 steps
- Test PageView navigation works
- Test progress bar updates
- Test close button marks completed and navigates to editor
- Test skip button advances without validation
- Test dependency on onboardingProgressProvider from Plan 08-01

**Task 2:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test genre grid displays 14 templates
- Test template card selection highlights border
- Test selectedTemplateId updates correctly

## Phase Gate

- All widget tests pass: `flutter test test/features/onboarding/presentation/`
- Manual: Open app fresh, verify wizard launches, navigate through step 1, test skip button
</verification>

<success_criteria>
1. OnboardingWizardPage displays 4-step PageView with progress bar
2. GenreStepPage shows 14 template cards from WorldTemplateRepository
3. Skip/close buttons work correctly
4. Form state preserved when swiping back (AutomaticKeepAliveClientMixin)
5. Wizard persists progress via OnboardingProgressRepository from Plan 08-01
6. All widget tests pass
7. Plan correctly declares depends_on: ["08-01"] for onboardingProgressProvider dependency
</success_criteria>

<output>
Create `.planning/phases/08-onboarding-guide/08-02-SUMMARY.md` when done
</output>
