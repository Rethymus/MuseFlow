---
phase: 08-onboarding-guide
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/onboarding/presentation/onboarding_wizard_page.dart
  - lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart
  - lib/features/onboarding/presentation/wizard_steps/world_step_page.dart
  - lib/features/onboarding/presentation/wizard_steps/character_step_page.dart
  - lib/features/onboarding/presentation/wizard_steps/opening_step_page.dart
  - lib/features/onboarding/presentation/opening_variant_card.dart
  - test/features/onboarding/presentation/onboarding_wizard_test.dart
autonomous: true
requirements:
  - ONBD-02
  - ONBD-03
user_setup: []

must_haves:
  truths:
    - "Wizard displays 4 steps with PageView swipe navigation"
    - "Step 1 shows genre grid with 14 template cards from WorldTemplateRepository"
    - "Step 2 allows world name/description entry with validation"
    - "Step 3 allows character name/description entry with validation"
    - "Step 4 shows AI opening generation placeholder (UI only, service in Plan 03)"
    - "Each step has skip button and progress persists via OnboardingProgressRepository"
    - "PageView pages preserve form state when swiping back (AutomaticKeepAliveClientMixin)"
  artifacts:
    - path: "lib/features/onboarding/presentation/onboarding_wizard_page.dart"
      provides: "Full-screen wizard host with PageView, progress bar, skip/close actions"
      contains: "PageController", "PageView", "AutomaticKeepAliveClientMixin"
    - path: "lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart"
      provides: "Genre selection grid reusing _TemplateCard pattern"
      contains: "GridView.builder", "WorldTemplateRepository"
    - path: "lib/features/onboarding/presentation/wizard_steps/world_step_page.dart"
      provides: "World setting creation form (simplified from WorldSettingForm)"
      contains: "TextFormField", "GlobalKey<FormState>"
    - path: "lib/features/onboarding/presentation/wizard_steps/character_step_page.dart"
      provides: "Character card creation form (simplified from CharacterCardForm)"
      contains: "TextFormField", "GlobalKey<FormState>"
    - path: "lib/features/onboarding/presentation/wizard_steps/opening_step_page.dart"
      provides: "AI opening generation UI placeholder (loading state, 3 variant cards skeleton)"
      contains: "OpeningVariantCard", "CircularProgressIndicator"
    - path: "lib/features/onboarding/presentation/opening_variant_card.dart"
      provides: "Card widget for displaying one opening variant with style badge and select button"
      contains: "OpeningVariantStyle", "FilledButton"
  key_links:
    - from: "onboarding_wizard_page.dart"
      to: "onboardingProgressProvider"
      via: "provider read in initState"
      pattern: "ref.read\(onboardingProgressProvider\)"
    - from: "genre_step_page.dart"
      to: "worldTemplateRepositoryProvider"
      via: "provider read for template list"
      pattern: "ref.watch\(worldTemplateRepositoryProvider\)"
    - from: "world_step_page.dart"
      to: "templateInstantiationServiceProvider"
      via: "world creation service"
      pattern: "ref.read\(templateInstantiationServiceProvider\)"
    - from: "character_step_page.dart"
      to: "templateInstantiationServiceProvider"
      via: "character creation service"
      pattern: "ref.read\(templateInstantiationServiceProvider\)"
    - from: "onboarding_wizard_page.dart"
      to: "wizard_steps/"
      via: "PageView children"
      pattern: "PageView\(children: \[GenreStepPage, WorldStepPage, CharacterStepPage, OpeningStepPage\]\)"
---

<objective>
Implement the 4-step onboarding wizard UI with PageView navigation, genre selection from Phase 7 templates, simplified world/character forms, and skip/resume functionality.

Purpose: Provide a complete wizard flow that collects user choices (genre, world, character) while allowing step skipping and state persistence across interruptions.

Output: Working OnboardingWizardPage with 4 step pages, genre grid, world/character forms, progress bar, and state management.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-onboarding-guide/08-RESEARCH.md
@.planning/phases/08-onboarding-guide/08-PATTERNS.md
@.planning/phases/08-onboarding-guide/08-UI-SPEC.md

@lib/features/knowledge/presentation/skill_generation_wizard.dart (multi-step wizard pattern)
@lib/features/knowledge/presentation/world_setting_form.dart (form pattern, field validation)
@lib/features/knowledge/presentation/character_card_form.dart (form pattern, field validation)
@lib/features/templates/presentation/template_gallery_page.dart (_TemplateCard, _PassiveTag patterns)
@lib/features/templates/infrastructure/world_template_repository.dart (template loading)
@lib/features/templates/application/template_instantiation_service.dart (entity creation)
@lib/core/presentation/providers.dart (provider registrations)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingWizardPage with PageView and progress bar</name>
  <files>
    lib/features/onboarding/presentation/onboarding_wizard_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/skill_generation_wizard.dart (wizard state management, step index, controllers)
    lib/app.dart (router structure, for understanding navigation context)
  </read_first>
  <action>
    Create ConsumerStatefulWidget OnboardingWizardPage:
    - State fields: PageController, int currentStep, TextEditingController for world/character names
    - initState: read onboardingProgressProvider, restore currentStep, initialize PageController
    - dispose: dispose PageController and controllers

    Build method structure:
    - Scaffold with transparent AppBar (no title, actions: close icon on right, skip button on right)
    - Body: Column with progress indicator (LinearProgressIndicator or custom dots), step title/subtitle, PageView
    - Bottom navigation bar: "上一步" button (disabled if step 0), "下一步" or "开始创作" button (step 3)

    PageView configuration:
    - 4 children: GenreStepPage, WorldStepPage, CharacterStepPage, OpeningStepPage
    - onPageChange callback updates currentStep state
    - physics: NeverScrollableScrollPhysics (disable swipe, use buttons only) OR allow with ClampingScrollPhysics

    Progress display:
    - Horizontal LinearProgressIndicator with value: (currentStep + 1) / 4
    - Label: "Step X of 4" or progress dots (4 dots, filled up to currentStep)

    Navigation methods:
    - _nextStep(): validate current step, save progress to OnboardingProgressRepository, animateToPage
    - _skipStep(): mark step as completed without saving entities, advance
    - _previousStep(): animateToPage(currentStep - 1)
    - _completeOnboarding(): call markCompleted(), context.go(AppConstants.editor)

    Skip button behavior:
    - Always visible, calls _skipStep()
    - Step 4 (opening) skip means complete wizard without inserting opening

    Close button behavior:
    - Top-right X icon
    - Calls _completeOnboarding() directly (marks complete, navigates to editor)

    State preservation:
    - Pass TextEditingController instances to step pages via constructor
    - Use AutomaticKeepAliveClientMixin on step pages (see Task 2-5)
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart</automated>
  </verify>
  <done>
    OnboardingWizardPage builds without errors, PageView has 4 children, progress bar updates on step change, navigation buttons advance/retreat correctly, close/skip buttons work, state persists across widget rebuild
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GenreStepPage with template grid</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/genre_step_page.dart
  </files>
  <read_first>
    lib/features/templates/presentation/template_gallery_page.dart (_TemplateCard layout, _PassiveTag widget, icon mapping)
    lib/features/templates/infrastructure/world_template_repository.dart (getAll method, template structure)
  </read_first>
  <action>
    Create ConsumerStatefulWidget GenreStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - State field: String? selectedTemplateId

    Build method:
    - Read worldTemplateRepositoryProvider
    - Call getAll() to get List<WorldTemplate>
    - Display GridView.builder with 3 columns (crossAxisCount: 3)
    - Each item: template card with icon, title, description, tags

    Card design (reuse _TemplateCard pattern):
    - Card with 12px border radius, margin 12px bottom
    - GestureDetector wrapping card
    - Leading: CircleAvatar with icon (use _iconFor mapping from template_gallery_page)
    - Title: template.displayTitle (label style, 12px w500)
    - Subtitle: template.description (body, 14px w400, maxLines: 2, ellipsis)
    - Tags: Wrap with _PassiveTag widgets for channel (男频/女频) and template.tags
    - Selected state: border color changes to colorScheme.primary, width 2px

    _iconFor mapping (copy from template_gallery_page):
    - filter_vintage, location_city, science, fantasy, martial_arts, history, etc.
    - Default: Icons.auto_awesome

    Tap behavior:
    - Update selectedTemplateId in parent OnboardingWizardPage via callback or direct provider access
    - Update card visual selection state

    Empty state:
    - If template list is empty, show "暂无模板" message

    Note: This is a simplified gallery - no search/filter, just the full grid.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests genre step rendering and selection)</automated>
  </verify>
  <done>
    GenreStepPage displays all 14 templates in 3-column grid, card selection highlights border, selectedTemplateId updates on tap, empty state shows message, AutomaticKeepAliveClientMixin preserves state
  </done>
</task>

<task type="auto">
  <name>Task 3: Create WorldStepPage with simplified world form</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/world_step_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/world_setting_form.dart (form structure, validation, save pattern)
    lib/features/templates/domain/world_template.dart (WorldSetting fields)
  </read_first>
  <action>
    Create ConsumerStatefulWidget WorldStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - Constructor takes TextEditingController for worldName and worldDescription (passed from OnboardingWizardPage)

    Build method:
    - Form with GlobalKey<FormState>
    - TextFormField for world name:
      * controller: worldNameController
      * labelText: '世界观名称 *'
      * hintText: '为你的故事世界命名'
      * validator: required check, max length 100
      * maxLength: 100
    - TextFormField for world description:
      * controller: worldDescriptionController
      * labelText: '世界简介'
      * hintText: '描述你的故事背景、设定、规则等'
      * maxLines: 3
      * maxLength: 500
    - No submit button - parent OnboardingWizardPage handles "下一步"

    Validation:
    - Name is required (non-empty after trim)
    - Description is optional
    - Name length <= 100 characters

    Skip behavior:
    - Form can be skipped even if invalid (parent _skipStep doesn't call validate)
    - Only validate when "下一步" is clicked (parent calls validate before advancing)

    Note: This is a simplified version of WorldSettingForm - only 2 fields, no separate Scaffold, no immediate save. Entity creation happens when wizard completes (via TemplateInstantiationService).
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests world step validation)</automated>
  </verify>
  <done>
    WorldStepPage has 2 TextFormFields, name validation works, description is optional, form state preserved via AutomaticKeepAliveClientMixin, controllers update parent state
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CharacterStepPage with simplified character form</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/character_step_page.dart
  </files>
  <read_first>
    lib/features/knowledge/presentation/character_card_form.dart (form structure, validation, CharacterCard fields)
    lib/features/knowledge/domain/character_card.dart (CharacterCard fields)
  </read_first>
  <action>
    Create ConsumerStatefulWidget CharacterStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - Constructor takes TextEditingController for characterName and characterDescription

    Build method:
    - Form with GlobalKey<FormState>
    - TextFormField for character name:
      * controller: characterNameController
      * labelText: '角色名称 *'
      * hintText: '主角的名字'
      * validator: required check, max length 50
      * maxLength: 50
    - TextFormField for character description:
      * controller: characterDescriptionController
      * labelText: '角色简介'
      * hintText: '描述角色的性格、外貌、背景等'
      * maxLines: 3
      * maxLength: 500
    - No submit button - parent handles navigation

    Validation:
    - Name is required (non-empty after trim)
    - Description is optional
    - Name length <= 50 characters

    Skip behavior:
    - Form can be skipped even if invalid
    - Only validate when "下一步" is clicked

    Note: Simplified CharacterCardForm - only 2 core fields, no separate Scaffold, no immediate save. Entity creation via TemplateInstantiationService on wizard completion.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests character step validation)</automated>
  </verify>
  <done>
    CharacterStepPage has 2 TextFormFields, name validation works, description is optional, form state preserved, controllers update parent state
  </done>
</task>

<task type="auto">
  <name>Task 5: Create OpeningStepPage and OpeningVariantCard</name>
  <files>
    lib/features/onboarding/presentation/wizard_steps/opening_step_page.dart
    lib/features/onboarding/presentation/opening_variant_card.dart
  </files>
  <read_first>
    lib/features/templates/domain/world_template.dart (OpeningSampleStyle enum for scene/character/suspense)
    lib/features/templates/presentation/template_gallery_page.dart (card and tag patterns)
  </read_first>
  <action>
    Create OpeningVariantCard StatelessWidget:
    - Constructor parameters: OpeningVariant object (or similar), Function() onSelect, bool isSelected
    - Card with 12px border radius
    - Selected state: border colorScheme.primary, width 2px
    - Top: style badge (_PassiveTag pattern) - "场景切入" / "人物切入" / "悬念切入"
    - Body: opening text preview, maxLines: 4, TextOverflow.ellipsis
    - Bottom: "使用此开篇" FilledButton.tonal (unselected) or FilledButton (selected)
    - onTap: calls onSelect callback

    Create ConsumerStatefulWidget OpeningStepPage with AutomaticKeepAliveClientMixin:
    - wantKeepAlive: true
    - State fields: TextEditingController conceptController, List<OpeningVariant>? variants, OpeningVariant? selectedVariant, bool isGenerating

    Build method structure:
    - Optional TextField for story concept:
      * labelText: '补充描述你的故事概念（可选）'
      * maxLines: 3, maxLength: 500
    - "生成开篇" FilledButton (full width) - calls _generateOpenings()
    - Generation state: when isGenerating, show CircularProgressIndicator with "正在生成开篇..."
    - Results: ListView with 3 OpeningVariantCard widgets (12px spacing)
    - Empty state: "点击上方按钮生成开篇" hint text
    - Error state: error message + "重试" OutlinedButton

    _generateOpenings method:
    - Set isGenerating = true
    - Placeholder: Show loading for 2 seconds (simulated), then show empty variant list
    - Real implementation will be added in Plan 03 (OpeningGeneratorService integration)

    Skip behavior:
    - Skip button enabled always
    - Skip means complete wizard without opening

    Note: This is UI skeleton only. Plan 03 adds real AI generation and editor insertion.
  </action>
  <verify>
    <automated>flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart (tests opening step UI and selection)</automated>
  </verify>
  <done>
    OpeningVariantCard displays style badge and text, selection changes border style, OpeningStepPage shows concept input and generate button, loading state displays correctly, variant list displays on completion, skip button works
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input→form fields | Untrusted user input in world/character name and description fields |
| form→OnboardingProgress | Persisted wizard state may contain malformed user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Input Validation | WorldStepPage/CharacterStepPage text fields | mitigate | Validators enforce maxLength (name: 100/50, description: 500), required field checks on non-skip navigation |
| T-08-05 | Tampering | Persisted OnboardingProgress with malicious templateId | accept | TemplateId is validated against WorldTemplateRepository lookup during entity creation; invalid IDs result in empty/null, no crash |
| T-08-06 | Information Disclosure | Story concept input may contain sensitive user content | accept | Local-only storage, no network transmission; concept is only sent to AI API in Plan 03 (already covered by AI provider privacy) |
</threat_model>

<verification>
## Per-Task Verification

**Task 1:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test wizard builds with 4 steps
- Test PageView navigation works
- Test progress bar updates
- Test close button marks completed and navigates to editor
- Test skip button advances without validation

**Task 2:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test genre grid displays 14 templates
- Test template card selection highlights border
- Test selectedTemplateId updates correctly

**Task 3:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test world name validation (required, max length)
- Test world description optional
- Test form state preserved with AutomaticKeepAliveClientMixin

**Task 4:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test character name validation (required, max length)
- Test character description optional
- Test form state preserved

**Task 5:** `flutter test test/features/onboarding/presentation/onboarding_wizard_test.dart`
- Test OpeningVariantCard displays style badge and text
- Test card selection changes border
- Test OpeningStepPage shows concept input and generate button
- Test loading state displays

## Phase Gate

- All widget tests pass: `flutter test test/features/onboarding/presentation/`
- Manual: Open app fresh, verify wizard launches, navigate through all 4 steps, test skip buttons
</verification>

<success_criteria>
1. OnboardingWizardPage displays 4-step PageView with progress bar
2. GenreStepPage shows 14 template cards from WorldTemplateRepository
3. WorldStepPage and CharacterStepPage validate form input
4. OpeningStepPage shows placeholder generation UI
5. Skip/close buttons work correctly
6. Form state preserved when swiping back (AutomaticKeepAliveClientMixin)
7. All widget tests pass
</success_criteria>

<output>
Create `.planning/phases/08-onboarding-guide/08-02-SUMMARY.md` when done
</output>
