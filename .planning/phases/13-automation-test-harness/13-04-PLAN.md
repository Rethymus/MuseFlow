---
phase: 13-automation-test-harness
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - integration_test/manuscript_flow_test.dart
  - lib/features/editor/presentation/floating_toolbar.dart
  - lib/features/manuscript/presentation/editor_with_sidebar.dart
autonomous: true
gap_closure: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Flutter integration test creates and opens a real chapter before AI generation"
    - "Flutter integration test enters user-editable chapter body text and verifies the modified text is observable in editor/chapter state"
    - "Flutter integration test triggers AI through visible UI using Key('ai_synthesis_button'), not editorAINotifierProvider.notifier.startOperation"
    - "Flutter integration test verifies deterministic xianxia FakeAdapter text appears in editor/chapter content, not only EditorAIState.progressText"
    - "Editor toolbar AI operation receives real manuscriptId and chapterId when mounted inside EditorWithSidebar"
  artifacts:
    - path: "integration_test/manuscript_flow_test.dart"
      provides: "TEST-02 gap closure: user-facing chapter/editor AI + editing assertions"
      contains: "Key('ai_synthesis_button')"
    - path: "lib/features/editor/presentation/floating_toolbar.dart"
      provides: "Optional manuscript/chapter context plumbing for editor AI audit and chapter UI path"
      contains: "chapterId: widget.chapterId"
    - path: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      provides: "Passes active manuscript/chapter IDs into FloatingToolbar"
      contains: "FloatingToolbar("
  key_links:
    - from: "integration_test/manuscript_flow_test.dart"
      to: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      via: "create manuscript -> open manuscript -> create/open chapter -> edit SuperEditor"
      pattern: "add_chapter_button"
    - from: "integration_test/manuscript_flow_test.dart"
      to: "lib/features/editor/presentation/floating_toolbar.dart"
      via: "tester taps find.byKey(const Key('ai_synthesis_button'))"
      pattern: "find\.byKey\(const Key\('ai_synthesis_button'\)\)"
    - from: "lib/features/manuscript/presentation/editor_with_sidebar.dart"
      to: "lib/features/editor/presentation/floating_toolbar.dart"
      via: "FloatingToolbar(manuscriptId: widget.manuscriptId, chapterId: _currentChapterId)"
      pattern: "manuscriptId: widget\.manuscriptId"
---

<objective>
Close the Phase 13 TEST-02 verification gaps by replacing the hollow integration-test AI coverage with a real user-facing chapter/editor path. The gap closure must prove a user can create/open a chapter, modify chapter body text, select editor text, trigger AI through the visible toolbar button `Key('ai_synthesis_button')`, and observe deterministic FakeAdapter xianxia text in editor/chapter content.

Purpose: Phase 13 cannot claim Flutter UI-node coverage while AI generation is triggered by direct provider calls and editing is unasserted. This plan makes TEST-02 verify the same visible path a user will use.
Output: Updated integration test plus minimal context plumbing so FloatingToolbar can carry the active manuscript/chapter IDs when used from the chapter editor.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-automation-test-harness/13-CONTEXT.md
@.planning/phases/13-automation-test-harness/13-RESEARCH.md
@.planning/phases/13-automation-test-harness/13-PATTERNS.md
@.planning/phases/13-automation-test-harness/13-VERIFICATION.md
@.planning/phases/13-automation-test-harness/13-03-SUMMARY.md

<gap_source>
From 13-VERIFICATION.md gaps:
- Current `integration_test/manuscript_flow_test.dart` directly calls `editorAINotifierProvider.notifier.startOperation(...)` and only asserts `state.progressText`.
- Missing: create/open chapter, enter or select text in editor, trigger AI through `Key('ai_synthesis_button')` or equivalent visible widget, and verify deterministic xianxia text enters editor/chapter content.
- Missing: editing-node assertion that user-modified chapter body is observable in chapter/editor state.
</gap_source>

<interfaces>
From `lib/features/editor/presentation/floating_toolbar.dart`:
- `FloatingToolbar` currently requires `Editor editor` and `SelectionLayerLinks selectionLayerLinks`.
- `_startOperation(...)` calls `editorAINotifierProvider.notifier.startOperation(..., manuscriptId: null, chapterId: null)`.
- `_ToolbarContent` contains `_ActionButton(key: const Key('ai_synthesis_button'), label: '语气改写', onTap: () => onStartOperation(EditorAIOperation.toneRewrite))`.

From `lib/features/manuscript/presentation/editor_with_sidebar.dart`:
- `EditorWithSidebar` has `widget.manuscriptId` and `_currentChapterId`.
- `_loadChapter(Chapter chapter)` loads `chapter.documentContent` into a `MutableDocument`, creates `_editor`, and calls `ref.read(editorProvider.notifier).setEditor(_editor)`.
- `_onDocumentChanged()` serializes `_editor!.document` and calls `_autoSave?.onDocumentChanged(_currentChapterId!, markdown)`.
- `_forceSaveAsync()` serializes current editor content and awaits `autoSave.forceSave()`.
- The SuperEditor overlay currently creates `FloatingToolbar(editor: _editor!, selectionLayerLinks: _selectionLinks!)`.

From `lib/features/editor/application/editor_ai_notifier.dart`:
- `startOperation(...)` accepts optional `manuscriptId` and `chapterId` and records them through token audit callback.
- `acceptAll()` applies pending AI diff text to the current `editorProvider` editor.

From `integration_test/manuscript_flow_test.dart`:
- `_pumpApp(tester)` already wraps `MuseFlowApp` in `ProviderScope` with `openaiAdapterProvider.overrideWithValue(FakeAdapter())`, `activeProviderProvider.overrideWithValue(_fakeProvider)`, and `activeApiKeyProvider.overrideWithValue('fake-api-key')`.
- `_createManuscript`, `_openManuscript`, and `_createChapter` already drive visible UI for manuscript/chapter creation.
- The failing test is named `should trigger AI generation and show xianxia content` and currently calls `editorAINotifierProvider.notifier.startOperation(...)` directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire real chapter context into FloatingToolbar AI operations</name>
  <files>lib/features/editor/presentation/floating_toolbar.dart, lib/features/manuscript/presentation/editor_with_sidebar.dart</files>
  <read_first>
    - lib/features/editor/presentation/floating_toolbar.dart (current `FloatingToolbar` constructor and `_startOperation` implementation)
    - lib/features/manuscript/presentation/editor_with_sidebar.dart (current overlay builder and `_currentChapterId` state)
    - lib/features/editor/application/editor_ai_notifier.dart (`startOperation` optional `manuscriptId`/`chapterId` parameters and audit behavior)
    - .planning/phases/13-automation-test-harness/13-VERIFICATION.md (gap lines describing `manuscriptId/chapterId` null and direct notifier bypass)
    - .planning/phases/13-automation-test-harness/13-CONTEXT.md (D-08/D-09 TEST-02 integration test constraints)
  </read_first>
  <behavior>
    - Test 1: `FloatingToolbar` remains usable from `EditorPage` without manuscript/chapter context.
    - Test 2: `EditorWithSidebar` passes `widget.manuscriptId` and `_currentChapterId` into `FloatingToolbar` when an active chapter is loaded.
    - Test 3: `_startOperation` forwards `manuscriptId: widget.manuscriptId` and `chapterId: widget.chapterId` to `editorAINotifierProvider.notifier.startOperation` instead of hard-coded nulls.
    - Test 4: `flutter analyze` reports no new compile errors for both modified files.
  </behavior>
  <action>
    Add optional fields to `FloatingToolbar`: `final String? manuscriptId;` and `final String? chapterId;`. Add optional named constructor parameters `this.manuscriptId` and `this.chapterId` so the existing `EditorPage` call site continues to compile without changes. In `_startOperation`, replace the current hard-coded `manuscriptId: null` and `chapterId: null` arguments with `manuscriptId: widget.manuscriptId` and `chapterId: widget.chapterId`.

    In `EditorWithSidebar`, update the `FloatingToolbar` construction inside the SuperEditor overlay to pass `manuscriptId: widget.manuscriptId` and `chapterId: _currentChapterId`. Do not change editor creation, auto-save timing, repository APIs, or AI notifier behavior beyond passing the existing IDs through. This preserves Phase 13 scope while removing the specific verification blind spot that toolbar-triggered AI had no chapter context.
  </action>
  <acceptance_criteria>
    - Source assertion: `lib/features/editor/presentation/floating_toolbar.dart` contains `final String? manuscriptId;` and `final String? chapterId;`.
    - Source assertion: `floating_toolbar.dart` contains `manuscriptId: widget.manuscriptId` and `chapterId: widget.chapterId` in the `_startOperation` call.
    - Source assertion: `lib/features/manuscript/presentation/editor_with_sidebar.dart` passes `manuscriptId: widget.manuscriptId` and `chapterId: _currentChapterId` to `FloatingToolbar`.
    - Regression assertion: `lib/features/editor/presentation/editor_page.dart` does not need modification because both new parameters are optional.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/features/editor/presentation/floating_toolbar.dart lib/features/manuscript/presentation/editor_with_sidebar.dart</automated>
  </verify>
  <done>
    FloatingToolbar-triggered AI operations carry real chapter context when the toolbar is mounted in EditorWithSidebar, and existing non-chapter EditorPage usage remains source-compatible.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace direct-notifier AI integration test with visible editor UI flow and editing assertions</name>
  <files>integration_test/manuscript_flow_test.dart</files>
  <read_first>
    - integration_test/manuscript_flow_test.dart (replace the current direct-notifier AI test; reuse `_pumpApp`, `_createManuscript`, `_openManuscript`, `_createChapter`, `_flushTokenAudit` helpers)
    - lib/features/manuscript/presentation/editor_with_sidebar.dart (how chapter editor loads/saves document content and exposes `editorProvider`)
    - lib/features/editor/presentation/floating_toolbar.dart (visible `Key('ai_synthesis_button')` toolbar trigger)
    - lib/features/editor/application/editor_ai_notifier.dart (`acceptAll()` applies AI diff to `editorProvider`)
    - lib/features/manuscript/infrastructure/chapter_repository.dart (`getByManuscriptId`, `updateDocumentContent` for verifying persisted chapter body)
    - test/automation/helpers/fake_adapter.dart (deterministic FakeAdapter behavior)
    - test/automation/fixtures/xianxia_content.dart (assertable substrings: `林风`, `筑基`, `剑光`, `灵力`, `月华`, `斩仙`)
    - .planning/phases/13-automation-test-harness/13-VERIFICATION.md (the exact TEST-02 missing assertions this task closes)
  </read_first>
  <behavior>
    - Test 1: The AI integration test creates a manuscript through visible UI, opens it, creates at least one chapter through visible UI, and opens/selects that chapter before AI generation.
    - Test 2: The test modifies chapter body text through the editor path with exact text `碎片整理：少年拔剑，风雪满山。`, then asserts the editor plain text contains that text.
    - Test 3: The test makes an expanded selection in the editor and taps `find.byKey(const Key('ai_synthesis_button'))` to trigger AI; it must not call `editorAINotifierProvider.notifier.startOperation` anywhere in the test file.
    - Test 4: The test waits for FakeAdapter streaming to complete, asserts `EditorAIState.progressText` contains deterministic xianxia text such as `林风` or `剑光`, applies the AI result through visible accept UI if available (`find.text('接受')`/`find.text('全部接受')`) or through `editorAINotifierProvider.notifier.acceptAll()` only after the visible toolbar trigger has produced a real diff, then asserts the editor plain text contains the deterministic xianxia text.
    - Test 5: The test forces chapter persistence by switching chapter, invoking existing save path, or waiting for the autosave debounce and then verifies `chapterRepositoryProvider.future.getByManuscriptId(...)` returns a chapter whose `documentContent` contains the user edit text and/or deterministic FakeAdapter text.
  </behavior>
  <action>
    In `integration_test/manuscript_flow_test.dart`, replace the current `should trigger AI generation and show xianxia content` test body that reads `editorAINotifierProvider.notifier` and calls `startOperation(...)` directly. The replacement test must be named `should edit chapter body and trigger AI through visible editor toolbar`.

    Required flow:
    1. `await _pumpApp(tester); await _createManuscript(tester); await _openManuscript(tester); await _createChapter(tester, '第一章 青云试剑');`.
    2. Tap/open the created chapter if it is not already active. Use `find.text('第一章 青云试剑')` or the existing chapter row text; do not seed chapter content through repositories.
    3. Enter or insert the exact user text `碎片整理：少年拔剑，风雪满山。` into the active editor. Prefer real widget interaction with SuperEditor (`tester.tap`/keyboard text entry) when supported. If SuperEditor text entry is unstable in integration_test, use the exposed `editorProvider` only to perform editor document edit requests, because the test has already reached the real chapter editor UI; still assert visible/editor state afterward. Do not use repositories to seed the body.
    4. Create an expanded selection over the inserted text in the active editor. Prefer user-like gestures/keyboard selection. If the test harness must use `editorProvider` to set `composer.selection`, do so only for selection setup, not for triggering AI.
    5. Assert `find.byKey(const Key('ai_synthesis_button'))` finds one widget, then call `await tester.tap(find.byKey(const Key('ai_synthesis_button'))); await tester.pump(const Duration(seconds: 2)); await _pumpFrame(tester);`.
    6. Assert the AI state has no error and contains deterministic FakeAdapter text using a source assertion such as `expect(state.progressText, anyOf(contains('林风'), contains('剑光'), contains('灵力')));`. This assertion alone is not sufficient; continue to apply and verify editor/chapter content.
    7. Apply the generated diff to the editor. Prefer visible accept controls: if `find.text('接受')` or `find.text('全部接受')` is present, tap it. If the accept overlay is not findable due to selection-layer timing, call `tester.container().read(editorAINotifierProvider.notifier).acceptAll()` only after the visible `ai_synthesis_button` tap has produced `diffResult != null`. This preserves the gap requirement: direct `startOperation` remains forbidden, while accept application may use the existing notifier API as a fallback for overlay flakiness.
    8. Read the active editor through `editorProvider`, derive plain text by iterating TextNode nodes, and assert it contains both the original user edit marker `少年拔剑` and at least one deterministic xianxia substring (`林风`, `剑光`, or `灵力`).
    9. Force persistence through an existing app path: wait longer than the 2s `ChapterAutoSave` debounce (`await tester.pump(const Duration(seconds: 3)); await _pumpFrame(tester);`) and/or switch away from the chapter to trigger forced save. Then read `chapterRepositoryProvider.future` and assert the persisted chapter `documentContent` contains `少年拔剑` or the deterministic FakeAdapter substring. Use the actual manuscript ID from `manuscriptNotifierProvider` and chapter ID/title from `chapterNotifierProvider`; do not hardcode IDs.

    Add private helper functions inside the integration test file if needed: `_editorPlainText(tester)`, `_setExpandedSelectionOnEditor(tester, text)`, `_persistedChapterContent(tester, title)`. These helpers may use `editorProvider`, `chapterRepositoryProvider`, and `chapterNotifierProvider` for observation/selection/persistence verification, but the AI trigger must be the visible `find.byKey(const Key('ai_synthesis_button'))` tap. Remove all direct calls to `editorAINotifierProvider.notifier.startOperation` from this file, including the AI anomaly test. For the AI anomaly test required by D-08, keep UI-level coverage: use the same real chapter/editor path with `FakeAdapter(errorRate: 1.0, errorText: '网络异常')`, create/open a chapter, enter/select editor text, tap `find.byKey(const Key('ai_synthesis_button'))`, and assert the visible UI/error state displays or records `网络异常` without any direct `.startOperation(` call.
  </action>
  <acceptance_criteria>
    - Source assertion: `integration_test/manuscript_flow_test.dart` contains the test name `should edit chapter body and trigger AI through visible editor toolbar`.
    - Source assertion: excluding comments, `integration_test/manuscript_flow_test.dart` has zero occurrences of `.startOperation(`. Use `python3 - <<'PY'` or `grep -v '^#'` style filtering if checking by CLI; do not count comments.
    - Source assertion: the test contains `find.byKey(const Key('ai_synthesis_button'))` and taps that finder.
    - Source assertion: the test contains the exact user edit string `碎片整理：少年拔剑，风雪满山。` and asserts `少年拔剑` is observable after editing.
    - Source assertion: the test asserts at least one deterministic FakeAdapter substring in editor/chapter content: `林风`, `剑光`, or `灵力`.
    - Behavior assertion: targeted integration test command passes with FakeAdapter override and no real API key.
  </acceptance_criteria>
  <verify>
    <automated>flutter test --no-pub integration_test/manuscript_flow_test.dart --name "should edit chapter body and trigger AI through visible editor toolbar"</automated>
    <automated>python3 - <<'PY'
from pathlib import Path
p = Path('integration_test/manuscript_flow_test.dart')
lines = [line for line in p.read_text().splitlines() if not line.lstrip().startswith('//')]
text = '\n'.join(lines)
assert '.startOperation(' not in text, 'Direct startOperation call remains in integration test'
assert "Key('ai_synthesis_button')" in text, 'Visible AI toolbar finder missing'
assert '少年拔剑' in text, 'Editing assertion marker missing'
assert any(s in text for s in ['林风', '剑光', '灵力']), 'FakeAdapter content assertion missing'
PY</automated>
  </verify>
  <done>
    TEST-02 gap is closed: the integration test covers the real chapter/editor UI path for editing and AI generation, uses FakeAdapter provider override, triggers AI through the visible toolbar button, and verifies generated xianxia text in editor/chapter content rather than only notifier progress text.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test -> app widget tree | Test drives real MuseFlow widgets, including manuscript/chapter creation, editor input/selection, toolbar AI action, and export-adjacent existing helpers |
| app widget tree -> FakeAdapter | Visible toolbar trigger crosses into AI adapter boundary through Riverpod provider override |
| test/app -> local Hive | Manuscript/chapter edits and token audit records persist to temporary Hive boxes during the integration test |
| test code -> secure storage | Test saves only fake credentials to satisfy active provider/key guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Information Disclosure | integration_test/manuscript_flow_test.dart fake credentials | mitigate | Use literal `fake-api-key` and `FakeAdapter`; do not read `process.env`, real secure-storage keys, or external provider configuration |
| T-13-10 | Tampering | Hive manuscript/chapter boxes | mitigate | Reuse `setUpHiveTest`/`tearDownHiveTest`, `Hive.close()`, and `Hive.deleteFromDisk()` so test data stays in temp storage and is deleted after each test |
| T-13-11 | Spoofing | UI AI generation path | mitigate | Block direct `editorAINotifierProvider.notifier.startOperation` calls in integration test; require visible `Key('ai_synthesis_button')` trigger with FakeAdapter override |
| T-13-12 | Repudiation | TEST-02 coverage evidence | mitigate | Add source gates for `ai_synthesis_button`, absence of `.startOperation(`, editing marker `少年拔剑`, and FakeAdapter substrings so verifier can prove the user-facing path is covered |
| T-13-13 | Denial of Service | integration_test streaming/autosave timing | accept | Targeted single integration test uses bounded FakeAdapter streaming and bounded 3s autosave wait; no unbounded loops or real network calls |
| T-13-SC | Tampering | package installs | accept | No new npm/pub/cargo packages are installed by this plan |
</threat_model>

<verification>
- `flutter analyze lib/features/editor/presentation/floating_toolbar.dart lib/features/manuscript/presentation/editor_with_sidebar.dart integration_test/manuscript_flow_test.dart`
- `flutter test --no-pub integration_test/manuscript_flow_test.dart --name "should edit chapter body and trigger AI through visible editor toolbar"`
- `flutter test --no-pub integration_test/manuscript_flow_test.dart`
- Source gate: no direct `.startOperation(` calls remain in `integration_test/manuscript_flow_test.dart` outside comments.
</verification>

<success_criteria>
- TEST-02 gap truth is covered: Flutter integration test drives manuscript creation, chapter management, AI generation, editing, and export-adjacent existing flow with a real widget tree and FakeAdapter.
- AI generation coverage is no longer hollow: the trigger is `Key('ai_synthesis_button')` in the visible editor toolbar, not a direct notifier call.
- Editing coverage exists: user-modified chapter body text `少年拔剑` is observable in editor/chapter state.
- Deterministic xianxia FakeAdapter output is observable in editor/chapter content after the visible AI trigger and accept/apply step.
- Test-only security constraints hold: fake credentials only, temporary Hive storage only, no real API keys, no test data persistence outside temp storage.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 13 | Automated test harness validates core creation flow without real API key | 13-04 | COVERED | Closes remaining UI AI/editing coverage gap using FakeAdapter
REQ | TEST-02 | Flutter integration test covers manuscript creation → chapter management → AI generation → editing → export | 13-04 | COVERED | Targets failed TEST-02 only; TEST-01/TEST-03 already satisfied by executed plans
RESEARCH | UI integration via widget tree + FakeAdapter override | 13-04 | COVERED | Uses ProviderScope override and visible finder interactions
RESEARCH | Avoid direct repository/notifier bypass in integration tests | 13-04 | COVERED | Source gate forbids direct `.startOperation(` in integration test
CONTEXT | D-08 | TEST-02 error/UI scenarios | 13-04 | COVERED | Preserves existing scenarios while replacing hollow AI path
CONTEXT | D-09 | Reuse Hive test helper | 13-04 | COVERED | Existing setup/teardown retained; temp storage enforced in threat model
VERIFICATION | GAP-01 | Add integration test creating/opening chapter, editing/selecting editor text, tapping `ai_synthesis_button`, verifying xianxia content | 13-04 | COVERED | Task 2 explicit flow
VERIFICATION | GAP-02 | Editing-node assertion: modified chapter body observable in chapter/editor state | 13-04 | COVERED | Task 2 exact marker `少年拔剑`
VERIFICATION | GAP-03 | Wire AI generation test to actual chapter/editor UI with FakeAdapter override, not direct `startOperation` | 13-04 | COVERED | Task 2 source gate
</source_audit>

<output>
Create `.planning/phases/13-automation-test-harness/13-04-SUMMARY.md` when done
</output>
