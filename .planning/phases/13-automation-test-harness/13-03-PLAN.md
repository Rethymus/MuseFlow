---
phase: 13-automation-test-harness
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - integration_test/manuscript_flow_test.dart
  - lib/features/manuscript/presentation/manuscript_create_dialog.dart
  - lib/features/manuscript/presentation/chapter_sidebar.dart
  - lib/features/manuscript/presentation/chapter_create_dialog.dart
  - lib/features/editor/presentation/floating_toolbar.dart
  - lib/features/story_structure/presentation/export_dialog.dart
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "Integration test launches app with FakeAdapter override and navigates to manuscript library"
    - "Integration test creates a manuscript via UI (tap FAB, enter title/genre, confirm)"
    - "Integration test creates 3-5 chapters via UI and verifies they appear in chapter list"
    - "Integration test triggers AI generation on a chapter and verifies xianxia content appears"
    - "Integration test triggers export and verifies success feedback"
    - "Error scenario tests cover 4 categories: empty states, AI anomalies, post-delete navigation, rapid operations (per D-08)"
    - "Integration test reuses existing hive_test_helper.dart pattern for Hive initialization (per D-09)"
    - "All 6 required ValueKeys added to existing widgets without breaking rendering"
  artifacts:
    - path: "integration_test/manuscript_flow_test.dart"
      provides: "TEST-02: Flutter integration tests with UI flow + error scenarios"
      contains: "IntegrationTestWidgetsFlutterBinding"
  key_links:
    - from: "integration_test/manuscript_flow_test.dart"
      to: "lib/app.dart"
      via: "pumpWidget with MuseFlowApp"
      pattern: "MuseFlowApp"
    - from: "integration_test/manuscript_flow_test.dart"
      to: "test/automation/helpers/fake_adapter.dart"
      via: "ProviderScope override with FakeAdapter"
      pattern: "openaiAdapterProvider.overrideWithValue"
    - from: "integration_test/manuscript_flow_test.dart"
      to: "lib/features/manuscript/presentation/manuscript_create_dialog.dart"
      via: "find.byKey(Key('manuscript_title'))"
      pattern: "Key\\('manuscript_title'\\)"
---

<objective>
Implement TEST-02 (Flutter integration tests covering manuscript creation, chapter management, AI generation, editing, and export via UI) and add 6 required ValueKeys to existing widgets. Error scenario tests cover empty states, AI anomalies, post-delete navigation, and rapid operations per CONTEXT D-08.

Purpose: Verify the full user-facing flow works end-to-end with a real widget tree, using FakeAdapter to avoid real API dependency. Widget keys enable reliable finders for integration tests.
Output: manuscript_flow_test.dart (main flow + error scenarios), 6 widget files with added ValueKeys.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing integration test. -->

From integration_test/app_test.dart (existing pattern to follow):
```dart
IntegrationTestWidgetsFlutterBinding.ensureInitialized();
// setUp: _initializeTestStorage() -- Hive init + adapter registration
// tearDown: Hive.close() + Hive.deleteFromDisk()
// _pumpApp: tester.pumpWidget(const MuseFlowApp()) + pumpAndSettle()
// Hive adapter registration: FragmentAdapter, AppSettingsAdapter, CharacterCardAdapter,
//   WorldSettingAdapter, SkillDocumentAdapter, ForeshadowingEntryAdapter,
//   PlotNodeAdapter, GuardianAnnotationAdapter
```

From test/automation/helpers/fake_adapter.dart (created in Plan 01):
```dart
class FakeAdapter implements AIAdapter {
  FakeAdapter({double? errorRate, String? errorText, bool emptyResponse = false});
}
```

From lib/app.dart:
```dart
class MuseFlowApp extends ConsumerWidget { ... }  // line 33
```

From lib/core/presentation/providers.dart (line 183):
```dart
final openaiAdapterProvider = Provider<AIAdapter>((ref) { ... });  // After Plan 01 refactor
```

From .planning/phases/13-automation-test-harness/13-UI-SPEC.md (Widget Finder Map, lines 49-71):
- ManuscriptCreateDialog: `find.byKey(Key('manuscript_title'))`, `find.byKey(Key('manuscript_genre'))`, `find.text('确认')`
- Chapter sidebar: `find.byKey(Key('add_chapter_button'))`
- Chapter create dialog: `find.byKey(Key('chapter_title_field'))`
- Floating toolbar: `find.byKey(Key('ai_synthesis_button'))`
- Export dialog: `find.byKey(Key('export_button'))`, `find.text('Markdown')`, `find.text('导出')`
- ManuscriptLibraryPage: `find.byIcon(Icons.add)`, `find.text('创建你的第一部作品')`, `find.text('{title}')`
- CapturePage: `find.text('输入灵感碎片，按回车添加...')`, `find.text('AI 整理')`
</interfaces>

<existing_key_locations>
<!-- Exact locations for key additions, verified from codebase. -->

From lib/features/manuscript/presentation/manuscript_create_dialog.dart:
- Line 66: `TextField(` -- add `key: const Key('manuscript_title')`
- Line 82: `key: const Key('manuscript-create-genre-dropdown')` -- existing key on genre dropdown (EXISTS)
- Line 107: `TextField(` (custom genre field) -- add `key: const Key('manuscript_genre')`

From lib/features/manuscript/presentation/chapter_sidebar.dart:
- OutlinedButton.icon with label '新建章节' -- add `key: const Key('add_chapter_button')`

From lib/features/manuscript/presentation/chapter_create_dialog.dart:
- TextField for title -- add `key: const Key('chapter_title_field')`

From lib/features/editor/presentation/floating_toolbar.dart:
- First AI action button (tone rewrite) -- add `key: const Key('ai_synthesis_button')`

From lib/features/story_structure/presentation/export_dialog.dart:
- Line 286: `ElevatedButton(` with child Text('导出') -- add `key: const Key('export_button')`
</existing_key_locations>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 ValueKeys to existing widgets for integration test finders</name>
  <files>lib/features/manuscript/presentation/manuscript_create_dialog.dart, lib/features/manuscript/presentation/chapter_sidebar.dart, lib/features/manuscript/presentation/chapter_create_dialog.dart, lib/features/editor/presentation/floating_toolbar.dart, lib/features/story_structure/presentation/export_dialog.dart</files>
  <read_first>
    - lib/features/manuscript/presentation/manuscript_create_dialog.dart (lines 66, 82, 107 for TextField locations)
    - lib/features/manuscript/presentation/chapter_sidebar.dart (find the '新建章节' OutlinedButton.icon)
    - lib/features/manuscript/presentation/chapter_create_dialog.dart (find the title TextField)
    - lib/features/editor/presentation/floating_toolbar.dart (find the first _ActionButton with '语气改写')
    - lib/features/story_structure/presentation/export_dialog.dart (line 286 for ElevatedButton)
    - .planning/phases/13-automation-test-harness/13-PATTERNS.md (key addition patterns at lines 517-594)
    - .planning/phases/13-automation-test-harness/13-UI-SPEC.md (key list at lines 76-85)
  </read_first>
  <action>
    Add ValueKeys to exactly 6 widgets across 5 files. Each key addition is a single line `key: const Key('...'),` parameter on the target widget constructor. No other changes to these files.

    1. In `manuscript_create_dialog.dart`: Add `key: const Key('manuscript_title')` to the title TextField at line 66. Add `key: const Key('manuscript_genre')` to the custom genre TextField at line 107. Note: line 82 already has `key: const Key('manuscript-create-genre-dropdown')` on the genre dropdown -- leave that unchanged.

    2. In `chapter_sidebar.dart`: Add `key: const Key('add_chapter_button')` to the OutlinedButton.icon that has label '新建章节'.

    3. In `chapter_create_dialog.dart`: Add `key: const Key('chapter_title_field')` to the title TextField.

    4. In `floating_toolbar.dart`: Add `key: const Key('ai_synthesis_button')` to the first _ActionButton (the one with icon Icons.auto_fix_high and label '语气改写').

    5. In `export_dialog.dart`: Add `key: const Key('export_button')` to the ElevatedButton at line 286 (child: Text('导出')).

    These keys do not affect rendering -- they are test-only attributes for `find.byKey()`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/manuscript/presentation/manuscript_create_dialog.dart lib/features/manuscript/presentation/chapter_sidebar.dart lib/features/manuscript/presentation/chapter_create_dialog.dart lib/features/editor/presentation/floating_toolbar.dart lib/features/story_structure/presentation/export_dialog.dart 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `find.byKey(Key('manuscript_title'))` locates the title TextField in ManuscriptCreateDialog
    - `find.byKey(Key('manuscript_genre'))` locates the custom genre TextField in ManuscriptCreateDialog
    - `find.byKey(Key('add_chapter_button'))` locates the chapter creation button
    - `find.byKey(Key('chapter_title_field'))` locates the chapter title TextField
    - `find.byKey(Key('ai_synthesis_button'))` locates the AI tone rewrite button
    - `find.byKey(Key('export_button'))` locates the export button
    - All 5 files pass `flutter analyze` with no new issues
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flutter integration tests for UI flow and error scenarios (TEST-02)</name>
  <files>integration_test/manuscript_flow_test.dart</files>
  <read_first>
    - integration_test/app_test.dart (existing integration test pattern: _initializeTestStorage, Hive adapter registration, setUp/tearDown, _pumpApp)
    - test/automation/helpers/fake_adapter.dart (FakeAdapter from Plan 01)
    - lib/app.dart (MuseFlowApp class at line 33)
    - lib/core/presentation/providers.dart (openaiAdapterProvider after Plan 01 refactor)
    - lib/core/infrastructure/hive_adapters.dart (HiveTypeIds for adapter registration)
    - .planning/phases/13-automation-test-harness/13-UI-SPEC.md (Integration Test Flow Contract at lines 147-181, Finder Map at lines 49-71, Test Data Fixtures at lines 188-192)
    - .planning/phases/13-automation-test-harness/13-PATTERNS.md (integration test pattern at lines 444-514)
    - .planning/phases/13-automation-test-harness/13-CONTEXT.md (D-08: 4 error categories, D-09: reuse hive_test_helper)
  </read_first>
  <behavior>
    - Main flow test 1 "launch and verify empty state": pump app with ProviderScope(FakeAdapter override, child: MuseFlowApp), verify ManuscriptLibraryPage shows empty state heading '创建你的第一部作品' or FAB
    - Main flow test 2 "create manuscript": tap FAB (find.byIcon(Icons.add)), enter title '剑道苍穹' in Key('manuscript_title'), enter genre '修仙' in Key('manuscript_genre'), tap '确认', verify find.text('剑道苍穹') visible
    - Main flow test 3 "create chapters": tap manuscript card, tap add_chapter_button 3 times, enter chapter titles, verify all 3 appear in list
    - Main flow test 4 "AI generation": navigate to chapter 1, tap ai_synthesis_button, pump(Duration(seconds: 2)), verify xianxia content '林风' appears in editor
    - Main flow test 5 "export": tap export_button, select Markdown format, tap '导出', verify success indicator (SnackBar or completion message)
    - Error test 1 "empty manuscript library": launch without creating manuscripts, verify empty state
    - Error test 2 "AI error handling": use FakeAdapter(errorRate: 1.0, errorText: '网络异常'), trigger AI, verify error message displayed
    - Error test 3 "post-delete navigation": create manuscript, delete it, verify navigation back to library without crash
    - Error test 4 "rapid operations": create manuscript, rapidly tap add_chapter_button multiple times, verify no crash or duplicate errors
  </behavior>
  <action>
    Create `integration_test/manuscript_flow_test.dart` following the exact pattern from `integration_test/app_test.dart`. Use `IntegrationTestWidgetsFlutterBinding.ensureInitialized()`. Copy the `_initializeTestStorage()` function from app_test.dart (it registers all Hive adapters and initializes Hive to a temp directory). In setUp, call `_initializeTestStorage()`. In tearDown, call `Hive.close()` then `Hive.deleteFromDisk()`.

    For the app pump, wrap MuseFlowApp in ProviderScope with FakeAdapter override:
    ```dart
    await tester.pumpWidget(
      ProviderScope(
        overrides: [openaiAdapterProvider.overrideWithValue(FakeAdapter())],
        child: const MuseFlowApp(),
      ),
    );
    await tester.pumpAndSettle();
    ```

    For error test 2 (AI anomalies), use a separate testWidgets that pumps with `FakeAdapter(errorRate: 1.0, errorText: '网络异常')` in the override.

    Organize tests into groups: `group('Main flow', ...)` with 5 testWidgets, and `group('Error scenarios', ...)` with 4 testWidgets per CONTEXT D-08 categories.

    Follow the UI-SPEC Integration Test Flow Contract (6-step sequence) for the main flow tests. Use `tester.pumpAndSettle()` after every tap. Use `tester.pump(Duration(seconds: 2))` after triggering AI to wait for streaming. Use `find.text('剑道苍穹')` instead of find.byKey for manuscript card verification (the card uses Text widget, not a keyed widget).

    Import `package:flutter_riverpod/flutter_riverpod.dart` for ProviderScope.
  </action>
  <verify>
    <automated>flutter test integration_test/manuscript_flow_test.dart 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `integration_test/manuscript_flow_test.dart` has 9 testWidgets (5 main flow + 4 error scenarios)
    - Main flow covers: empty state, manuscript creation, chapter creation, AI generation, export
    - Error scenarios cover: empty states, AI anomalies, post-delete navigation, rapid operations
    - All tests use FakeAdapter via ProviderScope override -- no real API calls
    - All 6 ValueKeys added in Task 1 are used by finders in the tests
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test -> app widget tree | Test drives real widgets with FakeAdapter override |
| test -> Hive | Integration tests share Hive temp directory lifecycle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Information Disclosure | FakeAdapter in ProviderScope | mitigate | Override uses FakeAdapter() literal -- never reads real API keys |
| T-13-07 | Tampering | Hive adapter registration | mitigate | Use same _initializeTestStorage pattern as app_test.dart; tearDown calls Hive.close() + deleteFromDisk() |
| T-13-08 | Denial of Service | Rapid operation test | accept | Test validates app doesn't crash under rapid taps; bounded by test timeout |
| T-13-SC | Tampering | npm/pub install | accept | No new packages installed this plan |

</threat_model>

<verification>
- `flutter analyze` passes on all 5 modified widget files (no regressions from key additions)
- `flutter test integration_test/manuscript_flow_test.dart` passes (all 9 tests)
- `flutter test` passes (all existing unit tests, including Plan 01 and Plan 02 tests)
</verification>

<success_criteria>
- TEST-02 covered: Main flow tests verify manuscript creation, chapter management, AI generation, export through real UI
- TEST-02 covered: Error scenario tests verify empty states, AI errors, post-delete navigation, rapid operations
- All 6 ValueKeys added to correct widgets and used by integration test finders
- Widget rendering unchanged (keys are test-only attributes)
- Integration tests use FakeAdapter via ProviderScope -- zero real API calls
</success_criteria>

<output>
Create `.planning/phases/13-automation-test-harness/13-03-SUMMARY.md` when done
</output>
