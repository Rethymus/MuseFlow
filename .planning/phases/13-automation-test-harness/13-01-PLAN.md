---
phase: 13-automation-test-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ai/domain/ai_adapter.dart
  - lib/features/ai/infrastructure/openai_adapter.dart
  - lib/core/presentation/providers.dart
  - test/automation/helpers/fake_adapter.dart
  - test/automation/helpers/test_container.dart
  - test/automation/fixtures/xianxia_content.dart
  - test/automation/fixtures/manuscript_fixtures.dart
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "AIAdapter abstract interface exists with createStream method matching OpenAIAdapter signature (per D-01: extract AIAdapter from OpenAIAdapter)"
    - "OpenAIAdapter implements AIAdapter without breaking existing consumers (per D-02: refactor scope limited to OpenAIAdapter + providers.dart)"
    - "openaiAdapterProvider typed as Provider<AIAdapter> so tests can override with FakeAdapter (per D-02: refactor scope limited to OpenAIAdapter + providers.dart)"
    - "FakeAdapter returns deterministic xianxia text per operation type"
    - "FakeAdapter calls onUsage callback after stream completes"
    - "FakeAdapter supports configurable error mode (errorRate, errorText, emptyResponse) (per D-03: FakeAdapter supports configurable error parameters)"
    - "Test container factory creates ProviderContainer with FakeAdapter override and Hive temp directory"
  artifacts:
    - path: "lib/features/ai/domain/ai_adapter.dart"
      provides: "Abstract AIAdapter interface"
      contains: "abstract class AIAdapter"
    - path: "test/automation/helpers/fake_adapter.dart"
      provides: "Test double for AIAdapter"
      contains: "class FakeAdapter implements AIAdapter"
    - path: "test/automation/helpers/test_container.dart"
      provides: "ProviderContainer factory for tests"
      contains: "Future<ProviderContainer> createTestContainer"
    - path: "test/automation/fixtures/xianxia_content.dart"
      provides: "Deterministic xianxia test content"
      contains: "class XianxiaContent"
    - path: "test/automation/fixtures/manuscript_fixtures.dart"
      provides: "Factory functions for test entities"
      contains: "class ManuscriptFixtures"
  key_links:
    - from: "test/automation/helpers/fake_adapter.dart"
      to: "lib/features/ai/domain/ai_adapter.dart"
      via: "implements AIAdapter"
      pattern: "class FakeAdapter implements AIAdapter"
    - from: "test/automation/helpers/test_container.dart"
      to: "lib/core/presentation/providers.dart"
      via: "openaiAdapterProvider.overrideWithValue"
      pattern: "openaiAdapterProvider.overrideWithValue.*FakeAdapter"
    - from: "lib/core/presentation/providers.dart"
      to: "lib/features/ai/domain/ai_adapter.dart"
      via: "import and type reference"
      pattern: "Provider<AIAdapter>"
---

<objective>
Extract AIAdapter abstract interface from OpenAIAdapter and build FakeAdapter test double with supporting test infrastructure (test container factory, fixtures). This is the foundational plan that Plan 02 and Plan 03 depend on.

Purpose: Enable dependency substitution in tests so all downstream test plans can replace real AI calls with deterministic fake responses.
Output: AIAdapter interface, refactored OpenAIAdapter, FakeAdapter, test container factory, test fixture classes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types the executor must implement against. -->

From lib/features/ai/infrastructure/openai_adapter.dart (current signature to match exactly):
```dart
Stream<String> createStream({
  required String apiKey,
  required String baseUrl,
  required String model,
  required List<ChatMessage> messages,
  double? temperature,
  double? topP,
  int? maxTokens,
  void Function(Usage?)? onUsage,
});
```

From lib/core/presentation/providers.dart (line 183-185, current provider):
```dart
final openaiAdapterProvider = Provider<OpenAIAdapter>((ref) {
  return OpenAIAdapter();
});
```

From lib/features/ai/domain/ai_exception.dart (sealed hierarchy for error simulation):
```dart
sealed class AIException implements Exception {
  final String message;
  const AIException(this.message);
  String get userMessage;
}
// Subclasses: AIAuthException, AIRateLimitException, AINetworkException, AIStreamException
```

From openai_dart package (types used in interface):
```dart
// ChatMessage has .content field (String) for text messages
// Usage has: promptTokens, completionTokens, totalTokens
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract AIAdapter interface, refactor OpenAIAdapter, update provider type</name>
  <files>lib/features/ai/domain/ai_adapter.dart, lib/features/ai/infrastructure/openai_adapter.dart, lib/core/presentation/providers.dart</files>
  <read_first>
    - lib/features/ai/infrastructure/openai_adapter.dart (current OpenAIAdapter to extract interface from)
    - lib/core/presentation/providers.dart (openaiAdapterProvider at line 183-185, and all consumers at lines 362, 387, 431, 446)
    - .planning/phases/13-automation-test-harness/13-PATTERNS.md (exact interface pattern at lines 29-64, provider change at lines 89-116)
  </read_first>
  <behavior>
    - Test 1: AIAdapter abstract class exists and declares createStream with exact same parameter signature as OpenAIAdapter
    - Test 2: OpenAIAdapter implements AIAdapter (runtime type check)
    - Test 3: openaiAdapterProvider returns type AIAdapter (not OpenAIAdapter)
    - Test 4: Existing consumers (synthesis_notifier.dart line 217, editor_ai_notifier.dart line 154) still compile and work unchanged
  </behavior>
  <action>
    Create `lib/features/ai/domain/ai_adapter.dart` containing the `AIAdapter` abstract class with exactly one method: `createStream`. The method signature must match OpenAIAdapter.createStream parameter-for-parameter (apiKey, baseUrl, model, messages, temperature, topP, maxTokens, onUsage). Import `dart:async` and `package:openai_dart/openai_dart.dart`. The interface does NOT include `fetchModelList`, `dispose`, `isActive`, or `classifyException` -- those are OpenAIAdapter-specific per CONTEXT D-01.

    In `lib/features/ai/infrastructure/openai_adapter.dart`, add `import 'package:museflow/features/ai/domain/ai_adapter.dart';` and change line 23 from `class OpenAIAdapter {` to `class OpenAIAdapter implements AIAdapter {`. No other changes to this file -- the existing `createStream` already matches the interface signature.

    In `lib/core/presentation/providers.dart`, change line 183 from `Provider<OpenAIAdapter>` to `Provider<AIAdapter>`. Update the doc comment to reference AIAdapter. Add import for `package:museflow/features/ai/domain/ai_adapter.dart`. Downstream consumers (skillGenerationServiceProvider, deviationDetectionServiceProvider, templateCompletionServiceProvider, openingGeneratorServiceProvider) use `ref.watch(openaiAdapterProvider)` and call `.createStream()` -- these are unaffected by the type widening since the method signature is identical.
  </action>
  <verify>
    <automated>flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `lib/features/ai/domain/ai_adapter.dart` exists with exported `abstract class AIAdapter`
    - `OpenAIAdapter` declaration includes `implements AIAdapter`
    - `openaiAdapterProvider` typed as `Provider<AIAdapter>`
    - All existing `flutter test` unit tests still pass (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build FakeAdapter, test container factory, and test fixtures</name>
  <files>test/automation/helpers/fake_adapter.dart, test/automation/helpers/test_container.dart, test/automation/fixtures/xianxia_content.dart, test/automation/fixtures/manuscript_fixtures.dart</files>
  <read_first>
    - lib/features/ai/domain/ai_adapter.dart (interface FakeAdapter must implement, created in Task 1)
    - lib/features/ai/infrastructure/openai_adapter.dart (onUsage callback pattern at lines 84-91 to replicate)
    - lib/core/presentation/providers.dart (provider override pattern, all Hive boxes needed at lines 85-90, 481-483, 652-665)
    - test/helpers/hive_test_helper.dart (setUpHiveTest / tearDownHiveTest pattern to reuse)
    - .planning/phases/13-automation-test-harness/13-PATTERNS.md (FakeAdapter skeleton at lines 119-198, test_container at lines 202-250, fixtures at lines 254-318)
    - .planning/phases/13-automation-test-harness/13-UI-SPEC.md (FakeAdapter Output Contract table at lines 102-112, assertion patterns at lines 117-123)
    - .planning/phases/13-automation-test-harness/13-CONTEXT.md (D-03 error config, D-09 hive_test_helper reuse)
  </read_first>
  <behavior>
    - Test 1 (FakeAdapter synthesis): createStream with messages containing '碎片' yields text containing '林风' and '筑基'
    - Test 2 (FakeAdapter rewrite): createStream with messages containing '改写' yields text containing '剑光'
    - Test 3 (FakeAdapter polish): createStream with messages containing '润色' yields text containing '灵力' or '月华'
    - Test 4 (onUsage callback): onUsage is called after stream completes; captured usage has promptTokens > 0 and completionTokens > 0; totalTokens == promptTokens + completionTokens
    - Test 5 (error mode): FakeAdapter(errorRate: 1.0, errorText: '网络异常') yields the error text
    - Test 6 (empty mode): FakeAdapter(emptyResponse: true) yields empty stream
    - Test 7 (deterministic): calling createStream twice with same operation type returns same text both times
    - Test 8 (test container): createTestContainer() returns ProviderContainer; reading manuscriptRepositoryProvider.future returns a ManuscriptRepository instance
  </behavior>
  <action>
    Create `test/automation/fixtures/xianxia_content.dart` with class `XianxiaContent` containing static const lists for each operation type (synthesis, rewrite, polish, freeInput) with the exact strings from UI-SPEC FakeAdapter Output Contract. Also include a `Map<String, List<String>> assertableSubstrings` map.

    Create `test/automation/fixtures/manuscript_fixtures.dart` with class `ManuscriptFixtures` containing static factory methods: `xianxiaManuscript({String? id})` returning a Manuscript with title '剑道苍穹', genre '修仙', and `chapter({required String manuscriptId, required int number, String? content})` returning a Chapter with title '第N章'.

    Create `test/automation/helpers/fake_adapter.dart` with `class FakeAdapter implements AIAdapter`. Constructor accepts optional named params: `double? errorRate` (probability 0.0-1.0 of returning error text), `String? errorText` (text to return when error triggers), `bool emptyResponse = false` (yield empty string). The `createStream` method is an `async*` generator that: (1) checks error conditions first, (2) detects operation type from messages content by scanning for keywords ('碎片'/'整理' -> synthesis, '改写'/'语气' -> rewrite, '润色' -> polish, else freeInput), (3) yields characters from the deterministic response map in XianxiaContent, (4) after all characters yielded, calls `onUsage?.call(Usage(...))` with token estimation using the formula `text.replaceAll(RegExp(r'\s'), '').length * 2` for Chinese text. Token estimation extracts content from ChatMessage objects (access `.content` on each message, join them).

    Create `test/automation/helpers/test_container.dart` with `Future<ProviderContainer> createTestContainer()` function that: (1) calls `TestWidgetsFlutterBinding.ensureInitialized()`, (2) creates temp dir with `Directory.systemTemp.createTempSync('automation_test_')`, (3) calls `Hive.init(tempDir.path)`, (4) opens required Hive boxes: 'manuscripts', 'chapters', 'token_audit', 'ai_providers', 'fragments' (use `Hive.openBox<dynamic>` for all), (5) returns `ProviderContainer(overrides: [openaiAdapterProvider.overrideWithValue(FakeAdapter())])`. Also provide `Future<void> cleanupTestContainer(ProviderContainer container)` that calls `container.dispose()` then `Hive.deleteFromDisk()`.
  </action>
  <verify>
    <automated>flutter test test/automation/helpers/ -x 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `test/automation/helpers/fake_adapter.dart` implements AIAdapter, passes all 7 behavior tests
    - `test/automation/helpers/test_container.dart` creates ProviderContainer with Hive + FakeAdapter override
    - `test/automation/fixtures/xianxia_content.dart` has all operation type strings from UI-SPEC
    - `test/automation/fixtures/manuscript_fixtures.dart` has ManuscriptFixtures.xianxiaManuscript() and ManuscriptFixtures.chapter()
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production code | Test doubles and overrides interact with real domain/infrastructure layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | FakeAdapter | mitigate | Hardcode API key as 'fake-key-for-testing' in test_container.dart; never read from env vars or secure storage in test code |
| T-13-02 | Tampering | Hive temp directory | mitigate | Each test uses `Directory.systemTemp.createTempSync('automation_test_')` with unique suffix; tearDown calls `Hive.deleteFromDisk()` to prevent data leakage |
| T-13-03 | Spoofing | FakeAdapter onUsage | mitigate | FakeAdapter MUST call `onUsage?.call(usage)` with non-zero token values after stream completes, otherwise token audit tests produce false negatives |
| T-13-SC | Tampering | npm/pub install | accept | No new packages installed this plan -- all dependencies already in pubspec.yaml |

</threat_model>

<verification>
- `flutter test` passes (no regressions from AIAdapter extraction)
- `flutter test test/automation/helpers/` passes (FakeAdapter and test container work)
- `flutter analyze lib/features/ai/domain/ai_adapter.dart lib/features/ai/infrastructure/openai_adapter.dart lib/core/presentation/providers.dart` reports no issues
</verification>

<success_criteria>
- AIAdapter abstract interface extracted from OpenAIAdapter with matching createStream signature
- OpenAIAdapter declares `implements AIAdapter` without breaking any existing consumers
- openaiAdapterProvider typed as `Provider<AIAdapter>` enabling `overrideWithValue(FakeAdapter())`
- FakeAdapter returns deterministic xianxia text per UI-SPEC contract for all 4 operation types
- FakeAdapter calls onUsage after stream completes with token estimates > 0
- FakeAdapter supports errorRate/errorText/emptyResponse configuration
- Test container factory provides ProviderContainer with Hive init and FakeAdapter override
- All existing unit tests still pass (zero regressions)
</success_criteria>

<output>
Create `.planning/phases/13-automation-test-harness/13-01-SUMMARY.md` when done
</output>
