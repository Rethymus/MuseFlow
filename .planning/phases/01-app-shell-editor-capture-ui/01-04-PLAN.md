---
phase: 01-app-shell-editor-capture-ui
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - lib/features/capture/presentation/quick_capture.dart
  - lib/shared/utils/keyboard_shortcuts.dart
  - lib/core/presentation/app_shell.dart
  - lib/core/presentation/sidebar.dart
  - test/features/capture/quick_capture_test.dart
  - test/app/navigation_test.dart
  - test/app/adaptive_layout_test.dart
autonomous: true
requirements:
  - CAPT-05
  - TECH-05
  - TECH-07

must_haves:
  truths:
    - "Pressing Ctrl+Shift+N opens a quick-capture overlay dialog from any screen"
    - "Typing text in the overlay and pressing Enter or clicking Save saves a fragment"
    - "Saved quick-capture fragment appears in the capture page fragment list"
    - "Pressing Escape or clicking Cancel closes the overlay without saving"
    - "Sidebar collapses to icon-only on narrow windows (600-1000px)"
    - "Below 600px width, NavigationBar replaces sidebar at bottom"
  artifacts:
    - path: "lib/features/capture/presentation/quick_capture.dart"
      provides: "Quick-capture dialog with TextField + save/cancel buttons"
      contains: "AlertDialog"
    - path: "lib/shared/utils/keyboard_shortcuts.dart"
      provides: "Ctrl+Shift+N shortcut definition using Shortcuts/Actions"
      contains: "LogicalKeySet"
    - path: "test/features/capture/quick_capture_test.dart"
      provides: "Quick-capture dialog tests"
      contains: "testWidgets"
    - path: "test/app/navigation_test.dart"
      provides: "Navigation state preservation tests"
      contains: "goBranch"
    - path: "test/app/adaptive_layout_test.dart"
      provides: "Adaptive layout breakpoint tests"
      contains: "NavigationRail"
  key_links:
    - from: "lib/shared/utils/keyboard_shortcuts.dart"
      to: "lib/features/capture/presentation/quick_capture.dart"
      via: "CallbackAction invokes showDialog(QuickCaptureDialog)"
      pattern: "QuickCaptureDialog"
    - from: "lib/features/capture/presentation/quick_capture.dart"
      to: "lib/features/capture/presentation/capture_provider.dart"
      via: "ref.read captureProvider.notifier.addFragment"
      pattern: "addFragment"
    - from: "lib/core/presentation/app_shell.dart"
      to: "lib/shared/utils/keyboard_shortcuts.dart"
      via: "Shortcuts widget wrapping scaffold"
      pattern: "QuickCaptureShortcut"
    - from: "lib/core/presentation/sidebar.dart"
      to: "lib/shared/constants/app_constants.dart"
      via: "breakpoint constants for extended/collapsed/mobile"
      pattern: "sidebarExtendedBreakpoint"
---

<objective>
Add the global quick-capture overlay triggered by Ctrl+Shift+N, and complete the adaptive layout so the sidebar collapses gracefully and switches to bottom NavigationBar on narrow/mobile screens.

Purpose: Quick-capture is the "catch fleeting ideas before they disappear" feature. Adaptive layout ensures the app works on Android and narrow desktop windows. Per D-11 (global hotkey) and D-12 (text-only minimal capture).

Output: Quick-capture overlay accessible from any screen via keyboard shortcut, and responsive sidebar/bottom-nav layout.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-editor-capture-ui/01-CONTEXT.md
@.planning/phases/01-app-shell-editor-capture-ui/01-RESEARCH.md
@.planning/phases/01-app-shell-editor-capture-ui/01-UI-SPEC.md
@.planning/phases/01-app-shell-editor-capture-ui/01-01-SUMMARY.md
@.planning/phases/01-app-shell-editor-capture-ui/01-03-SUMMARY.md
@lib/core/presentation/app_shell.dart
@lib/core/presentation/sidebar.dart
@lib/shared/constants/app_constants.dart

<interfaces>
<!-- From Plan 01 and Plan 03 outputs that this plan depends on -->

From lib/core/presentation/app_shell.dart (created in Plan 01):
```dart
// AppShellScaffold: ConsumerWidget
// Takes StatefulNavigationShell parameter
// Builds: Scaffold(body: Row([AdaptiveSidebar, Expanded(navigationShell)]))
```

From lib/core/presentation/sidebar.dart (created in Plan 01):
```dart
// AdaptiveSidebar: StatelessWidget
// Parameters: currentIndex (int), onDestinationSelected (ValueChanged<int>)
// Uses LayoutBuilder + MediaQuery to toggle NavigationRail extended property
```

From lib/features/capture/presentation/capture_provider.dart (created in Plan 03):
```dart
// CaptureNotifier: AsyncNotifier<CaptureState>
// addFragment(String text, {List<String>? tags}) -> creates fragment, reloads list
// captureProvider: AsyncNotifierProvider for CaptureNotifier
```

From lib/shared/constants/app_constants.dart (created in Plan 01):
```dart
class AppConstants {
  static const double sidebarExtendedBreakpoint = 1000.0;
  static const double sidebarCollapsedBreakpoint = 600.0;
  // ...
}
```

From lib/core/domain/fragment_tag.dart (created in Plan 01):
```dart
class FragmentTags {
  static const String story = '故事';
  // ...
}
```

From UI-SPEC:
```
// Quick-capture: AlertDialog, width 480px max, TextField autofocus multiline max 3 lines
// "保存" button: primary accent
// "取消" button: text button
// On Enter in TextField: same as "保存"
// On Escape: close without save
// Success feedback: snackbar "灵感已保存"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Quick-capture overlay with Ctrl+Shift+N global shortcut</name>
  <files>
    lib/features/capture/presentation/quick_capture.dart,
    lib/shared/utils/keyboard_shortcuts.dart,
    lib/core/presentation/app_shell.dart,
    test/features/capture/quick_capture_test.dart
  </files>
  <read_first>
    lib/core/presentation/app_shell.dart,
    lib/features/capture/presentation/capture_provider.dart,
    lib/core/domain/fragment_tag.dart,
    lib/shared/constants/app_constants.dart,
    .planning/phases/01-app-shell-editor-capture-ui/01-CONTEXT.md (D-11: Ctrl+Shift+N, D-12: text-only minimal form),
    .planning/phases/01-app-shell-editor-capture-ui/01-RESEARCH.md (Pattern 6: Quick-Capture Global Shortcut, Pitfall 6: Shortcut Not Working),
    .planning/phases/01-app-shell-editor-capture-ui/01-UI-SPEC.md (Quick-Capture Overlay layout, Quick-Capture Interactions, Animation: dialog open 150ms easeOut / close 100ms easeIn, Copywriting Contract)
  </read_first>
  <behavior>
    - Test: QuickCaptureDialog renders with TextField, 保存 button, and 取消 button
    - Test: Entering text and pressing 保存 saves a fragment via captureProvider
    - Test: Pressing 取消 closes the dialog without saving
    - Test: Ctrl+Shift+N logical key set is registered in the Shortcuts widget
  </behavior>
  <action>
    **Quick-capture dialog (lib/features/capture/presentation/quick_capture.dart):**

    Create QuickCaptureDialog as ConsumerWidget.

    Layout (per UI-SPEC Quick-Capture Overlay):
    - Uses showDialog() from caller, so build returns AlertDialog content
    - AlertDialog with title '快速捕捉', content: Column(mainAxisSize: min):
      - TextField: autofocus true, maxLines 3, decoration with hintText '输入你的灵感...'
      - On submitted (Enter key in field): call _saveAndClose
      - Use a TextEditingController and FocusNode for control
    - Actions row:
      - TextButton '取消' (onSurface color) -> Navigator.pop without saving
      - ElevatedButton '保存' (primary accent per UI-SPEC) -> _saveAndClose

    _saveAndClose logic:
    - Read text from controller, trim, return early if empty
    - Call ref.read(captureProvider.notifier).addFragment(text) -- uses default tags from settings (per D-12: fragments go to default tag)
    - Navigator.pop(context)
    - After pop, show SnackBar with '灵感已保存' on the scaffold messenger (per UI-SPEC copywriting)

    Handle keyboard:
    - Wrap the AlertDialog content in a Shortcuts widget that maps Enter (without Shift/Ctrl) to a save intent, and Escape to cancel
    - TextField multiline Enter should insert newline (Shift+Enter), Ctrl+Enter or standalone Enter in non-multiline context saves -- since TextField maxLines is 3, use a different approach: detect Enter via onSubmitted if maxLines is 1, or use a keyboard listener for multiline

    Simplification: Use maxLines: null (unlimited) for the text field but add an explicit "Enter to save" behavior via onSubmitted -- actually for multiline, onSubmitted only fires on Enter when maxLines is 1. For multiline with maxLines 3, use a raw keyboard listener: if Enter is pressed without Shift, save; Shift+Enter inserts newline.

    **Keyboard shortcuts (lib/shared/utils/keyboard_shortcuts.dart):**

    Create QuickCaptureShortcut as a ConsumerWidget that wraps its child with Shortcuts + Actions.

    Shortcuts mapping:
    - LogicalKeySet(LogicalKeyboardKey.control, LogicalKeyboardKey.shift, LogicalKeyboardKey.keyN) -> QuickCaptureIntent

    Actions:
    - CallbackAction<QuickCaptureIntent>: onInvoke calls showDialog with QuickCaptureDialog

    Define QuickCaptureIntent as a const Intent subclass.

    Per RESEARCH Pitfall 6: Place the Shortcuts widget high in the tree. Wrap the entire Scaffold body in AppShellScaffold so the shortcut works from any branch.

    **App shell update (lib/core/presentation/app_shell.dart):**

    Modify AppShellScaffold to wrap its Scaffold body with QuickCaptureShortcut from keyboard_shortcuts.dart. This ensures Ctrl+Shift+N works from any screen.

    Structure:
    ```
    QuickCaptureShortcut(
      child: Scaffold(
        body: Row([AdaptiveSidebar, Expanded(navigationShell)])
      )
    )
    ```

    **Tests (test/features/capture/quick_capture_test.dart):**

    Widget tests for QuickCaptureDialog:
    - Test dialog renders with title '快速捕捉', TextField with hint, 保存 and 取消 buttons
    - Test tapping 保存 with non-empty text calls addFragment (mock provider)
    - Test tapping 取消 closes dialog without calling addFragment
    - Test submitting empty text does not call addFragment

    Test for shortcut registration:
    - Verify QuickCaptureShortcut contains LogicalKeySet with Ctrl+Shift+N
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/features/capture/quick_capture_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>
    - QuickCaptureDialog with text field, 保存/取消 buttons, and autofocus
    - Ctrl+Shift+N opens the dialog from any screen via Shortcuts widget in app shell
    - Enter saves the fragment (without Shift), Shift+Enter inserts newline
    - Saved fragment goes to default tag and appears in capture page list
    - SnackBar shows '灵感已保存' after successful save
    - Escape and 取消 close without saving
    - All quick-capture tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adaptive layout with NavigationBar fallback and navigation tests</name>
  <files>
    lib/core/presentation/sidebar.dart,
    test/app/navigation_test.dart,
    test/app/adaptive_layout_test.dart
  </files>
  <read_first>
    lib/core/presentation/sidebar.dart,
    lib/core/presentation/app_shell.dart,
    lib/shared/constants/app_constants.dart,
    .planning/phases/01-app-shell-editor-capture-ui/01-CONTEXT.md (D-02: sidebar collapses on narrow windows, D-04: 3 nav items),
    .planning/phases/01-app-shell-editor-capture-ui/01-UI-SPEC.md (App Shell Layout: breakpoints at 1000px and 600px, Adaptive layout section, Interaction Contracts: Navigation Interactions)
  </read_first>
  <behavior>
    - Test: At width >= 1000px, NavigationRail is extended (shows labels)
    - Test: At width 600-999px, NavigationRail is collapsed (icons only)
    - Test: At width < 600px, NavigationBar is shown at bottom instead of NavigationRail
    - Test: Tapping a destination switches the branch and preserves other branch state
    - Test: All 3 destinations render with correct Chinese labels
  </behavior>
  <action>
    **Sidebar update (lib/core/presentation/sidebar.dart):**

    Refactor AdaptiveSidebar to handle three layout modes based on MediaQuery width:

    Mode 1: width >= AppConstants.sidebarExtendedBreakpoint (1000px)
    - Show NavigationRail with extended: true
    - Leading section: Padding(vertical: 16) with Text('灵韵') in display style (28px w700)
    - 3 destinations with icons and Chinese labels

    Mode 2: width >= AppConstants.sidebarCollapsedBreakpoint (600px) AND < 1000px
    - Show NavigationRail with extended: false
    - Leading section: just icon or compact logo
    - 3 destinations with icons only (labels hidden by NavigationRail)

    Mode 3: width < 600px
    - Do NOT show NavigationRail at all
    - Return SizedBox.shrink() from AdaptiveSidebar
    - The parent (AppShellScaffold) must show a NavigationBar at bottom instead

    Update AppShellScaffold to check the same breakpoint:
    - If width < sidebarCollapsedBreakpoint: use Column layout with [Expanded(navigationShell), NavigationBar(...)]
    - NavigationBar items: same 3 destinations with icons and labels, at bottom of screen
    - If width >= sidebarCollapsedBreakpoint: keep existing Row layout with [AdaptiveSidebar, Expanded(navigationShell)]

    The NavigationBar items match the NavigationRail destinations exactly:
    1. icon: Icons.bookmark_outline, activeIcon: Icons.bookmark, label: '捕捉器'
    2. icon: Icons.edit_note_outlined, activeIcon: Icons.edit_note, label: '编辑器'
    3. icon: Icons.settings_outlined, activeIcon: Icons.settings, label: '设置'

    Animation (per UI-SPEC): NavigationRail extend/collapse transition is handled by NavigationRail internally (200ms easeInOut). No custom animation needed.

    **Navigation tests (test/app/navigation_test.dart):**

    Widget tests:
    - Test 3 navigation destinations render with correct Chinese labels
    - Test initial branch is editor (index 1, per D-03)
    - Test branch switching: tapping capture shows capture content, tapping settings shows settings content
    - Test that switching branches preserves the editor's state (text content remains)

    Use go_router testing patterns with StatefulNavigationShell.

    **Adaptive layout tests (test/app/adaptive_layout_test.dart):**

    Widget tests:
    - Test at 1200px width: NavigationRail is visible with extended: true and labels shown
    - Test at 800px width: NavigationRail is visible with extended: false (icons only)
    - Test at 400px width: NavigationBar is visible at bottom, NavigationRail is hidden

    Use MediaQuery.setSize or LayoutBuilder test patterns to simulate different widths.
  </action>
  <verify>
    <automated>cd /home/re/code/MuseFlow && flutter test test/app/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    - NavigationRail extends at >= 1000px width with Chinese labels
    - NavigationRail collapses at 600-999px with icons only
    - NavigationBar appears at bottom below 600px (Android phone portrait)
    - All 3 destinations have correct icons and Chinese labels in both modes
    - Branch switching preserves state across all navigation modes
    - Navigation and adaptive layout tests pass
    - Full test suite passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input -> Quick-capture | Global shortcut triggers dialog; text input stored as fragment |
| Layout breakpoint -> Navigation mode | Media query width determines sidebar vs bottom nav |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Keyboard shortcut injection | accept | Shortcuts widget only responds to specific LogicalKeySet, no injection vector |
| T-01-09 | Information Disclosure | Quick-capture dialog | accept | Dialog content is user creative text, stored locally |
| T-01-SC | Tampering | No new packages | accept | This plan uses only packages from prior plans |
</threat_model>

<verification>
1. `flutter test` -- full suite passes including all Phase 1 tests
2. `flutter analyze` -- zero errors
3. Manual: Press Ctrl+Shift+N from editor page, type text, Enter saves, navigate to capture to verify fragment appears
4. Manual: Resize window to test sidebar extend/collapse behavior at 1000px and 600px breakpoints
</verification>

<success_criteria>
- Ctrl+Shift+N opens quick-capture overlay from any screen
- Quick-capture saves fragment to default tag with success snackbar
- Escape/Cancel closes without saving
- Sidebar extends/collapses at correct breakpoints (1000px / 600px)
- NavigationBar replaces sidebar below 600px
- Branch switching preserves state across all layout modes
- All Phase 1 tests pass (infrastructure, editor, capture, navigation, adaptive layout)
</success_criteria>

<output>
Create `.planning/phases/01-app-shell-editor-capture-ui/01-04-SUMMARY.md` when done
</output>
