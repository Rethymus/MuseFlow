---
phase: 09-writing-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/stats/domain/writing_session.dart
  - lib/features/stats/domain/daily_writing_stats.dart
  - lib/features/stats/domain/stats_snapshot.dart
  - lib/features/stats/domain/writing_unit_counter.dart
  - lib/features/stats/infrastructure/writing_stats_repository.dart
  - lib/features/stats/application/writing_stats_collector.dart
  - lib/features/stats/application/writing_stats_notifier.dart
  - lib/core/presentation/providers.dart
  - lib/features/editor/presentation/editor_page.dart
  - lib/features/ai/presentation/synthesis_notifier.dart
  - lib/features/onboarding/presentation/opening_text_insertion.dart
  - test/features/stats/domain/writing_unit_counter_test.dart
  - test/features/stats/infrastructure/writing_stats_repository_test.dart
  - test/features/stats/application/writing_stats_collector_test.dart
  - test/features/stats/application/writing_stats_notifier_test.dart
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-05
user_setup: []

must_haves:
  truths:
    - "Stats domain models are immutable and serialize to/from Hive-friendly maps"
    - "Writing unit counter correctly handles Chinese characters and Latin words"
    - "Collector keeps typing-path updates in memory and debounces Hive flushes by 30 seconds"
    - "Global and default current-document stats can be loaded through Riverpod"
    - "AI insertion paths record AI-written units without double-counting human edits"
  artifacts:
    - path: "lib/features/stats/domain/writing_unit_counter.dart"
      provides: "deterministic Chinese/Latin writing unit count helper"
      contains: "countWritingUnits"
    - path: "lib/features/stats/infrastructure/writing_stats_repository.dart"
      provides: "Hive persistence for global, daily, and project stats"
      contains: "clearAll", "loadSnapshot", "recordSessionDelta"
    - path: "lib/features/stats/application/writing_stats_collector.dart"
      provides: "in-memory collector with 30-second debounced persistence"
      contains: "recordTextSnapshot", "recordAiInsertion", "flush"
    - path: "lib/features/stats/application/writing_stats_notifier.dart"
      provides: "dashboard-facing async stats state"
      contains: "AsyncNotifier", "refresh"
    - path: "lib/core/presentation/providers.dart"
      provides: "stats repository, collector, and notifier providers"
      contains: "writingStatsRepositoryProvider", "writingStatsCollectorProvider"
  key_links:
    - from: "editor_page.dart"
      to: "writingStatsCollectorProvider"
      via: "editor document snapshots and dispose flush"
      pattern: "ref.read\(writingStatsCollectorProvider\)"
    - from: "synthesis_notifier.dart"
      to: "writingStatsCollector.recordAiInsertion"
      via: "AI text insertion boundary"
      pattern: "recordAiInsertion\(state.accumulatedText\)"
    - from: "opening_text_insertion.dart"
      to: "writingStatsCollector.recordAiInsertion"
      via: "Phase 8 generated opening insertion boundary"
      pattern: "recordAiInsertion"
---

<objective>
Build the Phase 9 stats foundation: domain models, Hive repository, in-memory/debounced collector, provider wiring, editor snapshot integration, and AI insertion accounting.

Purpose: establish accurate local stats collection without degrading editor typing performance.

Output: test-covered stats infrastructure that future UI plans can query for global, daily, and current-document/project snapshots.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-writing-stats/09-RESEARCH.md
@.planning/phases/09-writing-stats/09-PATTERNS.md
@.planning/phases/09-writing-stats/09-VALIDATION.md

@lib/core/presentation/providers.dart (Hive provider patterns)
@lib/features/editor/presentation/editor_page.dart (Editor lifecycle and editorProvider)
@lib/features/ai/presentation/synthesis_notifier.dart (AI insertion boundary)
@lib/features/onboarding/presentation/opening_text_insertion.dart (generated opening insertion helper)
@test/features/story_structure/infrastructure/plot_node_repository_test.dart (Hive repository test style)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stats domain models and writing unit counter</name>
  <files>
    lib/features/stats/domain/writing_session.dart
    lib/features/stats/domain/daily_writing_stats.dart
    lib/features/stats/domain/stats_snapshot.dart
    lib/features/stats/domain/writing_unit_counter.dart
    test/features/stats/domain/writing_unit_counter_test.dart
  </files>
  <read_first>
    lib/features/story_structure/domain/plot_node.dart
    lib/features/knowledge/domain/character_card.dart
  </read_first>
  <action>
    Create immutable domain classes with const constructors and copyWith:
    - WritingSession: id, projectId nullable, documentId nullable, startedAt, endedAt nullable, humanUnits, aiUnits, editSeconds
    - DailyWritingStats: dateKey yyyy-mm-dd, humanUnits, aiUnits, sessionCount, editSeconds
    - StatsSnapshot: totalUnits, humanUnits, aiUnits, writingDays, sessionCount, editSeconds, daily list, project map or currentProject nullable

    Create writing_unit_counter.dart:
    - int countWritingUnits(String text)
    - Count each CJK unified ideograph as 1
    - Count contiguous Latin/digit runs as 1
    - Ignore whitespace and punctuation
    - Return 0 for empty/punctuation-only text

    Add tests for:
    - Chinese sentence counts by character excluding punctuation
    - English words count by token
    - Mixed Chinese/English text
    - Empty, whitespace, punctuation
  </action>
  <verify>
    <automated>flutter test test/features/stats/domain/writing_unit_counter_test.dart</automated>
  </verify>
  <done>
    Domain classes compile, counter has deterministic tests, no generated code required.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Hive-backed WritingStatsRepository</name>
  <files>
    lib/features/stats/infrastructure/writing_stats_repository.dart
    test/features/stats/infrastructure/writing_stats_repository_test.dart
  </files>
  <read_first>
    lib/features/story_structure/infrastructure/plot_node_repository.dart
    lib/features/knowledge/infrastructure/character_card_repository.dart
    test/features/story_structure/infrastructure/plot_node_repository_test.dart
  </read_first>
  <action>
    Create WritingStatsRepository using Hive boxes passed into constructor:
    - Box<dynamic> aggregateBox for `writing_stats`
    - Box<dynamic> dailyBox for `daily_writing_stats`
    - Optional Box<dynamic> badgeBox can be deferred to Plan 09-03, do not implement badge logic here

    Implement methods:
    - Future<StatsSnapshot> loadSnapshot({String? projectId})
    - Future<void> recordSessionDelta({String? projectId, String? documentId, required int humanUnits, required int aiUnits, required Duration editDuration, required DateTime occurredAt})
    - Future<List<DailyWritingStats>> loadDailyStats({int days = 30})
    - Future<void> clearAll()

    Storage keys:
    - `global` for aggregate stats
    - `project:<projectId or default>` for current-document/project aggregate
    - `yyyy-mm-dd` for daily stats

    Keep repository local-only. Do not add network calls.
  </action>
  <verify>
    <automated>flutter test test/features/stats/infrastructure/writing_stats_repository_test.dart</automated>
  </verify>
  <done>
    Repository records deltas, aggregates global/daily/project stats, loads snapshots, and clears boxes in tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add debounced WritingStatsCollector and notifier</name>
  <files>
    lib/features/stats/application/writing_stats_collector.dart
    lib/features/stats/application/writing_stats_notifier.dart
    test/features/stats/application/writing_stats_collector_test.dart
    test/features/stats/application/writing_stats_notifier_test.dart
  </files>
  <read_first>
    lib/features/ai/presentation/synthesis_notifier.dart
    lib/features/knowledge/application/skill_notifier.dart
  </read_first>
  <action>
    Create WritingStatsCollector:
    - Constructor takes WritingStatsRepository and optional debounce duration defaulting to 30 seconds
    - Fields track lastTextUnits, pendingHumanUnits, pendingAiUnits, session start time, last activity
    - recordTextSnapshot(String plainText, {String? projectId, String? documentId}) counts units and accumulates positive human delta only
    - recordAiInsertion(String text, {String? projectId, String? documentId}) accumulates AI units and records corresponding total delta safely
    - scheduleFlush() starts/restarts a Timer; no awaited Hive write in recordTextSnapshot
    - flush() writes pending values to repository and resets pending counters
    - dispose() flushes or cancels timer safely

    Create WritingStatsNotifier as AsyncNotifier<StatsSnapshot>:
    - build() loads repository snapshot
    - refresh() reloads
    - clearAll() may delegate to repository but final settings UI is Plan 09-03

    Tests should use a short debounce duration and fake/in-memory repository where practical.
  </action>
  <verify>
    <automated>flutter test test/features/stats/application/writing_stats_collector_test.dart test/features/stats/application/writing_stats_notifier_test.dart</automated>
  </verify>
  <done>
    Collector debounces flush, snapshots produce positive deltas only, AI insertion units are tracked separately, notifier loads snapshots.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire providers and editor/AI collection hooks</name>
  <files>
    lib/core/presentation/providers.dart
    lib/features/editor/presentation/editor_page.dart
    lib/features/ai/presentation/synthesis_notifier.dart
    lib/features/onboarding/presentation/opening_text_insertion.dart
  </files>
  <read_first>
    lib/core/presentation/providers.dart
    lib/features/editor/presentation/editor_page.dart
    lib/features/ai/presentation/synthesis_notifier.dart
    lib/features/onboarding/presentation/opening_text_insertion.dart
  </read_first>
  <action>
    Provider wiring:
    - Add imports for stats repository/application files
    - Add writingStatsRepositoryProvider that opens Hive boxes `writing_stats` and `daily_writing_stats`
    - Add writingStatsCollectorProvider that awaits repository and returns collector
    - Add writingStatsNotifierProvider as AsyncNotifierProvider

    Editor integration:
    - In EditorPage initState, after editor creation, capture initial document plain text as baseline
    - Register a lightweight listener or lifecycle hook to call collector.recordTextSnapshot(plainText)
    - If direct document listener API is unclear, use the minimal existing SuperEditor listenable available in this version; do not poll rapidly
    - On dispose, call collector.flush() before clearing editor provider where feasible

    AI integration:
    - In SynthesisNotifier.confirmAndInsert(), call recordAiInsertion before/after successful editor insertion
    - In opening_text_insertion.dart, add an optional callback or provider-safe hook only if it does not create a circular dependency; otherwise document in the plan summary that Phase 8 opening insertion is covered by editor snapshot collection

    Avoid creating imports from low-level helpers back into core providers if that causes cycles.
  </action>
  <verify>
    <automated>flutter test test/features/stats/ test/features/ai/presentation/synthesis_notifier_test.dart test/features/onboarding/application/opening_insertion_test.dart</automated>
  </verify>
  <done>
    Providers compile, editor typing path records snapshots without awaiting Hive writes, AI insertion updates AI counters, existing AI/onboarding tests still pass.
  </done>
</task>

</tasks>

<verification>
## Per-Task Verification

Run each task command above immediately after implementation.

## Plan Gate

- `flutter test test/features/stats/`
- `flutter test test/features/ai/presentation/synthesis_notifier_test.dart`
- `flutter test test/features/onboarding/application/opening_insertion_test.dart`
</verification>

<success_criteria>
1. Stats domain and repository are test-covered.
2. Writing unit counts handle Chinese and mixed text deterministically.
3. Collector uses 30-second debounced Hive writes and no synchronous storage in typing path.
4. Global/current-document snapshot can be loaded through Riverpod.
5. AI insertion events update AI-unit counters.
6. Existing editor/AI/onboarding behavior is not regressed.
</success_criteria>

<output>
Create `.planning/phases/09-writing-stats/09-01-SUMMARY.md` when done.
</output>
