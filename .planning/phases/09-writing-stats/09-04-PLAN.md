---
phase: 09-writing-stats
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - test/app/adaptive_layout_test.dart
  - test/app/navigation_test.dart
  - test/app/window_management_test.dart
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "All navigation/shell test assertions for destination count match the actual 6 destinations in sidebar.dart"
    - "flutter test passes with zero failures in test/app/"
  artifacts:
    - path: "test/app/adaptive_layout_test.dart"
      provides: "Adaptive layout breakpoint tests with correct destination count"
      contains: "equals(6)"
    - path: "test/app/window_management_test.dart"
      provides: "Window management tests with correct destination count"
      contains: "equals(6)"
    - path: "test/app/navigation_test.dart"
      provides: "Navigation tests with correct settings selectedIndex"
      contains: "equals(5)"
  key_links:
    - from: "test/app/*.dart"
      to: "lib/core/presentation/sidebar.dart"
      via: "destination count assertions must match sidebar's 6 NavigationRailDestination entries"
      pattern: "destinations\\.length.*equals\\(6\\)"
---

<objective>
Fix 5 test assertion failures caused by Phase 09 adding the "统计" (stats) navigation destination to sidebar.dart, increasing the total from 5 to 6 destinations.

Purpose: Close the navigation count regression gap identified in 09-VERIFICATION.md so the full test suite passes cleanly.
Output: Updated test assertions from equals(5) to equals(6) in 2 files, and equals(4) to equals(5) for settings selectedIndex in 1 file.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-writing-stats/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update navigation destination count assertions from 5 to 6</name>
  <files>test/app/adaptive_layout_test.dart, test/app/navigation_test.dart, test/app/window_management_test.dart</files>
  <read_first>
    - test/app/adaptive_layout_test.dart (see 5 assertions with equals(5) at lines 188, 212, 227; also update test description strings from "5" to "6")
    - test/app/navigation_test.dart (see settings selectedIndex equals(4) at line 219; update to equals(5))
    - test/app/window_management_test.dart (see equals(5) at lines 132, 202; also update test description strings from "5" to "6")
    - lib/core/presentation/sidebar.dart (source of truth: 6 NavigationRailDestination entries and 6 NavigationDestination entries)
  </read_first>
  <action>
    Make these exact changes:

    **test/app/adaptive_layout_test.dart** (3 changes):
    - Line 184: change comment "NavigationBar should have 5 destinations" to "NavigationBar should have 6 destinations"
    - Line 188: change `equals(5)` to `equals(6)` (navBar.destinations.length assertion)
    - Line 199: change test description "should have exactly 5 destinations with matching icons" to "should have exactly 6 destinations with matching icons"
    - Line 212: change `equals(5)` to `equals(6)` (navRail.destinations.length assertion)
    - Line 227: change `equals(5)` to `equals(6)` (navBar.destinations.length assertion)

    **test/app/navigation_test.dart** (1 change):
    - Line 219: change `equals(4)` to `equals(5)` (settings selectedIndex moved from 4 to 5 because "统计" was inserted at index 4)

    **test/app/window_management_test.dart** (2 changes):
    - Line 116: change test description "should render NavigationRail with 5 destinations" to "should render NavigationRail with 6 destinations"
    - Line 127: change comment "Should have 5 navigation destinations" to "Should have 6 navigation destinations"
    - Line 132: change `equals(5)` to `equals(6)` (navRail.destinations.length assertion)
    - Line 198: change comment "Should have 5 destinations" to "Should have 6 destinations"
    - Line 202: change `equals(5)` to `equals(6)` (navBar.destinations.length assertion)

    Do NOT modify anything else in these files. Do NOT add a stats branch to the test routers -- that is out of scope for this gap closure.
  </action>
  <verify>
    <automated>flutter test test/app/adaptive_layout_test.dart test/app/navigation_test.dart test/app/window_management_test.dart</automated>
  </verify>
  <done>
    - test/app/adaptive_layout_test.dart has 3 assertions with equals(6), zero with equals(5)
    - test/app/navigation_test.dart has settings selectedIndex with equals(5)
    - test/app/window_management_test.dart has 2 assertions with equals(6), zero with equals(5)
    - All 3 test files pass: flutter test test/app/ returns zero failures
    - grep -c "equals(5)" across the 3 files returns 0
  </done>
</task>

</tasks>

<verification>
Run `flutter test test/app/` and confirm zero failures.
Run `flutter test` and confirm the previously reported 5 failures are now resolved.
</verification>

<success_criteria>
- `flutter test test/app/` passes with 0 failures
- `flutter test` shows no destination-count-related failures
- Total test count unchanged (no tests added or removed)
</success_criteria>

<output>
Create `.planning/phases/09-writing-stats/09-04-SUMMARY.md` when done
</output>
