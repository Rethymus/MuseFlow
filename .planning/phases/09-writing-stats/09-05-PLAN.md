---
phase: 09-writing-stats
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - test/app/navigation_test.dart
autonomous: true
requirements: []
gap_closure: true
gaps_addressed:
  - truth: "Existing app shell tests pass after stats navigation addition"
    missing:
      - "Add stats StatefulShellBranch to test router at index 4"
      - "Move settings branch to index 5 in test router"
      - "Create _TestStatsPage placeholder widget"
      - "Update assertion at line 219 from equals(4) to equals(5)"

must_haves:
  truths:
    - "Navigation test router has 6 StatefulShellBranch branches matching sidebar destinations"
    - "All navigation destination taps work without RangeError"
    - "Settings destination tap test expects selectedIndex equals(5)"
  artifacts:
    - path: "test/app/navigation_test.dart"
      provides: "Test router with matching branch structure"
      min_lines: 140
    - path: "test/app/navigation_test.dart"
      contains: "_TestStatsPage"
    - path: "test/app/navigation_test.dart"
      contains: "StatefulShellBranch" # 6 occurrences (one per destination)
  key_links:
    - from: "test/app/navigation_test.dart"
      to: "lib/shared/constants/app_constants.dart"
      via: "AppConstants.stats import"
      pattern: "AppConstants\\.stats"
    - from: "test router branches"
      to: "sidebar.dart destinations"
      via: "Index alignment (0-5)"
      pattern: "StatefulShellBranch.*at index [0-5]"
---

<objective>
Fix navigation_test.dart regression caused by stats navigation addition

**Purpose:** Close the gap where navigation_test.dart has only 5 StatefulShellBranch branches while sidebar.dart has 6 destinations, causing a RangeError when tapping settings at index 5.

**Output:** Test router with 6 matching branches, updated assertion, and passing navigation tests.
</objective>

<execution_context>
@/home/re/code/MuseFlow/.claude/get-shit-done/workflows/execute-plan.md
@/home/re/code/MuseFlow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-writing-stats/09-VERIFICATION.md
@.planning/ROADMAP.md

# Gap context from VERIFICATION.md

**Sidebar destinations (6 total):**
- Index 0: 捕捉器 (capture)
- Index 1: 编辑器 (editor)
- Index 2: 知识库 (knowledge)
- Index 3: 故事结构 (storyStructure)
- Index 4: 统计 (stats) ← Added in Phase 9
- Index 5: 设置 (settings)

**Test router branches (currently 5 total):**
- Index 0: capture → _TestCapturePage
- Index 1: editor → _TestEditorPage
- Index 2: knowledge → _TestKnowledgePage
- Index 3: storyStructure → _TestStoryStructurePage
- Index 4: settings → _TestSettingsPage ← Missing stats branch

**Current branches in navigation_test.dart:**
```dart
StatefulShellBranch(routes: [GoRoute(path: AppConstants.capture, ...)])
StatefulShellBranch(routes: [GoRoute(path: AppConstants.editor, ...)])
StatefulShellBranch(routes: [GoRoute(path: AppConstants.knowledge, ...)])
StatefulShellBranch(routes: [GoRoute(path: AppConstants.storyStructure, ...)])
StatefulShellBranch(routes: [GoRoute(path: AppConstants.settings, ...)])
```

**Route constant needed:** `AppConstants.stats = '/stats'` (from app_constants.dart)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stats test page placeholder and branch to test router</name>
  <files>test/app/navigation_test.dart</files>
  <read_first>
    test/app/navigation_test.dart (lines 62-68 show pattern for _TestStoryStructurePage)
    lib/shared/constants/app_constants.dart (line 34: stats route constant)
  </read_first>
  <action>
    Add a new StatefulShellBranch for the stats route between storyStructure (index 3) and settings (index 5) in the _createTestRouter() function:

    1. Create a new _TestStatsPage widget following the same pattern as _TestStoryStructurePage:
       - Class extends StatelessWidget
       - build() returns Scaffold(body: Center(child: Text('TEST_STATS')))

    2. Insert a new StatefulShellBranch at index 4 (between storyStructure branch and settings branch):
       - Create StatefulShellBranch with routes array containing a GoRoute
       - GoRoute path: AppConstants.stats
       - GoRoute builder: (context, state) => const _TestStatsPage()

    3. Move the existing settings StatefulShellBranch to appear after the new stats branch (becoming index 5).

    The branches array should now have 6 StatefulShellBranch entries in this order:
    - Index 0: capture
    - Index 1: editor
    - Index 2: knowledge
    - Index 3: storyStructure
    - Index 4: stats (NEW)
    - Index 5: settings (MOVED)
  </action>
  <verify>
    <automated>grep -c "StatefulShellBranch" /home/re/code/MuseFlow/test/app/navigation_test.dart | grep -q "^6$"</automated>
    <automated>grep -c "_TestStatsPage" /home/re/code/MuseFlow/test/app/navigation_test.dart | grep -q "^2$"</automated>
  </verify>
  <done>
    - _TestStatsPage class defined
    - Test router has 6 StatefulShellBranch entries
    - Stats branch at index 4, settings branch at index 5
  </done>
</task>

<task type="auto">
  <name>Task 2: Update settings navigation test assertion</name>
  <files>test/app/navigation_test.dart</files>
  <read_first>
    test/app/navigation_test.dart (lines 200-223: 'should switch branch when tapping settings destination' test)
  </read_first>
  <action>
    In the testWidgets 'should switch branch when tapping settings destination' test, update line 219:

    Change:
    ```dart
    expect(navRail.selectedIndex, equals(4));
    ```

    To:
    ```dart
    expect(navRail.selectedIndex, equals(5));
    ```

    This reflects that settings is now at index 5 after adding the stats destination at index 4.
  </action>
  <verify>
    <automated>grep -n "equals(5)" /home/re/code/MuseFlow/test/app/navigation_test.dart | grep "219:"</automated>
  </verify>
  <done>
    - Assertion at line 219 expects equals(5) for settings selectedIndex
    - Test correctly validates settings destination selection
  </done>
</task>

<task type="auto">
  <name>Task 3: Run navigation test suite to verify fix</name>
  <files>test/app/navigation_test.dart</files>
  <read_first>
    test/app/navigation_test.dart (full file to understand test structure)
  </read_first>
  <action>
    Run the navigation test suite to verify that all tests pass:
    
    1. Execute: `flutter test test/app/navigation_test.dart --no-pub`
    2. Verify that 'should switch branch when tapping settings destination' test passes without RangeError
    3. Verify that all 5 navigation tests in the file pass

    Do NOT proceed if any test fails. The tests must all pass to close this gap.
  </action>
  <verify>
    <automated>flutter test test/app/navigation_test.dart --no-pub 2>&1 | grep -q "All tests passed"</automated>
  </verify>
  <done>
    - All navigation tests pass
    - No RangeError when tapping settings destination
    - Test suite output shows 5/5 tests passed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | Test imports app_constants.dart and uses real route paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-T | Tampering | Test router configuration | accept | Test-only code, no production impact. Malicious modification would cause test failures, not runtime issues. |
| T-09-05-I | Information disclosure | Test page placeholder text | accept | Placeholder text "TEST_STATS" has no sensitive information. |
</threat_model>

<verification>
## Gap Closure Verification

**Gap truth:** "Existing app shell tests pass after stats navigation addition"

**Verification steps:**
1. Confirm test router has 6 StatefulShellBranch entries
2. Confirm _TestStatsPage placeholder exists
3. Confirm stats branch uses AppConstants.stats path
4. Confirm settings branch is at index 5
5. Run `flutter test test/app/navigation_test.dart --no-pub` and verify all tests pass
6. Verify no RangeError when tapping settings destination

**Expected test output:**
```
00:00 +0: should render destinations with correct Chinese labels
00:00 +1: should have editor as initial branch (index 1)
00:00 +2: should switch branch when tapping capture destination
00:00 +3: should switch branch when tapping settings destination
00:00 +4: should preserve editor state when switching branches
00:00 +5: All tests passed!
```
</verification>

<success_criteria>
**Gap closed when:**
- [ ] Test router has 6 StatefulShellBranch branches matching sidebar's 6 destinations
- [ ] _TestStatsPage placeholder widget exists
- [ ] Settings destination test expects selectedIndex equals(5)
- [ ] `flutter test test/app/navigation_test.dart --no-pub` shows 5/5 tests passed
- [ ] No RangeError occurs when tapping settings destination in tests
</success_criteria>

<output>
Create `.planning/phases/09-writing-stats/09-05-SUMMARY.md` when done
</output>
